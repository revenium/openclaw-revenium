Spike 008 (SPIKE-01, candidate (a)) — sample rows, per cell, per D-02 fidelity field.
Host: 52.90.9.242. Date: 2026-09-24.
Versions in effect: standalone OpenClaw 2026.9.6 (eb377ac), NemoClaw-managed OpenClaw 2026.9.1
(ad6fe23), sqlite3 CLI 3.46.1. Source: .planning/spikes/007-live-2-0-host-provisioning/versions-resolved.txt.

Every query below used `sqlite3 -cmd "PRAGMA busy_timeout=5000;" "file:<path>?mode=ro" "<SQL>"`
(read-only URI + explicit busy timeout, per RESEARCH.md Pattern 1). Zero SQLITE_BUSY errors,
zero stalls, on either cell for any query in this file. All prompt text and assistant/tool
message bodies are redacted below (marked [REDACTED-TEXT]) per D-12/T-17-05 — every other field
(numeric usage counts, model ids, tool names, structural argument/result keys, status codes,
durations, timestamps, table/column names) is verbatim.

================================================================================
CELL HOST-LOCAL — standalone path
Store: /home/ubuntu/.openclaw/agents/main/agent/openclaw-agent.sqlite
PRAGMA journal_mode: wal
================================================================================

Session under test: 73f57495-1725-4ae7-8ef7-bb4608a501e9 (session_key agent:main:main).
Two real turns exist in this session: (1) the plan-17-01 tracer turn (chat only, no tool
call), (2) a plan-17-03 Task-1 driven turn that invoked the `exec` tool
(`openclaw agent --agent main --message [REDACTED-TEXT] --local --model
anthropic/claude-sonnet-4-6 --json`).

--- FIELD: per-completion token usage ---
Table: transcript_events. Column: event_json (JSON path $.message.usage.*).
Query: SELECT seq, json_extract(event_json,'$.message.role'),
  json_extract(event_json,'$.message.provider'), json_extract(event_json,'$.message.model'),
  json_extract(event_json,'$.message.usage.input'), json_extract(event_json,'$.message.usage.output'),
  json_extract(event_json,'$.message.usage.cacheRead'), json_extract(event_json,'$.message.usage.cacheWrite'),
  json_extract(event_json,'$.message.usage.totalTokens'), json_extract(event_json,'$.message.stopReason')
  FROM transcript_events WHERE session_id='73f57495-1725-4ae7-8ef7-bb4608a501e9' AND seq=5;
Raw output: 5|assistant|anthropic|claude-sonnet-4-6|74|9|0|17535|17618|stop
VERDICT: RECOVERABLE. Found in transcript_events.event_json -> $.message.usage
  (input, output, cacheRead, cacheWrite, totalTokens, plus a nested $.message.usage.cost
  object and $.message.usage.contextUsage — all present in the raw row, see
  schema-capture.sql.txt CELL HOST-LOCAL, table transcript_events). Zero loss vs. the raw
  Anthropic usage object.

--- FIELD: model id ---
Table: transcript_events. Column: event_json (JSON path $.message.model / $.message.provider).
Same query as above. Raw output fields 3-4: anthropic|claude-sonnet-4-6.
VERDICT: RECOVERABLE. $.message.provider and $.message.model are both present on every
  assistant-role message row; $.message.responseModel is also present (not queried above,
  visible in schema/raw capture from plan 17-01's tracer-turn-readback.txt) for the
  model that actually answered vs. the one requested.

--- FIELD: toolCalls (name, arguments, result, error, duration) ---
Table: transcript_events. Column: event_json (JSON path $.message.content[].type='toolCall'
  for the call; a second row with $.message.role='toolResult' for the result).
Query (call): SELECT seq, json_extract(event_json,'$.message.role'),
  json_extract(event_json,'$.message.content[1].type'),
  json_extract(event_json,'$.message.content[1].name'),
  json_extract(event_json,'$.message.content[1].id')
  FROM transcript_events WHERE session_id='73f57495-1725-4ae7-8ef7-bb4608a501e9' AND seq=9;
Raw output: 9|assistant|toolCall|exec|toolu_01Qg4SPbxkj4jK4E3v6sK3Dv
  Full row (arguments value redacted): {"type":"toolCall","id":"toolu_01Qg4SPbxkj4jK4E3v6sK3Dv",
  "name":"exec","arguments":{"command":"[REDACTED-TEXT]","title":"[REDACTED-TEXT]"}}
Query (result): SELECT seq, json_extract(event_json,'$.message.role'),
  json_extract(event_json,'$.message.toolName'), json_extract(event_json,'$.message.details.status'),
  json_extract(event_json,'$.message.details.exitCode'), json_extract(event_json,'$.message.details.exitReason'),
  json_extract(event_json,'$.message.details.durationMs'), json_extract(event_json,'$.message.isError')
  FROM transcript_events WHERE session_id='73f57495-1725-4ae7-8ef7-bb4608a501e9' AND seq=10;
Raw output: 10|toolResult|exec|completed|0|exit|2085|0
  Full row (content/aggregated values redacted): {"type":"message",...,"message":{"role":"toolResult",
  "toolCallId":"toolu_01Qg4SPbxkj4jK4E3v6sK3Dv","toolName":"exec",
  "content":[{"type":"text","text":"[REDACTED-TEXT]"}],
  "details":{"status":"completed","exitCode":0,"exitSignal":null,"exitReason":"exit",
  "durationMs":2085,"aggregated":"[REDACTED-TEXT]","noOutputTimedOut":false,
  "cwd":"/home/ubuntu/.openclaw/workspace"},"isError":false}}
VERDICT: RECOVERABLE — full fidelity. Tool name (`exec`), tool-call id, argument key
  structure, result status, exit code, exit reason, duration (ms), error flag (isError),
  and working directory are all present and joinable by `toolCallId`/`id` across the two
  rows. Only the argument VALUES and result CONTENT text are redacted here per this task's
  own evidence-hygiene requirement (T-17-05) — they are present verbatim in the live store,
  not absent from it.

--- FIELD: session parentage ---
Table: session_windows. Columns: session_id, session_key, previous_session_id,
  parent_session_key, spawned_by, model_provider, model.
Query: SELECT session_id, session_key, previous_session_id, parent_session_key, spawned_by,
  model_provider, model FROM session_windows WHERE session_id='73f57495-1725-4ae7-8ef7-bb4608a501e9';
Raw output: 73f57495-1725-4ae7-8ef7-bb4608a501e9|agent:main:main||||anthropic|claude-sonnet-4-6
Table: session_nodes. Columns: session_key, current_session_id, parent_session_key,
  spawned_by, fork_source_session_key, fork_source_session_id, created_via.
Query: SELECT session_key, current_session_id, parent_session_key, spawned_by,
  fork_source_session_key, fork_source_session_id, created_via
  FROM session_nodes WHERE session_key='agent:main:main';
Raw output: agent:main:main|73f57495-1725-4ae7-8ef7-bb4608a501e9|||||
VERDICT: COLUMN-LEVEL PRESENT, VALUE-LEVEL UNTESTED FOR A REAL PARENT/CHILD PAIR.
  session_windows.parent_session_key/spawned_by and session_nodes.parent_session_key/
  spawned_by/fork_source_session_key/fork_source_session_id are all present columns
  (see schema-capture.sql.txt CELL HOST-LOCAL) but are NULL for this session because
  it is a root session with no subagent spawned under it — that is the correct, expected
  value for a root session, not a missing-field finding. This probe did not drive a
  subagent-spawning turn to populate a real non-NULL parentage row; a future probe
  (SPIKE-02/plan 19) exercising `subagent_spawned`/`subagent_ended` is the way to confirm
  these columns actually populate correctly for a live parent/child pair. Recorded as
  present-but-unverified-with-real-data, not as a fidelity gap in the schema itself.

================================================================================
CELL SSHFS — NemoClaw/OpenShell path
Store: /home/ubuntu/nemoclaw-revenium-2-0-mount/agents/main/agent/openclaw-agent.sqlite
  (in-sandbox: /sandbox/.openclaw/agents/main/agent/openclaw-agent.sqlite)
PRAGMA journal_mode: wal
================================================================================

IMPORTANT: the read-only SQLite connection itself worked cleanly over SSHFS on every query
in this file — mode=ro, busy_timeout=5000, zero SQLITE_BUSY, zero stalls, zero errors. The
finding below is NOT a candidate-(a) read-path failure. It is an upstream, write-side
blocker that prevented ANY turn from completing in this sandbox during this probe run, so
there is currently no live completion to sample for the fidelity fields.

Three attempts were made to drive one real turn in the sandbox, all on this host/date:
  1. `openclaw agent --agent main --message [REDACTED-TEXT] --local --model
     nvidia/nemotron-3-super-120b-a12b --json` via `nemoclaw revenium-2-0 exec` —
     REFUSED outright: "'openclaw agent --local' is not supported inside NemoClaw
     sandboxes. The --local flag bypasses the gateway's security protections (secret
     scanning, network policy, inference auth) and can crash the sandbox." This is
     NemoClaw's own documented guard, not a bug.
  2. `openclaw agent --agent main --json -m [REDACTED-TEXT]` (gateway-routed, no
     --local) via `nemoclaw revenium-2-0 exec` — FAILED with:
     `{"ok":false,"error":{"type":"cli_error","message":"Auth profile store
     $OPENCLAW_HOME/.openclaw/agents/main/agent/openclaw-agent.sqlite requires legacy
     credential migration; run openclaw doctor --fix. | AUTH_PROFILE_MIGRATION_REQUIRED"}}`
  3. `nemoclaw revenium-2-0 agent --agent main --json -m [REDACTED-TEXT]` (NemoClaw's
     own first-class "run one agent turn" wrapper, not raw `exec`) — FAILED with the
     IDENTICAL error: AUTH_PROFILE_MIGRATION_REQUIRED.
Attempted remediation: `openclaw doctor --fix --non-interactive` (and plain `--fix`) via
  `nemoclaw revenium-2-0 exec` — FAILED: "Doctor could not enter maintenance. Stop the
  Gateway through its service owner, then run openclaw doctor --fix.
  StateDatabaseCoordinatorContentionError: another OpenClaw process owns
  gateway-lifecycle." Stopping the sandbox's live gateway to force this migration was
  judged out of scope for a read-only fidelity probe (risk of disrupting the shared
  host's other concurrent plan-17 work and the sandbox's own always-on posture) and was
  NOT attempted — this is recorded as a live finding, not routed around.

All three failed attempts still created a session_windows/session_nodes row (the CLI
reaches session-shell creation before the auth-profile check aborts it), confirmed via:
  SELECT session_id, session_key, model_provider, model, started_at, status
  FROM session_windows;
Raw output:
  e1c90898-2ca1-4b26-a788-92489d13d3bb|agent:main:main|||1790229269968|failed
  45e7c792-ea39-4858-8e4c-7afa00fa35c8|agent:main:main|||1790229533223|failed
  92d5ab49-f175-494a-ba5a-f4a595bca303|agent:main:main|||1790229670922|failed
  SELECT session_key, current_session_id, parent_session_key, spawned_by, created_via,
  status FROM session_nodes;
Raw output: agent:main:main|92d5ab49-f175-494a-ba5a-f4a595bca303|||run|failed
  (model_provider/model are empty because no model call ever happened; created_via='run'
  confirms attempt 3, NemoClaw's `agent` subcommand, is what created the surviving row.)

  SELECT count(*) FROM transcript_events;  ->  0

--- FIELD: per-completion token usage --- NOT RECOVERABLE THIS RUN (no completion exists to sample).
--- FIELD: model id --- NOT RECOVERABLE THIS RUN (session_windows.model is empty on all three
  failed session shells — the model call never happened).
--- FIELD: toolCalls --- NOT RECOVERABLE THIS RUN (zero transcript_events rows of any kind).
--- FIELD: session parentage --- COLUMN-LEVEL PRESENT (session_windows.parent_session_key/
  spawned_by and session_nodes.parent_session_key/spawned_by/fork_source_session_key exist
  in this cell's schema too — see schema-capture.sql.txt CELL SSHFS), same as HOST-LOCAL,
  but likewise unverified with real parent/child data.

VERDICT for this cell, this run: the direct-SQLite READ mechanism (candidate (a)) is
  confirmed viable over the SSHFS mount for schema/metadata reads (clean mode=ro,
  busy_timeout read of a live, actively-growing store — see journal_mode/table-count
  results above), but no live-turn fidelity data could be captured for this cell because
  every tested way of DRIVING a turn in this sandbox hit AUTH_PROFILE_MIGRATION_REQUIRED,
  and the documented fix (`doctor --fix`) requires stopping the sandbox's gateway, which
  this probe did not do. This is a genuine, reproducible, live-host finding — not an
  artifact of the read-path candidate being tested — and should be flagged to Phase 19/22
  as a NemoClaw-path blocker independent of SPIKE-01's read-path ranking.
