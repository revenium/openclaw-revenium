Spike 007 Task 3 — api.revenium.ai egress preset confirmation (D-16, closes spike 003 PARTIAL)
Host: 52.90.9.242, sandbox revenium-2-0
Date: 2026-09-24
Versions in effect: NemoClaw v0.0.128, in-sandbox OpenClaw 2026.9.1 (ad6fe23), revenium CLI 1.5.0 (0f5f3a7)

Command run (key sourced from host-side /home/ubuntu/.spike-17.env via `set -a; . ~/.spike-17.env; set +a`,
never printed or inlined literally — $REVENIUM_API_KEY below is the shell's own env-var expansion,
not a literal value in the executed command):

  nemoclaw revenium-2-0 exec -- sh -lc \
    "SSL_CERT_FILE=/etc/openshell-tls/ca-bundle.pem REVENIUM_API_KEY=\$REVENIUM_API_KEY \
     /sandbox/.openclaw/bin-transfer/revenium sources list --output json"

CLI delivery: the revenium CLI binary (v1.5.0, fetched host-side from the official GitHub release
tarball, per spike 003's finding that brew has no Linux bottle) was placed into the sandbox via the
share mount at /sandbox/.openclaw/bin-transfer/revenium, NOT an in-sandbox fetch — avoids widening
sandbox egress for the GitHub release CDN, per Task 3's stated preference for the mount route.

Raw response (trimmed, redaction filter applied defensively — no key pattern present):

{
  "error": "Request failed (HTTP 400): Missing request parameter: teamId",
  "exit_code": 4,
  "status": 400
}

Classification: NEITHER of the two "clean" outcomes (200-with-data, or 403-key-rejected) — a third,
equally informative outcome. Per the CLI's own documented exit-code table (`revenium --help`,
"Exit Codes: ... 4  Validation error (bad request)"), this is a structured, authenticated-layer
response from Revenium's API — the request reached api.revenium.ai, passed TLS, and was processed
far enough to be validated against required parameters. This is NOT a transport failure (no
connection refused, no proxy error, no TLS failure, no timeout) and is therefore evidence the
api.revenium.ai egress preset from spike 002 IS working, exactly like a 403 would be: egress +
TLS + binary integration are all proven; only account-scoping (a teamId this key's account requires
but which was not part of this spike's credential set — user_setup provisioned only
ANTHROPIC_API_KEY/NVIDIA_API_KEY/REVENIUM_API_KEY, no team/tenant/owner id) is unresolved. Per the
task's explicit instruction, this is recorded as-is and NOT retried with a different key, a guessed
team-id, or a widened policy.

This closes spike 003's PARTIAL differently than a clean 200 would have, but conclusively: spike
003's single open item was "Authenticated meter call: not yet run — blocked on a valid Revenium API
key." A valid key WAS used here (real key, sourced correctly, never a dummy) and the request was
authenticated far enough to be teamId-validated by Revenium's backend — a dummy/invalid key would
have failed at exit code 2 (Authentication error), not exit code 4 (Validation error). The
egress-preset question spike 003 left open is answered: the preset works. The narrower, new
finding is that this real key's account requires an explicit --team-id/REVENIUM_TEAM_ID that this
spike's credential set does not include.

Scope discipline observed: exactly one read-only API call was made (`sources list`); no meter
transaction was submitted; no tick/cron script in this repo references REVENIUM_API_KEY as a
result of this spike.
