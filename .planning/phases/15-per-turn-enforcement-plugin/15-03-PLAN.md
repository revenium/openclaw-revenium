---
phase: 15-per-turn-enforcement-plugin
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md
autonomous: false
requirements: [NCENF-01, NCENF-02]
user_setup:
  - service: nemoclaw-live-host
    why: "SC1-SC3 are only honestly verifiable against a real gateway turn on the live sandbox; isolated unit tests cannot prove trusted-plugin injection or the marker chain"
    dashboard_config:
      - task: "Ensure live host 34.224.27.67 is reachable and sandbox revenium-spike is running before this plan executes"
        location: "ssh 34.224.27.67; nemoclaw revenium-spike status (or recover if stopped)"
must_haves:
  truths:
    - "On the live sandbox, a real gateway turn shows <revenium-guard> in finalPromptText (SC1 — directive on every turn, trusted)"
    - "openclaw plugins inspect shows before_prompt_build AND before_agent_finalize trusted/active (SC1/SC3/SC5)"
    - "A turn under a halted guardrail-status.json honors the halt directive under NemoClaw (SC2)"
    - "A substantive turn writes a marker that lands in <mount>/markers and the host loop can meter it (SC3 — attribution flows)"
    - "A hook error/timeout does not block the agent reply (SC4 fail-open) — verified on live host"
    - "Results recorded in 15-VALIDATION.md mapping each observation to SC1-SC5"
  artifacts:
    - path: ".planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md"
      provides: "Live-host validation record mapping observations to SC1-SC5"
      contains: "SC1"
  key_links:
    - from: "live sandbox turn"
      to: "finalPromptText"
      via: "openclaw agent --json on host 34.224.27.67"
      pattern: "revenium-guard"
---

<objective>
Validate the combined `revenium-enforcement` plugin end-to-end on the LIVE sandbox host 34.224.27.67 (sandbox `revenium-spike`) via the gateway turn path — the only honest verification of SC1-SC3 (spike 006 proved isolated unit tests are insufficient; a hand-stub passed locally yet hung live). Run the real install path from Plan 02, then assert: the `<revenium-guard>` directive reaches every turn (trusted, not inert), a halted status is honored under NemoClaw, the marker chain writes attribution over the mount, and a hook fault is fail-open. Record results in `15-VALIDATION.md` mapped to SC1-SC5.

Purpose: Close the highest-risk requirement (NCENF-01) on real hardware before the phase ships. This plan is NON-AUTONOMOUS — it runs commands against a live remote host and pauses for human confirmation of the live observations.
Output: `15-VALIDATION.md` with per-criterion evidence.

NOTE: Live validation runs on remote host 34.224.27.67, NOT this laptop. All `nemoclaw`/`openclaw` commands execute over ssh to that host against sandbox `revenium-spike`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-turn-enforcement-plugin/15-CONTEXT.md
@.planning/phases/15-per-turn-enforcement-plugin/15-PATTERNS.md
@scripts/post-install-nemoclaw.sh
@.claude/skills/spike-findings-openclaw-revenium/sources/006-plugin-directive-injection/README.md
@.claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the real install path on the live sandbox and capture install-gate evidence (SC1/SC3/SC5)</name>
  <files>.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md</files>
  <read_first>
    - scripts/post-install-nemoclaw.sh (the install_skill_nemoclaw + install_enforcement_plugin functions from Plan 02; the fail-HARD gate)
    - .planning/phases/15-per-turn-enforcement-plugin/15-CONTEXT.md (§specifics: install sequence + live host 34.224.27.67 / sandbox revenium-spike)
    - .claude/skills/spike-findings-openclaw-revenium/sources/006-plugin-directive-injection/README.md (exact install/recover/validate sequence + the trusted-vs-inert finding)
  </read_first>
  <action>
    On the live host 34.224.27.67 against sandbox `revenium-spike` (over ssh; honor MEMORY: ssh via the user's agent socket, set PATH for revenium/nemoclaw; nemoclaw exec rejects newline argv): export `REVENIUM_SANDBOX_NAME=revenium-spike`, ensure the sandbox is running (`nemoclaw revenium-spike recover` if needed), then run the Plan 02 install path — first `install_skill_nemoclaw` then `install_enforcement_plugin` (either by invoking `scripts/post-install-nemoclaw.sh` end-to-end, or by sourcing it and calling the two functions if upstream provisioning is already ledgered). Capture the full output. Confirm the install completed NON-ZERO-free (i.e. did not abort) — which means its own fail-HARD gates passed: `<revenium-guard>` present in `openclaw agent --json` finalPromptText (SC1), `before_prompt_build` + `before_agent_finalize` present in `openclaw plugins inspect revenium-enforcement` and reported trusted (SC1/SC3/SC5), python3 present, and a marker appeared over the mount (SC3). Independently re-run `nemoclaw revenium-spike exec -- sh -lc "openclaw plugins inspect revenium-enforcement"` and copy the trust/hook lines into `15-VALIDATION.md`. Record exact command output (redact secrets) under headings SC1, SC3, SC5 in `15-VALIDATION.md`. If the install ABORTS (non-zero), capture the failing gate output, STOP, and record it as a blocker — do not fabricate a pass (spike 006 lesson).
  </action>
  <verify>
    <automated>test -f .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md && grep -q "revenium-guard" .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md && grep -q "before_prompt_build" .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The install path ran on 34.224.27.67 / revenium-spike and completed without aborting (its fail-HARD gates passed) OR the abort is captured as a recorded blocker
    - `15-VALIDATION.md` contains the live `openclaw plugins inspect revenium-enforcement` output showing before_prompt_build + before_agent_finalize as trusted/active
    - `15-VALIDATION.md` contains the live finalPromptText evidence with the `<revenium-guard>` tag (SC1)
    - Evidence is real captured command output, not narrative
  </acceptance_criteria>
  <done>The enforcement plugin is installed and trusted on the live sandbox, with finalPromptText + inspect evidence recorded for SC1/SC3/SC5.</done>
</task>

<task type="auto">
  <name>Task 2: Live halt-honoring + marker-attribution + fail-open checks (SC2/SC3/SC4)</name>
  <files>.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md</files>
  <read_first>
    - .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md (the SC1/SC3/SC5 evidence from Task 1)
    - BUDGET-GUARD.md (the halt/warn branches the directive points the agent to)
    - SKILL.md §"HALT CHECK" / "Guardrail Check Procedure" (the exact halt + warn-and-ask behavior)
    - scripts/write-marker.sh (local markers/<sid>.jsonl output the host loop reads)
  </read_first>
  <action>
    On the live sandbox, perform three live checks and record each in `15-VALIDATION.md` under SC2/SC3/SC4:
    (1) SC2 halt-honoring: write a `halted: true` `guardrail-status.json` into the in-sandbox skill path over the mount (set `haltedRule` per the skill contract), run a real gateway turn (`openclaw agent --json --message "do something"`), and confirm the agent's reply is the halt message (does not proceed). Restore a clean status afterward. Capture the turn output.
    (2) SC3 marker-attribution: run a substantive turn that invokes an exec/bash tool, confirm a `markers/<sid>.jsonl` lands in `<mount>/markers/`, and confirm the Phase 14 host loop reads/meters it (or that the marker is well-formed for it). Capture the marker file contents.
    (3) SC4 fail-open: simulate a hook fault tolerance check — confirm that even under an induced error path the turn still returns a reply (e.g. observe that the plugin's try/catch design plus the unit-test fail-open coverage from Plan 01 hold, and that a normal live turn after any transient plugin error still completes). Record the observation. For any check that genuinely cannot be exercised remotely (e.g. inducing a real hook timeout), note it explicitly and reference the Plan 01 unit-test fail-open coverage as the standing evidence rather than fabricating a live result.
  </action>
  <verify>
    <automated>grep -q "SC2" .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md && grep -q "SC3" .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md && grep -q "SC4" .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `15-VALIDATION.md` records a live turn under `halted:true` where the agent emits the halt message and does not proceed (SC2), with the status restored afterward
    - `15-VALIDATION.md` records a marker file landing in `<mount>/markers/` from a substantive turn (SC3) with its contents
    - `15-VALIDATION.md` records the fail-open observation (SC4), either live or explicitly deferred to the Plan 01 unit-test evidence with a stated reason
    - No fabricated results — any non-exercisable check is named as such
  </acceptance_criteria>
  <done>SC2 halt-honoring, SC3 marker attribution, and SC4 fail-open are evidenced (live where possible, explicitly cross-referenced where not) in 15-VALIDATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The combined revenium-enforcement plugin installed and validated on the live sandbox host 34.224.27.67 (revenium-spike): per-turn <revenium-guard> directive injection, trusted hooks, halt-honoring, marker attribution, and fail-open — recorded in 15-VALIDATION.md.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md` and confirm each of SC1-SC5 has real captured evidence (not narrative).
    2. SC1: `<revenium-guard>` appears in a live finalPromptText AND `openclaw plugins inspect revenium-enforcement` shows before_prompt_build trusted/active.
    3. SC2: a live `halted:true` turn produced the halt message (agent did not proceed).
    4. SC3: a marker landed in `<mount>/markers/` from a substantive turn and before_agent_finalize is active in inspect.
    5. SC4: fail-open evidence present (live or cross-referenced to Plan 01 unit tests with a reason).
    6. SC5: plugin authored from scaffold shape with configSchema + openclaw.extensions (confirmed via the committed plugin-nemoclaw/ + trusted install).
    7. Confirm no install ABORT was silently overridden — if the install aborted, it is recorded as an open blocker, not a pass.
  </how-to-verify>
  <resume-signal>Type "approved" if SC1-SC5 are honestly evidenced on the live host, or describe which criterion lacks evidence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this laptop → ssh → live host 34.224.27.67 | Remote command execution over the user's agent socket against a live sandbox |
| host → nemoclaw exec → in-sandbox gateway turn | Live agent turns drive the validation; a misbehaving plugin could hang the turn |
| mount write of halted status → agent behavior | A forced halt status drives enforcement; must be restored to avoid leaving the sandbox halted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Denial of Service | live turn hangs (spike-006 mode) | mitigate | The Plan 02 install gate already fail-HARD aborts on a hung/inert plugin before this plan; if a live turn hangs here, record as blocker and uninstall via scripts/uninstall-enforcement-nemoclaw.sh rather than leaving a broken gateway (Task 1/2) |
| T-15-11 | Tampering | leftover halted:true status after SC2 | mitigate | Task 2 restores a clean guardrail-status.json immediately after the halt-honoring check; checkpoint confirms the sandbox is not left halted |
| T-15-12 | Repudiation | fabricated live pass | mitigate | Acceptance criteria require real captured command output; the human checkpoint blocks on honest SC1-SC5 evidence and explicitly rejects narrative-only or overridden-abort passes (spike 006 lesson) |
| T-15-SC | Tampering | install over mount on live host | mitigate | Reuses the Plan 02 trusted `openclaw plugins install` + inspect gate; no new package fetch on the live host |
</threat_model>

<verification>
- 15-VALIDATION.md exists with real captured evidence for SC1-SC5
- SC1: <revenium-guard> in live finalPromptText + trusted hooks in inspect
- SC2: live halt-honoring turn captured; status restored
- SC3: marker over the mount + before_agent_finalize active
- SC4: fail-open evidenced (live or cross-referenced)
- SC5: trusted install of the scaffold-shaped plugin with configSchema + openclaw.extensions
- Human checkpoint blocks on honest evidence; an aborted install is recorded as a blocker, never overridden
</verification>

<success_criteria>
- The enforcement plugin is installed, trusted, and validated on the live sandbox revenium-spike (34.224.27.67)
- SC1-SC5 each have real captured evidence in 15-VALIDATION.md
- The sandbox is left healthy (not halted, plugin trusted/active)
- The human checkpoint is approved on honest evidence
</success_criteria>

<output>
Create `.planning/phases/15-per-turn-enforcement-plugin/15-03-SUMMARY.md` when done
</output>
