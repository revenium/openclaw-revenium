---
phase: 15-per-turn-enforcement-plugin
plan: "06"
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - scripts/post-install-nemoclaw.sh
  - plugin/src/gate.js
  - plugin/src/index.ts
  - plugin/src/index.test.js
  - plugin-nemoclaw/src/index.ts
  - plugin-nemoclaw/src/index.test.js
  - plugin-nemoclaw/src/gate.js
  - plugin-nemoclaw/dist/index.js
  - .planning/phases/15-per-turn-enforcement-plugin/15-B05-SCHEMA-PROBE.md
autonomous: true
requirements: [NCENF-01, NCENF-02]

must_haves:
  truths:
    - "CR-01 (NCENF-01 / SC1): when Gate A receives a JSON with NO promptChars field, the script prints the actionable 'guard directive NOT injected — could not parse currentTurn.promptChars' diagnostic AND exits non-zero — it fails CLOSED with the message, not a bare pipefail abort."
    - "WR-01 (NCENF-02): a marker exec followed by a non-string-command exec in the same run no longer downgrades the disk record from marked:true to marked:false — persistRunState on the non-string path preserves the prior marked state via markedTaskRuns.has(runId)."
    - "B-05 (NCENF-02 / SC3): the real Nemotron tool_search_code exec transcript schema is captured live from host 34.224.27.67 BEFORE any observation code is written, and handleBeforeAgentFinalize gains a transcript-scan observation source (unioned with the existing before_tool_call path) that classifies a run from a tool_search_code toolResult containing an openclaw:core:exec invocation. The transcript reaches handleBeforeAgentFinalize at RUNTIME through the safeBeforeAgentFinalize wrapper (the wrapper threads the transcript to impl — not only via direct test calls)."
    - "gate.js remains the single source of truth (D-06): every gate.js change is made only in plugin/src/gate.js; plugin-nemoclaw/src/gate.js is regenerated as a byte-identical build copy (diff -q identical)."
    - "Fail-open contract (SC4) preserved: the transcript scan and the WR-01 fix never throw and never block the reply; all existing plugin and plugin-nemoclaw unit tests stay green."
  artifacts:
    - path: ".planning/phases/15-per-turn-enforcement-plugin/15-B05-SCHEMA-PROBE.md"
      provides: "Live-captured Nemotron tool_search_code exec transcript schema: exact message fields, toolName value, and where the openclaw:core:exec string appears (content vs params vs result) — the contract the observation code is written against"
      contains: "tool_search_code"
    - path: "scripts/post-install-nemoclaw.sh"
      provides: "Gate A _prompt_chars pipeline guarded with || true so the no-match path reaches the -z diagnostic and fails closed with the actionable message"
      contains: "|| true"
    - path: "plugin/src/gate.js"
      provides: "WR-01 non-downgrade fix on the non-string-command exec path + a transcript-scan observation function consumed by handleBeforeAgentFinalize + safeBeforeAgentFinalize wrapper extended to thread the transcript to impl"
      contains: "markedTaskRuns.has(runId)"
    - path: "plugin-nemoclaw/src/gate.js"
      provides: "Byte-identical build copy of plugin/src/gate.js (D-06)"
      contains: "markedTaskRuns.has(runId)"
  key_links:
    - from: "scripts/post-install-nemoclaw.sh Gate A _prompt_chars pipeline"
      to: "the -z diagnostic on the no-match path"
      via: "|| true guard preventing pipefail abort"
      pattern: "\\|\\| true"
    - from: "plugin/src/gate.js handleBeforeAgentFinalize"
      to: "Nemotron tool_search_code exec evidence in the conversation transcript"
      via: "transcript-scan observation unioned with the existing execRuns / disk-fallback path"
      pattern: "openclaw:core:exec"
    - from: "plugin/src/index.ts before_agent_finalize handler"
      to: "handleBeforeAgentFinalize transcript argument"
      via: "ctx transcript field passed to safeBeforeAgentFinalize, which threads it through to impl(runId, transcript) at runtime"
      pattern: "before_agent_finalize"
    - from: "plugin/src/gate.js safeBeforeAgentFinalize wrapper"
      to: "handleBeforeAgentFinalize transcript parameter"
      via: "wrapper signature extended to accept the transcript in a non-opts-colliding position and body forwards it as impl(runId, transcript)"
      pattern: "impl\\(runId, transcript\\)"
---

<objective>
Close all three remaining Phase 15 verification gaps IN CODE (the live re-validation is plan 15-07, which depends on this plan):

- **CR-01 (BLOCKER, NCENF-01 / SC1):** Gate A's `_prompt_chars` parse pipeline in `scripts/post-install-nemoclaw.sh` runs under `set -euo pipefail` with no `|| true` guard. When the agent JSON lacks a `promptChars` field (the exact failure Gate A exists to detect), the first `grep` exits 1, pipefail aborts the command substitution before the `-z` check, and the actionable diagnostic is dead code. Add the `|| true` guard so the gate fails CLOSED with its message.
- **WR-01 (WARNING, NCENF-02):** `plugin/src/gate.js` line ~189 calls `persistRunState(runId, false)` on the non-string-command exec path, unconditionally downgrading a prior `marked:true` disk record. Change it to `persistRunState(runId, markedTaskRuns.has(runId))` and propagate via the D-06 build copy + add the WR-04 regression test in both suites.
- **B-05 (BLOCKER, NCENF-02 / SC3):** The Nemotron model routes all shell exec through `tool_search_code` + `openclaw.tools.call('openclaw:core:exec', ...)`, so `before_tool_call` never fires and no run-state is written. Per the USER-SELECTED remediation, add a transcript-scan observation in `handleBeforeAgentFinalize` (which already runs with `allowConversationAccess: true`) that detects exec evidence in `tool_search_code` toolResults and drives the existing observe→revise→write-marker.sh flow. The transcript scan is an ADDITIONAL source unioned with the existing `before_tool_call` path — NOT a replacement. The observation code MUST be written against the REAL schema captured by a mandatory live probe (Task 1) — do not hardcode field names until the probe confirms them. The transcript MUST reach handleBeforeAgentFinalize at RUNTIME: the deployed path is `index.ts → safeBeforeAgentFinalize(...) → impl(runId, ...)`, so the `safeBeforeAgentFinalize` wrapper itself must be extended to accept and forward the transcript (its current `return impl(runId)` body drops every extra arg, and its second positional is already `opts`).

Purpose: NCENF-01's install gate currently fails opaquely on real injection failure (CR-01), the disk-persistence guarantee is undermined by a downgrade bug (WR-01), and NCENF-02 produces no end-to-end marker on Nemotron (B-05). All three must be fixed before the phase goal is achievable.

Output: a live schema-probe record (15-B05-SCHEMA-PROBE.md), the CR-01 `|| true` fix, the WR-01 non-downgrade fix, the B-05 transcript-scan observation in the single-source `gate.js` (D-06) threaded through the `safeBeforeAgentFinalize` wrapper and wired through both `index.ts` files, regenerated byte-identical nemoclaw `gate.js` + committed `dist/`, and new regression/transcript-scan tests in both suites (including a test that drives the transcript THROUGH `safeBeforeAgentFinalize`) — all unit suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-turn-enforcement-plugin/15-VERIFICATION.md
@.planning/phases/15-per-turn-enforcement-plugin/15-CONTEXT.md
@.planning/phases/15-per-turn-enforcement-plugin/15-PATTERNS.md
@.planning/phases/15-per-turn-enforcement-plugin/15-04-SUMMARY.md
@.planning/phases/15-per-turn-enforcement-plugin/15-05-SUMMARY.md
@./CLAUDE.md
@.claude/skills/spike-findings-openclaw-revenium/SKILL.md
@.claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the REAL Nemotron tool_search_code exec transcript schema on the live host (B-05 mandatory first step)</name>
  <files>.planning/phases/15-per-turn-enforcement-plugin/15-B05-SCHEMA-PROBE.md</files>
  <read_first>
    - .planning/phases/15-per-turn-enforcement-plugin/15-VERIFICATION.md (B-05 root cause lines 189-206: Nemotron routes exec through tool_search_code + openclaw.tools.call('openclaw:core:exec', ...); toolName=tool_search_code not exec; session rv-b05-1780978277 evidence)
    - .planning/phases/15-per-turn-enforcement-plugin/15-05-SUMMARY.md (the live re-validation that diagnosed B-05; the session id, the NO_RUN_STATE / NO_MARKERS observation, the exact gateway/turn invocation used)
    - plugin-nemoclaw/src/index.ts lines 59-68 (the before_agent_finalize handler already registered with allowConversationAccess:true — the hook that will receive the transcript; today it passes only ctx?.runId)
    - plugin/src/gate.js lines 218-250 (handleBeforeAgentFinalize current signature takes only runId — Task 4 extends it to also accept a transcript)
    - .claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md (install → recover → gateway-turn sequence; how to drive a turn over nemoclaw exec)
    - .claude/skills/spike-findings-openclaw-revenium/SKILL.md (live host 34.224.27.67 / sandbox revenium-spike; /sandbox/.openclaw layout)
    - MEMORY: nemoclaw-spike-host / clawhub-test-host / revenium-cli-sandbox-gotchas / b05-nemotron-tool-search-code-exec (live host access; nemoclaw exec rejects newline argv → single-line sh -lc only; api-key field; the confirmed tool_search_code indirection)
  </read_first>
  <action>
    On the live host 34.224.27.67 (sandbox revenium-spike), capture a REAL Nemotron turn in which the model runs a shell command, and record the exact transcript/conversation schema the before_agent_finalize hook will see. This is the single source of truth the Task 4 observation code is written against — do NOT assume or hardcode any field name before this probe confirms it.

    Honor the live constraints: nemoclaw exec rejects newline argv (single-line sh -lc only); the share mount is the Phase 14 mount; revenium reads the key from the api-key field in config.yaml.

    Steps:
    1. With the currently-installed revenium-enforcement plugin loaded, temporarily add a diagnostic log inside the before_agent_finalize handler (or a one-off instrumented dist) that JSON-stringifies the conversation/transcript object the hook receives (ctx.conversation, or whatever the real ctx field is) to the plugin logger, so a real turn dumps the structure. Drive a substantive turn (a message that makes Nemotron run a shell command such as echo) in a fresh session id; turns take ~70-90s with the plugin, so use a 120-180s timeout.
    2. From the captured dump (and corroborated by the raw session .jsonl if accessible), record in 15-B05-SCHEMA-PROBE.md the EXACT, confirmed answers to: (a) what is the field path from the hook ctx to the message list (e.g. ctx.conversation.messages); (b) what is the literal toolName value for the wrapper call (expect "tool_search_code"); (c) WHERE the "openclaw:core:exec" string actually appears — in the message content, in a toolResult content field, in params, or in a result field — with the exact JSON key path and a representative (redacted) excerpt; (d) what distinguishes a toolResult/tool message from an assistant/user message in this schema; (e) whether a runId is present on the messages or only on the hook ctx.
    3. Remove the temporary diagnostic instrumentation and restore the sandbox to a healthy state (recover; restore guardrail-status.json if touched) exactly as plan 15-05 did. If a stray SUMMARY.md appears in a worktree merge, rm it and git merge --ff-only per the known worktree-cleanup gotcha.

    Write 15-B05-SCHEMA-PROBE.md with a `## Confirmed Schema` section (the five answers above with literal key paths and a redacted excerpt) and a `## Capture Evidence` section (the exact commands, session id, exit codes, and the raw dump excerpt). If the probe reveals the schema is NOT as the verification report assumed (e.g. exec evidence lives somewhere unexpected, or the transcript is not reachable from ctx at all), record that honestly and STOP — flag it for the plan reviewer rather than writing observation code against a guessed schema.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-per-turn-enforcement-plugin/15-B05-SCHEMA-PROBE.md &amp;&amp; grep -q "tool_search_code" .planning/phases/15-per-turn-enforcement-plugin/15-B05-SCHEMA-PROBE.md &amp;&amp; grep -q "Confirmed Schema" .planning/phases/15-per-turn-enforcement-plugin/15-B05-SCHEMA-PROBE.md</automated>
  </verify>
  <acceptance_criteria>
    - 15-B05-SCHEMA-PROBE.md exists with a `## Confirmed Schema` section answering all five questions (message-list field path, toolName literal, openclaw:core:exec location key path, tool-vs-other message discriminator, runId presence) with LITERAL JSON key paths — not prose guesses.
    - The `## Capture Evidence` section shows the live command(s), the session id, exit codes, and a redacted excerpt of the real dumped transcript containing the literal strings `tool_search_code` and `openclaw:core:exec`.
    - The temporary diagnostic instrumentation was removed (no leftover debug dump in committed plugin source/dist): `grep -c "JSON.stringify(ctx" plugin/src/gate.js plugin-nemoclaw/src/gate.js` returns 0 for both files.
    - The sandbox-restore commands are recorded; host left healthy.
    - If the schema is NOT as assumed, the file records the discrepancy and explicitly halts observation-code work (no guessed-schema code shipped).
  </acceptance_criteria>
  <done>
    15-B05-SCHEMA-PROBE.md records the live-confirmed Nemotron tool_search_code exec transcript schema (exact field paths + redacted excerpt) that Task 4's transcript-scan observation is written against, with capture evidence and a healthy restored sandbox; no debug instrumentation left in committed code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-01 — add the missing || true guard to Gate A's _prompt_chars pipeline</name>
  <files>scripts/post-install-nemoclaw.sh</files>
  <read_first>
    - scripts/post-install-nemoclaw.sh lines 226-238 (the _min_prompt_chars threshold at 226; the _prompt_json capture at 228-229 which ALREADY has || true; the _prompt_chars pipeline at 230-231 which LACKS it; the -z diagnostic at 232-234; the threshold-compare at 235-237)
    - scripts/post-install-nemoclaw.sh line ~30 (set -euo pipefail — the reason the unguarded pipeline aborts the command substitution)
    - .planning/phases/15-per-turn-enforcement-plugin/15-VERIFICATION.md (CR-01 section lines 175-185 + anti-pattern table line 151: exact fix is appending || true to the _prompt_chars pipeline; acceptance = a no-promptChars JSON prints the diagnostic and exits non-zero)
  </read_first>
  <action>
    In `install_enforcement_plugin()`, change the `_prompt_chars` assignment (currently lines ~230-231) so the parse pipeline ends with `|| true`, matching the guard already present on the `_prompt_json` capture at line 229. The corrected assignment ends the command substitution with the literal ` || true` after `head -1`, so the substitution succeeds with an empty result on a no-match instead of aborting under pipefail. Control then reaches the `-z "${_prompt_chars}"` check on line 232 and the actionable `fail "guard directive NOT injected — could not parse currentTurn.promptChars ..."` diagnostic on line 233 fires (fail closed WITH the message, not a bare pipefail exit). Do NOT change the threshold value, the diagnostic message text, the `fail` (not `warn`) hard-gate semantics (D-09), or the Gate B/C/D blocks. Optionally apply WR-02 scoping (restrict the first grep to the currentTurn object) ONLY if it is minimal and does not regress the live-passing path; the `|| true` guard is the required fix.
  </action>
  <verify>
    <automated>bash -n scripts/post-install-nemoclaw.sh &amp;&amp; grep -v '^[[:space:]]*#' scripts/post-install-nemoclaw.sh | grep -A2 '_prompt_chars=' | grep -c '|| true' | grep -q '[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/post-install-nemoclaw.sh` exits 0 (syntax valid).
    - The `_prompt_chars=$(...)` command substitution ends with `|| true` (the grep gate above, comments stripped, finds it in the pipeline region).
    - A reproduction assertion passes: feeding the Gate A pipeline a JSON with NO promptChars field, under `set -euo pipefail`, yields an empty `_prompt_chars` and the `-z` branch fires (the diagnostic path is reached, not aborted). Capture this in the SUMMARY as a `bash -c 'set -euo pipefail; ...'` run that prints the diagnostic and exits non-zero rather than a bare pipefail exit 1.
    - `grep -c "finalPromptText" scripts/post-install-nemoclaw.sh` still returns 0 (no regression of the B-01 removal).
    - Gate B/C/D unchanged: `grep -c "plugins inspect revenium-enforcement" scripts/post-install-nemoclaw.sh` still returns 1; the python3 and marker-smoke gates are intact.
  </acceptance_criteria>
  <done>
    Gate A's _prompt_chars pipeline carries `|| true` so a no-promptChars JSON reaches the `-z` check and the script prints the actionable "could not parse currentTurn.promptChars" diagnostic and exits non-zero (fails closed WITH the message). `bash -n` is clean; Gate B/C/D and the B-01 removal are untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix WR-01 — preserve marked state on the non-string-command exec path in plugin/src/gate.js</name>
  <files>plugin/src/gate.js, plugin/src/index.test.js</files>
  <read_first>
    - plugin/src/gate.js lines 117-128 (persistRunState signature {exec,marked,updatedAt}); lines 185-201 (the non-string guard at 186-191 calling persistRunState(runId, false) at line 189 — the WR-01 site; the normal path at 201 which correctly ORs isMarked || markedTaskRuns.has(runId))
    - plugin/src/index.test.js lines 1-37 (imports from ./gate.js; resetState in beforeEach; RUN_A/RUN_B fixtures) and the persistence describe block added in 15-04 (around lines 300-365) to mirror its tmp-dir + setRunStateDir setup
    - .planning/phases/15-per-turn-enforcement-plugin/15-VERIFICATION.md (WR-01 section lines 39-49 + 210-223: exact fix persistRunState(runId, markedTaskRuns.has(runId)); WR-04 regression test sequence = marker exec → non-string-command exec → resetState() → handleBeforeAgentFinalize returns undefined)
  </read_first>
  <behavior>
    - Test (WR-04): sequence marker exec (handleBeforeToolCall with a command invoking write-marker.sh) → non-string-command exec (handleBeforeToolCall with a non-string command on the same runId) → resetState() (simulating a nemoclaw recover that clears in-process Sets) → handleBeforeAgentFinalize(runId) returns undefined (pass-through) because the disk record still says marked:true — NO spurious revise.
    - Test: directly assert the disk record after the marker→non-string sequence parses to marked === true (the non-string path did not downgrade it).
    - Test: a non-string-command exec on a runId that was NEVER marked still persists marked:false (no false upgrade) — the fix is a non-DOWNGRADE, not an unconditional true.
    - Test: all existing plugin/src/index.test.js cases still pass unchanged.
  </behavior>
  <action>
    Edit ONLY `plugin/src/gate.js` (single source of truth — D-06; never hand-edit the nemoclaw copy). On the non-string-command exec path inside `handleBeforeToolCall` (the guard block at lines 186-191), change the persistence call from `persistRunState(runId, false)` to `persistRunState(runId, markedTaskRuns.has(runId))` so a prior marker classification recorded for this runId is preserved rather than downgraded to false. This mirrors the OR already used on the normal path at line 201. Do not change any other behavior of the guard block (execRuns.add(runId) and the early return stay).

    Add the WR-04 regression describe-block to `plugin/src/index.test.js` (do NOT modify existing cases): use the same tmp-dir + setRunStateDir isolation pattern as the 15-04 persistence block. Reference the gap id (`WR-01` / `WR-04`) in the describe or test name. Do NOT touch `plugin-nemoclaw/src/gate.js` — it is regenerated at build (Task 5).
  </action>
  <verify>
    <automated>node --test plugin/src/index.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "persistRunState(runId, markedTaskRuns.has(runId))" plugin/src/gate.js` returns TWO matches (the existing normal path used `isMarked || markedTaskRuns.has(runId)`; the non-string path now uses `markedTaskRuns.has(runId)`) — specifically `grep -c "persistRunState(runId, false)" plugin/src/gate.js` returns 0 (no hardcoded-false persist remains).
    - `node --test plugin/src/index.test.js` exits 0 with the new WR-04 cases added beyond the prior 37.
    - `grep -c "WR-01\|WR-04" plugin/src/index.test.js` returns >= 1 (the regression block cites the gap).
    - No edit to plugin-nemoclaw/src/gate.js in this task: `git diff --name-only` does NOT list `plugin-nemoclaw/src/gate.js` after Task 3 (it is regenerated in Task 5).
    - No existing test name removed.
  </acceptance_criteria>
  <done>
    The non-string-command exec path in plugin/src/gate.js persists `markedTaskRuns.has(runId)` instead of hardcoded false, so a marker→non-string sequence no longer downgrades the disk record; the WR-04 regression (marker exec → non-string exec → resetState → finalize returns undefined) passes and all prior tests stay green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add the B-05 transcript-scan observation to handleBeforeAgentFinalize (against the Task 1 confirmed schema), thread it through the safeBeforeAgentFinalize wrapper, and wire it through both index.ts files</name>
  <files>plugin/src/gate.js, plugin/src/index.ts, plugin/src/index.test.js, plugin-nemoclaw/src/index.ts</files>
  <read_first>
    - .planning/phases/15-per-turn-enforcement-plugin/15-B05-SCHEMA-PROBE.md (Task 1 output — the LITERAL confirmed schema: the ctx→messages field path, the tool_search_code toolName literal, the exact JSON key path where openclaw:core:exec appears, the tool-message discriminator, runId presence — write the scan against THIS, not assumptions)
    - plugin/src/gate.js lines 80-107 (execRuns/markedTaskRuns Sets, MARKER_INVOKE regex — reuse the same "is this a real exec, not a mere mention" discipline); lines 218-273 (handleBeforeAgentFinalize current signature(runId) and the in-process + disk-fallback union, plus _buildReviseAction — the static revise instruction that must stay byte-identical)
    - plugin/src/gate.js lines ~316-335 (the EXPORTED safeBeforeAgentFinalize wrapper — current signature `safeBeforeAgentFinalize(runId, opts = {}, impl = handleBeforeAgentFinalize)` whose body is `return impl(runId)`. CRITICAL: `opts` is the SECOND positional and the body DROPS every arg after runId, so the runtime path index.ts → safeBeforeAgentFinalize → impl currently CANNOT deliver a transcript. Task 4 MUST extend this wrapper too, or the transcript never reaches handleBeforeAgentFinalize at runtime despite unit tests passing.)
    - plugin/src/index.ts lines 40-49 (the before_agent_finalize handler; today it calls `safeBeforeAgentFinalize(ctx?.runId, { log: (msg) => api.log?.(msg) })` — logger field is `api.log?.`; extend it to also pass the transcript field confirmed by Task 1; before_prompt_build is NOT a conversation hook and must not gain one)
    - plugin-nemoclaw/src/index.ts lines 59-68 (the identical handler in the nemoclaw wiring; today it calls `safeBeforeAgentFinalize(ctx?.runId, { log: (msg) => api.logger.info(msg) })` — logger field is `api.logger.info`, DIFFERENT from plugin/src/index.ts; apply the same ctx-transcript pass-through but PRESERVE this file's `api.logger.info` field; this file is NOT regenerated by bake-directive, so edit it directly to match plugin/src/index.ts in structure)
    - .planning/phases/15-per-turn-enforcement-plugin/15-VERIFICATION.md (B-05 remediation option (b) lines 204 + 27: transcript scan in before_agent_finalize, allowConversationAccess already true; union with existing source, do not replace)
    - plugin/src/index.test.js (the persistence + WR-04 describe blocks — mirror their structure for a new transcript-scan describe block; resetState isolation)
  </read_first>
  <behavior>
    - Test: handleBeforeAgentFinalize, given a runId with EMPTY in-process Sets and NO disk state, but a transcript argument containing a tool_search_code toolResult whose content includes an `openclaw:core:exec` invocation (shaped per the Task 1 confirmed schema), returns the revise action (the run is classified substantive from the transcript).
    - Test: the same transcript that ALSO contains evidence the agent ran write-marker.sh (a tool_search_code exec of write-marker.sh, matched with the same MARKER_INVOKE discipline) → handleBeforeAgentFinalize returns undefined (already classified — no revise).
    - Test: a transcript with NO exec evidence (only assistant/user messages) → handleBeforeAgentFinalize returns undefined (non-substantive, pass-through).
    - Test (union, not replacement): the existing in-process path still wins — when execRuns.has(runId) is true and unmarked, the revise action is returned EVEN IF no transcript is passed (existing non-Nemotron behavior intact); when the disk fallback marks it, transcript absence still passes through.
    - Test (WRAPPER THREADING — the load-bearing B-05 path): drive the transcript THROUGH the exported `safeBeforeAgentFinalize` wrapper (NOT only `handleBeforeAgentFinalize` directly). Given a runId with empty Sets/no disk state and a transcript containing a tool_search_code openclaw:core:exec result (no write-marker.sh), `safeBeforeAgentFinalize(runId, transcript, ...)` (in the wrapper's chosen non-opts-colliding arg order) returns the revise action — proving the wrapper forwards the transcript to impl. A companion case: the same call with a transcript that DOES show write-marker.sh returns undefined through the wrapper.
    - Test (fail-open): a malformed/undefined transcript (null, wrong shape, circular-ish) does NOT throw — handleBeforeAgentFinalize / safeBeforeAgentFinalize returns undefined or the in-process result, never rejects.
    - Test: the revise instruction string is byte-identical (no transcript content interpolated — T-11 injection mitigation).
    - Test: all prior plugin/src/index.test.js cases pass unchanged.
  </behavior>
  <action>
    Edit ONLY `plugin/src/gate.js` for the logic (D-06), extend the exported `safeBeforeAgentFinalize` wrapper in the SAME file to thread the transcript, then wire BOTH `index.ts` files (the nemoclaw index.ts is hand-maintained, not baked).

    In `plugin/src/gate.js`:
    1. Add a private `scanTranscriptForExec(transcript)` helper that, using the LITERAL field paths confirmed in 15-B05-SCHEMA-PROBE.md, walks the message list, finds tool_search_code tool/result messages, and inspects the confirmed key path for the `openclaw:core:exec` string. Return a small classification: whether an exec ran, and whether write-marker.sh was invoked (reuse the MARKER_INVOKE regex against the exec command text where the schema exposes it). Wrap the whole walk in try/catch returning a safe "no evidence" result on any error (fail-open; tolerate missing/extra fields). Do NOT interpolate any transcript content into the revise instruction.
    2. Change `handleBeforeAgentFinalize(runId)` to `handleBeforeAgentFinalize(runId, transcript)` (transcript optional/undefined-safe). Keep the existing in-process and disk-fallback union FIRST and unchanged. Then ADD the transcript source: if the run is not already classified substantive-and-marked by the existing sources, and `scanTranscriptForExec(transcript)` reports an exec ran, treat the run as substantive — if that scan also shows write-marker.sh was invoked, pass through (undefined); otherwise return the SAME static `_buildReviseAction(runId)`. The three sources (in-process Set, disk fallback, transcript scan) are UNIONED: any one reporting substantive+unmarked yields a revise; any one reporting marked yields pass-through.
    3. **Extend the exported `safeBeforeAgentFinalize` wrapper (REQUIRED — without this the transcript NEVER reaches the handler at runtime).** Today its signature is `safeBeforeAgentFinalize(runId, opts = {}, impl = handleBeforeAgentFinalize)` and its body `return impl(runId)` drops every extra arg. Change the signature so it ALSO accepts the transcript in a NON-colliding position (the existing second positional is `opts` — the transcript MUST NOT reuse that slot), e.g. `safeBeforeAgentFinalize(runId, transcript, opts = {}, impl = handleBeforeAgentFinalize)`, and change the body to forward it: `return impl(runId, transcript)`. Keep the try/catch fail-open boundary and the `opts.log` error logging exactly as-is (only the threading changes). You may choose a different non-colliding arg order, but the wrapper body MUST pass the transcript through to `impl`, and the new positions MUST be reflected at the index.ts call sites.
    4. Keep `_buildReviseAction` and its instruction string byte-identical.

    In `plugin/src/index.ts` and `plugin-nemoclaw/src/index.ts`: change the `before_agent_finalize` handler so it reads the confirmed ctx transcript field (per Task 1, e.g. `ctx?.conversation?.messages`) and passes it to `safeBeforeAgentFinalize` in the matching NEW non-colliding position chosen in gate.js step 3 (so it does NOT land in the `opts` slot). Preserve the double try/catch fail-open and the `log` option. **PRESERVE each file's existing logger field — `api.log?.(msg)` in `plugin/src/index.ts`, `api.logger.info(msg)` in `plugin-nemoclaw/src/index.ts`. Do NOT swap them when copy-editing the two handlers (index.ts is NOT covered by the Task 5 diff -q check, so a logger swap here would go uncaught).** Do NOT add allowConversationAccess to before_prompt_build or before_tool_call. The before_agent_finalize hook already has conversation access (Phase 11 D-05).

    Add a new `describe("transcript-scan observation (B-05)")` block to `plugin/src/index.test.js` covering the behaviors above — INCLUDING the wrapper-threading case that drives the transcript through `safeBeforeAgentFinalize` (not only `handleBeforeAgentFinalize` directly). Build the test transcripts from the Task 1 confirmed schema shape. Do NOT touch plugin-nemoclaw/src/gate.js (regenerated in Task 5) or modify existing test cases.
  </action>
  <verify>
    <automated>node --test plugin/src/index.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "openclaw:core:exec" plugin/src/gate.js` returns >= 1 (the transcript scan matches the confirmed exec evidence string).
    - `grep -nE "scanTranscript|transcript" plugin/src/gate.js` shows a transcript-scan helper consumed by handleBeforeAgentFinalize, and handleBeforeAgentFinalize accepts a second (transcript) parameter.
    - **Wrapper threading (Warning 1): `safeBeforeAgentFinalize` forwards the transcript to impl. `grep -c "impl(runId, transcript)" plugin/src/gate.js` returns >= 1 (the wrapper body passes the transcript through, not the old `return impl(runId)`), and the wrapper signature accepts the transcript in a position that does NOT collide with the existing `opts` positional. A unit test drives a tool_search_code openclaw:core:exec transcript THROUGH `safeBeforeAgentFinalize` and asserts it returns the revise action (the wrapper did not drop the transcript); the companion write-marker.sh transcript through the wrapper returns undefined.**
    - Both index.ts files pass the transcript through `safeBeforeAgentFinalize` in the new non-colliding position: `grep -c "before_agent_finalize" plugin/src/index.ts` and `grep -c "before_agent_finalize" plugin-nemoclaw/src/index.ts` each return 1, and each handler reads the confirmed ctx transcript field (matching 15-B05-SCHEMA-PROBE.md) and passes it to safeBeforeAgentFinalize (NOT into the `opts` argument).
    - **Logger preservation (Warning 2): each index.ts retains its existing logger field — `grep -c "api.log?." plugin/src/index.ts` returns >= 1 (and `api.logger.info` does NOT appear in plugin/src/index.ts), and `grep -c "api.logger.info" plugin-nemoclaw/src/index.ts` returns >= 1 (and `api.log?.` does NOT appear in the plugin-nemoclaw before_agent_finalize handler). The transcript edit did not swap the loggers.**
    - The revise instruction is unchanged: `grep -c "classify this turn by running" plugin/src/gate.js` returns 1 (no interpolation added).
    - `node --test plugin/src/index.test.js` exits 0 with the new transcript-scan cases (substantive-from-transcript → revise; marker-in-transcript → pass-through; no-evidence → pass-through; union with in-process source; THE WRAPPER-THREADING case through safeBeforeAgentFinalize; fail-open on malformed transcript) added beyond the prior cases.
    - `grep -c "B-05" plugin/src/index.test.js` returns >= 1.
    - before_prompt_build did NOT gain conversation access: `grep -c "allowConversationAccess" plugin/src/index.ts plugin-nemoclaw/src/index.ts` is unchanged from before this task (config-level, not added to the prompt-build hook).
    - No edit to plugin-nemoclaw/src/gate.js in this task (regenerated in Task 5): `git diff --name-only` does not list it after Task 4.
  </acceptance_criteria>
  <done>
    plugin/src/gate.js scans the conversation transcript (against the Task 1 confirmed schema) for Nemotron tool_search_code openclaw:core:exec evidence as an additional observation source unioned with the existing before_tool_call/disk paths; the exported safeBeforeAgentFinalize wrapper is extended to accept and forward the transcript to impl (so the runtime path index.ts → wrapper → handler actually delivers it, proven by a test that drives the transcript THROUGH the wrapper); both index.ts files read the confirmed ctx transcript field and pass it into safeBeforeAgentFinalize in the non-opts position while each retaining its own logger field (api.log?. for plugin/, api.logger.info for plugin-nemoclaw/); the revise instruction is byte-identical; fail-open holds on malformed transcripts; and the new transcript-scan tests plus all prior tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: Regenerate the nemoclaw build copy + dist, port new tests, and confirm D-06 sync + all suites green</name>
  <files>plugin-nemoclaw/src/gate.js, plugin-nemoclaw/dist/index.js, plugin-nemoclaw/src/index.test.js</files>
  <read_first>
    - plugin-nemoclaw/scripts/bake-directive.js lines 55-80 (the build: bakes GUARD_DIRECTIVE then copyFileSync plugin/src/gate.js → plugin-nemoclaw/src/gate.js; build is `node scripts/bake-directive.js && tsc` per package.json)
    - plugin-nemoclaw/package.json line ~11 (build script `node scripts/bake-directive.js && tsc`)
    - plugin-nemoclaw/src/index.test.js (imports its local ./gate.js; mirror the new WR-04 + transcript-scan describe blocks added to plugin/src/index.test.js in Tasks 3-4, including the wrapper-threading case)
    - plugin/src/index.test.js (the WR-04 + transcript-scan blocks to port verbatim, adjusted only for the local import)
  </read_first>
  <action>
    1. Run the nemoclaw plugin build (`npm --prefix plugin-nemoclaw run build`, i.e. `node scripts/bake-directive.js && tsc`) so plugin-nemoclaw/src/gate.js is regenerated as a byte-identical copy of plugin/src/gate.js (D-06) AND plugin-nemoclaw/dist/index.js is recompiled with the Task 4 index.ts transcript wiring + the updated gate.js (the host has no tsc — the committed dist must reflect the fixes). Confirm the build emits no errors.
    2. Port the SAME new describe blocks added in Tasks 3-4 (WR-04 regression + transcript-scan observation, including the safeBeforeAgentFinalize wrapper-threading case) into plugin-nemoclaw/src/index.test.js, adjusting only the import to its local ./gate.js. Do NOT modify existing nemoclaw test cases.
    3. Verify D-06 sync and full green: `diff -q plugin/src/gate.js plugin-nemoclaw/src/gate.js` identical; both `node --test` suites exit 0. Re-commit the regenerated dist/index.js.
  </action>
  <verify>
    <automated>node --test plugin/src/index.test.js &amp;&amp; node --test plugin-nemoclaw/src/index.test.js &amp;&amp; diff -q plugin/src/gate.js plugin-nemoclaw/src/gate.js</automated>
  </verify>
  <acceptance_criteria>
    - `diff -q plugin/src/gate.js plugin-nemoclaw/src/gate.js` reports the files are identical (D-06 build copy in sync — includes WR-01 fix + transcript scan + the threaded safeBeforeAgentFinalize wrapper).
    - `grep -c "openclaw:core:exec" plugin-nemoclaw/src/gate.js` returns >= 1 and `grep -c "persistRunState(runId, false)" plugin-nemoclaw/src/gate.js` returns 0 (the fixes propagated).
    - `node --test plugin/src/index.test.js` exits 0 and `node --test plugin-nemoclaw/src/index.test.js` exits 0 (both suites include the WR-04 + transcript-scan blocks, including the wrapper-threading case).
    - `grep -c "B-05" plugin-nemoclaw/src/index.test.js` returns >= 1 and `grep -c "WR-01\|WR-04" plugin-nemoclaw/src/index.test.js` returns >= 1.
    - plugin-nemoclaw/dist/index.js was regenerated and contains the transcript wiring: `grep -c "before_agent_finalize" plugin-nemoclaw/dist/index.js` returns >= 1 (committed compiled artifact reflects Task 4).
  </acceptance_criteria>
  <done>
    The nemoclaw build copy is regenerated byte-identical to plugin/src/gate.js (D-06), the committed dist/index.js reflects the Task 4 transcript wiring + gate.js fixes, both test suites carry the WR-04 and transcript-scan blocks, and both `node --test` runs exit 0 — all three gaps are closed in code and ready for live re-validation (plan 15-07).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenClaw gateway process → conversation transcript | The before_agent_finalize hook reads model-produced conversation messages (tool_search_code params/results contain model-chosen, untrusted shell command text). This text crosses into the plugin's classification logic. |
| install script (host) → sandbox agent JSON via `nemoclaw exec` | Gate A parses agent JSON returned across the sandbox boundary; a no-promptChars JSON (the failure case) must fail closed with a diagnostic, not abort opaquely. |
| live host instrumentation (Task 1) → committed code | A temporary debug dump must not leak into committed plugin source/dist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-G06-01 | Tampering | transcript-scan classification input | mitigate | The scan reads only the confirmed key paths and matches `openclaw:core:exec` / MARKER_INVOKE patterns; no transcript content is interpolated into the static revise instruction (`_buildReviseAction` stays byte-identical). A forged transcript can at most cause one extra revise pass (bounded by maxAttempts:1) or a missed marker — it cannot block the reply (fail-open) or inject instructions. |
| T-15-G06-02 | Denial of Service | malformed/large transcript blocks the reply | mitigate | scanTranscriptForExec is wrapped in try/catch returning a safe no-evidence result; safeBeforeAgentFinalize is the second containment layer → a throw resolves to undefined (pass-through), never a rejected hook promise. The wrapper now also threads the transcript to impl, so the same containment covers the transcript-bearing path. Asserted by a fail-open unit test. |
| T-15-G06-03 | Information Disclosure | Gate A no-match path | mitigate | The `|| true` guard makes the no-promptChars case reach the actionable diagnostic and exit non-zero (fail closed WITH context) instead of a bare pipefail abort that leaves the operator blind. |
| T-15-G06-04 | Repudiation | live schema-probe accuracy | mitigate | 15-B05-SCHEMA-PROBE.md records raw command/exit/dump-excerpt evidence; observation code is written against the literal captured key paths, and Task 1 halts code work if the schema is not as assumed (no guessed-schema code). |
| T-15-G06-05 | Tampering | debug instrumentation leaks into committed code | mitigate | Acceptance asserts `grep -c "JSON.stringify(ctx" plugin/src/gate.js plugin-nemoclaw/src/gate.js` returns 0 after Task 1; the instrumentation is removed before any committed change. |
| T-15-G06-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — only `node:fs`/`node:path` builtins (already present) and the existing tsc devDependency build. slopcheck N/A; no `[ASSUMED]`/`[SUS]` packages introduced. |
</threat_model>

<verification>
- `bash -n scripts/post-install-nemoclaw.sh` exits 0; the Gate A _prompt_chars pipeline carries `|| true`; a no-promptChars JSON reproduction reaches the `-z` diagnostic (CR-01 closed).
- `grep -c "persistRunState(runId, false)" plugin/src/gate.js` returns 0; the WR-04 regression (marker → non-string → resetState → finalize undefined) passes (WR-01 closed).
- 15-B05-SCHEMA-PROBE.md records the live-confirmed Nemotron transcript schema; the transcript-scan observation in gate.js matches `openclaw:core:exec` and is unioned with the existing source; the safeBeforeAgentFinalize wrapper threads the transcript to impl; both index.ts files forward the confirmed ctx transcript field via the wrapper while each keeping its own logger field (B-05 closed in code).
- `diff -q plugin/src/gate.js plugin-nemoclaw/src/gate.js` identical (D-06); committed dist regenerated.
- `node --test plugin/src/index.test.js` and `node --test plugin-nemoclaw/src/index.test.js` both exit 0 (including the wrapper-threading test).
- Live end-to-end re-validation (CR-01 diagnostic on no-injection, B-05 marker produced, WR-01 across recover) is OUT OF SCOPE here — it is plan 15-07 (depends on this plan).
</verification>

<success_criteria>
- CR-01 closed in code: Gate A fails closed WITH the actionable diagnostic on a no-promptChars JSON.
- WR-01 closed in code: the non-string-command exec path no longer downgrades a marked:true disk record; WR-04 regression green in both suites.
- B-05 closed in code: transcript-scan observation written against the live-confirmed schema, unioned with the existing before_tool_call/disk paths, threaded through the safeBeforeAgentFinalize wrapper and wired through both index.ts files (each retaining its own logger field); new transcript-scan tests (including the wrapper-threading case) green in both suites.
- D-06 preserved: plugin-nemoclaw/src/gate.js byte-identical to plugin/src/gate.js; committed dist regenerated.
- Fail-open (SC4) preserved; all prior tests stay green.
</success_criteria>

<output>
Create `.planning/phases/15-per-turn-enforcement-plugin/15-06-SUMMARY.md` when done.
</output>
