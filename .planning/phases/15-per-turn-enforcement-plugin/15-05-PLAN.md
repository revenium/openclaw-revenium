---
phase: 15-per-turn-enforcement-plugin
plan: "05"
type: execute
gap_closure: true
wave: 2
depends_on: ["15-04"]
files_modified:
  - .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md
autonomous: false
requirements: [NCENF-01, NCENF-02]

must_haves:
  truths:
    - "SC1 re-validated (NCENF-01): a real install run of post-install-nemoclaw.sh against the live sandbox passes the NEW Gate A (promptChars threshold) instead of aborting on the removed finalPromptText field."
    - "SC3 re-validated (NCENF-02): a substantive NemoClaw turn (one that runs an exec/bash tool) writes a marker .jsonl end-to-end that appears under the host share mount markers/ directory — surviving the nemoclaw recover process boundary via the new disk-persisted run-state."
    - "Evidence is recorded in 15-VALIDATION.md the same way Plan 03 recorded it: exact commands, exit codes, and observed output (promptChars value, the marker filename + first line)."
  artifacts:
    - path: ".planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md"
      provides: "Re-validation addendum: B-01 RESOLVED (Gate A passes on live host) + B-05 RESOLVED (end-to-end marker .jsonl observed) with command/exit/output evidence"
      contains: "RE-VALIDATION"
  key_links:
    - from: "scripts/post-install-nemoclaw.sh Gate A (promptChars)"
      to: "live sandbox revenium-spike (34.224.27.67)"
      via: "nemoclaw exec openclaw agent --json"
      pattern: "promptChars"
    - from: "plugin/src/gate.js persisted run-state (post-recover fallback)"
      to: "host mount markers/<sid>.jsonl"
      via: "before_agent_finalize revise → write-marker.sh → local markers/ → share mount"
      pattern: "\\.jsonl"
---

<objective>
Re-validate both closed gaps on the LIVE sandbox the same way Plan 03 did — this is the honesty gate that proves the Plan 15-04 code fixes work end-to-end on OpenClaw 2026.5.22, not just in unit tests.

- **B-01 / SC1:** Deploy the updated plugin + rewritten install script to the live host (34.224.27.67, sandbox `revenium-spike`) and confirm the NEW Gate A (promptChars threshold) passes a real install instead of aborting on `finalPromptText`.
- **B-05 / SC3:** Run a substantive NemoClaw turn that invokes an exec/bash tool, then confirm a marker `.jsonl` appears under the host share mount — proving the disk-persisted run-state carries the exec observation across the `nemoclaw recover` process boundary so `before_agent_finalize` fires.

Purpose: SC1 and SC3 were the two FAILED roadmap truths; they can only be honestly marked verified with live evidence (per phase CONTEXT: "Validate on the live sandbox host — not only an isolated unit test").

Output: a RE-VALIDATION addendum appended to 15-VALIDATION.md with exact commands, exit codes, and observed output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md
@.planning/phases/15-per-turn-enforcement-plugin/15-VERIFICATION.md
@.planning/phases/15-per-turn-enforcement-plugin/15-CONTEXT.md
@./CLAUDE.md
@.claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the 15-04 fixes to the live sandbox and gather B-01 + B-05 evidence</name>
  <files>.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md</files>
  <read_first>
    - .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md (Plan 03's evidence format — replicate it: command, Exit, Output blocks; the SC1 promptChars section lines 107-135; the SC3 marker section lines 182-247; Deviation 1 SSHFS ~/ skill-install workaround lines 311-346)
    - .planning/phases/15-per-turn-enforcement-plugin/15-04-SUMMARY.md (what the code fixes changed — the new Gate A threshold value, the run-state path/persistence behavior)
    - scripts/post-install-nemoclaw.sh (the rewritten Gate A — confirm the promptChars threshold and message text to look for in install output)
    - plugin/src/gate.js (the persistence behavior — the run-state dir under OPENCLAW_HOME the live turn will populate)
    - .claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md (the install → recover → gateway-turn validation sequence; skill install upload mechanic)
    - MEMORY: clawhub-test-host / nemoclaw-spike-host / worktree-cleanup-stray-summary / revenium-cli-sandbox-gotchas (live host access, the SSHFS ~/ skill-dir workaround, newline-argv constraint, api-key field)
  </read_first>
  <action>
    On the live host 34.224.27.67 (sandbox `revenium-spike`), deploy the rebuilt `plugin-nemoclaw/` (with the 15-04 persistence fix baked into its dist) and the rewritten `scripts/post-install-nemoclaw.sh`, then gather two pieces of evidence. Honor the known live constraints: `nemoclaw exec` rejects newline argv (single-line `sh -lc` only); skill install must use a clean staging dir, not `~/` (SSHFS node_modules with unsafe filenames — Plan 03 Deviation 1); the share mount is `~/sbx-openclaw-revenium-spike` → `/sandbox/.openclaw`.

    B-01 evidence (SC1): re-run `install_enforcement_plugin` (clear the `enforcement-plugin-installed` ledger key first so it re-runs) — or invoke just the Gate A turn-test path against the installed plugin — and capture that the NEW Gate A passes: the `openclaw agent --json` turn returns `currentTurn.promptChars` above the threshold and the script prints the "Gate A passed: currentTurn.promptChars=<n>" info line with exit 0. Record the actual promptChars value (expect ~1637 with plugin). Confirm the install completes through all four gates without aborting.

    B-05 evidence (SC3): with the rebuilt plugin loaded (after `nemoclaw revenium-spike recover`), run a substantive turn that triggers an exec/bash tool call (e.g. a message that makes the agent run a shell command such as `echo`), in a fresh session id. The new gateway process starts with empty in-process Sets, so this is exactly the cross-recover scenario B-05 targets — the persisted run-state file must carry the exec observation so `before_agent_finalize` issues the revise and the agent runs `write-marker.sh`. Then list `~/sbx-openclaw-revenium-spike/markers/*.jsonl` over the mount and capture the marker filename and its first JSON line. Also capture the in-sandbox run-state file (`ls /sandbox/.openclaw/run-state/`) as corroborating evidence the persistence path wrote.

    If either turn hangs, use the Plan 03 timing finding (turns take ~70-90s with the plugin; use a 120-180s timeout, not 45s). Restore the sandbox to a healthy state when done exactly as Plan 03 did (re-enable/disable as appropriate, `nemoclaw recover`, restore guardrail-status.json if touched). If a stray SUMMARY.md appears in a worktree merge, remove it and `git merge --ff-only` per the known worktree-cleanup gotcha.

    Append a `## RE-VALIDATION (2026-06-08, Plan 15-04 fixes)` section to 15-VALIDATION.md with the exact commands, exit codes, and output for both B-01 and B-05 — mark each blocker RESOLVED or, if still failing, record the honest failure (do NOT claim a pass that did not happen — CRITICAL HONESTY RULE, same as Plan 03).
  </action>
  <verify>
    <human-check>
      The RE-VALIDATION addendum in 15-VALIDATION.md shows: (1) a live Gate A pass with a real promptChars value above threshold and exit 0; (2) a real marker .jsonl filename + first line captured from the host mount after a substantive turn. Both came from the live sandbox, not a unit test.
    </human-check>
  </verify>
  <acceptance_criteria>
    - 15-VALIDATION.md contains a new `RE-VALIDATION` section dated 2026-06-08 referencing the 15-04 fixes.
    - The B-01 evidence block shows an `openclaw agent --json` command, exit 0, and a captured `currentTurn.promptChars` value at or above the Gate A threshold, plus the script's "Gate A passed" line.
    - The B-05 evidence block shows the substantive-turn command, exit code, and a real `markers/*.jsonl` filename with its first JSON line — captured from `~/sbx-openclaw-revenium-spike/markers/`.
    - Each blocker is explicitly marked RESOLVED (or an honest still-failing note is recorded with root cause).
    - The sandbox-restore commands are recorded (host left healthy).
  </acceptance_criteria>
  <done>
    15-VALIDATION.md has a RE-VALIDATION addendum proving, on the live sandbox, that the new Gate A passes a real install (B-01) and a substantive turn writes an end-to-end marker .jsonl across the recover boundary (B-05) — with command/exit/output evidence in the Plan 03 format, and the host restored healthy.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirms live re-validation of SC1 and SC3</name>
  <files>.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md</files>
  <action>
    PAUSE for human verification. No automated change — the human reviews the live re-validation evidence recorded by Task 1 in 15-VALIDATION.md and confirms both blockers are genuinely resolved on the live sandbox before the phase is re-verified.
  </action>
  <what-built>
    Plan 15-04 fixed B-01 (Gate A now asserts promptChars, not the removed finalPromptText) and B-05 (exec observations persist to disk so they survive nemoclaw recover). Task 1 deployed those fixes to the live sandbox revenium-spike (34.224.27.67) and recorded evidence in 15-VALIDATION.md that a real install passes the new Gate A and a substantive turn writes a marker .jsonl end-to-end.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md` and read the new `## RE-VALIDATION` section.
    2. Confirm the B-01 block shows a live `openclaw agent --json` turn with `currentTurn.promptChars` above the Gate A threshold and the install reaching "Gate A passed" with exit 0 (the install no longer aborts on finalPromptText).
    3. Confirm the B-05 block shows a real marker `.jsonl` filename and first line captured from `~/sbx-openclaw-revenium-spike/markers/` after a substantive exec turn — i.e. attribution flowed end-to-end across a gateway recover.
    4. Confirm the sandbox was restored to healthy (restore commands recorded).
    If the evidence is convincing and both blockers are marked RESOLVED, approve. If the marker did not appear or Gate A still failed, describe what is missing so the fix can be revised.
  </how-to-verify>
  <verify>
    <human-check>Human reads the RE-VALIDATION section and confirms live B-01 (Gate A pass) and B-05 (end-to-end marker) evidence is genuine and both blockers are RESOLVED.</human-check>
  </verify>
  <resume-signal>Type "approved" to mark SC1 + SC3 re-validated, or describe the gap that remains.</resume-signal>
  <done>Human has reviewed the live re-validation evidence and approved that B-01 and B-05 are resolved on the live sandbox (or returned specific gaps to fix).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host install script / operator → live sandbox revenium-spike | Commands run over `nemoclaw exec` into the sandbox; a substantive agent turn runs untrusted model-chosen shell via the exec tool. |
| sandbox markers/ → host share mount | Marker `.jsonl` files written in-sandbox become visible to the host metering loop over SSHFS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-RV-01 | Tampering | run-state file written by the live turn | mitigate | The persistence path (validated in 15-04) sanitizes runId and writes owner-only under OPENCLAW_HOME/run-state; this plan only observes the file, it does not weaken those guards. |
| T-15-RV-02 | Repudiation | re-validation evidence accuracy | mitigate | Evidence is captured as raw command/exit/output blocks (Plan 03 format) so a false pass cannot be asserted without fabricated output; the blocking human checkpoint independently reviews the evidence (CRITICAL HONESTY RULE). |
| T-15-RV-03 | Denial of Service | live sandbox left in a broken/halted state | mitigate | Task 1 restores the sandbox (disable/enable + recover + restore guardrail-status.json) exactly as Plan 03 did; the checkpoint confirms restore commands are recorded. |
| T-15-RV-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — deploy uses already-committed dist + existing scripts; no new dependencies. |
</threat_model>

<verification>
- 15-VALIDATION.md RE-VALIDATION section present with live B-01 (promptChars Gate A pass) and B-05 (end-to-end marker .jsonl) evidence.
- Human checkpoint approved (SC1 + SC3 re-validated).
- Live sandbox restored healthy.
</verification>

<success_criteria>
- SC1 (NCENF-01) re-validated live: new Gate A passes a real install on OpenClaw 2026.5.22.
- SC3 (NCENF-02) re-validated live: a substantive turn writes a marker .jsonl end-to-end across the recover boundary.
- Evidence recorded honestly in 15-VALIDATION.md; human checkpoint approves.
</success_criteria>

<output>
Create `.planning/phases/15-per-turn-enforcement-plugin/15-05-SUMMARY.md` when done.
</output>
