---
phase: 12-parallel-install-scaffolding-detection
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - scripts/install.sh
  - scripts/post-install-nemoclaw.sh
autonomous: true
requirements: [NCINST-01, NCINST-02]
tags: [bash, install, nemoclaw, dispatcher, detection]

must_haves:
  truths:
    - "Running install.sh on Linux with ~/.nemoclaw/ only (or --nemoclaw / NEMOCLAW=1) enters the NemoClaw path; on a dual-home or standalone host with no flag it routes to the untouched post-install.sh (D-03)."
    - "Running install.sh on macOS WITH a NemoClaw signal prints an explicit 'unsupported' message naming the Darwin graceful-skip trap and exits non-zero; macOS WITHOUT a NemoClaw signal falls through to standalone (D-05/D-06)."
    - "post-install-nemoclaw.sh runs probe-host-compat.sh as a hard gate (FAIL->non-zero exit, WARN->continue per D-08), checks the nemoclaw CLI, runs Phase 13+ stub no-ops, and is idempotent on a second run (D-07/D-11)."
    - "scripts/post-install.sh is byte-stable — git diff --name-only HEAD -- scripts/post-install.sh is empty."
    - "tests/test_install_dispatcher.sh goes GREEN (exit 0, all six groups pass)."
  artifacts:
    - path: "scripts/install.sh"
      provides: "Thin dispatcher: flag parse, D-03 routing precedence, macOS refusal, passthrough exec to standalone or NemoClaw path"
      contains: "STUB_UNAME_S"
      min_lines: 40
    - path: "scripts/post-install-nemoclaw.sh"
      provides: "NemoClaw install path skeleton: preflight hard gate + CLI check + Phase 13+ stub functions"
      contains: "probe-host-compat.sh"
      min_lines: 50
  key_links:
    - from: "scripts/install.sh"
      to: "scripts/post-install-nemoclaw.sh"
      via: "bash subprocess exec on NemoClaw target with PASSTHROUGH_ARGS"
      pattern: "bash .*post-install-nemoclaw.sh"
    - from: "scripts/install.sh"
      to: "scripts/post-install.sh"
      via: "bash subprocess exec on standalone target (unmodified)"
      pattern: "bash .*post-install.sh"
    - from: "scripts/post-install-nemoclaw.sh"
      to: "scripts/probe-host-compat.sh"
      via: "bash subprocess exec as hard preflight gate (never sourced)"
      pattern: "bash .*probe-host-compat.sh"
---

<objective>
Implement the parallel install path's two new scripts: the thin `install.sh` dispatcher (detection + D-03 routing + macOS refusal) and the `post-install-nemoclaw.sh` skeleton (preflight hard gate + CLI check + Phase 13+ stub functions). Together they satisfy NCINST-01 (parallel path, standalone untouched) and NCINST-02 (detect target, refuse macOS explicitly) and turn the plan-01 test suite GREEN.

Purpose: This is the routing brain of v1.4. It must route correctly on every D-03 branch, refuse macOS with the Darwin graceful-skip trap named, gate the NemoClaw path behind the probe's exit-code contract, and leave `post-install.sh` byte-stable.
Output: `scripts/install.sh` and `scripts/post-install-nemoclaw.sh`, with `tests/test_install_dispatcher.sh` passing all six groups.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-parallel-install-scaffolding-detection/12-CONTEXT.md
@.planning/phases/12-parallel-install-scaffolding-detection/12-RESEARCH.md
@.planning/phases/12-parallel-install-scaffolding-detection/12-PATTERNS.md
@.planning/phases/12-parallel-install-scaffolding-detection/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write install.sh dispatcher (flag parse, D-03 routing, macOS refusal)</name>
  <files>scripts/install.sh</files>
  <read_first>
    - scripts/post-install.sh (lines 1-40: shebang, `set -euo pipefail`, `SCRIPT_DIR` resolution via `${BASH_SOURCE[0]}`, the `for arg in "$@"; case` flag parse, and the verbatim `info`/`warn`/`step`/`fail` + `command_exists` helpers to mirror — do NOT import these from common.sh; and the `${ARRAY[@]+"${ARRAY[@]}"}` array-expansion idiom used elsewhere in the file)
    - scripts/install-cron.sh (lines 1-40: secondary analog for `case` arg parsing)
    - .planning/phases/12-parallel-install-scaffolding-detection/12-PATTERNS.md (§ scripts/install.sh — exact flag-parse, STUB_UNAME_S OS detection, D-03 routing precedence, macOS refusal message, script-existence guard, and routing-dispatch patterns)
    - .planning/phases/12-parallel-install-scaffolding-detection/12-CONTEXT.md (D-01..D-06 locked decisions)
    - tests/test_install_dispatcher.sh (the suite this script must satisfy — read it to match the exact markers it greps for: NemoClaw-path markers, "graceful-skip", "Revenium skill installed" absence)
  </read_first>
  <action>
    Create `scripts/install.sh` as a thin dispatcher (per D-01). Use `#!/usr/bin/env bash`, `set -euo pipefail`, `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`, and copy the `info`/`warn`/`step`/`fail` helpers verbatim from `post-install.sh` lines 34-39 (do NOT source common.sh — its helpers log to a file). Parse flags with a `for arg in "$@"; case "${arg}" in --nemoclaw) NEMOCLAW_FLAG=true ;; *) PASSTHROUGH_ARGS+=("${arg}") ;; esac done` loop, initializing `NEMOCLAW_FLAG=false` and `PASSTHROUGH_ARGS=()`.

    OS detection MUST use the testable form `_os="${STUB_UNAME_S:-$(uname -s)}"` — never bare `$(uname -s)` (D-06 testability / Pitfall 3).

    Implement the D-03 routing precedence verbatim: default `TARGET="standalone"`; set `TARGET="nemoclaw"` if `NEMOCLAW_FLAG == true` OR `${NEMOCLAW:-} == "1"`; else set `TARGET="nemoclaw"` if `[[ -d "${HOME}/.nemoclaw" ]] && [[ ! -d "${HOME}/.openclaw" ]]`. When both `${HOME}/.nemoclaw` and `${HOME}/.openclaw` exist and no flag is given, TARGET stays "standalone" (dual-home rule). Use `${HOME}/.nemoclaw` and `${HOME}/.openclaw` (never hardcoded paths) so HOME-override tests work.

    macOS refusal (D-05/D-06): AFTER routing resolves, if `[[ "${TARGET}" == "nemoclaw" ]] && [[ "${_os}" == "Darwin" ]]`, call `fail` with a multi-line message that explicitly names the Darwin graceful-skip trap (state that NemoClaw's own installer graceful-skips on Darwin / exits 0 without provisioning the sandbox → false success), tells the operator to use a Linux + Docker host, and notes the standalone path still works on macOS. `fail` exits non-zero. The refusal must NOT fire when TARGET is standalone (macOS-without-NemoClaw-signal falls through).

    Add a script-existence guard before each exec: `[[ -f "${SCRIPT_DIR}/post-install-nemoclaw.sh" ]] || fail "..."` and the same for `post-install.sh`. Then dispatch: if `TARGET == nemoclaw`, `bash "${SCRIPT_DIR}/post-install-nemoclaw.sh" "${PASSTHROUGH_ARGS[@]+"${PASSTHROUGH_ARGS[@]}"}"`, else `bash "${SCRIPT_DIR}/post-install.sh" "${PASSTHROUGH_ARGS[@]+"${PASSTHROUGH_ARGS[@]}"}"`. No `read`/interactive prompts anywhere (D-04 / Pitfall 4) — disambiguation is the explicit flag. Mark executable (`chmod +x`). Do NOT modify `scripts/post-install.sh`.
  </action>
  <verify>
    <automated>bash -n scripts/install.sh && grep -q 'STUB_UNAME_S' scripts/install.sh && grep -q 'graceful-skip' scripts/install.sh && grep -q 'case "${arg}" in' scripts/install.sh && ! grep -qE '(^|[^_])read ' scripts/install.sh && { git diff --name-only HEAD -- scripts/post-install.sh | grep -q . && { echo "POSTINSTALL_MODIFIED_FAIL"; exit 1; } || true; } && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/install.sh` passes; file is executable.
    - Contains `case "${arg}" in` flag parse, `_os="${STUB_UNAME_S:-$(uname -s)}"`, and uses `${HOME}/.nemoclaw` / `${HOME}/.openclaw` (no hardcoded home paths).
    - D-03 routing: explicit flag/`NEMOCLAW=1` OR (`~/.nemoclaw` present AND `~/.openclaw` absent) -> nemoclaw; dual-home no-flag -> standalone.
    - macOS refusal message contains "graceful-skip" and the script exits non-zero only when TARGET resolved to nemoclaw on Darwin.
    - No `read` calls (detached-run safe, D-04).
    - `git diff --name-only HEAD -- scripts/post-install.sh` is empty (post-install.sh untouched); a non-empty diff drives the verify to a non-zero exit.
  </acceptance_criteria>
  <done>scripts/install.sh routes per D-03, refuses macOS-on-NemoClaw with the graceful-skip trap named and non-zero exit, passes args through, and leaves post-install.sh byte-stable.</done>
</task>

<task type="auto">
  <name>Task 2: Write post-install-nemoclaw.sh skeleton (preflight gate + CLI check + Phase 13+ stubs)</name>
  <files>scripts/post-install-nemoclaw.sh</files>
  <read_first>
    - scripts/post-install.sh (lines 1-136: structure to mirror — shebang, `set -euo pipefail`, constants, verbatim `info`/`warn`/`step`/`fail` + `command_exists` helpers, sequential `step` blocks, command_exists-guarded idempotency idiom; and the success-banner style near lines 738-751)
    - scripts/install-cron.sh (the `~/.local/bin` PATH-extension pattern for the NemoClaw CLI location)
    - scripts/probe-host-compat.sh (the preflight probe created in plan 01 — note its exit-code contract: fail>0 -> exit 1, warn/clean -> exit 0; MUST be exec'd via `bash`, never sourced — Pitfall 2)
    - .planning/phases/12-parallel-install-scaffolding-detection/12-PATTERNS.md (§ scripts/post-install-nemoclaw.sh — PATH extension, preflight invocation, CLI check, stub function pattern, idempotency, success banner)
    - .planning/phases/12-parallel-install-scaffolding-detection/12-CONTEXT.md (D-07..D-11 locked decisions)
    - tests/test_install_dispatcher.sh (GROUP A/F markers the skeleton output must satisfy — "preflight"/"Phase 13" markers and stable idempotent output)
  </read_first>
  <action>
    Create `scripts/post-install-nemoclaw.sh` (the NemoClaw path script invoked by the dispatcher, per D-02). Use `#!/usr/bin/env bash`, `set -euo pipefail`, `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`, `PROBE_SCRIPT="${SCRIPT_DIR}/probe-host-compat.sh"`, and the verbatim `info`/`warn`/`step`/`fail` + `command_exists` helpers from post-install.sh (do NOT source common.sh). Extend PATH for the NemoClaw CLI location: `export PATH="${HOME}/.local/bin:${PATH}"` (Pitfall 6).

    Preflight hard gate (D-08/D-09): `step "Running host compatibility preflight"`, then `if ! bash "${PROBE_SCRIPT}"; then fail "Host compatibility check failed — NemoClaw requires a Linux host with Docker."; fi`, then `info "Preflight complete (warnings above are non-blocking)"`. CRITICAL: exec via `bash "${PROBE_SCRIPT}"` — NEVER `. "${PROBE_SCRIPT}"` (sourcing collides the probe's pass/warn/fail counters with this script's `fail()` and bypasses error handling — Pitfall 2). The probe's `exit 1` (OS/Docker FAIL) propagates as a non-zero exit and stops the install; `exit 0` (RAM/disk/GPU/Node WARN) continues.

    NemoClaw CLI check (D-10 identity-vs-capability): `step "Checking NemoClaw CLI"`; if `command_exists nemoclaw`, `info "nemoclaw CLI found: $(command -v nemoclaw)"`; else `warn` that the CLI is not on PATH and that NemoClaw installs it at `~/.local/bin` (warn-and-continue, not fail).

    Phase 13+ stub functions (D-07): define named no-op functions `stub_provision_egress_policy` (Phase 13+ egress policy), `stub_deliver_revenium_cli` (Phase 13+ in-sandbox revenium CLI), `stub_install_metering_loop` (Phase 14+ host-side metering loop), `stub_install_enforcement_plugin` (Phase 15+ per-turn enforcement plugin). Each just calls `warn "Phase NN+: <name> deferred — skipping."` and returns cleanly. Call all four in sequence after the CLI check. NO `read`/interactive steps anywhere (Pitfall 4). NO ledger (D-11) — detection/preflight are read-only and stubs are no-ops, so the script is naturally idempotent: running it twice produces identical output and exit 0 both times.

    End with a success banner (mirror post-install.sh style) stating the NemoClaw path skeleton is complete and Phases 13–16 will implement sandbox provisioning. Mark executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash -n scripts/post-install-nemoclaw.sh && grep -q 'bash "${PROBE_SCRIPT}"' scripts/post-install-nemoclaw.sh && ! grep -qE '^\s*\.\s+"\$\{PROBE_SCRIPT\}"' scripts/post-install-nemoclaw.sh && grep -q 'stub_provision_egress_policy' scripts/post-install-nemoclaw.sh && grep -q 'local/bin' scripts/post-install-nemoclaw.sh && ! grep -qE '(^|[^_])read ' scripts/post-install-nemoclaw.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/post-install-nemoclaw.sh` passes; file is executable.
    - Invokes the probe via `bash "${PROBE_SCRIPT}"` (subprocess) and NOT via `. "${PROBE_SCRIPT}"` (source).
    - Probe FAIL (exit 1) propagates as a non-zero exit from this script; probe WARN (exit 0) continues (D-08).
    - Defines all four `stub_*` functions, each a warn-and-return no-op, and calls them in sequence (D-07).
    - Extends PATH with `${HOME}/.local/bin` before the `command_exists nemoclaw` check (Pitfall 6).
    - No `read` calls (D-04). No ledger file created (D-11). Running twice against the same HOME yields identical output and exit 0 both times.
  </acceptance_criteria>
  <done>scripts/post-install-nemoclaw.sh gates on the probe, checks the CLI, runs Phase 13+ stub no-ops, and is idempotent — completing the NemoClaw path skeleton.</done>
</task>

<task type="auto">
  <name>Task 3: Turn the suite GREEN and confirm byte-stability + idempotency</name>
  <files>scripts/install.sh, scripts/post-install-nemoclaw.sh</files>
  <read_first>
    - tests/test_install_dispatcher.sh (the full suite from plan 01 — all six groups must pass; this file is READ ONLY to run the suite, it is NEVER edited by this task — see action)
    - .planning/phases/12-parallel-install-scaffolding-detection/12-VALIDATION.md (the four-SC observable-behavior table + full-suite regression command)
    - scripts/install.sh (task 1 output)
    - scripts/post-install-nemoclaw.sh (task 2 output)
  </read_first>
  <action>
    Run `bash tests/test_install_dispatcher.sh` and resolve any failing group by correcting `install.sh` or `post-install-nemoclaw.sh` ONLY. NOTE: `tests/test_install_dispatcher.sh` is read solely to run the suite and inspect failures — it is NEVER edited, and `files_modified` for this plan stays limited to the two scripts. Never edit `scripts/post-install.sh` and never weaken the test assertions. Confirm all six groups (A auto-detect, B explicit flag, C standalone default, D macOS refusal, E macOS standalone passthrough, F idempotency) plus the byte-stable group pass and the suite exits 0. Then run the full regression suite `bash tests/test_install_dispatcher.sh && bash tests/test_write_marker.sh && bash tests/test_guardrail_argv.sh` to confirm no regression in the existing harness. Finally confirm `git diff --name-only HEAD -- scripts/post-install.sh` is empty. If a group reveals a real routing bug, fix the dispatcher/skeleton; do not adjust expected behavior to mask it.
  </action>
  <verify>
    <automated>bash tests/test_install_dispatcher.sh && bash tests/test_write_marker.sh && bash tests/test_guardrail_argv.sh && { git diff --name-only HEAD -- scripts/post-install.sh | grep -q . && { echo "FAIL_POSTINSTALL_MODIFIED"; exit 1; } || echo BYTE_STABLE_OK; }</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/test_install_dispatcher.sh` exits 0 with all six groups + byte-stable group reporting PASS (0 FAIL).
    - The full regression suite (`test_install_dispatcher.sh && test_write_marker.sh && test_guardrail_argv.sh`) is green.
    - `git diff --name-only HEAD -- scripts/post-install.sh` is empty (NCINST-01 no-regression guarantee, SC2).
    - The idempotency group (F) confirms a second NemoClaw-path run yields identical output and exit 0 (SC4).
    - No test assertion was weakened, `tests/test_install_dispatcher.sh` was not edited, and post-install.sh was not edited to make the suite pass.
  </acceptance_criteria>
  <done>The dispatcher suite is GREEN, existing tests still pass, post-install.sh is byte-stable, and the NemoClaw skeleton is idempotent — all four phase success criteria are observably satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args / NEMOCLAW env -> install.sh | Untrusted `--nemoclaw` flag and `NEMOCLAW` env value cross into routing logic |
| install.sh -> routed script (subprocess) | PASSTHROUGH_ARGS forwarded to post-install.sh / post-install-nemoclaw.sh |
| post-install-nemoclaw.sh -> probe-host-compat.sh | Host capability check; exec'd as subprocess (never sourced) |
| host environment -> probe-host-compat.sh | Read-only probes of /proc/meminfo, uname, docker info, df, nvidia-smi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Spoofing | macOS graceful-skip false-success (NCINST-02) | mitigate | install.sh refuses macOS-on-NemoClaw with a message naming the Darwin graceful-skip trap and a non-zero exit; GROUP D asserts both |
| T-12-02 | Tampering | argument injection via `--nemoclaw` / `NEMOCLAW` env | mitigate | flag parsed with a `case` switch (no `eval`); `NEMOCLAW` compared as boolean `== "1"` only, never evaluated (RESEARCH § Security Domain) |
| T-12-03 | Tampering | post-install.sh byte-stability (NCINST-01 no-regression) | mitigate | dispatcher invokes post-install.sh unmodified; Task 3 asserts `git diff --name-only HEAD -- scripts/post-install.sh` is empty |
| T-12-04 | Elevation of Privilege | probe counter collision corrupting dispatcher control flow | mitigate | probe is exec'd via `bash "${PROBE_SCRIPT}"`, never sourced — pass/warn/fail counters stay isolated (Pitfall 2) |
| T-12-05 | Denial of Service | detached/non-interactive run hangs on `read` | mitigate | no `read`/interactive prompts in either new script (D-04); disambiguation is the explicit flag |

No npm/pip/cargo installs — Package Legitimacy Gate N/A (pure bash). No tmpfiles created by production scripts (no `trap cleanup` needed; detection + stubs are read-only/no-op).
</threat_model>

<verification>
- All six dispatcher test groups pass; suite exits 0.
- macOS-on-NemoClaw refusal: non-zero exit + "graceful-skip" in message.
- macOS-without-NemoClaw-signal: falls through to standalone (refusal does not fire).
- Probe exec'd (not sourced); FAIL propagates non-zero, WARN continues.
- post-install.sh byte-stable (`git diff --name-only` empty).
- NemoClaw skeleton idempotent (second run identical, exit 0).
- Existing tests (test_write_marker.sh, test_guardrail_argv.sh) still green.
</verification>

<success_criteria>
- SC1: Linux + NemoClaw signal enters the NemoClaw path without invoking post-install.sh.
- SC2: Standalone host routes through the unchanged post-install.sh — no regression (byte-stable).
- SC3: macOS + NemoClaw signal prints an explicit unsupported/graceful-skip error and exits non-zero.
- SC4: NemoClaw skeleton is idempotent — running it twice produces the same result without duplication or error.
</success_criteria>

<output>
Create `.planning/phases/12-parallel-install-scaffolding-detection/12-02-SUMMARY.md` when done.
</output>
</invoke>
