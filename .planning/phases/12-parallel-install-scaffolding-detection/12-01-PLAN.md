---
phase: 12-parallel-install-scaffolding-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_install_dispatcher.sh
  - scripts/probe-host-compat.sh
autonomous: true
requirements: [NCINST-01, NCINST-02]
tags: [bash, install, nemoclaw, detection, test-harness]

must_haves:
  truths:
    - "Running tests/test_install_dispatcher.sh executes a PASS/FAIL-counted suite covering all six VALIDATION.md test rows and exits non-zero while install.sh is absent (RED), zero once it exists (GREEN)."
    - "scripts/probe-host-compat.sh exists as a first-class install-time script with the verbatim exit-code contract (fail>0 -> exit 1; warn>0 or clean -> exit 0)."
    - "The test exercises all four D-03 routing branches plus the macOS refusal and the post-install.sh byte-stability check using STUB_UNAME_S + mktemp -d HOME isolation."
  artifacts:
    - path: "tests/test_install_dispatcher.sh"
      provides: "Hermetic routing/refusal/idempotency/byte-stability test suite for the dispatcher"
      contains: "STUB_UNAME_S"
      min_lines: 120
    - path: "scripts/probe-host-compat.sh"
      provides: "Host compatibility preflight probe (OS/Docker/RAM/disk/GPU) with fail/warn exit-code contract"
      contains: "VERDICT"
      min_lines: 80
  key_links:
    - from: "tests/test_install_dispatcher.sh"
      to: "scripts/install.sh"
      via: "STUB_UNAME_S + HOME env-override subprocess invocation"
      pattern: "bash .*install.sh"
    - from: "tests/test_install_dispatcher.sh"
      to: "scripts/post-install.sh"
      via: "git diff --name-only byte-stability assertion"
      pattern: "git .*diff --name-only"
---

<objective>
Lay the Nyquist test foundation and the preflight probe for the parallel install path. This plan creates the hermetic test harness FIRST (per the Validation Architecture / Wave 0 gap) so the Wave-1 dispatcher and NemoClaw skeleton are implemented against a red suite, and copies the spike-validated `probe-host-compat.sh` into `scripts/` verbatim (D-09) so the NemoClaw path has a first-class preflight gate available at install time.

Purpose: Without the test harness, the routing/refusal/idempotency logic in plan 02 cannot be verified hermetically on a macOS dev machine (no live NemoClaw host). Without the copied probe, plan 02's preflight gate (D-08) has nothing to exec.
Output: `tests/test_install_dispatcher.sh` (RED until plan 02 lands `install.sh`) and `scripts/probe-host-compat.sh` (verbatim copy, banner-only edit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-parallel-install-scaffolding-detection/12-CONTEXT.md
@.planning/phases/12-parallel-install-scaffolding-detection/12-RESEARCH.md
@.planning/phases/12-parallel-install-scaffolding-detection/12-PATTERNS.md
@.planning/phases/12-parallel-install-scaffolding-detection/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy probe-host-compat.sh verbatim into scripts/</name>
  <files>scripts/probe-host-compat.sh</files>
  <read_first>
    - .claude/skills/spike-findings-openclaw-revenium/sources/001-nemoclaw-bootstrap/probe-host-compat.sh (the source to copy — read it entirely; note `set -u` only, the `pass`/`warn`/`fail` counter vars, the `no`/`wn`/`ok` helpers, the Docker-on-Linux warn-not-fail branch, and the `exit 1` on fail>0 / `exit 0` otherwise contract at the verdict block)
    - .planning/phases/12-parallel-install-scaffolding-detection/12-PATTERNS.md (§ scripts/probe-host-compat.sh — "copy verbatim; only permitted modification is the banner comment")
    - .claude/skills/spike-findings-openclaw-revenium/references/install-and-bootstrap.md (auto-loaded by the skill — Linux-only gate, ~/.nemoclaw host config, ~/.local/bin CLI location context)
  </read_first>
  <action>
    Copy `.claude/skills/spike-findings-openclaw-revenium/sources/001-nemoclaw-bootstrap/probe-host-compat.sh` to `scripts/probe-host-compat.sh`. Preserve ALL logic, the `set -u` (NOT `set -euo pipefail`) discipline, the `pass`/`warn`/`fail` counter variables, the `ok`/`wn`/`no` helpers, the Docker-on-Linux warn-not-fail branch, and the verdict block's exit-code contract byte-for-byte: `fail>0` -> `exit 1` (INCOMPATIBLE), `warn>0` -> `exit 0` (USABLE WITH CAVEATS), clean -> `exit 0` (COMPATIBLE). The ONLY permitted change is the banner comment at the top: replace the "Spike 001 — NemoClaw bootstrap feasibility probe" header line with "Revenium NemoClaw Install — Host Compatibility Preflight" while keeping the NON-DESTRUCTIVE / documented-requirements comment body intact. Mark the file executable (`chmod +x scripts/probe-host-compat.sh`). Do NOT inline this probe into any other script (D-09) and do NOT change the counter variable names (they would collide with the dispatcher's `fail()` if ever sourced).
  </action>
  <verify>
    <automated>bash -n scripts/probe-host-compat.sh && grep -q 'set -u' scripts/probe-host-compat.sh && grep -q 'VERDICT' scripts/probe-host-compat.sh && grep -q 'Host Compatibility Preflight' scripts/probe-host-compat.sh && ! grep -q 'set -euo pipefail' scripts/probe-host-compat.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/probe-host-compat.sh` exists and `bash -n scripts/probe-host-compat.sh` reports no syntax errors.
    - File contains `set -u` and does NOT contain `set -euo pipefail` (preserves the probe's own discipline).
    - File contains `VERDICT` and the verdict block exits 1 when `fail>0`, exit 0 otherwise (diff against source shows only the banner line changed).
    - Banner contains "Host Compatibility Preflight"; the string "Spike 001 — NemoClaw bootstrap feasibility probe" is gone.
    - `diff <(tail -n +3 .claude/skills/spike-findings-openclaw-revenium/sources/001-nemoclaw-bootstrap/probe-host-compat.sh) <(tail -n +3 scripts/probe-host-compat.sh)` shows no logic differences (only the banner near the top differs).
  </acceptance_criteria>
  <done>scripts/probe-host-compat.sh is a verbatim copy of the spike probe with only the banner comment updated; exit-code contract and `set -u` discipline preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Write hermetic test_install_dispatcher.sh covering all VALIDATION rows</name>
  <files>tests/test_install_dispatcher.sh</files>
  <read_first>
    - tests/test_guardrail_argv.sh (primary analog — PASS/FAIL counters via `pass()`/`fail()` with `((PASS++)) || true`, `set -uo pipefail` without -e, `mktemp -d` HOME isolation, `STUB_*` + HOME env-override subprocess invocation, `|| exit_code=$?` capture idiom, `trap cleanup EXIT`, final `Results: N passed, M failed` summary + `exit 1` on failures)
    - tests/test_write_marker.sh (secondary analog — `mktemp -d` per-test HOME, group structure)
    - .planning/phases/12-parallel-install-scaffolding-detection/12-VALIDATION.md (the six Per-Task Verification rows + four-SC observable-behavior table this suite must cover)
    - .planning/phases/12-parallel-install-scaffolding-detection/12-PATTERNS.md (§ tests/test_install_dispatcher.sh — exact `run_install`, exit-capture, byte-stability group patterns)
    - scripts/post-install.sh (lines 1-40 + the line ~739 "Revenium skill installed successfully!" footer — the standalone-path marker string; and note line ~100 `fail` when ~/.openclaw absent, which constrains how the standalone branch is asserted)
  </read_first>
  <action>
    Create `tests/test_install_dispatcher.sh` mirroring `tests/test_guardrail_argv.sh` conventions: `#!/usr/bin/env bash`, `set -uo pipefail` (NOT `-e`), `SCRIPT_DIR`/`REPO_ROOT` resolution, `INSTALL_SH="${REPO_ROOT}/scripts/install.sh"`, `PASS=0`/`FAIL=0` with `pass()`/`fail()` using `((PASS++)) || true`, a `run_install()` helper taking `<uname_stub> <home_dir> <extra_args...>` that invokes `STUB_UNAME_S=... HOME=... bash "${INSTALL_SH}" "$@" 2>&1`, per-group `mktemp -d` HOME isolation, a top-level `trap cleanup EXIT`, and a final `Results: ${PASS} passed, ${FAIL} failed` block that `exit 1`s when `FAIL>0`.

    Cover all SIX VALIDATION.md rows as labeled groups:
    - GROUP A (NCINST-01, D-03 auto-detect): Linux + `~/.nemoclaw/` only (no `~/.openclaw/`) routes to the NemoClaw path — assert output contains a NemoClaw-path marker (e.g. "preflight" or "Phase 13") and does NOT contain "Revenium skill installed".
    - GROUP B (NCINST-01, D-03 explicit flag): Linux + `--nemoclaw` flag (or `NEMOCLAW=1` env) with a dual-home HOME (`~/.nemoclaw/` AND `~/.openclaw/`) still routes NemoClaw — confirms the flag overrides dual-home.
    - GROUP C (NCINST-01, D-03 standalone default): Linux + dual-home (`~/.nemoclaw/` AND `~/.openclaw/`) + NO flag routes standalone — assert NemoClaw markers absent. Because `post-install.sh` itself `fail`s when `~/.openclaw` is incomplete, assert the ROUTING DECISION via output (NemoClaw markers absent / standalone marker or post-install.sh entry observed), NOT a full standalone install success.
    - GROUP D (NCINST-02, macOS refusal — threat T-12-01): `STUB_UNAME_S=Darwin` + NemoClaw signal (`--nemoclaw`) -> capture exit via `|| exit_code=$?`; assert `exit_code != 0` AND output matches `grep -qi "unsupported\|graceful-skip\|linux-only"`.
    - GROUP E (NCINST-02, macOS standalone passthrough, D-05): `STUB_UNAME_S=Darwin` + NO NemoClaw signal + `~/.openclaw/` only -> assert the macOS refusal did NOT fire (output lacks "graceful-skip"/"unsupported") i.e. it routed to standalone.
    - GROUP F (NCINST-01, idempotency, SC4): run the NemoClaw path twice against the same `mktemp -d` HOME (Linux stub) and assert identical exit code (0) on both runs and stable output — proving the read-only/stub skeleton is idempotent.
    - GROUP byte-stable (NCINST-01/02): assert `git -C "${REPO_ROOT}" diff --name-only HEAD -- scripts/post-install.sh` is empty (post-install.sh untouched).

    Use `STUB_UNAME_S=Linux` for NemoClaw-path groups so the copied probe's OS check passes on the macOS dev machine; where the probe would otherwise hard-fail or hit Docker, assert on OUTPUT TEXT (routing markers / refusal message) rather than requiring full end-to-end probe success. Do NOT add `set -e`. Do NOT call any real `nemoclaw`/`docker` binary — the suite must be fully hermetic.
  </action>
  <verify>
    <automated>bash -n tests/test_install_dispatcher.sh && grep -q 'STUB_UNAME_S' tests/test_install_dispatcher.sh && grep -q 'graceful-skip' tests/test_install_dispatcher.sh && grep -q 'diff --name-only' tests/test_install_dispatcher.sh && grep -cv '^#' tests/test_install_dispatcher.sh | awk '{exit ($1>=120)?0:1}' && echo HARNESS_OK; bash tests/test_install_dispatcher.sh; test $? -ne 0 && echo "RED_AS_EXPECTED (install.sh absent)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/test_install_dispatcher.sh` passes; file is >= 120 non-comment lines.
    - File contains `STUB_UNAME_S`, `graceful-skip`, and `git ... diff --name-only ... scripts/post-install.sh`.
    - All six VALIDATION.md groups (A–F + byte-stable) are present as labeled `echo "--- GROUP ..."` sections.
    - Running the suite NOW (before plan 02) exits non-zero because `scripts/install.sh` does not yet exist — the RED state proves the tests actually exercise routing rather than passing vacuously.
    - No `set -e` in the file; assertions use `|| true` / `|| exit_code=$?` so one failing group does not abort the run.
    - No invocation of a real `nemoclaw` or `docker` binary; OS detection is driven solely by `STUB_UNAME_S` and dir presence by `mktemp -d` HOME.
  </acceptance_criteria>
  <done>tests/test_install_dispatcher.sh exists, is hermetic, covers all six VALIDATION.md rows as labeled groups, and is RED (exit != 0) until plan 02 creates scripts/install.sh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args -> install.sh | Untrusted flag input (`--nemoclaw`) and `NEMOCLAW` env cross into routing logic (consumed by plan 02; this plan's test asserts the safe parse) |
| host environment -> probe-host-compat.sh | Reads `/proc/meminfo`, `uname`, `docker info`, `df`, `nvidia-smi` — read-only, non-destructive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Spoofing/Information Disclosure | macOS graceful-skip false-success | mitigate | GROUP D asserts the refusal exits non-zero AND names the Darwin graceful-skip trap (verified here, implemented in plan 02) |
| T-12-PROBE | Tampering | probe-host-compat.sh copy drift | mitigate | Task 1 acceptance criteria diff the copy against source (logic byte-identical, banner-only edit) so the validated exit-code contract is preserved |
| T-12-TMP | Information Disclosure | test tmp HOME dirs | mitigate | `mktemp -d` per group + `trap cleanup EXIT` removes isolated HOMEs (no leftover temp state) |

No npm/pip/cargo installs in this phase — Package Legitimacy Gate is N/A (pure bash, per RESEARCH.md § Package Legitimacy Audit).
</threat_model>

<verification>
- `bash -n` clean on both new files.
- `scripts/probe-host-compat.sh` preserves the verbatim exit-code contract and `set -u` discipline (banner-only edit).
- `tests/test_install_dispatcher.sh` is RED before plan 02 (exit != 0, install.sh absent) and covers all six VALIDATION.md rows.
- `git diff --name-only HEAD -- scripts/post-install.sh` is empty (no accidental edits to the standalone path).
</verification>

<success_criteria>
- Hermetic test harness exists covering all NCINST-01/NCINST-02 routing, refusal, idempotency, and byte-stability behaviors using STUB_UNAME_S + mktemp -d HOME isolation.
- Probe copied verbatim into scripts/ with only the banner updated; exit-code contract intact.
- Suite is RED (proves tests exercise real routing), ready to go GREEN when plan 02 lands the dispatcher + skeleton.
</success_criteria>

<output>
Create `.planning/phases/12-parallel-install-scaffolding-detection/12-01-SUMMARY.md` when done.
</output>
