---
phase: 03-guardrail-engine
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - scripts/cron.sh
  - scripts/post-install.sh
  - scripts/clear-halt.sh
  - scripts/budget-check.sh
autonomous: true
requirements: [GUARD-01]
must_haves:
  truths:
    - "cron.sh runs report.sh then guardrail-check.sh, with no budget-check.sh reference"
    - "post-install.sh chmods common.sh, setup-guardrails.sh, and guardrail-check.sh and no longer references budget-check.sh"
    - "post-install.sh seeds guardrail-status.json placeholder instead of budget-status.json"
    - "clear-halt.sh clears halt in guardrail-status.json atomically, preserving haltedRule/haltedAt"
    - "budget-check.sh is deleted and no script references it"
  artifacts:
    - path: "scripts/cron.sh"
      provides: "Cron pipeline: report.sh -> guardrail-check.sh"
      contains: "guardrail-check.sh"
    - path: "scripts/clear-halt.sh"
      provides: "Halt clear targeting guardrail-status.json"
      contains: "guardrail-status.json"
  key_links:
    - from: "scripts/cron.sh"
      to: "scripts/guardrail-check.sh"
      via: "pipeline invocation after run_report"
      pattern: "guardrail-check.sh"
    - from: "scripts/post-install.sh"
      to: "guardrail-status.json"
      via: "seed placeholder + chmod new scripts"
      pattern: "guardrail-status.json"
---

<objective>
Wire the new guardrail scripts into the install and cron pipeline, retarget the halt-clear utility, and remove the legacy budget-check stage. Updates `cron.sh` (D-20 — report.sh then guardrail-check.sh, no migration stage D-21), `post-install.sh` (chmod the new scripts, seed guardrail-status.json, stop referencing budget-check.sh, update the AGENTS.md budget-check injection), and `clear-halt.sh` (target guardrail-status.json atomically, D-07/Pitfall 8). Then deletes `budget-check.sh` (D-20).

Purpose: completes GUARD-01 end-to-end — the cron tick now produces a fresh guardrail-status.json on every cycle, and the install path provisions the new engine. Ordering matters: cron.sh and post-install.sh must stop referencing budget-check.sh BEFORE it is deleted (Pitfall 7).

Output: updated cron.sh, post-install.sh, clear-halt.sh; deleted budget-check.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guardrail-engine/03-CONTEXT.md
@.planning/phases/03-guardrail-engine/03-RESEARCH.md
@.planning/phases/03-guardrail-engine/03-PATTERNS.md
@scripts/cron.sh
@scripts/post-install.sh
@scripts/clear-halt.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cron.sh pipeline and clear-halt.sh target (D-20, D-21, Pitfall 8)</name>
  <files>scripts/cron.sh, scripts/clear-halt.sh</files>
  <read_first>
    - scripts/cron.sh (lines 67-76 — current flock block with budget-check.sh first)
    - scripts/clear-halt.sh (current body targeting budget-status.json)
    - .planning/phases/03-guardrail-engine/03-PATTERNS.md (cron.sh section; clear-halt.sh new pattern)
    - .planning/phases/03-guardrail-engine/03-RESEARCH.md (cron.sh pipeline example; Pitfall 8 clear-halt target)
  </read_first>
  <action>
    In `scripts/cron.sh`, replace the flock pipeline body (currently `bash "${SKILL_DIR}/scripts/budget-check.sh" || true` then `run_report "$@" || true`) with the reversed D-20 ordering: `run_report "$@" || true` FIRST, then `bash "${SKILL_DIR}/scripts/guardrail-check.sh" || true`. Remove the comment block (current lines ~70-73) explaining why budget-check.sh ran first. Keep `flock -n 9 || exit 0`, the `9>"${LOCK_FILE}"` redirect, the OPENCLAW_HOME probe (lines 12-21), ENV_FILE source, PATH extension, and run_report unchanged. Add no migration stage (D-21).

    Rewrite `scripts/clear-halt.sh` body to target `guardrail-status.json` (Pitfall 8). Preserve the `#!/usr/bin/env bash` + `set -euo pipefail` header. Set `SKILL_DIR="${HOME}/.openclaw/skills/revenium"` and `GUARDRAIL_STATUS_FILE="${SKILL_DIR}/guardrail-status.json"`. If the file is missing, echo `No guardrail-status.json found — nothing to clear.` and exit 0. Otherwise, via python3: load the file; if `halted` is already false, print `No halt is currently active.`; else set `halted=false`, PRESERVE `haltedRule` and `haltedAt` (do NOT pop them — audit trail, differs from the old budget-check version which popped haltedAt), write atomically via tempfile.mkstemp + os.replace, and print `Guardrail halt cleared. The agent may now resume operations.`
  </action>
  <verify>
    <automated>bash -n scripts/cron.sh && bash -n scripts/clear-halt.sh && grep -q 'guardrail-check.sh' scripts/cron.sh && grep -c 'budget-check.sh' scripts/cron.sh | grep -qx 0 && grep -q 'guardrail-status.json' scripts/clear-halt.sh && grep -c 'budget-status.json' scripts/clear-halt.sh | grep -qx 0 && grep -Eq 'os\.(replace|rename)' scripts/clear-halt.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/cron.sh` and `bash -n scripts/clear-halt.sh` both exit 0
    - cron.sh pipeline order is run_report before guardrail-check.sh; `grep -c budget-check.sh scripts/cron.sh` is 0
    - cron.sh contains `guardrail-check.sh` and still has `flock -n 9`
    - clear-halt.sh targets `guardrail-status.json`; `grep -c budget-status.json scripts/clear-halt.sh` is 0
    - clear-halt.sh uses an atomic write (os.replace/os.rename) and does NOT pop haltedRule/haltedAt (no `pop('haltedAt')` or `pop('haltedRule')`)
    - Behavior: run clear-halt.sh against a staged guardrail-status.json with `halted:true` and a haltedRule block — afterward `halted` is false AND haltedRule is still present
  </acceptance_criteria>
  <done>cron.sh runs report.sh then guardrail-check.sh with no budget-check reference or migration stage; clear-halt.sh atomically clears halt in guardrail-status.json while preserving the haltedRule/haltedAt audit fields.</done>
</task>

<task type="auto">
  <name>Task 2: Update post-install.sh and delete budget-check.sh (D-20, Pitfall 7)</name>
  <files>scripts/post-install.sh, scripts/budget-check.sh</files>
  <read_first>
    - scripts/post-install.sh (line 114 chmod loop; lines 385-413 seed step; lines 458-498 AGENTS.md injection referencing budget-status.json; lines 505-518 BUDGET-GUARD deploy)
    - .planning/phases/03-guardrail-engine/03-PATTERNS.md (post-install.sh section — four targeted changes)
    - .planning/phases/03-guardrail-engine/03-RESEARCH.md (Runtime State Inventory — post-install.sh changes required; Pitfall 7 ordering)
  </read_first>
  <action>
    Make four targeted changes to `scripts/post-install.sh`, leaving everything else intact:

    1. chmod loop (line ~114): remove `budget-check.sh`; add `common.sh setup-guardrails.sh guardrail-check.sh`. Final list: `cron.sh report.sh common.sh setup-guardrails.sh guardrail-check.sh install-cron.sh uninstall-cron.sh clear-halt.sh post-install.sh`.

    2. Seed step (lines ~385-413): replace the budget-status.json seeding block (including the `bash "${SKILL_DIR}/scripts/budget-check.sh"` live-seed invocation — remove it per D-20) with a guardrail-status.json placeholder. If `${SKILL_DIR}/guardrail-status.json` is absent, write `{"halted": false, "lastChecked": null, "rules": []}` via python3 (indent=2 + trailing newline) and `info "Seeded guardrail-status.json placeholder"`.

    3. AGENTS.md budget-check injection (lines ~458-498): update the injected section so it reads `~/.openclaw/skills/revenium/guardrail-status.json` and checks `halted` (not `exceeded`), matching the D-07 BUDGET-GUARD.md directive: file missing -> proceed with caution; `halted` false -> proceed silently; `halted` true -> emit the SKILL.md HALT CHECK message from the haltedRule block and stop. Keep the existing injection anchors and idempotency markers.

    4. Verification section (line ~563): replace the budget-status.json/report.sh check with a guardrail-check.sh presence check — if `${SKILL_DIR}/scripts/guardrail-check.sh` exists, `info "Guardrail scripts present"`, else `warn "Guardrail scripts missing — cron enforcement will not work"`.

    The BUDGET-GUARD.md deploy step (copy SKILL_DIR/BUDGET-GUARD.md to workspace) needs no logic change — its source content is updated in Plan 05.

    AFTER all post-install.sh edits and after confirming cron.sh (Task 1) no longer references it, delete `scripts/budget-check.sh` (`git rm scripts/budget-check.sh` or remove the file). Then verify no script references it: `grep -rl budget-check.sh scripts/` must return nothing (Pitfall 7).
  </action>
  <verify>
    <automated>bash -n scripts/post-install.sh && grep -q 'guardrail-status.json' scripts/post-install.sh && grep -q 'setup-guardrails.sh' scripts/post-install.sh && grep -q 'guardrail-check.sh' scripts/post-install.sh && [ ! -f scripts/budget-check.sh ] && [ -z "$(grep -rl 'budget-check.sh' scripts/ 2>/dev/null)" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/post-install.sh` exits 0
    - chmod loop includes `common.sh`, `setup-guardrails.sh`, `guardrail-check.sh` and excludes `budget-check.sh`
    - post-install.sh seeds `guardrail-status.json` with `{"halted": false, ...}` and no longer invokes budget-check.sh to seed
    - The AGENTS.md injected section references `guardrail-status.json` and `halted`; `grep -c 'budget-status.json' scripts/post-install.sh` is 0
    - `scripts/budget-check.sh` no longer exists
    - `grep -rl 'budget-check.sh' scripts/` returns nothing (no remaining references in cron.sh or post-install.sh)
  </acceptance_criteria>
  <done>post-install.sh chmods the three new/updated scripts, seeds guardrail-status.json, references only guardrail-status.json in its injected guidance, and budget-check.sh is deleted with zero remaining references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron daemon → cron.sh | scheduled execution; failures must not abort or email |
| post-install.sh → ~/.openclaw config + workspace | writes seed files, chmods scripts, edits AGENTS.md |
| clear-halt.sh → guardrail-status.json | operator-invoked halt reset; must not corrupt the file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Denial of Service | guardrail-check.sh failure inside cron pipeline | mitigate | cron.sh wraps `guardrail-check.sh` with `|| true` and the outer `flock -n 9 || exit 0`; cron never exits non-zero |
| T-03-14 | Tampering | partial guardrail-status.json write by clear-halt.sh | mitigate | clear-halt.sh writes via tempfile.mkstemp + os.replace; haltedRule/haltedAt preserved for audit |
| T-03-15 | Elevation of Privilege | chmod +x on scripts in HOME | mitigate | post-install.sh chmods only the named scripts under ${SKILL_DIR}/scripts; no world-writable bits set; files owned by current user |
| T-03-16 | Tampering | stale budget-check.sh referenced after partial edit | mitigate | budget-check.sh deleted only AFTER cron.sh/post-install.sh edits; `grep -rl budget-check.sh scripts/` gate confirms zero references (Pitfall 7) |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- `bash -n scripts/cron.sh scripts/clear-halt.sh scripts/post-install.sh` all exit 0
- `grep -rl budget-check.sh scripts/` returns nothing and the file is gone
- cron.sh: report.sh before guardrail-check.sh
- clear-halt.sh and post-install.sh reference guardrail-status.json only
</verification>

<success_criteria>
- cron.sh, post-install.sh, clear-halt.sh updated per D-20/D-21/D-07/Pitfall 8
- budget-check.sh deleted with zero remaining references (Pitfall 7)
- guardrail-status.json is the sole status file referenced across the wiring scripts
</success_criteria>

<output>
Create `.planning/phases/03-guardrail-engine/03-04-SUMMARY.md` when done. Note the final cron.sh pipeline order and confirm budget-check.sh removal.
</output>
