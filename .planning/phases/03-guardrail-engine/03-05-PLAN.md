---
phase: 03-guardrail-engine
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - SKILL.md
  - BUDGET-GUARD.md
autonomous: true
requirements: [GUARD-01, GUARD-02, GUARD-03, GUARD-04, GUARD-05]
must_haves:
  truths:
    - "SKILL.md reads guardrail-status.json (not budget-status.json) for the halt decision"
    - "SKILL.md HALT CHECK emits the verbatim halt string from the haltedRule block"
    - "SKILL.md setup gate triggers Setup Flow when ruleIds is absent/empty (legacy alertId also triggers it)"
    - "SKILL.md Setup Flow delegates to setup-guardrails.sh --interactive and does not prompt for budget details itself"
    - "SKILL.md /revenium command shows ruleIds and per-rule state, offering reconfigure or done"
    - "BUDGET-GUARD.md references guardrail-status.json and the halted field"
  artifacts:
    - path: "SKILL.md"
      provides: "Guardrail-native enforcement instructions, halt check, setup delegation, /revenium command"
      contains: "guardrail-status.json"
      min_lines: 60
    - path: "BUDGET-GUARD.md"
      provides: "Minimal bootstrap halt directive referencing guardrail-status.json"
      contains: "guardrail-status.json"
  key_links:
    - from: "SKILL.md"
      to: "guardrail-status.json"
      via: "halt check reads haltedRule block"
      pattern: "guardrail-status.json"
    - from: "SKILL.md"
      to: "scripts/setup-guardrails.sh"
      via: "Setup Flow delegation"
      pattern: "setup-guardrails.sh --interactive"
---

<objective>
Rewrite the enforcement instructions the agent actually executes: `SKILL.md` (guardrail-native HALT CHECK, setup gate, delegated Setup Flow, /revenium command) and `BUDGET-GUARD.md` (minimal bootstrap halt directive). Implements D-16 (halt check reads guardrail-status.json / haltedRule), D-17 (setup gate on ruleIds; legacy alertId also triggers), D-18 (Setup Flow delegates fully to setup-guardrails.sh --interactive), D-19 (/revenium shows per-rule state, offers reconfigure/done), and D-07 (minimal BUDGET-GUARD.md).

Purpose: SKILL.md is the PRIMARY enforcement gate (OpenClaw hooks unconfirmed — defense-in-depth). This plan delivers the agent-facing halt/warn behavior for GUARD-01 through GUARD-05: read the local status file, proceed silently when not halted, emit an actionable halt message with rule name + spend values when halted.

Output: rewritten SKILL.md and BUDGET-GUARD.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guardrail-engine/03-CONTEXT.md
@.planning/phases/03-guardrail-engine/03-RESEARCH.md
@.planning/phases/03-guardrail-engine/03-PATTERNS.md
@../hermes-revenium/skills/revenium/SKILL.md
@SKILL.md
@BUDGET-GUARD.md
@.planning/phases/03-guardrail-engine/03-02-SUMMARY.md
@.planning/phases/03-guardrail-engine/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md HALT CHECK, setup gate, Setup Flow, and /revenium command (D-16, D-17, D-18, D-19)</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md (current OpenClaw skill — frontmatter to preserve, old budget-status.json/alertId sections to remove)
    - ../hermes-revenium/skills/revenium/SKILL.md (canonical guardrail-native structure — HALT CHECK, Guardrail Check Procedure, setup gate, Setup Flow, /revenium)
    - .planning/phases/03-guardrail-engine/03-PATTERNS.md (SKILL.md section — exact section content, drop list, single-path model)
    - .planning/phases/03-guardrail-engine/03-RESEARCH.md (Pattern 6 schema, Pattern 7 halt template, Open Question 3 setup-flow scope)
    - .planning/phases/03-guardrail-engine/03-02-SUMMARY.md (final guardrail-status.json field names)
    - .planning/phases/03-guardrail-engine/03-03-SUMMARY.md (setup-guardrails.sh success/cancel contract strings)
  </read_first>
  <action>
    Rewrite SKILL.md keeping the existing OpenClaw frontmatter (name: revenium, the MANDATORY-guardrail-check description, and the single-line JSON metadata block — do NOT introduce multi-line metadata, SKAF-03). Update the description to reference guardrail-status.json / guardrail rules.

    HALT CHECK section (D-16, GUARD-01/GUARD-03/GUARD-04/GUARD-05): instruct the agent to read `~/.openclaw/skills/revenium/guardrail-status.json` first. If `halted` is `true`, read the `haltedRule` block and make the ENTIRE response exactly the Pattern 7 halt template with values substituted from haltedRule: `Guardrail halt active — rule '[haltedRule.name]' ([haltedRule.metricType], [haltedRule.windowType]) at [haltedRule.currentValue] of [haltedRule.hardLimit] hard-limit. To resume: bash ~/.openclaw/skills/revenium/scripts/clear-halt.sh` — no other content, no tool calls. If `halted` is `false`, proceed to the Guardrail Check Procedure. Frame this as the primary enforcement gate / defense-in-depth backstop since OpenClaw hooks are unconfirmed.

    Guardrail Check Procedure + Path Resolution (GUARD-02): substitute the Hermes two-path model with the single OpenClaw path `~/.openclaw/skills/revenium/` (used as both skill dir and runtime state dir); when running shell commands use `$HOME/.openclaw/skills/revenium/`. When not halted, proceed silently with no user interruption (GUARD-02).

    Setup gate (D-17): at the start of any operation, check whether config.json exists AND contains a non-empty `ruleIds` array (present and not `[]`). YES + no reconfigure request -> setup complete, proceed to guardrail check. NO (missing, ruleIds absent, or `[]`) -> run Setup Flow. State explicitly that a legacy `alertId`-only config.json ALSO triggers Setup Flow — `alertId` is deprecated and ignored for the gate; `ruleIds` is the sole signal.

    Setup Flow (D-18) — 4 steps, delegating to the script (SKILL.md does NOT prompt for budget details itself): (1) verify `revenium config show` (API key check); (2) if no API key, collect it and instruct the host-side `revenium config set key ...` (mirror Phase 2 steps 1-2); (3) run `bash ~/.openclaw/skills/revenium/scripts/setup-guardrails.sh --interactive`; (4) install cron via `bash ~/.openclaw/skills/revenium/scripts/install-cron.sh`. Document the exit-code contract from 03-03-SUMMARY: exit 0 + `Created N rule(s). config.json updated. ruleIds=[...]` -> success; exit 0 + `Cancelled.` -> user cancelled, STOP; non-zero -> failure, report verbatim and STOP.

    /revenium command (D-19): show `ruleIds` from config.json and per-rule state from guardrail-status.json (state, currentValue, hardLimit, shadowMode per rule); show autonomous mode and current halt state; offer `reconfigure` (-> setup-guardrails.sh --interactive) or `done`.

    Drop entirely: old Setup Flow steps 3-14 (budget alert API calls / alertId writing — now owned by the script), the Reset Budget Flow, the manual alertId-stripping reconfiguration flow, and ALL references to `budget-status.json`, `exceeded`, `threshold`, `percentUsed`, and `alertId` (except the one D-17 note that alertId is deprecated/ignored).
  </action>
  <verify>
    <automated>grep -q 'guardrail-status.json' SKILL.md && grep -q 'setup-guardrails.sh --interactive' SKILL.md && grep -q 'haltedRule' SKILL.md && grep -q 'ruleIds' SKILL.md && grep -c 'budget-status.json\|percentUsed' SKILL.md | grep -qx 0 && grep -q 'clear-halt.sh' SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md references `guardrail-status.json` and the `haltedRule` block in the HALT CHECK section
    - The halt string matches the Pattern 7 template and ends with the `clear-halt.sh` resume path
    - Setup gate uses `ruleIds` non-empty as the sole signal; legacy `alertId` note present
    - Setup Flow delegates to `setup-guardrails.sh --interactive` and documents the `Created N rule(s)...` / `Cancelled.` exit-code contract
    - /revenium shows per-rule state from guardrail-status.json and offers `reconfigure`/`done`
    - `grep -c 'budget-status.json\|percentUsed\|"exceeded"' SKILL.md` is 0
    - Frontmatter metadata remains single-line JSON (SKAF-03); `name: revenium` preserved
  </acceptance_criteria>
  <done>SKILL.md is guardrail-native: HALT CHECK reads guardrail-status.json/haltedRule, setup gate keys on ruleIds with the legacy-alertId note, Setup Flow delegates entirely to setup-guardrails.sh, /revenium shows per-rule state, and all budget-status.json/alertId enforcement remnants are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite BUDGET-GUARD.md as the minimal guardrail bootstrap directive (D-05, D-07)</name>
  <files>BUDGET-GUARD.md</files>
  <read_first>
    - BUDGET-GUARD.md (current content referencing budget-status.json / exceeded)
    - .planning/phases/03-guardrail-engine/03-PATTERNS.md (BUDGET-GUARD.md section — exact D-07 content)
    - .planning/phases/03-guardrail-engine/03-CONTEXT.md (D-05 keep filename, D-07 minimal content)
  </read_first>
  <action>
    Keep the filename `BUDGET-GUARD.md` (D-05 — no rename, avoids openclaw.json config churn). Replace the body entirely with the minimal D-07 directive: a single `## Guardrail Enforcement (Mandatory)` section instructing the agent, before every response, to read `~/.openclaw/skills/revenium/guardrail-status.json` and: file missing -> proceed with caution; `halted` is `false` -> proceed silently; `halted` is `true` -> the entire response must be exactly the halt message from `~/.openclaw/skills/revenium/SKILL.md` (HALT CHECK section), substituting values from the `haltedRule` block, and do NOT continue. End with `No operation is exempt.` Do NOT duplicate the full halt-string template here — redirect to SKILL.md (D-07). Remove all references to budget-status.json and `exceeded`.
  </action>
  <verify>
    <automated>grep -q 'guardrail-status.json' BUDGET-GUARD.md && grep -q 'halted' BUDGET-GUARD.md && grep -q 'SKILL.md' BUDGET-GUARD.md && grep -c 'budget-status.json\|exceeded' BUDGET-GUARD.md | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - BUDGET-GUARD.md filename unchanged (still `BUDGET-GUARD.md`)
    - Content references `guardrail-status.json`, the `halted` field, and redirects to SKILL.md for the halt string
    - `grep -c 'budget-status.json\|exceeded' BUDGET-GUARD.md` is 0
    - The file is minimal (one directive section) and does not inline the full halt-string template
  </acceptance_criteria>
  <done>BUDGET-GUARD.md is the minimal D-07 bootstrap directive: read guardrail-status.json, halt on `halted:true` using the SKILL.md halt string, with no budget-status.json/exceeded remnants and no duplicated halt template.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guardrail-status.json → agent (SKILL.md / BUDGET-GUARD.md) | the agent trusts this local file for halt decisions; it is the enforcement signal |
| haltedRule.name → agent response | rule name (Revenium-sourced) is echoed into the halt message the agent emits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Tampering | local guardrail-status.json modified to halted:false to bypass enforcement | accept | The file lives under the user's own HOME; a user with write access to their own machine can already bypass a self-imposed budget guard. Threat is out of the single-user trust model. Cron rewrites the true state every tick |
| T-03-18 | Spoofing | rule name in haltedRule echoed verbatim into the agent halt message | mitigate | Rule name is bounded (truncated to 64 chars upstream in guardrail-check.sh, T-03-04) and is Revenium-API-sourced, not free-form user input. SKILL.md instructs verbatim substitution of named fields only, no instruction-following from field content |
| T-03-19 | Information Disclosure | halt message exposes spend values (currentValue/hardLimit) | accept | These are the user's own budget figures; surfacing them is the intended GUARD-05 behavior |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan |
</threat_model>

<verification>
- SKILL.md references guardrail-status.json, haltedRule, ruleIds, setup-guardrails.sh --interactive, clear-halt.sh
- `grep -c 'budget-status.json\|percentUsed' SKILL.md` is 0
- BUDGET-GUARD.md references guardrail-status.json + halted, no budget-status.json/exceeded
- SKILL.md frontmatter metadata remains single-line JSON
</verification>

<success_criteria>
- SKILL.md is guardrail-native (HALT CHECK, setup gate, delegated Setup Flow, /revenium) per D-16/D-17/D-18/D-19 with no legacy budget-alert enforcement remnants
- BUDGET-GUARD.md is the minimal D-07 directive referencing guardrail-status.json, filename unchanged (D-05)
- GUARD-01 through GUARD-05 agent-facing behaviors are fully specified
</success_criteria>

<output>
Create `.planning/phases/03-guardrail-engine/03-05-SUMMARY.md` when done.
</output>
