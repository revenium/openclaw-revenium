---
phase: 01-skill-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - ~/.openclaw/skills/revenium/SKILL.md
autonomous: false
requirements:
  - SKAF-01
  - SKAF-02
  - SKAF-03
  - SKAF-04

must_haves:
  truths:
    - "Skill appears in openclaw skills list when revenium binary is on PATH"
    - "Skill does NOT appear in openclaw skills list when revenium binary is removed from PATH"
    - "SKILL.md YAML frontmatter parses without error (no silent drop)"
    - "Skill directory exists at ~/.openclaw/skills/revenium/ with a valid SKILL.md"
  artifacts:
    - path: "SKILL.md"
      provides: "Source-of-truth skill file in repository root"
      contains: "requires.*bins.*revenium"
    - path: "~/.openclaw/skills/revenium/SKILL.md"
      provides: "Installed skill file at OpenClaw discovery path"
      contains: "requires.*bins.*revenium"
  key_links:
    - from: "SKILL.md (repo root)"
      to: "~/.openclaw/skills/revenium/SKILL.md"
      via: "cp command (install)"
      pattern: "cp SKILL.md ~/.openclaw/skills/revenium/SKILL.md"
    - from: "metadata.openclaw.requires.bins"
      to: "openclaw skills list"
      via: "OpenClaw binary gate at load time"
      pattern: "requires.*bins.*revenium"
---

<objective>
Create a valid SKILL.md with correct YAML frontmatter and body skeleton, install it to the OpenClaw skills directory, and verify it loads correctly with binary gating.

Purpose: Establish the revenium skill so OpenClaw discovers it and injects it into the agent system prompt when the revenium binary is on PATH. This is the foundation for Phase 2 (setup flow) and Phase 3 (budget guard).

Output: SKILL.md in repo root (source of truth) and installed at ~/.openclaw/skills/revenium/SKILL.md (runtime location).
</objective>

<execution_context>
@/Users/johndemic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/johndemic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-scaffolding/01-CONTEXT.md
@.planning/phases/01-skill-scaffolding/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author SKILL.md with frontmatter and body skeleton</name>
  <files>SKILL.md</files>
  <action>
Create `SKILL.md` in the repository root with the following exact structure:

**YAML Frontmatter (all constraints from RESEARCH.md must be met):**

```yaml
---
name: revenium
description: "Mandatory Revenium budget enforcement for every agent operation. Checks token spend against your configured budget before each tool call, warns when thresholds are exceeded, and handles first-time setup and reconfiguration."
metadata: {"openclaw":{"emoji":"💰","version":"0.1.0","homepage":"https://docs.revenium.io/for-ai-agents","requires":{"bins":["revenium"]},"user-invocable":true}}
---
```

Critical constraints (from RESEARCH.md):
- `description` MUST be wrapped in double quotes (colon-space in unquoted string silently drops skill)
- `metadata` MUST be a single-line JSON value (multi-line silently bypasses requires.bins gate)
- `name` value: `revenium` (per user decision)
- `metadata.openclaw.emoji`: "💰" (per user decision)
- `metadata.openclaw.version`: "0.1.0" (per user decision)
- `metadata.openclaw.homepage`: "https://docs.revenium.io/for-ai-agents" (per user decision)
- `metadata.openclaw.requires.bins`: ["revenium"] (per user decision -- binary gate)
- `metadata.openclaw.user-invocable`: true (per user decision -- enables /revenium slash command)

**Body skeleton (guard-first ordering per Claude's discretion -- optimizes LLM compliance with mandatory guard):**

```markdown
## Operation Guard

BEFORE executing any tool call, you MUST check the Revenium budget.

[Phase 3 will fill this section with the full budget check flow, warn-and-ask logic, and hard-stop mode.]

## Setup

When no configuration exists at `{baseDir}/config.json`, you MUST run the setup flow before any operation.

[Phase 2 will fill this section with API key configuration, budget alert creation, and anomaly ID persistence.]

## `/revenium` Command

When the user invokes `/revenium`:
- If setup is complete: show current budget status, then offer to reconfigure
- If setup is not complete: run setup flow

[Phase 2 will fill this section with the full slash command behavior.]
```

The body uses strong mandatory language (MUST, STOP, NEVER) per user decision. Guard section comes first to maximize LLM instruction compliance. Include a brief troubleshooting section at the end for common issues (binary not found, API key invalid) per Claude's discretion -- this improves agent reliability.
  </action>
  <verify>
    <automated>test -f SKILL.md && head -1 SKILL.md | grep -q "^---" && grep -q '"requires":{"bins":\["revenium"\]}' SKILL.md && grep -q 'user-invocable' SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SKILL.md exists in repo root with valid YAML frontmatter containing all locked metadata fields (name, description, emoji, version, homepage, requires.bins, user-invocable) on single-line JSON, and body skeleton with guard-first section ordering.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Install OpenClaw, deploy SKILL.md, and verify binary gating</name>
  <files>~/.openclaw/skills/revenium/SKILL.md</files>
  <action>
Automated steps the executor runs before the checkpoint pause:

1. Check if `openclaw` is on PATH (`which openclaw`). If not, install it by checking https://docs.openclaw.ai/getting-started/installation (likely `npm install -g @openclaw/cli` or `brew install openclaw` -- executor should check docs and use whatever install method is documented).

2. Create the skill directory:
   ```bash
   mkdir -p ~/.openclaw/skills/revenium
   ```

3. Copy SKILL.md to install location:
   ```bash
   cp SKILL.md ~/.openclaw/skills/revenium/SKILL.md
   ```

4. Run presence test (SKAF-04): `openclaw skills list | grep revenium` -- skill MUST appear.

5. Run absence test (SKAF-02): temporarily move `revenium` binary out of PATH (e.g., `mv $(which revenium) /tmp/revenium-backup`), run `openclaw skills list | grep revenium` -- skill MUST NOT appear. Then restore: `mv /tmp/revenium-backup $(which revenium)`.

If `openclaw skills list` does not show the skill after installation, the frontmatter has a parse error. Debug by checking: unquoted colon-space in description, multi-line metadata, or incorrect JSON in metadata field.

After running all automated steps, pause for human verification.
  </action>
  <verify>
    <automated>test -f ~/.openclaw/skills/revenium/SKILL.md && openclaw skills list 2>/dev/null | grep -q revenium && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SKILL.md installed at ~/.openclaw/skills/revenium/SKILL.md. Presence test passes (skill in list with revenium on PATH). Absence test passes (skill not in list with revenium off PATH). Human has verified both states.</done>
  <what-built>
Complete SKILL.md authoring, installation, and binary gate verification. The executor automated: OpenClaw installation (if needed), directory creation, file copy, presence test, and absence test.
  </what-built>
  <how-to-verify>
    1. Run `openclaw skills list` -- confirm revenium skill appears with correct emoji and name
    2. Confirm the skill shows the correct metadata (emoji, name)
    3. Temporarily remove revenium from PATH, run `openclaw skills list` again -- confirm revenium does NOT appear
    4. Restore revenium to PATH
  </how-to-verify>
  <resume-signal>Type "approved" if both presence and absence tests pass, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
All four SKAF requirements verified:
- SKAF-01: `test -f ~/.openclaw/skills/revenium/SKILL.md` returns true
- SKAF-02: Skill absent from `openclaw skills list` when revenium not on PATH
- SKAF-03: `openclaw skills list` shows skill (proves frontmatter parsed correctly -- silent drop means parse error)
- SKAF-04: `openclaw skills list | grep revenium` returns match
</verification>

<success_criteria>
1. SKILL.md exists in repo root with valid YAML frontmatter (all locked metadata fields present, single-line JSON metadata)
2. SKILL.md installed at ~/.openclaw/skills/revenium/SKILL.md
3. `openclaw skills list` shows revenium when binary is on PATH
4. `openclaw skills list` does NOT show revenium when binary is removed from PATH
5. Body skeleton has guard-first section ordering with placeholder content for Phase 2 and Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-scaffolding/01-01-SUMMARY.md`
</output>
