---
phase: 13-sandbox-provisioning-egress-cli-authenticated-metering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/revenium-policy.yaml
  - scripts/gh-release-policy.yaml
  - tests/stub-nemoclaw.sh
  - tests/test_nemoclaw_provisioning.sh
autonomous: true
requirements: [NCEGRESS-01, NCCLI-01, NCCLI-02]

must_haves:
  truths:
    - "scripts/revenium-policy.yaml exists as a host-scoped api.revenium.ai egress preset"
    - "scripts/gh-release-policy.yaml exists covering release-assets.githubusercontent.com AND objects.githubusercontent.com"
    - "tests/stub-nemoclaw.sh captures argv without eval and is switchable via STUB_NEMOCLAW_* env vars"
    - "tests/test_nemoclaw_provisioning.sh runs and reports PASS/FAIL counts for GROUP A–G"
  artifacts:
    - path: "scripts/revenium-policy.yaml"
      provides: "api.revenium.ai:443 tls:skip egress preset shipped as a first-class asset"
      contains: "api.revenium.ai"
    - path: "scripts/gh-release-policy.yaml"
      provides: "GitHub release CDN egress preset for the in-sandbox tarball fetch"
      contains: "release-assets.githubusercontent.com"
    - path: "tests/stub-nemoclaw.sh"
      provides: "argv-capturing nemoclaw stub with curl-http-code / sha256-match / meter-fail switches"
      min_lines: 40
    - path: "tests/test_nemoclaw_provisioning.sh"
      provides: "hermetic provisioning test harness (GROUP A–G)"
      min_lines: 60
  key_links:
    - from: "tests/test_nemoclaw_provisioning.sh"
      to: "tests/stub-nemoclaw.sh"
      via: "symlink named nemoclaw onto a tmp PATH"
      pattern: "ln -sf.*stub-nemoclaw"
    - from: "tests/test_nemoclaw_provisioning.sh"
      to: "scripts/post-install-nemoclaw.sh"
      via: "LEDGER_FILE + HOME override invocation"
      pattern: "post-install-nemoclaw"
---

<objective>
Build the Wave 0 validation scaffold for Phase 13 provisioning: ship the two egress preset YAMLs as first-class `scripts/` assets and create the hermetic test harness (`stub-nemoclaw.sh` + `test_nemoclaw_provisioning.sh`) that Plan 02 implements against.

Purpose: Per the Nyquist rule, the tests and shipped presets must exist before the provisioning logic so Plan 02 has a red→green target. The presets are shipped assets because the skill does not distribute `spike-findings/sources/`, so `policy-add --from-file` needs them present on the install target (same rationale as Phase 12's D-09 for probe-host-compat.sh).

Output: `scripts/revenium-policy.yaml`, `scripts/gh-release-policy.yaml`, `tests/stub-nemoclaw.sh`, `tests/test_nemoclaw_provisioning.sh`. The harness runs and reports PASS/FAIL; GROUP scenarios that exercise not-yet-implemented provisioning functions are expected to FAIL until Plan 02 lands (this is the intended RED state).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-RESEARCH.md
@.planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-PATTERNS.md
@.planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the two egress preset YAMLs into scripts/</name>
  <read_first>
    - .claude/skills/spike-findings-openclaw-revenium/sources/002-openshell-egress/revenium-policy.yaml (exact source to copy)
    - .claude/skills/spike-findings-openclaw-revenium/sources/003-revenium-cli-in-sandbox/gh-release-policy.yaml (exact source to copy)
    - .planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-PATTERNS.md (§ scripts/revenium-policy.yaml and § scripts/gh-release-policy.yaml — the verbatim content + key constraints)
    - .claude/skills/spike-findings-openclaw-revenium/references/sandbox-egress-policies.md (tls:skip rationale, host-scoped constraint)
  </read_first>
  <action>
    Copy the two spike preset YAMLs to scripts/ as first-class shipped assets (D-03):
    1. Copy sources/002-openshell-egress/revenium-policy.yaml → scripts/revenium-policy.yaml. Preserve the schema exactly: preset.name=revenium, network_policies.revenium with endpoint host=api.revenium.ai port=443 access=full tls=skip, binaries path=/**. The tls:skip line is non-negotiable (L7 REST mode breaks Node 22 undici CONNECT tunnels). Replace the spike-specific comment header (referencing "Spike 002") with a production-appropriate header referencing Phase 13 and the api.revenium.ai egress purpose.
    2. Copy sources/003-revenium-cli-in-sandbox/gh-release-policy.yaml → scripts/gh-release-policy.yaml. Preserve BOTH endpoints: host=release-assets.githubusercontent.com AND host=objects.githubusercontent.com, each port=443 access=full tls=skip, binaries path=/**. Both hosts are mandatory — the GitHub CDN redirects between them; omitting either breaks the in-sandbox tarball fetch (D-01) silently.
    Keep both presets narrow/host-scoped — do not add any other hosts or widen access. These two files become the policy-add --from-file inputs for Plan 02.
  </action>
  <verify>
    <automated>test -f scripts/revenium-policy.yaml &amp;&amp; test -f scripts/gh-release-policy.yaml &amp;&amp; grep -q 'api.revenium.ai' scripts/revenium-policy.yaml &amp;&amp; grep -q 'release-assets.githubusercontent.com' scripts/gh-release-policy.yaml &amp;&amp; grep -q 'objects.githubusercontent.com' scripts/gh-release-policy.yaml &amp;&amp; grep -cq 'tls: skip' scripts/revenium-policy.yaml</automated>
  </verify>
  <acceptance_criteria>
    - scripts/revenium-policy.yaml exists and contains api.revenium.ai, port 443, and a tls: skip line
    - scripts/gh-release-policy.yaml exists and contains BOTH release-assets.githubusercontent.com and objects.githubusercontent.com
    - Neither preset references any host beyond those three (host-scoped, not broad)
    - revenium-policy.yaml header references Phase 13 / api.revenium.ai egress, not "Spike 002"
  </acceptance_criteria>
  <done>Both presets ship in scripts/ with verbatim schema from the spike sources and host-scoped endpoints only.</done>
</task>

<task type="auto">
  <name>Task 2: Create the argv-capturing nemoclaw stub</name>
  <read_first>
    - tests/stub-revenium.sh (exact analog — header, argv-capture idiom, subcommand dispatch, default exit 0)
    - .planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-PATTERNS.md (§ tests/stub-nemoclaw.sh — header switches, argv capture, dispatch structure)
    - .planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-RESEARCH.md (§ Hermetic Test Strategy — the four stub env switches)
  </read_first>
  <action>
    Create tests/stub-nemoclaw.sh modeled on tests/stub-revenium.sh. Start with `set -uo pipefail` (no -e). Capture every positional arg with `printf '%s\n' "${arg}" >> "${STUB_NEMOCLAW_ARGV_FILE}"` when STUB_NEMOCLAW_ARGV_FILE is set — NEVER eval or string-interpolate captured argv (security: T-13-SC). Implement these env switches:
    - STUB_NEMOCLAW_CURL_HTTP_CODE (default "403") — value echoed when the captured exec payload contains an http_code probe to api.revenium.ai. Set "000" to simulate the proxy block.
    - STUB_NEMOCLAW_SHA256_MATCH (default "1") — when "0", the sha256/exec tarball path echoes "CHECKSUM_MISMATCH:badhash" to stderr and exits 2; when "1", echoes the pinned tarball sha256 cc4b07e94589af082dc21ecba7e235ebc1dd52f010238fd932dec6003a816f67 followed by CLI_DELIVERED_OK.
    - STUB_NEMOCLAW_METER_FAIL (set/non-empty) — the meter completion exec path echoes a non-2xx/bad JSON body (e.g. {"error":"unauthorized"}) to stderr and exits 1; otherwise echoes a 2xx success body (e.g. {"status":"ok"}).
    Dispatch by inspecting argv: a `policy-add` invocation prints a "Policy version loaded." line and exits 0; an `exec` invocation detects the in-sandbox payload type (http_code probe vs sha256/tarball deliver vs meter completion) by grepping the captured payload with `grep -qF` and emits the switch-controlled output. Any other subcommand falls through to a default `exit 0`. Make the script executable (chmod +x).
  </action>
  <verify>
    <automated>bash -n tests/stub-nemoclaw.sh &amp;&amp; STUB_NEMOCLAW_ARGV_FILE=/tmp/_argv.$$ bash tests/stub-nemoclaw.sh revenium-spike policy-add --from-file x.yaml --yes &amp;&amp; grep -qF 'policy-add' /tmp/_argv.$$; rm -f /tmp/_argv.$$</automated>
  </verify>
  <acceptance_criteria>
    - bash -n tests/stub-nemoclaw.sh passes (valid syntax)
    - Invoking the stub with policy-add appends the args to STUB_NEMOCLAW_ARGV_FILE one per line
    - The stub contains no `eval` of captured argv (grep -c 'eval' returns 0 over non-comment lines)
    - STUB_NEMOCLAW_CURL_HTTP_CODE, STUB_NEMOCLAW_SHA256_MATCH, STUB_NEMOCLAW_METER_FAIL are all honored in the exec dispatch branches
  </acceptance_criteria>
  <done>tests/stub-nemoclaw.sh captures argv safely and emits switch-controlled output for the http_code probe, sha256 verify, and meter completion exec payloads.</done>
</task>

<task type="auto">
  <name>Task 3: Create the hermetic provisioning test harness</name>
  <read_first>
    - tests/test_install_dispatcher.sh (exact analog — header, cleanup/trap, make_home, run-under-test invocation, assert-output, summary/exit)
    - tests/test_guardrail_argv.sh (stub-on-PATH symlink pattern; argv_vals awk helper)
    - .planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-PATTERNS.md (§ tests/test_nemoclaw_provisioning.sh — boilerplate + GROUP labels)
    - .planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-VALIDATION.md (Per-Task Verification Map — which GROUP maps to which requirement)
    - scripts/post-install-nemoclaw.sh (the script under test — current Phase 12 skeleton; understand its invocation contract)
  </read_first>
  <action>
    Create tests/test_nemoclaw_provisioning.sh modeled on tests/test_install_dispatcher.sh. Use `set -uo pipefail` (no -e). Define PASS/FAIL counters with pass()/fail() helpers. Use a cleanup trap that rm -rf's tmp HOMEs. Provide make_home() that mktemp's an isolated dir and mkdir -p's its .nemoclaw/ subdir. Symlink tests/stub-nemoclaw.sh as `nemoclaw` onto a tmp .local/bin and prepend it to PATH. Provide a run_provision() helper that invokes `bash scripts/post-install-nemoclaw.sh` with these env overrides: STUB_NEMOCLAW_ARGV_FILE, LEDGER_FILE=<home>/.nemoclaw/revenium-nemoclaw.ledger, HOME=<home>, REVENIUM_SANDBOX_NAME=test-sandbox, and REVENIUM_API_KEY=test-key (plus the STUB_NEMOCLAW_* switch under test). Capture combined stdout+stderr.
    Implement these GROUP scenarios (assertions use `grep -qi`/`grep -qF`; ledger assertions grep the LEDGER_FILE):
    - GROUP A (NCEGRESS-01 SC2): STUB_NEMOCLAW_CURL_HTTP_CODE=000 → output contains "api.revenium.ai" AND "policy" (policy-gap message), and the run exits non-zero.
    - GROUP B (NCEGRESS-01 SC2): STUB_NEMOCLAW_CURL_HTTP_CODE=403 → output does NOT contain the policy-gap failure; egress is reported confirmed.
    - GROUP C (NCCLI-01): STUB_NEMOCLAW_SHA256_MATCH=0 → output contains "sha256" or "checksum" mismatch wording and the run exits non-zero (install aborted).
    - GROUP D (NCCLI-01): STUB_NEMOCLAW_SHA256_MATCH=1 → CLI delivery proceeds; ledger gains cli-delivered.
    - GROUP E (NCCLI-01): pre-populate LEDGER_FILE with cli-delivered=v1.2.0:cc4b07e94589af082dc21ecba7e235ebc1dd52f010238fd932dec6003a816f67 → re-run skips delivery (output mentions "skipping" for the CLI step).
    - GROUP F (NCCLI-02): pre-populate LEDGER_FILE with meter-probe-passed=1 → meter probe is skipped (no new meter-completion exec captured in ARGV_FILE for that run).
    - GROUP G (all SC): all switches default/success → after a full run, the ledger contains all five keys: revenium-policy-applied, gh-release-policy-applied, cli-delivered, creds-written, meter-probe-passed.
    Print a "Results: N passed, M failed" summary and exit 1 if any failed. NOTE for the executor: these GROUPs exercise provisioning functions Plan 02 adds — running this harness now is expected to FAIL most GROUPs (the Phase 12 skeleton only prints stub warnings). That RED state is correct for Wave 0; the syntax/self-check below is the gate for this plan.
  </action>
  <verify>
    <automated>bash -n tests/test_nemoclaw_provisioning.sh &amp;&amp; bash -n tests/stub-nemoclaw.sh &amp;&amp; grep -q 'GROUP A' tests/test_nemoclaw_provisioning.sh &amp;&amp; grep -q 'GROUP G' tests/test_nemoclaw_provisioning.sh &amp;&amp; grep -qF 'meter-probe-passed' tests/test_nemoclaw_provisioning.sh &amp;&amp; grep -qF 'post-install-nemoclaw.sh' tests/test_nemoclaw_provisioning.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash -n tests/test_nemoclaw_provisioning.sh passes
    - The harness defines GROUP A through GROUP G and references all five ledger keys
    - The harness symlinks stub-nemoclaw.sh as `nemoclaw` onto PATH and invokes scripts/post-install-nemoclaw.sh with LEDGER_FILE + HOME overrides (does not touch the real ~/.nemoclaw/)
    - Running the harness produces a "Results: N passed, M failed" line (executing without crash, even though GROUPs fail in the RED Wave-0 state)
  </acceptance_criteria>
  <done>The hermetic harness exists, has valid syntax, covers GROUP A–G against post-install-nemoclaw.sh via the stub, and runs to a results summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → script under test | Hermetic; the stub replaces the real `nemoclaw`. No real network or sandbox is touched. |
| captured argv → assertions | Test reads argv from a file; must never execute it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | tests/stub-nemoclaw.sh argv handling | mitigate | Capture argv with `printf '%s\n'`; string-COMPARE only; never `eval` or interpolate captured argv into a shell command (mirrors tests/stub-revenium.sh security comment) |
| T-13-EGR | Information Disclosure / EoP | scripts/*.yaml egress presets | mitigate | Presets are host-scoped (api.revenium.ai + the two GitHub CDN hosts only); no broad allow; `tls: skip` is L4 CONNECT pass-through, not an interception widening |
| T-13-ISO | Tampering | test ledger writes | mitigate | Harness overrides LEDGER_FILE + HOME to tmp dirs so tests never mutate the real `~/.nemoclaw/revenium-nemoclaw.ledger` |
</threat_model>

<verification>
- `bash -n` passes on both new test scripts.
- `bash tests/test_nemoclaw_provisioning.sh` runs to a "Results:" summary (RED state acceptable this wave).
- `bash tests/test_install_dispatcher.sh` still passes (no regression — this plan does not touch the dispatcher).
- Both preset YAMLs present in scripts/ with host-scoped endpoints.
</verification>

<success_criteria>
- scripts/revenium-policy.yaml and scripts/gh-release-policy.yaml shipped with verbatim spike schema, host-scoped only.
- tests/stub-nemoclaw.sh captures argv safely and honors the three STUB_NEMOCLAW_* switches.
- tests/test_nemoclaw_provisioning.sh covers GROUP A–G and runs against post-install-nemoclaw.sh via LEDGER_FILE/HOME overrides.
</success_criteria>

<output>
Create `.planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-01-SUMMARY.md` when done.
</output>
