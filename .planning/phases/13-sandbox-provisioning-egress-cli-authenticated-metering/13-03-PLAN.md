---
phase: 13-sandbox-provisioning-egress-cli-authenticated-metering
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - .claude/skills/spike-findings-openclaw-revenium/sources/003-revenium-cli-in-sandbox/README.md
  - .claude/skills/spike-findings-openclaw-revenium/SKILL.md
autonomous: false
requirements: [NCCLI-02]
user_setup:
  - service: revenium
    why: "Authenticated meter call requires a real Revenium API key (D-LIVE — key available this session)"
    env_vars:
      - name: REVENIUM_API_KEY
        source: "Revenium dashboard — provided for this session per D-LIVE"
      - name: REVENIUM_TEAM_ID
        source: "Revenium dashboard (optional but recommended)"
      - name: REVENIUM_SANDBOX_NAME
        source: "The live NemoClaw sandbox name on 34.224.27.67 (revenium-spike)"

must_haves:
  truths:
    - "Running the real install on the live sandbox applies both policies, delivers the CLI, writes creds, and the authenticated meter probe returns HTTP 2xx (NCCLI-02, SC4)"
    - "The ledger on the live host has all five keys after the run"
    - "Spike 003 is flipped from PARTIAL to VALIDATED with the live-2xx evidence recorded"
  artifacts:
    - path: ".claude/skills/spike-findings-openclaw-revenium/sources/003-revenium-cli-in-sandbox/README.md"
      provides: "VALIDATED verdict + authenticated-2xx evidence note"
      contains: "VALIDATED"
  key_links:
    - from: ".claude/skills/spike-findings-openclaw-revenium/SKILL.md"
      to: "spike 003 verdict row"
      via: "verdict table update PARTIAL->VALIDATED"
      pattern: "003.*VALIDATED"
---

<objective>
Close NCCLI-02 / SC4 for real: run the full `post-install-nemoclaw.sh` provisioning flow against the live NemoClaw sandbox on host 34.224.27.67 with a real Revenium API key (D-LIVE), confirm the authenticated `revenium meter completion` returns HTTP 2xx, then flip spike 003 from PARTIAL to VALIDATED.

Purpose: The hermetic suite (Plan 02) proves the wiring; this plan proves the authenticated billable call actually succeeds against the real Revenium API — the phase's headline proof. D-LIVE makes this in-scope this phase rather than a deferred UAT.

Output: A passing live smoke (all five ledger keys + a 2xx-tagged synthetic event in Revenium) and an updated spike 003 README + SKILL.md verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-RESEARCH.md
@.planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-VALIDATION.md
@.planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-02-SUMMARY.md
@.claude/skills/spike-findings-openclaw-revenium/sources/003-revenium-cli-in-sandbox/README.md
@.claude/skills/spike-findings-openclaw-revenium/SKILL.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Live smoke on 34.224.27.67 — authenticated meter probe returns 2xx (D-LIVE)</name>
  <read_first>
    - .planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-VALIDATION.md (Manual-Only Verifications — the exact live-smoke assertions)
    - .planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-RESEARCH.md (Validation Architecture -> Live smoke (D-LIVE); Open Question 2 on meter exit behavior)
    - .claude/skills/spike-findings-openclaw-revenium/sources/003-revenium-cli-in-sandbox/README.md (the reopen note this run closes)
  </read_first>
  <what-built>
    Plan 02 delivered a fully provisioning scripts/post-install-nemoclaw.sh (two-preset egress, sha256-verified CLI delivery, config-file creds, ledger-gated authenticated meter probe). The hermetic suite is green. This checkpoint executes that script for real on the live NemoClaw host.
  </what-built>
  <how-to-verify>
    On host 34.224.27.67 (ssh via the user's agent socket; sandbox revenium-spike):
    1. Export the live credentials: REVENIUM_API_KEY (real key per D-LIVE), REVENIUM_TEAM_ID (and tenant/owner if available), and REVENIUM_SANDBOX_NAME=revenium-spike.
    2. (Optional clean re-run) Remove or back up ~/.nemoclaw/revenium-nemoclaw.ledger so the meter probe is not skipped by a prior run.
    3. Run: REVENIUM_SANDBOX_NAME=revenium-spike REVENIUM_API_KEY=... [REVENIUM_TEAM_ID=...] bash scripts/post-install-nemoclaw.sh
    4. Confirm ALL of:
       - The run completes with the Phase 13 success banner (exit 0).
       - cat ~/.nemoclaw/revenium-nemoclaw.ledger shows all five keys: revenium-policy-applied, gh-release-policy-applied, cli-delivered=v1.2.0:cc4b07e9..., creds-written, meter-probe-passed.
       - nemoclaw revenium-spike exec -- /sandbox/.local/bin/revenium version reports 1.2.0.
       - nemoclaw revenium-spike exec -- ls -l /sandbox/.config/revenium/config.yaml shows the file at mode 600.
       - The meter probe step reported a 2xx / success (authenticated meter call succeeded — NOT the dummy-key 403 reach proof).
       - The Revenium dashboard shows one new completion event tagged task-type install-smoke-test.
    5. (Resume check) Re-run the script unchanged -> every step reports "skipping (ledger)" and NO second synthetic meter event is emitted (exactly-once guarantee, D-06).
  </how-to-verify>
  <acceptance_criteria>
    - The full post-install-nemoclaw.sh run on 34.224.27.67 exits 0 with the Phase 13 success banner
    - All five ledger keys present in ~/.nemoclaw/revenium-nemoclaw.ledger
    - The authenticated revenium meter completion call returned HTTP 2xx (operator confirms the status)
    - A re-run emits no second synthetic meter event (exactly-once)
  </acceptance_criteria>
  <action>
    Operator-driven live smoke (no autonomous code changes in this task). Execute the full provisioning flow on the live host per the how-to-verify steps, then report the meter-probe HTTP status in the resume-signal. The executor performs the run if it has SSH access to 34.224.27.67 via the user's agent socket; otherwise it pauses for the operator to run it. The credentials (REVENIUM_API_KEY, optional REVENIUM_TEAM_ID/TENANT/OWNER, REVENIUM_SANDBOX_NAME=revenium-spike) are supplied as env vars — never committed to any file or SUMMARY.
  </action>
  <verify>
    <human-check>Operator confirms the authenticated meter probe returned HTTP 2xx on 34.224.27.67, all five ledger keys are present, and a re-run emits no second synthetic event.</human-check>
  </verify>
  <done>The live authenticated meter call returned HTTP 2xx and the operator approved with the status; exactly-once confirmed on re-run.</done>
  <resume-signal>Type "approved" with the meter-probe HTTP status (e.g. "approved — 200"), or describe the failure (HTTP code + which step).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Flip spike 003 to VALIDATED in the README and SKILL.md verdict table</name>
  <read_first>
    - .claude/skills/spike-findings-openclaw-revenium/sources/003-revenium-cli-in-sandbox/README.md (the PARTIAL verdict + reopen note to update)
    - .claude/skills/spike-findings-openclaw-revenium/SKILL.md (Spike verdicts table — row 003)
  </read_first>
  <action>
    After the Task 1 checkpoint is approved with a 2xx:
    1. In sources/003-revenium-cli-in-sandbox/README.md: change the spike verdict from PARTIAL to VALIDATED. Replace the reopen note ("authenticated meter call not yet run — provide a real key to flip to VALIDATED") with a closure note recording: the date (2026-06-08), the live host 34.224.27.67 / sandbox revenium-spike, the observed HTTP 2xx status from the operator's resume-signal, and that the call was made via the Phase 13 post-install-nemoclaw.sh provisioning flow with the install-smoke-test synthetic tag.
    2. In SKILL.md: update the spike-verdicts table row for 003 from "PARTIAL (auth meter pending a real Revenium key)" to "VALIDATED".
    Do not alter any other spike rows or content. Use the exact HTTP status the operator reported in the resume-signal.
  </action>
  <verify>
    <automated>grep -q 'VALIDATED' .claude/skills/spike-findings-openclaw-revenium/sources/003-revenium-cli-in-sandbox/README.md &amp;&amp; grep -Eq '003.*revenium-cli-in-sandbox.*VALIDATED' .claude/skills/spike-findings-openclaw-revenium/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - sources/003-revenium-cli-in-sandbox/README.md verdict reads VALIDATED with a dated live-2xx closure note (host, sandbox, HTTP status, provisioning-flow provenance)
    - SKILL.md spike-verdicts row 003 reads VALIDATED (no longer PARTIAL)
    - No other spike rows or content changed
  </acceptance_criteria>
  <done>Spike 003 is VALIDATED in both the README and SKILL.md, with the live authenticated-2xx evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live Revenium API | A real authenticated billable write crosses to production Revenium. |
| live sandbox → api.revenium.ai | The authenticated meter call leaves the sandbox over the applied egress policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-BILL | Spoofing | live billable meter probe | mitigate | The probe is ledger-gated exactly-once (meter-probe-passed) and tagged --task-type install-smoke-test; the live run emits a single, clearly-labeled synthetic event distinguishable from real agent metering |
| T-13-KEY | Information Disclosure | real REVENIUM_API_KEY on the live host | mitigate | Key supplied as env to the install which writes it to config.yaml chmod 600 (Plan 02 / D-05); operator must not paste the key into committed files or the SUMMARY |
| T-13-DOC | Repudiation | spike verdict update | accept | Verdict flip is documentation only; recorded with the operator-reported HTTP status as evidence |
</threat_model>

<verification>
- Live smoke on 34.224.27.67: all five ledger keys, binary 1.2.0, config.yaml mode 600, authenticated meter probe 2xx, single synthetic event in Revenium, re-run emits no second event.
- spike 003 README and SKILL.md both read VALIDATED.
</verification>

<success_criteria>
- NCCLI-02 / SC4 closed: an authenticated revenium meter completion from inside the live sandbox returns HTTP 2xx (D-LIVE).
- Spike 003 flipped PARTIAL -> VALIDATED with dated live evidence.
- Exactly-once billable guarantee confirmed on re-run.
</success_criteria>

<output>
Create `.planning/phases/13-sandbox-provisioning-egress-cli-authenticated-metering/13-03-SUMMARY.md` when done.
</output>
