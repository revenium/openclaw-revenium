---
phase: 14-host-side-metering-loop
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - scripts/uninstall-nemoclaw-cron.sh
  - scripts/post-install-nemoclaw.sh
autonomous: true
requirements: [NCMETER-01]
must_haves:
  truths:
    - "An operator can remove a single sandbox's NemoClaw metering cron (and optionally unmount) without touching the standalone cron or other sandboxes"
    - "Running the NemoClaw post-install actually installs the host-side metering loop (the Phase 13 stub is replaced with a real, ledger-gated call to install-nemoclaw-cron.sh)"
    - "The three shared workhorse scripts (cron.sh, report.sh, guardrail-check.sh) are byte-identical to their pre-phase state (SC4)"
  artifacts:
    - path: "scripts/uninstall-nemoclaw-cron.sh"
      provides: "Per-sandbox NemoClaw cron uninstaller with optional unmount"
      min_lines: 20
    - path: "scripts/post-install-nemoclaw.sh"
      provides: "Real install_metering_loop wired into the provisioning flow (replaces stub_install_metering_loop)"
      contains: "install-nemoclaw-cron.sh"
  key_links:
    - from: "scripts/post-install-nemoclaw.sh"
      to: "scripts/install-nemoclaw-cron.sh"
      via: "ledger-gated install_metering_loop invocation (replaces the Phase 13 stub)"
      pattern: "install-nemoclaw-cron\\.sh"
    - from: "scripts/uninstall-nemoclaw-cron.sh"
      to: "crontab"
      via: "remove only the # revenium-metering-nemoclaw:<sandbox> marker line"
      pattern: "revenium-metering-nemoclaw:"
---

<objective>
Close Phase 14: deliver the uninstall counterpart and wire the host-side metering loop into the
real provisioning flow by replacing the Phase 13 `stub_install_metering_loop`. Then prove the SC4
invariant holds (the shared workhorses are untouched).

Purpose: make NCMETER-01 actually fire during a NemoClaw install (not a deferred stub), give operators
a clean per-sandbox teardown, and lock the no-regression guarantee.
Output: scripts/uninstall-nemoclaw-cron.sh + the stub replacement in scripts/post-install-nemoclaw.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-host-side-metering-loop/14-CONTEXT.md
@.planning/phases/14-host-side-metering-loop/14-PATTERNS.md
@scripts/uninstall-cron.sh
@scripts/install-nemoclaw-cron.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-sandbox uninstaller (scripts/uninstall-nemoclaw-cron.sh)</name>
  <files>scripts/uninstall-nemoclaw-cron.sh</files>
  <read_first>
    - scripts/uninstall-cron.sh (the 14-line analog — exact structure to mirror)
    - .planning/phases/14-host-side-metering-loop/14-PATTERNS.md §"scripts/uninstall-nemoclaw-cron.sh" (the full NemoClaw variant: sandbox-scoped marker removal + optional fusermount unmount)
    - tests/test_nemoclaw_cron.sh (GROUP H assertion)
  </read_first>
  <action>
    Create scripts/uninstall-nemoclaw-cron.sh with `set -euo pipefail`. Resolve
    SANDBOX_NAME="${REVENIUM_SANDBOX_NAME:-${1:-}}"; if empty, print a usage line and `exit 2`.
    CRON_MARKER="# revenium-metering-nemoclaw:${SANDBOX_NAME}". If no line matches that exact marker
    (`grep -qF`), print "No NemoClaw cron entry found for sandbox '<name>'." and `exit 0` (idempotent).
    Otherwise remove ONLY matching lines: `crontab -l 2>/dev/null | grep -vF "${CRON_MARKER}" | crontab -`,
    then print a removal confirmation. Optional unmount (fail-open): MNT="${HOME}/sbx-openclaw-${SANDBOX_NAME}";
    if `mountpoint -q "${MNT}"`, run `fusermount -u "${MNT}" || umount "${MNT}" || true` and note it.
    The grep -vF on the sandbox-scoped marker MUST NOT remove the standalone `# revenium-metering` line
    nor other sandboxes' markers.
  </action>
  <verify>
    <automated>bash -n scripts/uninstall-nemoclaw-cron.sh && grep -qF '# revenium-metering-nemoclaw:' scripts/uninstall-nemoclaw-cron.sh && echo UNINSTALL_SHAPE_OK</automated>
  </verify>
  <acceptance_criteria>
    - bash -n scripts/uninstall-nemoclaw-cron.sh exits 0
    - Invoked with no sandbox name and no REVENIUM_SANDBOX_NAME → prints usage and exits 2
    - Removes only lines matching `# revenium-metering-nemoclaw:<sandbox>` via grep -vF
    - When no matching marker exists → exits 0 (idempotent, no error)
    - Optional unmount is fail-open (a missing/absent mount does not cause non-zero exit)
    - tests/test_nemoclaw_cron.sh GROUP H passes: after install, uninstall <sandbox> removes that marker and leaves a standalone `# revenium-metering` line intact
  </acceptance_criteria>
  <done>The uninstaller removes only the named sandbox's cron entry, is idempotent, optionally unmounts fail-open, and passes GROUP H.</done>
</task>

<task type="auto">
  <name>Task 2: Wire install_metering_loop into post-install-nemoclaw.sh + SC4 guard</name>
  <files>scripts/post-install-nemoclaw.sh</files>
  <read_first>
    - scripts/post-install-nemoclaw.sh (read the full file — especially the `stub_install_metering_loop` definition at line 103, its call site at line 351, and the ledger helpers `ledger_has`/`ledger_set` at lines 63-83 and the `deliver_revenium_cli` ledger-gate at lines 175-208 to mirror)
    - .planning/phases/14-host-side-metering-loop/14-PATTERNS.md §"scripts/post-install-nemoclaw.sh — stub replacement only" (the exact install_metering_loop replacement body + call-site rename)
    - scripts/install-nemoclaw-cron.sh (the script the new function invokes — confirm its --sandbox interface)
  </read_first>
  <action>
    In scripts/post-install-nemoclaw.sh, replace the body of `stub_install_metering_loop` (currently a
    single `warn "Phase 14+: ... skipping."` at lines 103-105) with a real `install_metering_loop` function
    modeled on the existing ledger-gated steps (e.g. deliver_revenium_cli):
      - If `ledger_has "metering-loop-installed"`, info-log "already installed — skipping" and return 0.
      - Otherwise `step "Installing host-side metering loop"`, then
        `bash "${SCRIPT_DIR}/install-nemoclaw-cron.sh" --sandbox "${SANDBOX_NAME}" || fail "install-nemoclaw-cron.sh failed"`.
      - On success `ledger_set "metering-loop-installed" "1"` and info-log the active cron.
    Rename the definition and the call site (line ~351) from `stub_install_metering_loop` to
    `install_metering_loop`. Use the SAME helper names already in the file (step/info/warn/fail/ledger_has/
    ledger_set/SCRIPT_DIR/SANDBOX_NAME) — do NOT invent new ones. This is the ONLY change to this file:
    the egress/CLI/meter-probe provisioning from Phase 13 is untouched.
    SC4 GUARD (no code change — verification only): confirm scripts/cron.sh, scripts/report.sh, and
    scripts/guardrail-check.sh are byte-identical to their pre-phase sha256 (the three constants recorded
    in 14-02-PLAN.md threat_model). If any differs, the phase has a regression that must be reverted.
  </action>
  <verify>
    <automated>bash -n scripts/post-install-nemoclaw.sh && grep -qF 'install-nemoclaw-cron.sh' scripts/post-install-nemoclaw.sh && ! grep -qF 'stub_install_metering_loop' scripts/post-install-nemoclaw.sh && printf '78124b27a78595821f9914c7c79211934da236aa5aa37c21261de34fa82ecaff  scripts/cron.sh\n238a08bf151d1ae4446f20d1ff20c382ec2ed18b482e6b8094b5c6147eb1741f  scripts/report.sh\n7a0f842d3ecc86246fb968ea2e7bc3a9d0a295fbc1c0ca94c60f1f7ae0dc8659  scripts/guardrail-check.sh\n' | shasum -a 256 -c && echo WIRED_AND_SC4_OK</automated>
  </verify>
  <acceptance_criteria>
    - bash -n scripts/post-install-nemoclaw.sh exits 0
    - scripts/post-install-nemoclaw.sh invokes `install-nemoclaw-cron.sh --sandbox "${SANDBOX_NAME}"` (the stub is gone — `grep -F stub_install_metering_loop` matches nothing)
    - The new step is ledger-gated with key `metering-loop-installed` (idempotent re-run skips)
    - No new helper functions invented — the function reuses step/info/fail/ledger_has/ledger_set/SCRIPT_DIR/SANDBOX_NAME
    - SC4: `shasum -a 256 -c` of the three recorded baselines (cron.sh / report.sh / guardrail-check.sh) reports OK for all three
    - The Phase 13 provisioning functions (egress, CLI delivery, meter probe) are unchanged (only the metering-loop function/call-site differs)
  </acceptance_criteria>
  <done>post-install-nemoclaw.sh runs the real, ledger-gated metering-loop install; the stub is removed; and the three SC4 workhorse scripts are proven byte-identical to their pre-phase state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/uninstall arg → crontab/unmount | `<sandbox>` flows into the marker grep and the unmount target. |
| provisioning flow → install-nemoclaw-cron.sh | post-install invokes the installer with the resolved sandbox name; the installer handles the secret-bearing host env. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | uninstall accidentally removing the standalone `# revenium-metering` cron or another sandbox's entry | mitigate | removal uses `grep -vF` against the exact sandbox-scoped marker `# revenium-metering-nemoclaw:<sandbox>`, which does not match the standalone marker or other sandboxes (GROUP H asserts) |
| T-14-08 | Denial of Service | unmount of a busy/absent mount hanging the uninstall | mitigate | unmount is fail-open (`fusermount -u || umount || true`) and gated on `mountpoint -q`; an absent mount is skipped silently |
| T-14-09 | Tampering | wiring change inadvertently modifying a shared SC4 workhorse | mitigate | Task 2 verify runs `shasum -a 256 -c` against the three recorded baselines; any drift fails the build |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | none — this plan installs no packages; no Package Legitimacy Gate triggered |
</threat_model>

<verification>
- bash -n passes on the uninstaller and the edited post-install-nemoclaw.sh.
- `bash tests/test_nemoclaw_cron.sh` is fully GREEN (Results: FAIL=0) — all GROUP A-H pass.
- `shasum -a 256 -c` confirms cron.sh/report.sh/guardrail-check.sh byte-identical to baseline (SC4).
- `grep -F stub_install_metering_loop scripts/post-install-nemoclaw.sh` matches nothing.
</verification>

<success_criteria>
- scripts/uninstall-nemoclaw-cron.sh exists, passes bash -n, and passes GROUP H.
- post-install-nemoclaw.sh wires the real ledger-gated metering-loop install (stub removed).
- The full harness is GREEN (FAIL=0) and the three SC4 workhorses are byte-identical to pre-phase.
- NCMETER-01 is fully delivered end-to-end (install path now stands up the host metering loop).
</success_criteria>

<output>
Create `.planning/phases/14-host-side-metering-loop/14-03-SUMMARY.md` when done.
</output>
