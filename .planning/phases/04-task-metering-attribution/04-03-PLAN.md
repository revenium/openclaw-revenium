---
phase: 04-task-metering-attribution
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - scripts/setup-guardrails.sh
  - tests/test_setup_guardrails_argv.sh
autonomous: true
requirements: [METER-01]
must_haves:
  truths:
    - "The base budget rule filters AGENT:STARTS_WITH:openclaw- (both occurrences switched, D-07)"
    - "setup-guardrails.sh --interactive offers optional per-task-type budget rules drawn from task-taxonomy.json (ROADMAP criterion 5, D-10)"
    - "Each per-task-type rule adds --filter TASK_TYPE:IS:<label> AND --group-by TASK_TYPE on top of the base AGENT filter (NP-4 fixes the Hermes identical-rule bug)"
    - "The picker is gated on CLI TASK_TYPE capability and skips gracefully (logged) if absent (D-10 defense-in-depth)"
  artifacts:
    - path: "scripts/setup-guardrails.sh"
      provides: "STARTS_WITH base filter + per-task-type picker"
      contains: "TASK_TYPE:IS:"
    - path: "tests/test_setup_guardrails_argv.sh"
      provides: "stubbed-revenium argv-capture test for base + per-task rules"
  key_links:
    - from: "scripts/setup-guardrails.sh"
      to: "task-taxonomy.json"
      via: "picker reads labels for the numbered menu"
      pattern: "TAXONOMY_FILE"
    - from: "scripts/setup-guardrails.sh"
      to: "config.json ruleIds"
      via: "per-task ruleIds appended via write_rule_ids helpers"
      pattern: "ruleIds"
---

<objective>
Switch the base budget-rule filter to the prefix scheme (D-07) and port the Hermes per-task-type budget-rule picker — fixing the Hermes identical-rule bug (NP-4) so each per-task rule actually scopes to its TASK_TYPE.

Purpose: ROADMAP criterion 5 (optional per-task-type rules) and the D-07 filter migration that makes the new `--agent "openclaw-{root}"` completions match. Consumes the taxonomy + REVENIUM_AGENT_PREFIX from plan 04-01.
Output: modified `scripts/setup-guardrails.sh` + an argv-capture integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-task-metering-attribution/04-RESEARCH.md
@.planning/phases/04-task-metering-attribution/04-PATTERNS.md
@.planning/phases/04-task-metering-attribution/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch base filter to AGENT:STARTS_WITH:openclaw- (D-07)</name>
  <files>scripts/setup-guardrails.sh</files>
  <read_first>
    - scripts/setup-guardrails.sh (full — base filter at lines 270 and 289; --group-by AGENT at 267/286; help text line 42; create_rule 248-352; base rule name lines 459/640; write_rule_ids helpers 360+/403+)
    - scripts/common.sh (post-04-01 — REVENIUM_AGENT_PREFIX, REVENIUM_AGENT_NAME both present)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (the "scripts/setup-guardrails.sh" assignment, Change 1)
    - .planning/phases/04-task-metering-attribution/04-RESEARCH.md (the "setup-guardrails.sh base-filter substitution" code example; STARTS_WITH operator VERIFIED on 1.1.2)
  </read_first>
  <action>
    In `scripts/setup-guardrails.sh`, replace BOTH base-filter occurrences (lines 270 and 289) `--filter "AGENT:IS:${REVENIUM_AGENT_NAME}"` with `--filter "AGENT:STARTS_WITH:${REVENIUM_AGENT_PREFIX}"` (per D-07; the openclaw- prefix catches both `openclaw-{root}` and the `openclaw-{sid}` fallback). Update the create_rule comment at line 242 and the help text at line 42 to read STARTS_WITH + prefix. Leave `--group-by AGENT` on the base rule (267/286) unchanged. REVENIUM_AGENT_PREFIX comes from common.sh (already sourced at the top of this script).
  </action>
  <verify>
    <automated>grep -c 'AGENT:STARTS_WITH:${REVENIUM_AGENT_PREFIX}' scripts/setup-guardrails.sh | grep -qx 2 && ! grep 'AGENT:IS:${REVENIUM_AGENT_NAME}' scripts/setup-guardrails.sh && echo OK</automated>
  </verify>
  <done>Both base-filter lines use AGENT:STARTS_WITH:${REVENIUM_AGENT_PREFIX}; no AGENT:IS:${REVENIUM_AGENT_NAME} filter remains; help text and comment updated.</done>
</task>

<task type="auto">
  <name>Task 2: Port per-task-type picker with TASK_TYPE filter + capability gate (D-10/NP-4)</name>
  <files>scripts/setup-guardrails.sh, tests/test_setup_guardrails_argv.sh</files>
  <read_first>
    - scripts/setup-guardrails.sh (full — create_rule 248-352; run_interactive 479-665; base rule name at 640; write_rule_ids_to_config 360+, write_rule_ids_and_config 403+; validate_hard_limit / compute_warn_threshold present)
    - ../hermes-revenium/skills/revenium/scripts/setup-guardrails.sh lines 699-797 (picker to port: taxonomy read, numbered menu, comma-index parse, per-label create loop — and its identical-rule bug to FIX)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (NP-4 — parameterize create_rule, add TASK_TYPE:IS:<label> + --group-by TASK_TYPE, capability gate, OpenClaw rule-name substitution; Change 2/Change 3)
    - .planning/phases/04-task-metering-attribution/04-RESEARCH.md (Pitfall 4 identical-rule bug; the TASK_TYPE filter + group-by VERIFIED on 1.1.2)
    - task-taxonomy.json (labels for the menu)
  </read_first>
  <action>
    Parameterize `create_rule` (248-352) to accept an optional extra filter and an optional group-by override, so a caller can append `--filter "TASK_TYPE:IS:<label>"` and switch `--group-by` to TASK_TYPE while keeping the base `AGENT:STARTS_WITH:${REVENIUM_AGENT_PREFIX}` filter; when the extra args are empty, behavior is identical to today (base rule keeps --group-by AGENT). After the base-rule creation in run_interactive (around line 640-655), add the picker ported from Hermes 699-797: a capability gate `revenium guardrails budget-rules create --help 2>/dev/null | grep -q 'TASK_TYPE'` — if absent, `info` a skip reason and continue (base rule still created); if present, read the 8 labels from TAXONOMY_FILE, print a numbered menu, parse a comma-separated index selection (bash-3.2 env-passing heredoc, never interpolate into `<<'PY'`), and for each selected label call the parameterized create_rule with rule name `"OpenClaw ${label_title} Budget"` (substitute Hermes->OpenClaw), the extra filter `TASK_TYPE:IS:<label>`, and `--group-by TASK_TYPE`. FIX the Hermes bug: every per-task rule MUST carry its own TASK_TYPE filter (Hermes emitted only the base AGENT filter so all per-task rules were identical — Pitfall 4). Reuse validate_hard_limit / compute_warn_threshold for the per-task hard limit (picker may reuse the base hard-limit prompt flow or ask separately — Claude's discretion). Append each new ruleId to config via the existing write_rule_ids helpers (do not drop the base rule's id). 64-char truncate any rule name in log lines (03-PATTERNS log-injection). Write `tests/test_setup_guardrails_argv.sh`: put a copy of stub-revenium.sh named `revenium` first on PATH with STUB_REVENIUM_ARGV_FILE set (and a stub `--help` output containing TASK_TYPE so the gate passes), drive `setup-guardrails.sh --interactive` with scripted stdin selecting one label, and assert the captured argv shows (a) the base rule with AGENT:STARTS_WITH:openclaw- + --group-by AGENT and (b) a per-task rule with both AGENT:STARTS_WITH:openclaw- AND TASK_TYPE:IS:<label> + --group-by TASK_TYPE. Also add an assertion that with a `--help` stub LACKING TASK_TYPE the picker is skipped (gate) and only the base rule is created.
  </action>
  <verify>
    <automated>bash tests/test_setup_guardrails_argv.sh</automated>
  </verify>
  <done>create_rule is parameterized; the picker offers labels from task-taxonomy.json, creates per-task rules that each carry TASK_TYPE:IS:<label> + --group-by TASK_TYPE (NP-4 bug fixed), is gated on CLI TASK_TYPE capability (skips gracefully when absent, base rule still created), appends ruleIds to config; test_setup_guardrails_argv.sh passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator stdin (menu index, hard limit) -> setup-guardrails.sh | interactive input parsed into rule-creation argv |
| taxonomy label -> rule name + TASK_TYPE filter -> revenium CLI argv | label flows into a CLI invocation and log lines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | per-task hard-limit input | mitigate | reuse validate_hard_limit numeric regex before create_rule (ASVS V5) |
| T-04-11 | Tampering | menu index selection (out-of-range / non-numeric) | mitigate | comma-index parse validates each index against the taxonomy length; skip invalid indices (no rule created for them) |
| T-04-12 | Tampering | log injection via per-task rule name | mitigate | 64-char truncation on rule names in log lines (03-PATTERNS) |
| T-04-13 | Spoofing/Tampering | TASK_TYPE-bypass: per-task rules silently lacking TASK_TYPE scoping (Hermes bug) | mitigate | NP-4 — parameterized create_rule appends `--filter TASK_TYPE:IS:<label>` per rule; integration test asserts the filter is present in captured argv |
| T-04-14 | Tampering | shell interpolation inside picker python heredoc | mitigate | env-passing heredoc discipline; never interpolate ${VAR} into `<<'PY'` |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "Package Legitimacy Audit": not applicable) |
</threat_model>

<verification>
- Both base-filter occurrences use AGENT:STARTS_WITH:${REVENIUM_AGENT_PREFIX}; no legacy AGENT:IS filter remains (D-07).
- `tests/test_setup_guardrails_argv.sh` green: base rule (AGENT:STARTS_WITH + group-by AGENT) and per-task rule (AGENT:STARTS_WITH + TASK_TYPE:IS:<label> + group-by TASK_TYPE) appear in captured argv; gate skips picker when --help lacks TASK_TYPE.
</verification>

<success_criteria>
- ROADMAP criterion 5: `setup-guardrails.sh --interactive` offers optional per-task-type budget rules from task-taxonomy.json.
- D-07 base filter migration complete so new openclaw- completions match.
- Hermes identical-rule bug fixed (each per-task rule is actually TASK_TYPE-scoped).
</success_criteria>

<output>
Create `.planning/phases/04-task-metering-attribution/04-03-SUMMARY.md` when done.
</output>
