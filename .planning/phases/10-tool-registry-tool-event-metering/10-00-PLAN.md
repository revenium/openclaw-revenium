---
phase: 10-tool-registry-tool-event-metering
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_report_tool_argv.sh
  - tests/stub-revenium.sh
autonomous: true
requirements: [TOOLEV-01, TOOLEV-02, TOOLEV-03, TOOLEV-04]

must_haves:
  truths:
    - "Running `bash tests/test_report_tool_argv.sh` executes and reports PASS/FAIL counts (RED before impl, GREEN after Plans 01+02)"
    - "The stub revenium responds to `tools --help`, `tools create`, and `meter tool-event --help` so report.sh's TOOLS_CLI_CAPABLE probe and registry/event calls are observable in argv capture"
    - "Setting STUB_REVENIUM_NO_TOOLS=1 forces the tools probe to fail; STUB_REVENIUM_TOOLS_FAIL=1 forces a non-409 tools create failure"
  artifacts:
    - path: "tests/test_report_tool_argv.sh"
      provides: "Hermetic integration test covering TOOLEV-01..04 via argv capture"
      min_lines: 120
    - path: "tests/stub-revenium.sh"
      provides: "Extended revenium stub with tools/tool-event switches"
      contains: "tools --help"
  key_links:
    - from: "tests/test_report_tool_argv.sh"
      to: "scripts/report.sh"
      via: "run_report invokes report.sh with OPENCLAW_HOME pointing at a tmp home"
      pattern: "bash .*report.sh"
    - from: "tests/test_report_tool_argv.sh"
      to: "tests/stub-revenium.sh"
      via: "symlink revenium -> stub on PATH; argv captured to STUB_REVENIUM_ARGV_FILE"
      pattern: "STUB_REVENIUM_ARGV_FILE"
---

<objective>
Create the Wave 0 test scaffolding that pins TOOLEV-01..04 behaviour before any production code exists. This plan adds a hermetic bash integration test (`tests/test_report_tool_argv.sh`) that drives `report.sh` against a tmp `OPENCLAW_HOME` and captures every `revenium` argv, and extends the existing `tests/stub-revenium.sh` with the `tools`/`meter tool-event` switches the test needs.

The test MUST be RED now (report.sh does no tool work yet) and turn GREEN after Plans 01 (registry) and 02 (tool-event emission) land. This is the Nyquist sampling harness referenced by every later task's `<verify>`.

Purpose: Establish the at-most-30s feedback loop so Plans 01/02 are implemented test-first against concrete argv assertions, not prose.
Output: `tests/test_report_tool_argv.sh` (new), `tests/stub-revenium.sh` (extended with tools switches).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tool-registry-tool-event-metering/10-RESEARCH.md
@.planning/phases/10-tool-registry-tool-event-metering/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend stub-revenium.sh with tools + meter tool-event switches</name>
  <files>tests/stub-revenium.sh</files>
  <read_first>
    - tests/stub-revenium.sh (the file being modified — read the full file; the argv-capture block at the top MUST remain first, the default `exit 0` MUST remain last)
    - tests/test_report_jobs_argv.sh (analog: how the existing jobs stub switches STUB_REVENIUM_NO_JOBS / STUB_REVENIUM_JOBS_FAIL are exercised)
    - .planning/phases/10-tool-registry-tool-event-metering/10-PATTERNS.md (section "tests/stub-revenium.sh — ADD tools + meter tool-event switches" — exact branch placement and switch semantics)
  </read_first>
  <action>
    Extend `tests/stub-revenium.sh` to support the Phase 10 probe and call paths, mirroring the existing `jobs --help` / `jobs create` / `STUB_REVENIUM_NO_JOBS` / `STUB_REVENIUM_JOBS_FAIL` blocks per PATTERNS.md (D-05 idempotency/fail-open precedent).

    Insert, AFTER the existing `jobs --help` probe block and BEFORE the final default `exit 0`:
    - A `tools --help` branch: exit 0 normally, but exit 1 when `STUB_REVENIUM_NO_TOOLS` is set (forces TOOLS_CLI_CAPABLE=false for the fail-open probe test).
    - A `meter tool-event --help` branch: print a line containing the literal token `--tool-id` and exit 0 (so report.sh's dual probe `grep -q -- '--tool-id'` passes). This branch fires ONLY for the `--help` invocation, not real metering.
    - A `tools create` branch: when `STUB_REVENIUM_TOOLS_FAIL` is set, print a NON-409 error (e.g. a 500) to stderr and exit 1; otherwise fall through to the default `exit 0`. Do NOT emit a 409 here — 409 backstop coverage is a separate switch concern handled by the existing `STUB_REVENIUM_409_FOR` mechanism if reused.

    Preserve: the argv-capture block stays FIRST (it must record `tools create` and `meter tool-event` argv to `STUB_REVENIUM_ARGV_FILE`); the default `exit 0` stays LAST (so real `meter tool-event` and `tools create` calls succeed by default). Document each new switch in the header comment block in the same style as the existing jobs switches. Argv discipline: never `eval`; match on positional `$1 $2`/`$1 $2 $3` exactly as the jobs blocks do.
  </action>
  <verify>
    <automated>bash -n tests/stub-revenium.sh && STUB_REVENIUM_NO_TOOLS=1 bash tests/stub-revenium.sh tools --help; test $? -eq 1 && bash tests/stub-revenium.sh meter tool-event --help | grep -q -- '--tool-id' && echo STUB_OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/stub-revenium.sh` contains a `tools --help` branch and a `meter tool-event --help` branch; the argv-capture block remains the first executable block and `exit 0` remains the last line.
    - Behavior assertion: `bash tests/stub-revenium.sh tools --help` exits 0 by default and exits 1 when `STUB_REVENIUM_NO_TOOLS=1`.
    - Behavior assertion: `bash tests/stub-revenium.sh meter tool-event --help` prints output containing `--tool-id` and exits 0.
    - Behavior assertion: `STUB_REVENIUM_TOOLS_FAIL=1 bash tests/stub-revenium.sh tools create --tool-id x` exits 1 with a non-409 stderr message.
    - CLI: the command in `<verify>` prints `STUB_OK`.
  </acceptance_criteria>
  <done>The stub responds correctly to all four Phase 10 invocation shapes (tools --help, meter tool-event --help, tools create, and the two failure switches) without disturbing existing jobs/guardrail/completion behaviour.</done>
</task>

<task type="auto">
  <name>Task 2: Create hermetic tool-metering integration test (RED)</name>
  <files>tests/test_report_tool_argv.sh</files>
  <read_first>
    - tests/test_guardrail_argv.sh (primary analog — full structure: make_openclaw_home, fake HOME, stub symlink, argv_vals/count_grep helpers, run helper, assertion + idempotency + fail-open patterns)
    - tests/test_report_jobs_argv.sh (analog: run_report helper signature and second-tick idempotency assertions)
    - .planning/phases/10-tool-registry-tool-event-metering/10-PATTERNS.md (section "tests/test_report_tool_argv.sh — CREATE hermetic test" — boilerplate, session fixture, assertion blocks)
    - .planning/phases/10-tool-registry-tool-event-metering/10-RESEARCH.md ("Validation Architecture" — Phase Requirements → Test Map and the Session Fixture)
  </read_first>
  <action>
    Create `tests/test_report_tool_argv.sh` following `test_guardrail_argv.sh` structure exactly (PATTERNS.md provides every helper verbatim). It MUST:

    1. Build a tmp `OPENCLAW_HOME` via a `make_openclaw_home` helper that creates `agents/main/sessions`, symlinks `get-root-session-id.py`, writes `revenium-offsets.json` (`{}`), and `touch`es `revenium-reported.ledger`, `revenium-jobs.ledger`, `revenium-tools.ledger`, and `revenium-tool-events.ledger`, plus `config.json` with `{"organizationName":"TestOrg"}`.
    2. Write the canonical session fixture from RESEARCH.md "Session Fixture for Tests" into the sessions dir — it yields one TOOL_CALL completion (`asst-001`, stopReason toolUse), one CHAT completion (`asst-002`), one `read` toolCall (`toolu_test001`) and its `read` toolResult (250ms, isError false).
    3. Symlink `stub-revenium.sh` to `revenium` on a fake-HOME `.local/bin` PATH and export `STUB_REVENIUM_ARGV_FILE`; add `argv_vals`, `count_grep`, and a `run_report` helper (mirror jobs test).
    4. Assert (these are RED until Plans 01/02 land):
       - TOOLEV-01: argv contains `tools create` with `--name read`, `--tool-id read`, `--tool-type BUILTIN`.
       - TOOLEV-01 idempotency: after a SECOND `run_report` on the same home, `tools create` appears exactly once (registry ledger dedup) — assert the count of the `tools`/`create` argv pair is 1.
       - TOOLEV-02: argv contains `meter tool-event` with `--tool-id read`, an `--agent` value starting `openclaw-`, and an explicit `--success` flag (the no-double-default-false assertion).
       - TOOLEV-02 timing: a `--duration-ms` value of `250` is present (from the 250ms fixture delta).
       - TOOLEV-03: `meter completion` is STILL emitted (assert at least one `meter completion` with `--operation-type TOOL_CALL` and one `CHAT`), proving the completion path is untouched; assert NO `--operation-type` value equal to a tool-event marker leaked onto completions.
       - TOOLEV-04 fail-open probe: a separate run with `STUB_REVENIUM_NO_TOOLS=1` on a fresh home produces ZERO `tools create` and ZERO `meter tool-event` argv (TOOLS_CLI_CAPABLE=false skip), while `meter completion` still appears.
       - TOOLEV-04 event idempotency: after two ticks, `meter tool-event` for `toolu_test001` appears exactly once (TOOLEV: ledger dedup).
    5. End with the standard `Results: N passed, M failed` footer and `exit 1` when any FAIL.

    Do NOT modify report.sh in this plan. The test is expected to FAIL now (that is the RED proof). Keep all assertions argv-based so they are deterministic. Sanitize/quote with argv arrays; never `eval`.
  </action>
  <verify>
    <automated>bash -n tests/test_report_tool_argv.sh && bash tests/test_report_tool_argv.sh; echo "exit=$? (non-zero RED expected pre-impl)"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/test_report_tool_argv.sh` defines `make_openclaw_home`, `argv_vals`, `count_grep`, and `run_report` helpers and writes the RESEARCH.md session fixture.
    - Behavior assertion: the test runs to completion (no bash syntax/runtime crash) and prints a `Results: N passed, M failed` line.
    - Behavior assertion (RED gate): with report.sh unmodified, the test exits non-zero and reports at least the TOOLEV-01 and TOOLEV-02 assertions as FAIL (no tool argv captured yet).
    - Test-command: each of TOOLEV-01, TOOLEV-02, TOOLEV-03, TOOLEV-04 has at least one named PASS/FAIL assertion line referencing the requirement ID.
    - Source assertion (TOOLEV-02 timing — explicit): the test contains a named assertion that `argv_vals "--duration-ms"` equals `250` for the `read` tool-event (the 250ms fixture delta), AND a named assertion that the bare `--success` flag is present (`grep -q "^--success$"` on the captured argv) — proving the no-default-false gotcha is covered. Do NOT rely solely on the PATTERNS.md template, which omits the duration check.
    - CLI: `bash -n tests/test_report_tool_argv.sh` exits 0 (valid syntax).
  </acceptance_criteria>
  <done>A hermetic, argv-capturing integration test exists covering TOOLEV-01..04; it is syntactically valid, runs cleanly, and is RED against the current (un-extended) report.sh — providing the GREEN target for Plans 01 and 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session JSONL → test fixture | The test authors its own fixture; no untrusted input. But the fixture mirrors real session shape so the production parser is exercised against realistic `toolCall`/`toolResult` linkage. |
| stub → argv file | The stub writes captured argv to a tmp file under a tmp HOME; no secrets, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-00-01 | Tampering | tmp OPENCLAW_HOME / argv file | accept | Test runs fully hermetic under `mktemp -d`; `trap cleanup EXIT` removes tmp dirs. No real Revenium API key is read (stub replaces the CLI). |
| T-10-00-02 | Information Disclosure | API key leakage into argv capture | mitigate | The stub never forwards to the real CLI; no `REVENIUM_API_KEY` is set in the hermetic home, so no key can be captured or logged. |
| T-10-00-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: N/A). No install tasks present. |
</threat_model>

<verification>
- `bash -n` passes on both modified files.
- `bash tests/test_report_tool_argv.sh` runs to a `Results:` line and is RED (non-zero) against unmodified report.sh.
- Existing suites unaffected: `bash tests/test_report_jobs_argv.sh` and `bash tests/test_guardrail_argv.sh` still pass (stub change is additive).
</verification>

<success_criteria>
- TOOLEV-01..04 each have at least one deterministic argv assertion in the new test.
- The test is RED now and is the GREEN target for Plans 01/02.
- The stub supports `tools --help`, `meter tool-event --help`, `tools create`, `STUB_REVENIUM_NO_TOOLS`, and `STUB_REVENIUM_TOOLS_FAIL` without breaking existing jobs/guardrail tests.
</success_criteria>

<output>
Create `.planning/phases/10-tool-registry-tool-event-metering/10-00-SUMMARY.md` when done.
</output>
