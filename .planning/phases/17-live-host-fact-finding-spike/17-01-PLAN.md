---
phase: 17-live-host-fact-finding-spike
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh
  - .planning/spikes/007-live-2-0-host-provisioning/README.md
  - .planning/spikes/007-live-2-0-host-provisioning/provision-run.log
  - .planning/spikes/007-live-2-0-host-provisioning/versions-resolved.txt
  - .planning/spikes/007-live-2-0-host-provisioning/tracer-turn-readback.txt
autonomous: true
requirements: [SPIKE-00]
tags: [spike, live-host, provisioning, nemoclaw, openshell, tracer]
user_setup:
  # HOST-SIDE, NOT OPERATOR-SIDE. Every key below is consumed only on
  # 52.90.9.242 — no task in this phase invokes a keyed API from the operator
  # machine (60 of the 65 <automated> commands are greps over repo artifacts;
  # the other 5 are ssh-wrapped). Putting these in the operator environment
  # would make the laptop a needless transit point for live credentials, and
  # would additionally risk ANTHROPIC_API_KEY overriding the operator's own
  # Claude Code subscription billing. This mirrors the established project
  # pattern in scripts/install-nemoclaw-cron.sh:116, which reads the key from
  # the HOST's environment and writes it to a mode-600 file there.
  - service: host-credential-file
    why: "D-16 + T-17-01 — real Anthropic / NVIDIA / Revenium keys must exist on the spike host before Task 1 runs, written once to a mode-600 file, never passed as an inline CLI argument and never echoed into provision-run.log. SPIKE-01/02/04 bars are per-completion token usage and zero missed completions; none is answerable on a dummy key (v1.4 spike 003 stalled at PARTIAL on exactly this)."
    host: "52.90.9.242"
    file: "/home/ubuntu/.spike-17.env"
    mode: "600"
    setup: |
      Run ONCE by the operator, directly on the host — not from the repo, and
      not via any committed script (a committed script would have to read the
      values from somewhere, reintroducing the transit problem):

        ssh -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242
        umask 077
        cat > ~/.spike-17.env <<'EOF'
        ANTHROPIC_API_KEY=sk-ant-...
        NVIDIA_API_KEY=nvapi-...
        REVENIUM_API_KEY=...
        EOF
        chmod 600 ~/.spike-17.env

      Use a quoted heredoc delimiter ('EOF') so the shell never expands the
      values, and prefix each command with a space if HISTCONTROL=ignorespace
      is set. The file is spike-scoped: delete it at teardown.
    keys:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> Settings -> API keys"
        why: "D-16 — the standalone pairing needs a real model key for the tracer turn (Task 1)."
      - name: NVIDIA_API_KEY
        source: "build.nvidia.com -> API key (nvapi- prefix)"
        why: "D-07/D-16 — the NemoClaw/OpenShell pairing runs Nemotron via NVIDIA cloud (host has no GPU); NEMOCLAW_PROVIDER=build needs this at install time (Task 2)."
      - name: REVENIUM_API_KEY
        source: "Revenium console -> API keys"
        why: "D-16 — spike meter calls land in the normal Revenium tenant; no scratch scope. Required for the SPIKE-04 ground-truth cross-check."

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Host 52.90.9.242 runs OpenClaw >= 2026.8.1, Node >= 24.16, Docker, and NemoClaw >= v0.0.128, each version read back from the live host rather than assumed — SPIKE-00"
    - "Re-running provision-2-0-host.sh on the already-provisioned host exits 0, performs no install action, and prints one detection-gated pass line per version floor (Node, Docker, OpenClaw, NemoClaw, sshfs, swap) — D-14 idempotency; edge-probe row SPIKE-01/idempotency resolved explicit"
    - "provision-2-0-host.sh refuses explicitly (non-zero exit, named detected-vs-required version) when a floor is present but below the required value, never silently continuing — project-wide explicit-refusal convention"
    - "Both install paths are live on the same host simultaneously: standalone OpenClaw + Docker AND NemoClaw/OpenShell running a sandbox — D-07/D-13"
    - "The standalone-path and NemoClaw-managed OpenClaw versions are each recorded separately and verbatim, with no attempt to normalize them to one another — D-15"
    - "One real agent turn completed on the standalone path with a real model key, and its completion is readable back out of the 2.0 SQLite session store — the tracer's end-to-end proof"
    - ".planning/spikes/007-live-2-0-host-provisioning/README.md carries, for every claim, the exact command, its trimmed raw output, the host, the date, and the OpenClaw/NemoClaw/Node versions in effect — D-12"
  prohibitions:
    - statement: "A determination in this phase must NOT assert a live-host behavior that was inferred from documentation rather than observed on 52.90.9.242, even when the doc-derived answer is almost certainly right."
      status: unresolved
      verification: judgment
    - statement: "A spike verdict must NOT be recorded for a question whose probe was not actually run on the live host — an unrun question is an open question, not a PARTIAL."
      status: unresolved
      verification: judgment
    - statement: "If no read candidate clears D-02's zero-loss fidelity bar, the phase must NOT promote the least-bad candidate to 'the chosen mechanism' — it must record the bar as unmet and escalate."
      status: unresolved
      verification: judgment
  artifacts:
    - path: ".planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh"
      provides: "Idempotent, detection-gated provisioning of both install paths on a bare Ubuntu 26.04 host (D-14)"
      contains: "provision-2-0-host"
    - path: ".planning/spikes/007-live-2-0-host-provisioning/README.md"
      provides: "SPIKE-00 determination in CONVENTIONS.md format — frontmatter verdict + evidence per D-12"
      contains: "52.90.9.242"
    - path: ".planning/spikes/007-live-2-0-host-provisioning/versions-resolved.txt"
      provides: "Verbatim resolved versions for both paths, recorded separately per D-15"
      contains: "openclaw"
    - path: ".planning/spikes/007-live-2-0-host-provisioning/provision-run.log"
      provides: "Raw, trimmed-not-paraphrased provisioning run output (D-12 evidence)"
    - path: ".planning/spikes/007-live-2-0-host-provisioning/tracer-turn-readback.txt"
      provides: "The one real turn's completion row read back out of the 2.0 SQLite store — the tracer's end-to-end evidence"
  key_links:
    - from: ".planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh"
      to: "host 52.90.9.242"
      via: "ssh -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242, script copied and run on the host"
      pattern: "52\\.90\\.9\\.242"
    - from: "one real standalone-path agent turn"
      to: "~/.openclaw/agents/<agentId>/agent/openclaw-agent.sqlite"
      via: "sqlite3 read-only URI connection with PRAGMA busy_timeout, output captured to tracer-turn-readback.txt"
      pattern: "mode=ro"
    - from: "provision-2-0-host.sh resolved-version capture"
      to: ".planning/spikes/007-live-2-0-host-provisioning/versions-resolved.txt"
      via: "per-path version capture written by the script and pasted into the determination (D-15)"
      pattern: "openclaw"
---

<objective>
Stand up the live OpenClaw 2.0 fact-finding host and prove the whole apparatus works end-to-end before any of the four fact-finding spikes spend time on it: a reproducibly-provisioned `52.90.9.242` carrying **both** production install paths (standalone OpenClaw + Docker with Claude, and NemoClaw/OpenShell with Nemotron), one real agent turn actually completed, that turn's completion read back out of the 2.0 SQLite session store, and a written SPIKE-00 determination in the established spike format.

Purpose: SPIKE-00 gates every other requirement in the v2.0 milestone. Everything downstream (SPIKE-01..04, then Phases 18-24) reads facts produced on this host, so the host's provenance has to be reproducible (D-14) and its resolved versions recorded exactly as they landed (D-15) rather than assumed. The tracer task deliberately touches every layer the phase will use — provision, real turn, read-back, determination doc — so an architectural dead end surfaces after one commit rather than after five spikes.
Output: `.planning/spikes/007-live-2-0-host-provisioning/` containing an idempotent `provision-2-0-host.sh`, the raw provisioning log, the per-path resolved versions, the tracer turn read-back, and the SPIKE-00 `README.md` determination.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-live-host-fact-finding-spike/17-CONTEXT.md
@.planning/phases/17-live-host-fact-finding-spike/17-RESEARCH.md
@.planning/phases/17-live-host-fact-finding-spike/17-PATTERNS.md
@.planning/spikes/CONVENTIONS.md
@.planning/spikes/001-nemoclaw-bootstrap/probe-host-compat.sh
@.planning/spikes/001-nemoclaw-bootstrap/README.md
@.planning/spikes/002-openshell-egress/revenium-policy.yaml
@.planning/spikes/003-revenium-cli-in-sandbox/gh-release-policy.yaml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a real 2.0 turn is readable" — one path only</name>
  <precondition>`ssh -o BatchMode=yes -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242 true` succeeds, AND the host credential file from `user_setup` exists at mode 600 carrying both keys this task needs — verify with `ssh -o BatchMode=yes -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242 'test "$(stat -c %a ~/.spike-17.env)" = 600 &amp;&amp; grep -qE "^ANTHROPIC_API_KEY=.+" ~/.spike-17.env &amp;&amp; grep -qE "^REVENIUM_API_KEY=.+" ~/.spike-17.env'` (checks presence and non-emptiness only — never print the file, never echo a value). These keys are NOT expected in the operator environment; if this check fails, halt and point the operator at `user_setup`, do not fall back to reading a local env var.
  <read_first>
    - `.planning/spikes/001-nemoclaw-bootstrap/probe-host-compat.sh` — the detection-gated pass/warn/fail accounting (`pass`/`warn`/`fail` counters, `line`/`ok`/`wn`/`no` helpers, the terminal `Summary:` + `VERDICT:` + exit-code block) this script reuses verbatim
    - `.planning/spikes/CONVENTIONS.md` — target-environment facts, paths table, operational hazards (`pkill` self-match over SSH; `exec` process pile-up)
    - `.planning/phases/17-live-host-fact-finding-spike/17-RESEARCH.md` §`## Host Provisioning` — the live probe of the current host state, the official install commands, and the Node/OpenClaw version floors confirmed against the npm registry
    - `.planning/phases/17-live-host-fact-finding-spike/17-RESEARCH.md` §`## Session Read Path` candidate (a) and §`## Code Examples` — the read-only WAL-aware connection form
    - `.planning/spikes/001-nemoclaw-bootstrap/README.md` — the determination-doc frontmatter (`spike`/`name`/`type`/`validates`/`verdict`/`related`/`tags`/`host`) and section skeleton
    - `scripts/common.sh` — how `OPENCLAW_HOME` is resolved today, so the probe names the same root the production scripts will
  </read_first>
  <files>.planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh, .planning/spikes/007-live-2-0-host-provisioning/provision-run.log, .planning/spikes/007-live-2-0-host-provisioning/versions-resolved.txt, .planning/spikes/007-live-2-0-host-provisioning/tracer-turn-readback.txt, .planning/spikes/007-live-2-0-host-provisioning/README.md</files>
  <action>
Wire ONE path end to end: bare host -> provisioned standalone OpenClaw -> one real completed agent turn -> that turn read back out of the 2.0 SQLite store -> a determination document. No second install path, no second read candidate, no batching.

Write `provision-2-0-host.sh` as an idempotent, detection-gated bash script (`set -u`, no `set -e` so a failed detection still reports). Reuse `probe-host-compat.sh`'s counter/helper scaffolding (`pass`/`warn`/`fail`, `line`, `ok`, `wn`, `no`) and its terminal `Summary:` / `VERDICT:` block verbatim, but state in the header comment that this script DOES install and DOES sudo — the analog's NON-DESTRUCTIVE framing is inverted here. In this task the script covers the standalone path only:
- swapfile step: the host has 7.7 GiB RAM and 0 swap, below NemoClaw's documented ~8 GB floor — create an 8 GB swapfile at `/swapfile` gated on `swapon --show` already reporting one.
- `sudo apt-get install -y sshfs` gated on `command -v sshfs`.
- Node + OpenClaw via the official installer `curl -fsSL https://openclaw.ai/install.sh | bash`, gated on `command -v openclaw`. Per D-15 install at `latest` and record what resolves; do not pin.
- Docker, gated on `command -v docker` plus a `docker info` daemon-reachability check.
- Version floors compared numerically with `sort -C -V`, never a version-string prefix test. Floors: OpenClaw `2026.8.1`, Node `24.16.0` (engine range `>=24.16.0 <25 || >=26.1.0`), NemoClaw `v0.0.128`. A floor that is present but below required emits a `no` line naming BOTH the detected and the required version and drives a non-zero exit — explicit refusal, never a silent continue.
- After every install step, capture the exact resolved version string (`openclaw --version`, `node --version`, `docker --version`) and append it to `versions-resolved.txt` labelled with the path that produced it (`standalone:`), per D-15.

Copy the script to the host with `scp -i ~/.ssh/hermes-sandbox.pem`, run it under `setsid ... </dev/null` detached (the v1.4 spikes found an apt SIGTTIN hang under tmux+tee), and capture stdout+stderr verbatim into `provision-run.log` — trimmed, never paraphrased (D-12).

Then produce ONE real turn on the standalone path using the real `ANTHROPIC_API_KEY`. The key is already on the host at `/home/ubuntu/.spike-17.env` (mode 600, written once by the operator per `user_setup`) — source it inside the host-side step with `set -a; . ~/.spike-17.env; set +a` and let it reach the agent through the environment only. Never pass it as an inline CLI argument, never `scp` or `ssh`-transport a key value from the operator machine, and never echo it into `provision-run.log` — the NemoClaw process-args key-exposure class, threat T-17-01. Drive the turn through the normal agent entry point with a distinctive prompt string so the completion is findable.

Read that completion back with a read-only WAL-aware connection against `~/.openclaw/agents/<agentId>/agent/openclaw-agent.sqlite`, using a `file:<path>?mode=ro` URI and an explicit `PRAGMA busy_timeout=5000`. Resolve `<agentId>` from the live filesystem rather than assuming `main`. Capture the read-back output verbatim into `tracer-turn-readback.txt`. Do NOT wrap the read in a retry/backoff loop — a `SQLITE_BUSY` here is a finding, not a defect to paper over.

Create `README.md` with the CONVENTIONS.md frontmatter (`spike: 007`, `name: live-2-0-host-provisioning`, `type: standard`, a `validates:` Given/when/then sentence, `verdict:`, `related: [001, 004]`, `tags: [infra, install, openclaw-2-0, sqlite, provisioning]`, `host: "52.90.9.242 (bare Ubuntu 26.04 LTS, x86_64, no GPU)"`) and the section skeleton `## What This Validates` / `## Research` / `## How to Run` / `## Investigation Trail` / `## Results`. Populate `## Results` with the standalone-path evidence triplet — exact command, trimmed raw output, interpretation — plus the host, the date, and the OpenClaw/Node versions in effect (D-12). Leave the NemoClaw-path and idempotency sections to Tasks 2 and 3; mark them explicitly as not-yet-run rather than absent.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242 'openclaw --version &amp;&amp; node --version &amp;&amp; docker --version' | grep -c .</automated>
    <fails_when>non-zero exit, or a printed count below 3 (one or more of the three CLIs is absent, so its `--version` never reached stdout)</fails_when>
    <automated>test -s .planning/spikes/007-live-2-0-host-provisioning/tracer-turn-readback.txt &amp;&amp; grep -c . .planning/spikes/007-live-2-0-host-provisioning/tracer-turn-readback.txt</automated>
    <fails_when>non-zero exit, or a printed line count of 0 (an empty read-back means the turn was never found in the store)</fails_when>
    <automated>grep -c '52\.90\.9\.242' .planning/spikes/007-live-2-0-host-provisioning/README.md</automated>
    <fails_when>non-zero exit, or a printed count of 0 — the determination does not name the host that produced it, violating D-12</fails_when>
    <automated>bash -n .planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh</automated>
    <fails_when>non-zero exit, or stderr containing "syntax error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh` exists, passes `bash -n`, and contains the strings `sort -C -V`, `busy_timeout`, `swapon`, and `VERDICT:`
    - `ssh ... 'openclaw --version'` on 52.90.9.242 prints a CalVer string whose numeric comparison against `2026.8.1` via `sort -C -V` succeeds
    - `ssh ... 'node --version'` prints a version inside `>=24.16.0 <25 || >=26.1.0`
    - `versions-resolved.txt` contains at least one line prefixed `standalone:` naming the resolved OpenClaw version
    - `provision-run.log` is non-empty and contains a terminal line beginning `VERDICT:`
    - `tracer-turn-readback.txt` is non-empty and contains at least one row attributable to the distinctive prompt string used for the tracer turn
    - `README.md` frontmatter contains `spike: 007` and `host: "52.90.9.242` and the body contains a `## Results` heading
    - No API key value appears anywhere under `.planning/spikes/007-live-2-0-host-provisioning/`
  </acceptance_criteria>
  <done>The single path works end to end and is committed: a bare host was provisioned by a committed idempotent script, a real agent turn completed on it, that turn's completion was read back out of the 2.0 SQLite store read-only, and the evidence is written into a spike determination in the established format.</done>
</task>

<task type="auto">
  <name>Task 2: Expand provisioning to the NemoClaw/OpenShell path on the same host</name>
  <precondition>The host credential file from `user_setup` carries a non-empty `NVIDIA_API_KEY` — verify with `ssh -o BatchMode=yes -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242 'grep -qE "^NVIDIA_API_KEY=.+" ~/.spike-17.env'` (presence check only — never print the file, never echo the value). This key is NOT expected in the operator environment; if the check fails, halt and point the operator at `user_setup`, do not fall back to reading a local env var.
  <read_first>
    - `.planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh` — the script being extended (current state, not assumptions)
    - `.planning/spikes/CONVENTIONS.md` §Install, §Paths, §CLI primitives, §Egress policy, §Operational hazards — the validated non-interactive NemoClaw install envelope and the in-sandbox path table
    - `.planning/spikes/002-openshell-egress/revenium-policy.yaml` — the `api.revenium.ai` egress preset applied via `policy-add --from-file` (D-16)
    - `.planning/spikes/003-revenium-cli-in-sandbox/gh-release-policy.yaml` — the `release-assets.githubusercontent.com` allowance needed if the revenium CLI is fetched in-sandbox
    - `.planning/phases/17-live-host-fact-finding-spike/17-RESEARCH.md` §`## Host Provisioning` items 9, 10, 11 — the non-interactive install command shape, the NemoClaw `v0.0.128` claim marked ASSUMED and requiring live confirmation, and the Docker-ownership note
    - `scripts/install-nemoclaw-cron.sh` — the established mode-600 host auth env write, the pattern this task's credential handling must follow
  </read_first>
  <files>.planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh, .planning/spikes/007-live-2-0-host-provisioning/provision-run.log, .planning/spikes/007-live-2-0-host-provisioning/versions-resolved.txt</files>
  <action>
Extend the same `provision-2-0-host.sh` with a second, independently detection-gated section that brings up the NemoClaw/OpenShell path on the SAME host (D-13 — one host, both paths), so the two coexist exactly as they will in production.

Steps, each gated on its own detection check so a re-run is a no-op:
- Install NemoClaw non-interactively via `curl -fsSL https://www.nvidia.com/nemoclaw.sh | ... bash` with the validated envelope: `NEMOCLAW_NON_INTERACTIVE=1`, `NEMOCLAW_NON_INTERACTIVE_SUDO_MODE=prompt`, `NEMOCLAW_ACCEPT_THIRD_PARTY_SOFTWARE=1`, `NEMOCLAW_PROVIDER=build`, `NEMOCLAW_SANDBOX_NAME=revenium-2-0`, `NEMOCLAW_POLICY_MODE=suggested`, and `NVIDIA_API_KEY` sourced on the host from `/home/ubuntu/.spike-17.env` via `set -a; . ~/.spike-17.env; set +a` — never inlined into the visible command line and never transported from the operator machine (threat T-17-01, the NVIDIA/NemoClaw#579 process-args class). Gate on `command -v nemoclaw`. NemoClaw's own installer owns Docker; do not install Docker twice.
- Run the installer detached (`setsid ... </dev/null`) and note in the script header that a fresh login is required between the Docker-group add and onboarding.
- Confirm the sandbox is up with `nemoclaw revenium-2-0 status` and confirm NemoClaw's own version with `nemoclaw --version`. RESEARCH marks the `v0.0.128` / OpenClaw-`2026.9.1` bundling claim as ASSUMED — record what the live host actually reports and correct the assumption in the determination if it differs.
- Apply the `revenium` egress preset with `nemoclaw revenium-2-0 policy-add --from-file` pointed at a copy of `.planning/spikes/002-openshell-egress/revenium-policy.yaml`, and the gh-release preset from `003-revenium-cli-in-sandbox/gh-release-policy.yaml` if the in-sandbox CLI is fetched from the GitHub release CDN. Gate on the preset already being present; the policy hot-reloads with no rebuild.
- Establish the host-side state channel with `nemoclaw revenium-2-0 share mount /sandbox/.openclaw <host-mount-path>` (requires the `sshfs` installed in Task 1). Do NOT use per-tick `nemoclaw exec` for anything that polls — it is synchronous, hang-prone, and accumulates hung host `node ... exec` processes.
- Capture the in-sandbox OpenClaw version separately and append it to `versions-resolved.txt` with a `nemoclaw-sandbox:` prefix. Per D-15 the two paths will NOT necessarily run the same OpenClaw and that mismatch is a finding in its own right — record both verbatim, do not normalize.
- Derive the in-sandbox 2.0 SQLite store path from the live filesystem under `/sandbox/.openclaw/` (HOME is `/sandbox`, user `sandbox`); do not assume the standalone `~/.openclaw/` layout carries over. Append the resolved in-sandbox store path to `versions-resolved.txt`.

Keep every `nemoclaw exec` command single-line: NemoClaw's gRPC argv rejects newlines. If a stray process needs killing on the host, kill by PID via a `ps | grep | awk | xargs kill` pipeline filtered on `node` — `pkill -f` self-matches over SSH and drops the session with exit 255.

Re-run the full script on the host, appending the new run's verbatim stdout+stderr to `provision-run.log`.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242 'nemoclaw --version &amp;&amp; nemoclaw revenium-2-0 status'</automated>
    <fails_when>non-zero exit, output containing "command not found", or the status output not naming the sandbox `revenium-2-0`</fails_when>
    <automated>grep -c '^nemoclaw-sandbox:' .planning/spikes/007-live-2-0-host-provisioning/versions-resolved.txt</automated>
    <fails_when>non-zero exit, or a printed count of 0 — the NemoClaw-managed OpenClaw version was never recorded separately, so D-15 cannot be evidenced</fails_when>
    <automated>grep -c '^standalone:' .planning/spikes/007-live-2-0-host-provisioning/versions-resolved.txt</automated>
    <fails_when>non-zero exit, or a printed count of 0 — the standalone-path version line was lost when the file was rewritten</fails_when>
    <automated>ssh -o BatchMode=yes -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242 'mount | grep -c "^revenium-2-0\|fuse.sshfs"'</automated>
    <fails_when>non-zero exit, or a printed count of 0 — the share mount is not established, so the NemoClaw-path read cells in later plans have no channel</fails_when>
  </verify>
  <acceptance_criteria>
    - `nemoclaw --version` on the host reports a version whose numeric comparison against `v0.0.128` via `sort -C -V` succeeds
    - `nemoclaw revenium-2-0 status` returns exit 0 and names the sandbox
    - `versions-resolved.txt` contains at least one `standalone:` line and at least one `nemoclaw-sandbox:` line, each carrying a resolved OpenClaw version string
    - `versions-resolved.txt` records the resolved in-sandbox session-store path under `/sandbox/.openclaw/`
    - An sshfs mount of `/sandbox/.openclaw` is present in the host's `mount` output
    - `provision-2-0-host.sh` contains the strings `policy-add`, `share mount`, and `NEMOCLAW_NON_INTERACTIVE`
    - No API key value appears in `provision-run.log` or in any committed file under the spike dir
  </acceptance_criteria>
  <done>Both production install paths are live on 52.90.9.242 at the same time, each path's resolved OpenClaw version is recorded separately and verbatim, the `api.revenium.ai` egress preset is applied to the sandbox, and the host-side share mount is established.</done>
</task>

<task type="auto">
  <name>Task 3: Prove re-runnability and finish the SPIKE-00 determination</name>
  <read_first>
    - `.planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh` — the completed script whose idempotency is under test
    - `.planning/spikes/007-live-2-0-host-provisioning/README.md` — the determination being finished (current state)
    - `.planning/spikes/001-nemoclaw-bootstrap/README.md` §`## Results` and its closing reusable-facts block — the evidence-pasting shape and the one-scannable-block convention this determination must match
    - `.planning/phases/17-live-host-fact-finding-spike/17-RESEARCH.md` §`## Common Pitfalls` Pitfall 4 — why a fresh, script-driven provision (not a reused sandbox) is the only acceptable provenance
  </read_first>
  <files>.planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh, .planning/spikes/007-live-2-0-host-provisioning/README.md, .planning/spikes/007-live-2-0-host-provisioning/provision-run.log</files>
  <action>
Run `provision-2-0-host.sh` a second time, start to finish, against the now-fully-provisioned host and capture the run verbatim into `provision-run.log` under a clearly labelled second-run section. This is D-14's re-runnability bar and also answers the edge-probe idempotency question for SPIKE-01 ("what happens if this runs twice on the same input"): Phase 22 needs a freshly-provisioned sandbox and Phase 24 needs a fresh host, so the script gets used at least twice more.

The second run must exit 0, perform zero install actions, and print one detection-gated pass line per floor — swap, sshfs, Node, Docker, OpenClaw, NemoClaw, sandbox, egress preset, share mount. If any step re-installs or re-downloads on the second run, fix the detection gate for that step rather than documenting the non-idempotency; a gate that fires twice is a defect in the script, not a property of the host.

Separately verify the explicit-refusal behavior without breaking the host: exercise the version-comparison helper directly with a crafted below-floor version string (for example an OpenClaw string one CalVer component below `2026.8.1`) and confirm it produces a `no` line naming both the detected and the required version and a non-zero exit. The check must be numeric-CalVer based; a crafted string designed to fool a naive string-prefix comparison must still be rejected correctly.

Finish `README.md`:
- Set the frontmatter `verdict` to one of `VALIDATED` / `PARTIAL` / `INVALIDATED` based on what actually ran. Do not record a verdict for any question whose probe was not run.
- `## Results`: the full evidence set per D-12 — for each claim the exact command, its trimmed raw output, the host, the date, and the OpenClaw/NemoClaw/Node versions in effect.
- Record the three provisioning findings explicitly: the 7.7 GiB RAM / 0 swap gap against NemoClaw's ~8 GB floor and the swapfile remedy; the absence of a GPU and the resulting cloud-inference routing for both pairings; and the D-15 finding that the two paths resolved to different OpenClaw versions (or, if they happened to match, that they matched at this point in time and are not guaranteed to).
- Record whether the live host corrected or confirmed the RESEARCH assumption that NemoClaw `v0.0.128` bundles OpenClaw `2026.9.1`.
- Close with a scannable reusable-facts block naming, for Phases 18-24: the resolved version per path, the resolved session-store path per path, the sandbox name, the share-mount point, and the mode-600 credential file location.
  </action>
  <verify>
    <automated>ssh -o BatchMode=yes -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242 'bash ~/provision-2-0-host.sh >/tmp/rerun.log 2>&amp;1; echo "EXIT=$?"; echo "FAILLINES=$(grep -c "^✗" /tmp/rerun.log)"'</automated>
    <fails_when>the output does not contain the line `EXIT=0`, or the `FAILLINES=` line reports anything other than 0</fails_when>
    <automated>grep -cE '^spike: 007$|^verdict: (VALIDATED|PARTIAL|INVALIDATED)$' .planning/spikes/007-live-2-0-host-provisioning/README.md</automated>
    <fails_when>non-zero exit, or a printed count below 2 — the determination is missing its spike number or a verdict from the three-state enum</fails_when>
    <automated>grep -c 'sort -C -V' .planning/spikes/007-live-2-0-host-provisioning/provision-2-0-host.sh</automated>
    <fails_when>non-zero exit, or a printed count of 0 — version comparison is not numeric-CalVer based</fails_when>
  </verify>
  <acceptance_criteria>
    - A second end-to-end run of `provision-2-0-host.sh` on the already-provisioned host exits 0 and its captured output contains one pass line per floor (swap, sshfs, Node, Docker, OpenClaw, NemoClaw, sandbox, egress preset, share mount) with zero install actions
    - The version-comparison helper, given a below-floor OpenClaw version string, emits a line naming both the detected and the required version and returns non-zero
    - A version string crafted to pass a naive string-prefix comparison but fail a numeric CalVer comparison is rejected
    - `README.md` frontmatter carries `spike: 007` and a `verdict:` value from `VALIDATED` / `PARTIAL` / `INVALIDATED`
    - `README.md` `## Results` names the host, the date, and the OpenClaw/NemoClaw/Node versions in effect for every claim
    - `README.md` contains a reusable-facts block naming the resolved session-store path for each of the two paths
    - The RAM/swap finding, the no-GPU finding, and the D-15 per-path version finding each appear in `README.md`
  </acceptance_criteria>
  <done>SPIKE-00 is answered and written: the host is provisioned, the provisioning is proven re-runnable and explicitly refusing below its floors, and `007/README.md` is a consumable determination carrying the evidence standard for every claim.</done>
</task>

</tasks>

<assumption_delta_decision>
**Detector:** `assumption-delta scan 17 --json` returned `detected: true`, one signal, `kind: chosen` (term `chosen`, from the ROADMAP Phase 17 success criterion "whether the **chosen** SQLite read mechanism sustains per-minute cron polling").

**What changed.** Through v1.4 the skill had exactly one way to read session data — parse `agents/main/sessions/*.jsonl` — so "the session file" was both the storage fact and the identity the code was written against. This phase turns that into a *chosen* mechanism with three candidates (direct SQLite read, `openclaw sessions` CLI surfaces, plugin-hook sidecar capture). A configuration-shaped fact has become a modeling decision.

**Noun that is now primary:** *the session read mechanism* — an interface with per-mechanism fidelity characteristics — not *the session file*.

**Decision: `promote`.** Promote "session read mechanism" to the primary noun and demote "JSONL glob" to a detail of one now-retired variant. Rationale: candidate (c) is explicitly framed as the skill *capturing* rather than *reading*, which means the winning candidate may not be a file read at all — an identity model named after a file cannot hold that variant. Phase 19 writes the production read path from scratch (D-11), so promoting now costs nothing and add-alongside would bake the retired variant's name into the new code.

**Consequence recorded for Phase 19:** the four current-session-id resolvers (`write-marker.sh:66`, `write-job-marker.sh:132`, `verify-markers.sh:91`, `guardrail-check.sh:496`) resolve identity by globbing the newest non-cron `*.jsonl` filename. Under the promoted noun they resolve "which session am I in" through the mechanism interface, not through a filename — which is precisely why D-03 makes session-id resolution its own determination rather than a footnote of the content read.

**Optional companion (suggested, not required):** Phase 19 may add a contract test that exercises the same session-read assertions against each mechanism behind one interface, so a later storage change fails the contract rather than silently metering zero. Not added as a task here — this phase writes no production code.
</assumption_delta_decision>

<flagged_planner_assumptions>
## Spec-less edge-probe accounting (phase-level — no silent drops)

This phase has no `*-SPEC.md`, so the deterministic edge probe ran over the phase requirement IDs. It surfaced **6** items. Accounting below covers all 6: **2** authored into `must_haves` + **4** surfaced as flagged assumptions. 6 == 2 + 4.

| # | Requirement | Probe category | Disposition | Where |
|---|-------------|----------------|-------------|-------|
| 1 | SPIKE-01 | idempotency — "What happens if this runs twice on the same input?" | **resolved (explicit)** — maps onto D-14's idempotent `provision-2-0-host.sh` | `must_haves.truths` in this plan ("Re-running provision-2-0-host.sh …"), gated by Task 3 |
| 2 | SPIKE-01 | concurrency — "If interrupted or run in parallel, what is guaranteed?" | **resolved (explicit)** — maps onto D-04's zero-loss concurrency soak | `must_haves.truths` in `17-05-PLAN.md` ("The soak reports zero SQLITE_BUSY …") |
| 3 | SPIKE-00 | unclassified | **unresolved — FLAGGED ASSUMPTION** | below |
| 4 | SPIKE-02 | unclassified | **unresolved — FLAGGED ASSUMPTION** | below + `17-05-PLAN.md` |
| 5 | SPIKE-03 | unclassified | **unresolved — FLAGGED ASSUMPTION** | below + `17-02-PLAN.md` |
| 6 | SPIKE-04 | unclassified | **unresolved — FLAGGED ASSUMPTION** | below + `17-05-PLAN.md` |

### Flagged assumptions (never auto-dismissed, never auto-resolved with a backstop)

- **SPIKE-00 (unclassified).** The probe could not classify this requirement into an edge category. Planner assumption: the only edge worth asserting for a provisioning requirement is re-runnability, which is already carried explicitly by item 1 above and by D-14. No additional edge predicate is authored. If a reviewer believes a provisioning edge is being missed (for example partial-provision recovery after a mid-run failure), it needs naming before this phase seals.
- **SPIKE-02 (unclassified).** Planner assumption: a hook-firing matrix has no idempotency/concurrency/ordering edge of its own — its correctness bar is coverage (every one of the six D-08 hooks recorded for both pairings), which is asserted as a plain truth in `17-05-PLAN.md`, not as a probe-derived edge predicate. Recorded here rather than dropped.
- **SPIKE-03 (unclassified).** Planner assumption: the edge that matters is *scope applicability*, which RESEARCH surfaced as the critical finding and which `17-02-PLAN.md` addresses as its first task. It is authored as a plain truth there rather than as a probe-category predicate, because no probe category (idempotency/concurrency/ordering/boundary) names it.
- **SPIKE-04 (unclassified).** Planner assumption: SPIKE-04's own edge is concurrency, which the probe already surfaced under SPIKE-01 (item 2) and which is authored explicitly in `17-05-PLAN.md`. The SPIKE-04 row is therefore a duplicate surface of a covered edge rather than an independent gap — flagged rather than silently merged.

### Canon-referral breadcrumbs (prohibition probe, Stage 2)

Two recalled prohibitions were dropped as canon security/compliance rather than minted, per the canon-referral rule, and are carried in `<threat_model>` instead:
- *"Real API keys must not be committed into a spike dir or pasted into a determination's raw-output block"* — canon secret-management; carried as **T-17-01**.
- *"Real session message content must not be pasted verbatim into a committed determination"* — canon privacy/GDPR; carried as **T-17-05**.

### Schema-push gate

Scanned the phase ROADMAP section, `17-CONTEXT.md`, and `17-RESEARCH.md` against the Payload / Prisma / Drizzle / Supabase / TypeORM path patterns: **no match**. The only SQLite store in scope is OpenClaw's own `~/.openclaw/agents/<agentId>/agent/openclaw-agent.sqlite`, which this repo neither owns nor migrates — every probe opens it `mode=ro`. No ORM is present in this repository. `SCHEMA_PUSH_REQUIRED=false`; no `[BLOCKING]` push task is injected.

### API-coverage gate

`api-coverage.cjs --json` over the phase scope returned `detected: false` (no `skipped` key) — this phase integrates no external API surface; it provisions credentials for services the project already integrates and reads a third-party local store. A reasoned `COVERAGE.md` declaration is written in `17-06-PLAN.md` Task 1 so the seal-time re-detection over the finished plan bodies has an explicit answer rather than re-deriving one from prose.
</flagged_planner_assumptions>

<source_audit>
## Multi-Source Coverage Audit (phase-level)

```
SOURCE    | ID      | Feature / Requirement                                            | Plan        | Status
--------- | ------- | ---------------------------------------------------------------- | ----------- | --------
GOAL      | —       | Ground truth on a live 2.0 host before any porting begins         | 01-06       | COVERED
REQ       | SPIKE-00| Provisioned, reproducible Linux host at all four version floors    | 01          | COVERED
REQ       | SPIKE-01| Evidence-backed determination of the SQLite session read path      | 03, 04      | COVERED
REQ       | SPIKE-02| Per-model hook-firing matrix recorded live                         | 05          | COVERED
REQ       | SPIKE-03| Yes/no verdict on command-dispatch: tool determinism               | 02          | COVERED
REQ       | SPIKE-04| Read mechanism sustains per-minute cron polling                    | 05          | COVERED
RESEARCH  | —       | Run the command-dispatch scope check FIRST (cheap; reframes soak)  | 02          | COVERED
RESEARCH  | —       | mode=ro + PRAGMA busy_timeout on every SQLite probe                | 01, 03, 05  | COVERED
RESEARCH  | —       | sessions tail redaction is a documented negative finding           | 03          | COVERED
RESEARCH  | —       | export-trajectory unattended approval gate (Open Question 4)       | 03          | COVERED
RESEARCH  | —       | Verbatim .schema capture (Open Question 3)                         | 03          | COVERED
RESEARCH  | —       | Full six-hook catalog + per-hook payload shapes                    | 05          | COVERED
RESEARCH  | —       | allowPromptInjection defaults-allowed side observation             | 05          | COVERED
RESEARCH  | —       | Swapfile before NemoClaw install (7.7 GiB RAM, 0 swap)             | 01          | COVERED
RESEARCH  | —       | sshfs is absent and apt-available                                  | 01          | COVERED
RESEARCH  | —       | SSHFS-mounted SQLite tested as its own cell (Pitfall 3)            | 03, 05      | COVERED
RESEARCH  | —       | verify-markers.sh-style accounting as the soak ground truth        | 05          | COVERED
RESEARCH  | —       | SPIKE-02 and SPIKE-04 share one live-traffic window                | 05          | COVERED
RESEARCH  | —       | SPIKE-04 scope open question resolved: BOTH pairings               | 05          | COVERED
RESEARCH  | —       | NemoClaw v0.0.128 bundling claim is ASSUMED; re-verify live (A1)   | 01          | COVERED
RESEARCH  | —       | Fresh script-driven provision, never a reused sandbox (Pitfall 4)  | 01          | COVERED
CONTEXT   | D-01    | All three read candidates exercised and ranked                     | 03, 04      | COVERED
CONTEXT   | D-02    | Fidelity-first ranking criterion, zero loss                        | 04          | COVERED
CONTEXT   | D-03    | Two determinations: content read AND current-session-id resolution | 03, 04      | COVERED
CONTEXT   | D-04    | Concurrency soak with zero loss as the pass bar                    | 05          | COVERED
CONTEXT   | D-05    | Determinism across models over a non-coincidental run              | 02          | COVERED
CONTEXT   | D-06    | Binary verdict + its stated consequence                            | 02, 06      | COVERED
CONTEXT   | D-07    | Both production pairings live on the host                          | 01, 02, 05  | COVERED
CONTEXT   | D-08    | Six fixed hooks incl. session_end recorded not skipped             | 05          | COVERED
CONTEXT   | D-09    | Determinations in .planning/spikes/007-011 + MANIFEST rows         | 01-05, 06   | COVERED
CONTEXT   | D-10    | Re-scope the existing findings skill, new topic reference file     | 06          | COVERED
CONTEXT   | D-11    | Throwaway probes + verbatim schema capture, no reference impl      | 03, 04      | COVERED
CONTEXT   | D-12    | Evidence standard on every claim                                   | 01-05       | COVERED
CONTEXT   | D-13    | One host, both paths, every finding records its context            | 01          | COVERED
CONTEXT   | D-14    | Idempotent committed provision-2-0-host.sh                         | 01          | COVERED
CONTEXT   | D-15    | Provision at latest, record exactly what resolved, per path        | 01          | COVERED
CONTEXT   | D-16    | Real model + Revenium keys, api.revenium.ai egress preset          | 01          | COVERED
```

No `⚠ MISSING` rows. Deferred Ideas (`nemoclaw-install-gate-a-exit1`, pre-2.0 JSONL backfill, `16-review-deferred-findings`) are excluded by rule, not dropped.

**Claude's-discretion calls made here, for the record:** one spike dir per SPIKE-nn (007=SPIKE-00, 008=SPIKE-01, 009=SPIKE-02, 010=SPIKE-03, 011=SPIKE-04); soak window fixed at >= 60 consecutive per-minute ticks per pairing; determinism run fixed at >= 30 qualifying turns per model; `MANIFEST.md`'s Requirements section does accrue 2.0 constraints (plan 06); the escalation path when no read candidate clears the fidelity bar is written into `008/README.md` rather than silently resolved (plan 04).
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator laptop -> 52.90.9.242 | SSH with `~/.ssh/hermes-sandbox.pem`; carries real credentials onto a shared host |
| host -> OpenShell sandbox | `nemoclaw exec` (gRPC argv) and the SSHFS `share mount`; the sandbox is the hardened side |
| host/sandbox -> external APIs | Anthropic, NVIDIA cloud, `api.revenium.ai` through the deny-by-default egress proxy `10.200.0.1:3128` |
| standalone path <-> NemoClaw path | D-13's named risk — two install paths sharing one host and one operator account |
| live agent writer -> read-only probe | Probes open OpenClaw's own SQLite store while the Gateway is actively writing it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure | Real Anthropic / NVIDIA / Revenium keys on the shared host | high | mitigate | Keys written once to a mode-600 env file on the host and sourced from it; never an inline CLI argument (the `NVIDIA/NemoClaw#579` process-args exposure class), never echoed into `provision-run.log`; the revenium CLI reads its key from the `api-key:` field of `config.yaml`, not a command line. Acceptance criteria in Tasks 1 and 2 assert no key value appears under the spike dir. |
| T-17-02 | Tampering | Shared host — a standalone-path probe perturbs sandbox state, or vice versa (D-13) | medium | mitigate | Every finding records which context produced it; probes are scoped to one path's paths (`~/.openclaw/` vs the `/sandbox/.openclaw/` share mount) rather than a shared `OPENCLAW_HOME` ambiguity; `versions-resolved.txt` labels every line with its path. |
| T-17-03 | Tampering | OpenClaw's live SQLite session store | high | mitigate | Every probe opens a `file:<path>?mode=ro` URI with an explicit `PRAGMA busy_timeout`; no bare-path connection and no write mode anywhere in the phase. This repo owns no ORM and performs no schema push against that store. |
| T-17-04 | Denial of Service | Gateway / host wedged by probe traffic | medium | mitigate | No per-tick `nemoclaw exec` polling (synchronous, hang-prone, accumulates hung host `node ... exec` processes) — host-side reads over the share mount instead; single-line `exec` argv (gRPC rejects newlines); stray-process cleanup by PID via `ps \| grep \| awk \| xargs kill` filtered on `node`, never `pkill -f` (self-matches over SSH, exit 255). |
| T-17-05 | Information Disclosure | Raw session content pasted into committed determination docs under D-12's verbatim-output standard | medium | mitigate | Captured samples are restricted to schema, usage/metadata, and structural rows; message bodies and prompt text are redacted before pasting. D-12 requires verbatim *evidence*, not verbatim *conversation*. |
| T-17-SC | Tampering | `curl \| bash` installers for OpenClaw and NemoClaw | high | mitigate | Install only from the two official documented endpoints (`openclaw.ai/install.sh`, `www.nvidia.com/nemoclaw.sh`); record the exact resolved version each run produced in `versions-resolved.txt` per D-15 so a substituted artifact is visible after the fact. RESEARCH's `## Package Legitimacy Audit` records this phase as adding no new npm/pip/cargo dependency to the project — no `[ASSUMED]`/`[SUS]` package rows exist, so no legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — not drift)

Every name below is created by Phase 17 and does not exist in the repository before it.

**Spike directories and determination documents**
- `.planning/spikes/007-live-2-0-host-provisioning/` — SPIKE-00; `README.md`, `provision-run.log`, `versions-resolved.txt`, `tracer-turn-readback.txt`
- `.planning/spikes/008-sqlite-session-read-path/` — SPIKE-01; `README.md`, `schema-capture.sql.txt`, `sample-rows.txt`, `cli-surface-output.txt`, `session-id-resolution.txt`, `fidelity-matrix.md`, `sidecar-capture.jsonl`
- `.planning/spikes/009-hook-firing-matrix-2-0/` — SPIKE-02; `README.md`, `hook-matrix.md`, `hook-fire-log.txt`
- `.planning/spikes/010-command-dispatch-tool-verdict/` — SPIKE-03; `README.md`, `dispatch-run.log`
- `.planning/spikes/011-read-path-concurrency-soak/` — SPIKE-04; `README.md`, `soak-log.txt`, `soak-summary.md`

**Scripts and throwaway probes (evidence, not production code — D-11)**
- `provision-2-0-host.sh` (in 007) — the only committed script this phase intends to be re-run
- `probe-sqlite-direct.sh`, `probe-cli-surfaces.sh`, `probe-session-id-resolution.sh` (in 008)
- `probe-sidecar-plugin/` (in 008) — `package.json`, `openclaw.plugin.json`, `index.js`
- `probe-hook-observer/` (in 009) — `package.json`, `openclaw.plugin.json`, `index.js`
- `probe-skill/SKILL.md` (in 010) — throwaway skill carrying `command-dispatch`, `command-tool`, `command-arg-mode` frontmatter
- `soak-tick.sh` (in 011)

**Host-side names created on 52.90.9.242 (not in the repo)**
- sandbox `revenium-2-0`; an 8 GB `/swapfile`; a mode-600 credential env file; an SSHFS mount of `/sandbox/.openclaw`

**Skill / routing symbols**
- `.claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md` — new topic reference file (D-10)
- `.claude/skills/spike-findings-openclaw-revenium/sources/007-live-2-0-host-provisioning/` … `/011-read-path-concurrency-soak/` — new mirrored source dirs
- `.planning/phases/17-live-host-fact-finding-spike/COVERAGE.md` — new reasoned API-coverage declaration
- New `MANIFEST.md` spike rows `007`-`011` and new `## Requirements` bullets

**Modified (existing, tracked) files**
- `.planning/spikes/MANIFEST.md`, `.claude/skills/spike-findings-openclaw-revenium/SKILL.md`, `CLAUDE.md`, and conditionally `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md`
</artifacts_this_phase_produces>

<verification>
- `ssh -o BatchMode=yes -i ~/.ssh/hermes-sandbox.pem ubuntu@52.90.9.242 'openclaw --version; node --version; docker --version; nemoclaw --version'` returns exit 0 and four version lines, each clearing its floor by numeric comparison
- A second end-to-end run of `provision-2-0-host.sh` on the provisioned host exits 0 with zero install actions
- `.planning/spikes/007-live-2-0-host-provisioning/README.md` exists, carries `spike: 007` and a `verdict:` from the three-state enum, and names the host, the date, and the per-path versions
- `versions-resolved.txt` carries at least one `standalone:` line and at least one `nemoclaw-sandbox:` line
- No credential value appears anywhere under `.planning/spikes/007-live-2-0-host-provisioning/`
</verification>

<success_criteria>
- SPIKE-00 is answerable from `007/README.md` alone, without re-reading this plan
- Both production install paths are live on 52.90.9.242 at the same time and each path's resolved OpenClaw version is recorded separately (D-15)
- `provision-2-0-host.sh` is committed, idempotent, and explicitly refuses a below-floor version rather than silently continuing (D-14)
- One real agent turn completed on the live host and was read back out of the 2.0 SQLite store read-only — the tracer's end-to-end proof that the apparatus the next five plans depend on actually works
</success_criteria>

<output>
Create `.planning/phases/17-live-host-fact-finding-spike/17-01-SUMMARY.md` when done
</output>
