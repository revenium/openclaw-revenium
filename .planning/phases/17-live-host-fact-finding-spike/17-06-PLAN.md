---
phase: 17-live-host-fact-finding-spike
plan: "06"
type: execute
wave: 6
depends_on: ["17-01", "17-02", "17-03", "17-04", "17-05"]
files_modified:
  - .planning/spikes/MANIFEST.md
  - .planning/phases/17-live-host-fact-finding-spike/COVERAGE.md
  - .claude/skills/spike-findings-openclaw-revenium/SKILL.md
  - .claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SPIKE-00, SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04]
tags: [spike, findings, manifest, skill, routing, requirements, roadmap]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/spikes/MANIFEST.md` carries one row per new spike (007 through 011) in the existing `| # | Name | Type | Validates | Verdict | Tags |` shape, continuing the numbering from 006 — D-09"
    - "Every non-negotiable 2.0 constraint the spikes discovered is accrued into MANIFEST.md's `## Requirements` prose block as one bullet per hard constraint, matching how that block already accrues"
    - "The `spike-findings-openclaw-revenium` skill is re-scoped in place — broadened description, one new topic reference file alongside the existing four, extended findings index, extended verdicts table, extended processed-spikes list — rather than a second skill being created — D-10"
    - "The single CLAUDE.md routing line's parenthetical is broadened to name live-host OpenClaw 2.0 verification, keeping the established `- **<label>** (<parenthetical>) -> Skill(\"<name>\")` shape"
    - "A downstream agent loading the re-scoped skill reaches all five determinations through one skill load, without needing to read .planning/spikes/ directly"
    - "If SPIKE-03's verdict was NO, ATTR-01 is moved to REQUIREMENTS.md Future Requirements, its traceability row updated, and Phase 21 removed from ROADMAP.md — and if the verdict was YES, both files are left intact and the confirmation recorded — D-06"
    - "`COVERAGE.md` records a reasoned declaration that this phase integrates no external API surface, so seal-time re-detection has an explicit answer rather than re-deriving one from plan prose"
  prohibitions:
    - statement: "A determination in this phase must NOT assert a live-host behavior that was inferred from documentation rather than observed on 52.90.9.242, even when the doc-derived answer is almost certainly right."
      status: unresolved
      verification: judgment
    - statement: "A spike verdict must NOT be recorded for a question whose probe was not actually run on the live host — an unrun question is an open question, not a PARTIAL."
      status: unresolved
      verification: judgment
  artifacts:
    - path: ".planning/spikes/MANIFEST.md"
      provides: "Spike index extended with rows 007-011 and the accrued 2.0 constraint bullets"
      contains: "| 011 |"
    - path: ".claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md"
      provides: "New topic reference file distilling SPIKE-00..04 into build guidance for Phases 18-24 (D-10)"
      contains: "## Origin"
    - path: ".claude/skills/spike-findings-openclaw-revenium/SKILL.md"
      provides: "Re-scoped skill — broadened description, extended findings index, verdicts table and processed-spikes list"
      contains: "live-host-2-0-facts.md"
    - path: "CLAUDE.md"
      provides: "Broadened routing parenthetical for the re-scoped skill"
      contains: "spike-findings-openclaw-revenium"
    - path: ".planning/phases/17-live-host-fact-finding-spike/COVERAGE.md"
      provides: "Reasoned no-external-API-integration declaration for the seal-time api-coverage gate"
  key_links:
    - from: ".planning/spikes/007-live-2-0-host-provisioning/README.md through 011-read-path-concurrency-soak/README.md"
      to: ".claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md"
      via: "the five determinations are distilled into one topic reference file with an Origin attribution line"
      pattern: "Synthesized from spike"
    - from: ".claude/skills/spike-findings-openclaw-revenium/SKILL.md"
      to: ".claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md"
      via: "a new row in the findings-index table points at the new topic file"
      pattern: "live-host-2-0-facts"
    - from: "CLAUDE.md routing line"
      to: "Skill(\"spike-findings-openclaw-revenium\")"
      via: "the single skill-routing line, parenthetical broadened to cover live-host 2.0 verification"
      pattern: "spike-findings-openclaw-revenium"
    - from: ".planning/spikes/010-command-dispatch-tool-verdict/README.md verdict"
      to: ".planning/REQUIREMENTS.md and .planning/ROADMAP.md"
      via: "D-06's stated consequence applied mechanically from the written verdict"
      pattern: "ATTR-01"
---

<objective>
Make the five determinations consumable by the seven phases downstream of them, and apply the one consequence SPIKE-03's verdict carries. Add MANIFEST rows 007-011 and accrue the hard 2.0 constraints the spikes discovered; re-scope the existing `spike-findings-openclaw-revenium` skill in place with a new topic reference file and a broadened description and routing line; then, mechanically from `010/README.md`'s written verdict, either move ATTR-01 to Future Requirements and delete Phase 21, or record the confirmation that Phase 21 stays.

Purpose: D-09 requires the determinations to live in `.planning/spikes/` with MANIFEST rows because phase SUMMARY docs alone are insufficient — anti-pattern rules cap how deeply later phases read transitive prior SUMMARYs, and Phase 24 sits seven phases downstream of these facts. D-10 then makes them auto-loadable: re-scoping the existing skill rather than creating a second one keeps a downstream agent to a single skill load, and the skill's references are already split by topic rather than by milestone, so a new topic file matches the structure.
Output: extended `MANIFEST.md`, a re-scoped skill with a new `references/live-host-2-0-facts.md`, a broadened `CLAUDE.md` routing line, a `COVERAGE.md` declaration, and — conditional on SPIKE-03's verdict — an updated `REQUIREMENTS.md` and `ROADMAP.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-live-host-fact-finding-spike/17-CONTEXT.md
@.planning/phases/17-live-host-fact-finding-spike/17-PATTERNS.md
@.planning/spikes/MANIFEST.md
@.planning/spikes/CONVENTIONS.md
@.claude/skills/spike-findings-openclaw-revenium/SKILL.md
@.claude/skills/spike-findings-openclaw-revenium/references/install-and-bootstrap.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MANIFEST.md with rows 007-011, accrue the 2.0 constraints, and write COVERAGE.md</name>
  <precondition>All five determination documents exist: `.planning/spikes/007-live-2-0-host-provisioning/README.md`, `008-sqlite-session-read-path/README.md`, `009-hook-firing-matrix-2-0/README.md`, `010-command-dispatch-tool-verdict/README.md`, and `011-read-path-concurrency-soak/README.md`</precondition>
  <read_first>
    - `.planning/spikes/MANIFEST.md` — the file being extended: the existing `## Requirements` prose block (one bullet per hard constraint, e.g. the "MUST ship + apply a `revenium` network-policy preset" bullet) and the spike table's exact column shape `| # | Name | Type | Validates | Verdict | Tags |`
    - All five `README.md` files under `.planning/spikes/007-...` through `011-...` — the frontmatter `validates:` sentence, `verdict:` value and `tags:` list for each row, and the `## Requirements / build guidance` sections that name the hard constraints to accrue
    - `.planning/spikes/CONVENTIONS.md` — the established conventions; note any that the 2.0 findings supersede or extend
    - `.planning/phases/17-live-host-fact-finding-spike/17-PATTERNS.md` §`.planning/spikes/MANIFEST.md` — the row pattern and the accrual rule (one bullet per hard constraint, not per finding)
  </read_first>
  <files>.planning/spikes/MANIFEST.md, .planning/phases/17-live-host-fact-finding-spike/COVERAGE.md</files>
  <action>
Append five rows to MANIFEST.md's spike table, continuing the numbering from 006, in the existing `| # | Name | Type | Validates | Verdict | Tags |` shape. Each row's `Validates` cell reuses that spike's own frontmatter `validates:` sentence; each `Verdict` cell carries the bolded verdict plus a short parenthetical naming what made it that (the existing rows do this — for example `**PARTIAL** (mechanism proven viable; hand-rolled replica hung the turn ...)`), and each `Tags` cell reuses that spike's frontmatter tags. Rows to add: `007 | live-2-0-host-provisioning`, `008 | sqlite-session-read-path`, `009 | hook-firing-matrix-2-0`, `010 | command-dispatch-tool-verdict`, `011 | read-path-concurrency-soak`.

Use `Edit` for scoped replacement, not `Write` — MANIFEST.md holds rows 001-006 and a Requirements block that must survive untouched.

Then accrue the new non-negotiable constraints into MANIFEST.md's `## Requirements` block. One bullet per hard constraint, not per finding — the existing block's granularity is the calibration. Candidates the five determinations will have produced, to be written only where the determination actually supports them:
- The selected session-read mechanism and the fields it must preserve without loss.
- The connection form any read of the 2.0 store must use.
- The current-session-id resolution mechanism the four marker/guardrail scripts must port onto.
- Which hooks may be relied on per pairing, and which may not.
- The numeric CalVer floor and the Node engine range, and the fact that the two install paths do not necessarily run the same OpenClaw build.
- Whether the `session_end` fallback is closed or open on 2.0.
Do not invent a constraint the determinations do not support, and do not restate a v1.4 constraint that is already in the block.

Separately, write `COVERAGE.md` in the phase directory as a reasoned declaration for the api-coverage gate. The detector returned `detected: false` over this phase's scope, and the phase genuinely integrates no external API surface: it provisions credentials for services the project already integrates (Anthropic, NVIDIA, Revenium), reads a third-party local SQLite store read-only, and writes documentation. The file's body is the single declaration line naming what the phase touches instead of an API integration, in the form the gate accepts: a `No external API integration:` statement followed by that one-line reason. No capability matrix rows — there is no capability surface to enumerate, and fabricating rows for a surface that does not exist is what the gate's declaration branch exists to prevent.
  </action>
  <verify>
    <automated>grep -cE '^\| 00[789] \||^\| 01[01] \|' .planning/spikes/MANIFEST.md</automated>
    <fails_when>non-zero exit, or a printed count below 5 — fewer than the five new spike rows were appended</fails_when>
    <automated>grep -cE '^\| 00[1-6] \|' .planning/spikes/MANIFEST.md</automated>
    <fails_when>non-zero exit, or a printed count below 6 — one or more of the pre-existing 001-006 rows was destroyed by a whole-file rewrite</fails_when>
    <automated>grep -c 'No external API integration:' .planning/phases/17-live-host-fact-finding-spike/COVERAGE.md</automated>
    <fails_when>non-zero exit, or a printed count of 0 — the declaration line the gate reads is absent</fails_when>
    <automated>grep -vE '^[[:space:]]*(#|\|)' .planning/spikes/MANIFEST.md | grep -c '^- '</automated>
    <fails_when>non-zero exit, or a printed count at or below 17 — MANIFEST.md's non-table bullets (Stack relationship plus Requirements) totalled exactly 17 before this phase, so no new 2.0 constraint was accrued</fails_when>
  </verify>
  <acceptance_criteria>
    - `MANIFEST.md` contains rows `| 007 |`, `| 008 |`, `| 009 |`, `| 010 |`, `| 011 |` in the existing six-column shape
    - Each new row's `Validates` cell matches that spike's own frontmatter `validates:` sentence, and its `Tags` cell matches that spike's frontmatter tags
    - Each new row's `Verdict` cell carries a bolded verdict plus a short parenthetical naming what made it that
    - The pre-existing rows 001 through 006 and the entire pre-existing `## Requirements` block are unchanged
    - At least one new bullet has been added to `## Requirements`, and every added bullet is supported by a claim in one of the five determinations
    - `COVERAGE.md` exists in the phase directory and contains a `No external API integration:` declaration with a one-line reason, and no capability-matrix rows
  </acceptance_criteria>
  <done>The five determinations are indexed in the spike manifest in the established format, the hard constraints they produced are accrued into the manifest's requirements block, and the api-coverage gate has an explicit reasoned declaration to read at seal time.</done>
</task>

<task type="auto">
  <name>Task 2: Re-scope the findings skill in place and broaden the CLAUDE.md routing line</name>
  <reversibility rating="costly">CLAUDE.md routing and the skill description are what make these facts discoverable at all; splitting them into a second skill later means re-deciding which facts belong where and re-pointing every downstream agent.</reversibility>
  <read_first>
    - `.claude/skills/spike-findings-openclaw-revenium/SKILL.md` — the file being edited: its frontmatter (`name` + a single `description` string), the `<context>` block, the `<requirements>` block, the `<findings_index>` Feature-Areas table and Spike-verdicts table, the `## Source Files` note, and the `<metadata>` Processed Spikes list
    - `.claude/skills/spike-findings-openclaw-revenium/references/install-and-bootstrap.md` — the reference-file skeleton every topic file follows: `# <Topic Title>`, `## Requirements`, `## How to Build It`, `## What to Avoid`, `## Constraints`, `## Origin` with a `Synthesized from spike: <NNN>...` attribution line
    - `CLAUDE.md` — the single routing line and its exact shape `- **<label>** (<parenthetical>) -> Skill("<name>")`
    - All five `.planning/spikes/007-...` through `011-...` `README.md` files, especially their `## Requirements / build guidance` and reusable-facts blocks — the raw material the new topic file distills
    - `.planning/phases/17-live-host-fact-finding-spike/17-PATTERNS.md` §SKILL.md re-scope edit and §references/<new-topic>.md — the exact tables to extend and the note about the `sources/` mirror directory
  </read_first>
  <files>.claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md, .claude/skills/spike-findings-openclaw-revenium/SKILL.md, CLAUDE.md</files>
  <action>
Write `references/live-host-2-0-facts.md` as one new topic file covering SPIKE-00 through SPIKE-04 as a single cohesive topic — they share the one live host and gate the same downstream phases, which is why this is one file and not five. Follow the existing reference skeleton exactly:
- `# Live-Host OpenClaw 2.0 Facts` (or an equivalent topic title).
- `## Requirements` — the locked constraints a downstream implementer must honor: the selected read mechanism and the fields it must preserve, the read-only WAL-aware connection form, the current-session-id mechanism, the numeric CalVer and Node floors, and the hooks that may be relied on per pairing.
- `## How to Build It` — the concrete recipe for the winning mechanism, numbered, at the same level of specificity as `install-and-bootstrap.md`'s numbered steps.
- `## What to Avoid` — the ruled-out candidates and why, each with the evidence in one clause: the `sessions tail` redaction, the `export-trajectory` unattended-invocation outcome, any hook that did not fire on a pairing, and any candidate that failed the fidelity bar.
- `## Constraints` — version floors, the two-install-paths-different-builds fact, the both-pairings caveat, and the SSHFS-cell-is-its-own-measurement rule.
- `## Origin` — the terminal attribution line `Synthesized from spike: 007, 008, 009, 010, 011. Sources: sources/<NNN>-<name>/`.
Keep it terse in the register of the existing four reference files; this is build guidance, not a second copy of the determinations.

Then edit `SKILL.md` in place (`Edit`, scoped — never a whole-file `Write`):
- Broaden the frontmatter `description` to cover whole-project live-host findings rather than NemoClaw/OpenShell alone. Keep the same shape — `name` plus a single `description` string — and extend the sentence rather than restructuring it; add a scope clause naming live-host verification of OpenClaw 2.0 session storage, plugin hooks and command-dispatch behavior, and keep the existing auto-load trigger clause.
- Update the `<context>` block so it no longer describes the project as only the NemoClaw/OpenShell parallel path, and add the new live host and spike date alongside the existing `34.224.27.67` line rather than replacing it — the v1.4 findings were produced on that host and their provenance still matters.
- Add one row to the `<findings_index>` Feature Areas table pointing at `references/live-host-2-0-facts.md`, with one sentence of key finding, matching the terseness of the existing four rows.
- Append rows `007` through `011` to the Spike verdicts table in the existing `| # | Spike | Verdict |` shape.
- Append `007-live-2-0-host-provisioning` through `011-read-path-concurrency-soak` to the `<metadata>` Processed Spikes list.
- Mirror the five new spike directories into `.claude/skills/spike-findings-openclaw-revenium/sources/` the same way 001-006 were preserved, matching the existing `## Source Files` note. This `sources/` tree is part of the git-tracked skill, not a build-output mirror, so writing into it directly is correct here.

Finally edit `CLAUDE.md`'s single routing line, broadening only the parenthetical so it names live-host OpenClaw 2.0 verification alongside NemoClaw/OpenShell support. Keep the label, the arrow, and the `Skill("spike-findings-openclaw-revenium")` call exactly as they are — this is the only routing line in the file and the only established pattern for it.
  </action>
  <verify>
    <automated>test -f .claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md &amp;&amp; grep -cE '^## (Requirements|How to Build It|What to Avoid|Constraints|Origin)$' .claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md</automated>
    <fails_when>non-zero exit, or a printed count below 5 — the new topic file does not follow the five-section reference skeleton the other four reference files use</fails_when>
    <automated>grep -c 'Synthesized from spike' .claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md</automated>
    <fails_when>non-zero exit, or a printed count of 0 — the terminal Origin attribution line is missing, so the file's provenance is unrecoverable</fails_when>
    <automated>grep -c 'live-host-2-0-facts.md' .claude/skills/spike-findings-openclaw-revenium/SKILL.md</automated>
    <fails_when>non-zero exit, or a printed count of 0 — the new topic file is not reachable from the skill's findings index, so a downstream agent loading the skill never sees it</fails_when>
    <automated>grep -cE '^\| 00[789] \||^\| 01[01] \|' .claude/skills/spike-findings-openclaw-revenium/SKILL.md</automated>
    <fails_when>non-zero exit, or a printed count below 5 — fewer than the five new verdict rows were appended to the skill's verdicts table</fails_when>
    <automated>grep -cE '^\| 00[1-6] \|' .claude/skills/spike-findings-openclaw-revenium/SKILL.md</automated>
    <fails_when>non-zero exit, or a printed count below 6 — one or more pre-existing verdict rows was destroyed by a whole-file rewrite instead of a scoped edit</fails_when>
    <automated>ls .claude/skills/spike-findings-openclaw-revenium/sources/ | grep -cE '^(007|008|009|010|011)-'</automated>
    <fails_when>non-zero exit, or a printed count below 5 — the five new spike source directories were not mirrored into the skill's `sources/` tree</fails_when>
    <automated>grep -c 'Skill("spike-findings-openclaw-revenium")' CLAUDE.md</automated>
    <fails_when>non-zero exit, or a printed count other than exactly 1 — the single routing line was removed, duplicated, or had its call shape altered</fails_when>
    <human-check>Open a fresh agent context, invoke `Skill("spike-findings-openclaw-revenium")`, and confirm that one load surfaces all five live-host determinations — that the broadened description reads as covering OpenClaw 2.0 live-host verification (not just NemoClaw/OpenShell), that the new topic row is visible in the findings index, and that `references/live-host-2-0-facts.md` is enough to start Phase 19 without opening `.planning/spikes/`.</human-check>
  </verify>
  <acceptance_criteria>
    - `references/live-host-2-0-facts.md` exists and contains all five headings `## Requirements`, `## How to Build It`, `## What to Avoid`, `## Constraints`, `## Origin`
    - Its `## Origin` line names all five spikes `007, 008, 009, 010, 011`
    - `SKILL.md` frontmatter `description` names live-host OpenClaw 2.0 verification and retains the auto-load trigger clause, and remains a single `description` string
    - `SKILL.md` `<findings_index>` Feature Areas table has a new row pointing at `references/live-host-2-0-facts.md`
    - `SKILL.md` Spike verdicts table contains rows 001-006 unchanged plus new rows 007-011
    - `SKILL.md` `<metadata>` Processed Spikes list contains all eleven spike directory names
    - `SKILL.md` `<context>` block retains the `34.224.27.67` v1.4 provenance line and adds the `52.90.9.242` line
    - `.claude/skills/spike-findings-openclaw-revenium/sources/` contains a directory for each of 007-011
    - `CLAUDE.md` contains exactly one `Skill("spike-findings-openclaw-revenium")` routing line, with the label and arrow shape unchanged and only the parenthetical broadened
    - No second skill directory was created
  </acceptance_criteria>
  <done>The existing findings skill is re-scoped in place to whole-project live-host findings: one new topic reference file, an extended findings index, verdicts table and processed-spikes list, the five spike sources mirrored, and a broadened routing line — so a downstream agent reaches all eleven spikes' facts in one skill load.</done>
</task>

<task type="auto">
  <name>Task 3: Apply SPIKE-03's verdict consequence to REQUIREMENTS.md and ROADMAP.md</name>
  <reversibility rating="costly">Deleting Phase 21 and moving ATTR-01 to Future Requirements is reversible only by re-running cross-model spike work on a host that may already be torn down; D-06 rates this consequence costly and it is carried here.</reversibility>
  <precondition>`.planning/spikes/010-command-dispatch-tool-verdict/README.md` exists and its `## Results` section contains exactly one of the three permitted binary verdict sentences</precondition>
  <read_first>
    - `.planning/spikes/010-command-dispatch-tool-verdict/README.md` `## Results` — the binary verdict sentence this task acts on, and its stated consequence
    - `.planning/REQUIREMENTS.md` — the `### Contingent` block holding ATTR-01, the `## Future Requirements` -> `### Attribution` block that already lists `ATTR-01: (moves here if SPIKE-03 returns no)`, the `## Traceability` table row for ATTR-01, and the `**Coverage:**` counts at the foot
    - `.planning/ROADMAP.md` — the `### Phase 21: Attribution Dispatch Resolution (Contingent)` section and every later phase's `**Depends on**` line that names Phase 21
    - `.planning/STATE.md` — the v2.0 Phase Map table, which also carries a Phase 21 row and dependency references
    - `.planning/phases/17-live-host-fact-finding-spike/17-CONTEXT.md` D-06 — the exact consequence wording and its costly-reversibility rating
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Read the single binary verdict sentence from `010/README.md` `## Results` and branch on it. Make every edit with `Edit` (scoped replacement), never `Write` — a whole-file rewrite of REQUIREMENTS.md or ROADMAP.md destroys every section outside the diff window.

**If the verdict is `YES — deterministic`:** change nothing structural. Append one line to REQUIREMENTS.md's `### Contingent` block recording that SPIKE-03 returned yes on the date it did and that ATTR-01 therefore remains a committed v1 requirement with Phase 21 in scope, citing `010/README.md`. Leave the `## Future Requirements` placeholder entry for ATTR-01 in place, since it documents the contingency that did not fire. Leave ROADMAP.md and STATE.md untouched.

**If the verdict is either `NO` variant** (`NO — mechanism inapplicable to this call shape` or `NO — model-gated`):
- In REQUIREMENTS.md: remove the ATTR-01 entry from the `### Contingent` block and move its full wording under `## Future Requirements` -> `### Attribution`, replacing the existing `ATTR-01: (moves here if SPIKE-03 returns no)` placeholder with the real entry plus a one-line note naming the verdict variant, the date, and `010/README.md` as the source. Update the ATTR-01 row in the `## Traceability` table so it no longer maps to Phase 21. Update the `**Coverage:**` counts at the foot so the v1 total and the mapped count stay internally consistent with the table above them.
- In ROADMAP.md: remove the `### Phase 21: Attribution Dispatch Resolution (Contingent)` section, and update every later phase's `**Depends on**` line that names Phase 21 so the dependency chain stays valid — Phase 22 depends on Phases 19 and 20 rather than 21, and Phase 24's dependency list drops 21. Do **not** renumber Phases 22, 23 or 24: their numbers are referenced from STATE.md, PROJECT.md, and the phase directory naming convention, and renumbering would invalidate all of them for no benefit. Leave a one-line note under the milestone heading recording that Phase 21 was removed on SPIKE-03's verdict, with the date and `010/README.md` as the source, so the gap in the numbering is explained rather than mysterious.
- Prefer the `gsd roadmap` subcommands for the structural ROADMAP mutation where one exists for the operation; fall back to scoped `Edit` only for the parts no subcommand covers.

Either branch: also update STATE.md's v2.0 Phase Map table to match, and add a line to STATE.md's `### Decisions` list recording the verdict and its consequence, so the next session's context reflects the milestone's actual shape.

Do not re-ask the verdict. CONTEXT.md records the human choosing this consequence in advance; the decision is already made and this task applies it.
  </action>
  <verify>
    <automated>grep -cE '\*\*(YES — deterministic|NO — mechanism inapplicable to this call shape|NO — model-gated)\.\*\*' .planning/spikes/010-command-dispatch-tool-verdict/README.md</automated>
    <fails_when>non-zero exit, or a printed count other than exactly 1 — there is no single unambiguous verdict to branch on, so the consequence cannot be applied correctly</fails_when>
    <automated>grep -c 'ATTR-01' .planning/REQUIREMENTS.md</automated>
    <fails_when>non-zero exit, or a printed count below 2 — ATTR-01 vanished from REQUIREMENTS.md entirely instead of being moved or retained, so the requirement was silently dropped rather than dispositioned</fails_when>
    <automated>grep -cE '^### Phase (18|19|20|22|23|24):' .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit, or a printed count below 6 — a phase other than 21 was removed, or phases were renumbered</fails_when>
    <automated>grep -c '010-command-dispatch-tool-verdict' .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md | grep -vc ':0$'</automated>
    <fails_when>non-zero exit, or a printed count below 2 — fewer than two of the three planning documents cite the determination that drove the change, leaving the edit unattributable</fails_when>
  </verify>
  <acceptance_criteria>
    - The branch taken matches the single binary verdict sentence in `010/README.md`
    - On a YES verdict: REQUIREMENTS.md's `### Contingent` block records the yes with its date and source; ROADMAP.md's Phase 21 section is intact; ROADMAP.md and STATE.md are otherwise unchanged
    - On a NO verdict: ATTR-01 appears under `## Future Requirements` -> `### Attribution` with its full wording, the verdict variant, the date and `010/README.md` as source; it no longer appears in `### Contingent`; its Traceability row no longer maps to Phase 21
    - On a NO verdict: ROADMAP.md has no `### Phase 21:` section, still has sections for Phases 18, 19, 20, 22, 23 and 24 under their original numbers, and no later phase's `**Depends on**` line still names Phase 21
    - On a NO verdict: REQUIREMENTS.md's `**Coverage:**` counts are internally consistent with its Traceability table
    - STATE.md's v2.0 Phase Map matches ROADMAP.md, and STATE.md's `### Decisions` list records the verdict and its consequence
    - All three planning documents were edited with scoped replacements; no section outside the intended change was lost
  </acceptance_criteria>
  <done>SPIKE-03's verdict has been applied to the milestone's own documents rather than left as a note for a future reader: either Phase 21 and ATTR-01 stand with the confirmation recorded, or ATTR-01 is in Future Requirements, Phase 21 is gone, the dependency chain is repaired without renumbering, and every change cites the determination that drove it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike determinations -> git-tracked skill tree | Findings copied into `.claude/skills/.../sources/`, which downstream agents auto-load |
| determination verdict -> milestone planning documents | A written verdict mechanically driving structural edits to REQUIREMENTS.md and ROADMAP.md |
| skill description + CLAUDE.md routing -> every future agent context | What these two strings say determines what downstream agents ever see |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-12 | Tampering | Whole-file `Write` against MANIFEST.md, SKILL.md, REQUIREMENTS.md or ROADMAP.md destroying content outside the intended diff | high | mitigate | Every edit in this plan is a scoped `Edit`; automated checks assert the pre-existing 001-006 rows survive in both MANIFEST.md and SKILL.md, that six phase sections survive in ROADMAP.md, and that ATTR-01 still appears somewhere in REQUIREMENTS.md. A destructive rewrite fails the verify rather than passing silently. |
| T-17-01 | Information Disclosure | Credentials leaking into the git-tracked skill `sources/` mirror when spike directories are copied | high | mitigate | The mirrored spike directories carry only the artifacts the earlier plans' acceptance criteria already assert are key-free; the mirror is a copy of already-committed evidence, not a fresh capture from the host. |
| T-17-05 | Information Disclosure | Session content reaching a broadly auto-loaded skill via the `sources/` mirror | medium | mitigate | Same control: the mirrored content is the already-redacted committed evidence, and the earlier plans assert no raw prompt text or message body was captured. |
| T-17-13 | Repudiation | A structural milestone change (Phase 21 deleted, ATTR-01 moved) with no trail back to the evidence that justified it | medium | mitigate | Every edit cites `010-command-dispatch-tool-verdict` by name with the verdict variant and date; an automated check requires at least two of the three planning documents to carry that citation; ROADMAP.md carries a note explaining the numbering gap. |
| T-17-14 | Tampering | Renumbering Phases 22-24 after deleting Phase 21, invalidating references in STATE.md, PROJECT.md and phase directory names | medium | mitigate | Renumbering is explicitly forbidden in the action; an automated check asserts sections for 18, 19, 20, 22, 23 and 24 all still exist under their original numbers. |
| T-17-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; it writes and edits documentation only. RESEARCH's `## Package Legitimacy Audit` records the phase as adding no new project dependency. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (new symbols — not drift)

Created by this plan, absent from the repository beforehand:
- `.claude/skills/spike-findings-openclaw-revenium/references/live-host-2-0-facts.md` — new topic reference file (D-10); the fifth file alongside `install-and-bootstrap.md`, `sandbox-egress-policies.md`, `revenium-cli-and-metering.md`, `skill-deploy-and-enforcement.md`
- `.claude/skills/spike-findings-openclaw-revenium/sources/007-live-2-0-host-provisioning/` … `/011-read-path-concurrency-soak/` — five new mirrored source directories
- `.planning/phases/17-live-host-fact-finding-spike/COVERAGE.md` — new reasoned api-coverage declaration
- New `MANIFEST.md` spike rows `007`-`011` and new `## Requirements` bullets
- New `SKILL.md` findings-index row, verdict rows `007`-`011`, and processed-spike entries

Modified (existing, tracked, edited with scoped replacements only): `.planning/spikes/MANIFEST.md`, `.claude/skills/spike-findings-openclaw-revenium/SKILL.md`, `CLAUDE.md`, and — conditional on SPIKE-03's verdict — `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`.

The full phase-wide artifact inventory is in `17-01-PLAN.md` -> `<artifacts_this_phase_produces>`.
</artifacts_this_phase_produces>

<verification>
- `MANIFEST.md` carries rows 001-006 unchanged plus new rows 007-011, and its `## Requirements` block has gained at least one bullet
- `COVERAGE.md` exists with a `No external API integration:` declaration and a one-line reason
- `references/live-host-2-0-facts.md` exists with all five skeleton headings and an Origin line naming spikes 007-011
- `SKILL.md` reaches the new topic file from its findings index, carries verdict rows 001-011, and lists all eleven processed spikes
- `.claude/skills/spike-findings-openclaw-revenium/sources/` holds a directory for each of 007-011
- `CLAUDE.md` holds exactly one `Skill("spike-findings-openclaw-revenium")` routing line
- REQUIREMENTS.md, ROADMAP.md and STATE.md reflect SPIKE-03's verdict consistently, cite `010-command-dispatch-tool-verdict`, and retain phase sections 18, 19, 20, 22, 23 and 24 under their original numbers
</verification>

<success_criteria>
- A Phase 24 agent seven phases downstream reaches all five determinations through one skill load, without reading `.planning/spikes/` directly — the discoverability D-10 exists for
- The spike manifest indexes 007-011 in the established format and accrues only constraints the determinations actually support
- SPIKE-03's verdict is applied to the milestone's own documents, with every structural edit attributable to the determination that drove it and no phase renumbered
</success_criteria>

<output>
Create `.planning/phases/17-live-host-fact-finding-spike/17-06-SUMMARY.md` when done
</output>