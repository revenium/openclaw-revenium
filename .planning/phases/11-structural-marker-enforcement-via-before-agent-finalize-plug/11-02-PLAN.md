---
phase: 11-structural-marker-enforcement-via-before-agent-finalize-plug
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-markers.sh
  - tests/test_verify_markers.sh
autonomous: true
requirements: [SC-4, SC-5]
must_haves:
  truths:
    - "verify-markers.sh reports, per session, completions vs task markers and a coverage % so the gap is measurable before/after the gate"
    - "verify-markers.sh excludes cron sessions using write-marker.sh's proven exclusion logic"
    - "verify-markers.sh reuses common.sh SESSIONS_DIR / MARKERS_DIR (no re-declaration) and does not touch report.sh or guardrail logic"
    - "A summary line reports total completions, total markers, total gap, and overall coverage %"
  artifacts:
    - path: "scripts/verify-markers.sh"
      provides: "Per-session completions-vs-markers diagnostic"
      min_lines: 40
      contains: "common.sh"
    - path: "tests/test_verify_markers.sh"
      provides: "Integration test with session + marker fixtures for SC-4"
      min_lines: 40
      contains: "verify-markers.sh"
  key_links:
    - from: "scripts/verify-markers.sh"
      to: "scripts/common.sh"
      via: "source for SESSIONS_DIR / MARKERS_DIR"
      pattern: "common.sh"
    - from: "tests/test_verify_markers.sh"
      to: "scripts/verify-markers.sh"
      via: "invokes the script under test with OPENCLAW_HOME override"
      pattern: "verify-markers.sh"
---

<objective>
Build `scripts/verify-markers.sh` — a per-session completions-vs-markers diagnostic that makes the classification gap measurable before/after the gate lands (SC-4) — plus its integration test. Built regardless of plugin progress (D-06) and strictly read-only: it touches no `report.sh`/guardrail logic and preserves the existing metering contract (SC-5, D-07).

Purpose: Give an observable baseline/after metric for the gate (the ~1/64 coverage premise).
Output: `scripts/verify-markers.sh` + `tests/test_verify_markers.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-structural-marker-enforcement-via-before-agent-finalize-plug/11-RESEARCH.md
@.planning/phases/11-structural-marker-enforcement-via-before-agent-finalize-plug/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scripts/verify-markers.sh</name>
  <read_first>
    - .planning/phases/11-structural-marker-enforcement-via-before-agent-finalize-plug/11-PATTERNS.md (lines 163–279: full verify-markers.sh blueprint — header/common.sh sourcing, cron exclusion, count_completions, count_task_markers, env-passing heredoc, output format)
    - .planning/phases/11-structural-marker-enforcement-via-before-agent-finalize-plug/11-RESEARCH.md (lines 472–483: verified data sources + algorithm)
    - scripts/write-marker.sh (the proven cron-exclusion + session-iteration logic to reuse; the marker JSONL format)
    - scripts/common.sh (lines 41–58: SESSIONS_DIR, MARKERS_DIR, STATE_DIR, get_root_session_id, info/warn/error — do NOT re-declare)
    - scripts/report.sh (marker-file reading discipline; confirm verify-markers.sh changes nothing here — SC-5)
  </read_first>
  <files>scripts/verify-markers.sh</files>
  <action>
    Create `scripts/verify-markers.sh` (chmod +x) using the PATTERNS.md blueprint. Header block + `set -uo pipefail`; `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` then `. "${SCRIPT_DIR}/common.sh"` to get `SESSIONS_DIR`, `MARKERS_DIR`, `info`/`warn` — do NOT redeclare them. Drive the logic with a single env-passing `python3 - <<'PY'` heredoc (never interpolate bash vars inside the body; pass `SESSIONS_DIR` / `MARKERS_DIR` via env per write-marker.sh discipline).
    Algorithm (per RESEARCH lines 479–483): (1) build the cron-sid exclusion set from `${SESSIONS_DIR}/sessions.json` using write-marker.sh's EXACT logic (keys starting `agent:main:cron:`, handle str + dict values, fail-open on parse error); (2) for each non-cron `*.jsonl` in SESSIONS_DIR, count assistant-message completions (`rec.type=="message"` and `rec.message.role=="assistant"`); (3) for the matching `${MARKERS_DIR}/{sid}.jsonl`, count TASK markers only (`'task_type' in rec` and `rec.get('kind') != 'job'` — exclude job markers per D-03); (4) per session print `session_id | completions | markers | gap | coverage%`; (5) print a summary line: total completions, total markers, total gap, overall coverage %.
    Output to stdout only — this is an interactive diagnostic, NOT a cron job: do not `tee` to LOG_FILE, do not write any file, do not call report.sh or any guardrail/config writer. It is strictly read-only (SC-5 / D-07 preservation).
    Do not place implementation fenced code in the executed action — write it directly into the file following the PATTERNS.md excerpts.
  </action>
  <verify>
    <automated>bash -n scripts/verify-markers.sh && grep -q 'common.sh' scripts/verify-markers.sh && grep -q "agent:main:cron:" scripts/verify-markers.sh && ! grep -q 'tee ' scripts/verify-markers.sh && ! grep -q 'report.sh' scripts/verify-markers.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify-markers.sh` parses clean; script has `set -uo pipefail` and sources `common.sh`.
    - Cron exclusion uses the `agent:main:cron:` key prefix (matches write-marker.sh).
    - Task-marker counting excludes job markers (`kind != "job"`).
    - The script writes no files, does not `tee`, and does not reference `report.sh` or guardrail/config writers (read-only — SC-5).
  </acceptance_criteria>
  <done>verify-markers.sh emits a per-session completions/markers/gap/coverage table + summary, reusing common.sh and write-marker.sh's cron exclusion, touching no metering logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tests/test_verify_markers.sh (SC-4)</name>
  <read_first>
    - .planning/phases/11-structural-marker-enforcement-via-before-agent-finalize-plug/11-PATTERNS.md (lines 283–350: full test harness template — tmp-home construction, PASS/FAIL helpers, session/marker fixtures, OPENCLAW_HOME override, summary block, the 5 key scenarios)
    - tests/test_write_marker.sh (exact analog: tmp-home, trap cleanup EXIT, PASS/FAIL counters, session fixture construction)
    - scripts/verify-markers.sh (the script under test from Task 1 — match its output column format in assertions)
  </read_first>
  <files>tests/test_verify_markers.sh</files>
  <action>
    Create `tests/test_verify_markers.sh` (chmod +x) following the test_write_marker.sh harness: `set -uo pipefail`; `SCRIPT_DIR`/`REPO_ROOT`; `VERIFY_MARKERS="${REPO_ROOT}/scripts/verify-markers.sh"`; `PASS`/`FAIL` counters with `pass()`/`fail()`; build `TMP_HOME` via `mktemp -d` with `agents/main/sessions` + `skills/revenium/markers` subdirs; `trap cleanup EXIT`; invoke as `OPENCLAW_HOME="${TMP_HOME}" bash "${VERIFY_MARKERS}"`.
    Cover the 5 scenarios from PATTERNS.md lines 334–340: (1) session with 3 completions + 3 markers → gap 0, coverage 100%; (2) 3 completions + 1 marker → gap 2, coverage ~33%; (3) 3 completions + 0 markers (no marker file) → gap 3, coverage 0%; (4) a cron session (registered in `sessions.json` under an `agent:main:cron:*` key) is excluded from the per-session output; (5) the summary line reports correct totals. Construct session fixtures as JSONL with `type:"message"` + `message.role:"assistant"` records (completions) and marker fixtures as JSONL with `task_type` records. End with the standard `Results: N passed, M failed` summary and `exit 1` on any failure.
  </action>
  <verify>
    <automated>bash tests/test_verify_markers.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/test_verify_markers.sh` exits 0 with all 5 scenarios passing.
    - The test drives the real `scripts/verify-markers.sh` under an isolated `OPENCLAW_HOME` tmp dir and cleans up via `trap cleanup EXIT`.
    - The cron-exclusion scenario asserts the cron session's id does NOT appear in the per-session output.
    - The coverage assertions confirm 100% / ~33% / 0% for the three completion-vs-marker ratios.
  </acceptance_criteria>
  <done>SC-4 proven by an automated shell test covering gap = completions − markers, coverage %, cron exclusion, and summary totals.</done>
</task>

<task type="auto">
  <name>Task 3: Regression-confirm no change to report.sh / guardrail contract (SC-5)</name>
  <read_first>
    - tests/test_report_argv.sh (existing regression suite for report.sh argv/contract)
    - .planning/phases/11-structural-marker-enforcement-via-before-agent-finalize-plug/11-RESEARCH.md (line 538: SC-5 regression mapping; D-07 constraints)
    - scripts/report.sh (confirm untouched — verify-markers.sh must not import or modify it)
  </read_first>
  <files>tests/test_verify_markers.sh</files>
  <action>
    No production change to report.sh, config.json ruleIds, or guardrail-status.json (D-07 / SC-5). This task is a regression gate: run the existing report/guardrail argv tests and confirm they remain green after Phase 11's additions, proving verify-markers.sh is purely additive. If `git status --porcelain scripts/report.sh scripts/guardrail-check.sh scripts/common.sh` shows any modification to those files beyond the additive verify-markers.sh, that is a defect — common.sh must only be SOURCED, not edited (reuse existing constants; if a genuinely new shared constant is unavoidable, append-only and confirm no existing test breaks). Do not introduce changes here; this task only verifies.
  </action>
  <verify>
    <automated>bash tests/test_report_argv.sh && bash tests/test_guardrail_argv.sh && test -z "$(git diff --name-only -- scripts/report.sh scripts/guardrail-check.sh)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash tests/test_report_argv.sh` and `bash tests/test_guardrail_argv.sh` both exit 0 (unchanged behavior — SC-5).
    - `git diff --name-only` shows no modification to `scripts/report.sh` or `scripts/guardrail-check.sh`.
    - No change to `config.json` ruleIds or the `guardrail-status.json` halt/warn contract (D-07).
  </acceptance_criteria>
  <done>SC-5 confirmed: existing report/guardrail tests stay green and the metering/guardrail contract files are untouched by Phase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session/marker JSONL files → verify-markers.sh | The diagnostic reads untrusted on-disk JSONL; malformed lines must not crash or alter any metering state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-vm-readonly | Tampering | verify-markers.sh write surface | mitigate | Script is read-only: no file writes, no tee, no report.sh/guardrail/config invocation (verified by grep gate + git-diff regression); preserves D-07 metering contract |
| T-11-vm-parse | Denial of Service | malformed session/marker JSONL | mitigate | Python heredoc wraps per-line `json.loads` in try/except and continues (fail-open parse, matching write-marker.sh) |
| T-11-vm-heredoc | Tampering | bash→python env passing | mitigate | Variables passed via env (never interpolated into `<<'PY'` body), preventing shell/command injection through paths |
</threat_model>

<verification>
- `bash tests/test_verify_markers.sh` passes (SC-4: per-session gap + coverage, cron exclusion, summary totals).
- `bash tests/test_report_argv.sh` and `bash tests/test_guardrail_argv.sh` pass (SC-5 regression).
- `scripts/report.sh` / `scripts/guardrail-check.sh` show no git diff.
- `bash -n scripts/verify-markers.sh` clean; script is read-only.
</verification>

<success_criteria>
- SC-4: verify-markers.sh reports per-session completions vs markers + coverage %, with cron sessions excluded.
- SC-5: report.sh / guardrail behavior and the unclassified default + completion_id correlation are unchanged.
</success_criteria>

<output>
Create `.planning/phases/11-structural-marker-enforcement-via-before-agent-finalize-plug/11-02-SUMMARY.md` when done. Record the verify-markers.sh output column format and the fixture scenarios covered.
</output>
