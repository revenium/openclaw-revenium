---
phase: 16-skill-deploy-docs
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/nemoclaw-setup.md
  - README.md
autonomous: true
requirements: [NCDEPLOY-02]
tags: [nemoclaw, docs, operator-runbook, macos]

must_haves:
  truths:
    - "docs/nemoclaw-setup.md is a full operator runbook covering prerequisites (Linux, Docker, NemoClaw, sshfs, prebuilt revenium binary — not brew), the install sequence, ✓ ready verification, the parallel-path guarantee, the macOS-unsupported constraint with the exact error string, troubleshooting, and uninstall — D-01/D-04 / NCDEPLOY-02"
    - "docs/nemoclaw-setup.md quotes the verbatim macOS refusal string from scripts/install.sh so docs and code agree word-for-word"
    - "README.md gains exactly one pointer link to docs/nemoclaw-setup.md and is otherwise unchanged (standalone path uncontradicted) — D-01 / SC4"
  artifacts:
    - path: "docs/nemoclaw-setup.md"
      provides: "NemoClaw operator runbook mirroring the README.md section structure"
      min_lines: 80
      contains: "✗ NemoClaw is unsupported on macOS."
    - path: "README.md"
      provides: "Single pointer link to the NemoClaw runbook"
      contains: "nemoclaw-setup.md"
  key_links:
    - from: "README.md installation section"
      to: "docs/nemoclaw-setup.md"
      via: "markdown blockquote pointer link"
      pattern: "nemoclaw-setup.md"
    - from: "docs/nemoclaw-setup.md macOS section"
      to: "scripts/install.sh lines 76-84"
      via: "verbatim quoted error string"
      pattern: "NemoClaw is unsupported on macOS"
---

<objective>
Give NemoClaw operators a complete, standalone runbook. Create `docs/nemoclaw-setup.md` (D-01/D-04) covering prerequisites, the install command sequence, `✓ ready` verification, the parallel-path guarantee (standalone path untouched), the macOS-unsupported constraint with its exact verbatim error string, troubleshooting, and uninstall — mirroring the structure and tone of `README.md` without changing it. Add exactly one pointer link from `README.md` to the new doc (the only allowed README edit, satisfying SC4).

Purpose: NCDEPLOY-02 requires setup docs that cover the NemoClaw path, prerequisites, and the macOS-unsupported constraint, kept separate from and non-contradictory with the standalone README (SC3 + SC4).
Output: new `docs/nemoclaw-setup.md`; one-line pointer edit in `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.claude/skills/spike-findings-openclaw-revenium/SKILL.md
@.claude/skills/spike-findings-openclaw-revenium/references/install-and-bootstrap.md
@.claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md
@.planning/phases/16-skill-deploy-docs/16-RESEARCH.md
@.planning/phases/16-skill-deploy-docs/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/nemoclaw-setup.md operator runbook</name>
  <files>docs/nemoclaw-setup.md</files>
  <read_first>
    - README.md (the full 316-line standalone runbook — the section structure, prose tone, fenced command-block style, and blockquote-caveat format to mirror; the `openclaw skills list` verify step showing the `✓ ready  💰 revenium` output shape at lines ~85-90 and ~152)
    - scripts/install.sh lines 75-85 (the EXACT macOS refusal string to quote verbatim — the `fail` helper prepends `  ✗ `, so the operator-visible first line is `  ✗ NemoClaw is unsupported on macOS.`; exit code 1)
    - scripts/post-install-nemoclaw.sh (the NemoClaw install sequence: provisioning → install_metering_loop → install_skill_nemoclaw → install_enforcement_plugin; the success banner; the credential location /sandbox/.config/revenium/config.yaml; the uninstall counterparts uninstall-nemoclaw-cron.sh and the plugin uninstall)
    - .claude/skills/spike-findings-openclaw-revenium/references/install-and-bootstrap.md (Linux-only prerequisites; ~11 min bootstrap; macOS graceful-skip → false success rationale)
    - .claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md (`openclaw skills list` as the discovery command; what `✓ ready` looks like)
    - SKILL.md and BUDGET-GUARD.md (cross-reference targets — the skill manifest and the guardrail directive content the plugin injects, explaining WHY the plugin is mandatory beyond `skill install`)
    - .planning/phases/16-skill-deploy-docs/16-PATTERNS.md (the README→nemoclaw section parallel map; the D-04 required-sections checklist; the macOS exact-string block; the required cross-references)
  </read_first>
  <action>
    Create `docs/nemoclaw-setup.md` as a full operator runbook that PARALLELS README.md's structure (numbered install steps, fenced bash blocks, blockquote caveats, a Troubleshooting section) without copying standalone-only steps that do not apply. Required sections (all of D-04 must appear): (1) Prerequisites — Linux host, Docker, NemoClaw installed, `sshfs`, and the prebuilt `revenium` binary delivered as a tarball into the sandbox by the installer (explicitly NOT via Homebrew — Pitfall 3); env vars (REVENIUM_API_KEY etc.). (2) Install command sequence — running the NemoClaw install path (`bash scripts/install.sh --nemoclaw` → `post-install-nemoclaw.sh`), noting the ~11 min bootstrap. (3) `✓ ready` verification — `openclaw skills list` in-sandbox shows `✓ ready  💰 revenium`; note the installer also asserts this automatically (the D-02 gate from Plan 16-01). (4) Parallel-path guarantee — state explicitly that the standalone OpenClaw + Docker path is untouched and the two paths do not share install scripts; point back to README.md for the standalone path. (5) macOS-unsupported constraint — quote the macOS refusal VERBATIM from scripts/install.sh (including the leading `  ✗ ` operator-visible line) and note exit code 1 and that NemoClaw's own installer graceful-skips on Darwin (false success). (6) Troubleshooting — the macOS refusal, the SSHFS unsafe-filename error ("File names must match [A-Za-z0-9._-/]") and the SKILL.md-guard message it now produces, and sandbox restart/recover. (7) Uninstall — clear the host cron (uninstall-nemoclaw-cron.sh), uninstall the enforcement plugin, and `nemoclaw <name> skill remove`. Cross-reference SKILL.md, BUDGET-GUARD.md, and README.md. Match README.md's NemoClaw-specific facts: credentials live at `/sandbox/.config/revenium/config.yaml` (not `~/.config`) using the `api-key:` field; metering runs host-side over a share mount (not a Docker bind-mount). Do NOT reference `brew install revenium` or `openclaw gateway restart` (standalone-only concepts) without qualification.
  </action>
  <verify>
    <automated>test -f docs/nemoclaw-setup.md && grep -Eq 'Prerequisites' docs/nemoclaw-setup.md && grep -q 'macOS' docs/nemoclaw-setup.md && grep -qF 'NemoClaw is unsupported on macOS.' docs/nemoclaw-setup.md && grep -qi 'uninstall' docs/nemoclaw-setup.md && grep -q 'openclaw skills list' docs/nemoclaw-setup.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/nemoclaw-setup.md exists and contains all D-04 sections: Prerequisites, install sequence, `✓ ready` verification, parallel-path guarantee, macOS-unsupported constraint, Troubleshooting, Uninstall.
    - The doc contains the verbatim string `NemoClaw is unsupported on macOS.` matching scripts/install.sh.
    - The doc references `openclaw skills list` and the `✓ ready` discovery output, and `/sandbox/.config/revenium/config.yaml` with the `api-key:` field.
    - The doc does NOT instruct `brew install revenium` for the NemoClaw path (Pitfall 3).
    - The doc cross-references SKILL.md, BUDGET-GUARD.md, and README.md.
  </acceptance_criteria>
  <done>
    docs/nemoclaw-setup.md is a complete operator runbook with all D-04 topics, the verbatim macOS error string, and correct NemoClaw-specific facts — mirroring README.md without contradicting it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the single README.md pointer link to the NemoClaw runbook</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 18-92 (the Installation section through `### 5. Verify` ending at line ~90, before the `### First-time setup (automatic)` subsection — the logical end of the primary install path where a NemoClaw operator diverges)
    - README.md lines 24-28 (the existing VirusTotal blockquote — the exact blockquote syntax/tone to match for the pointer link)
    - .planning/phases/16-skill-deploy-docs/16-PATTERNS.md (the exact pointer-link text and the placement anchor at the end of the `### 5. Verify` block)
  </read_first>
  <action>
    Insert exactly ONE blockquote pointer link into README.md at the end of the `### 5. Verify` block (after line ~90, before the `### First-time setup (automatic)` subsection), matching the existing blockquote style: `> **Running on NemoClaw/OpenShell?** See [NemoClaw Setup](docs/nemoclaw-setup.md) for the parallel install path.` Make NO other change to README.md — no edits to any standalone step, prerequisite, or troubleshooting entry (SC4: standalone docs must not be changed or contradicted). The relative link path `docs/nemoclaw-setup.md` is correct from the repo-root README.
  </action>
  <verify>
    <automated>grep -q 'nemoclaw-setup.md' README.md && test "$(git diff --numstat README.md | awk '{print $1}')" -le 4</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a single pointer link to `docs/nemoclaw-setup.md`.
    - The README.md diff adds only the pointer-link blockquote (a handful of inserted lines, no deletions of existing standalone content).
    - No standalone installation step, prerequisite, or troubleshooting entry is altered.
  </acceptance_criteria>
  <done>
    README.md has one pointer link to the NemoClaw runbook and is otherwise byte-unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan creates documentation and adds one pointer link; it introduces no executable code paths, no new credentials, no network calls, and no secret handling. There is no trust boundary crossed by the artifacts in this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-DOC | Information Disclosure | docs/nemoclaw-setup.md | accept | The runbook documents an existing, already-shipped install path; it must not embed real API keys or host secrets — env-var names and placeholders only (verified by review). No real threat surface beyond standard doc hygiene. |

No real security threats are introduced by this docs-only plan; the single entry above is documentation hygiene, not a code-level threat.
</threat_model>

<verification>
- `test -f docs/nemoclaw-setup.md` and the section greps in Task 1 all succeed.
- `grep -qF 'NemoClaw is unsupported on macOS.' docs/nemoclaw-setup.md` (verbatim string parity with scripts/install.sh).
- `grep -q 'nemoclaw-setup.md' README.md`.
- README.md diff is limited to the single pointer-link blockquote (no standalone content changed — SC4).
</verification>

<success_criteria>
- NCDEPLOY-02: the NemoClaw path, prerequisites, and macOS-unsupported constraint are documented in a standalone runbook.
- SC3: prerequisites (Linux, Docker, NemoClaw, sshfs), parallel-path guarantee, and the explicit macOS error message are all present.
- SC4: the standalone README is unchanged except for one pointer link.
</success_criteria>

<output>
Create `.planning/phases/16-skill-deploy-docs/16-02-SUMMARY.md` when done.
</output>
