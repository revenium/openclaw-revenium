---
phase: 09-guardrail-event-metering
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_guardrail_argv.sh
  - tests/stub-revenium.sh
autonomous: false
requirements: [GRDEV-01, GRDEV-02, GRDEV-03, GRDEV-04, GRDEV-05]
must_haves:
  truths:
    - "tests/test_guardrail_argv.sh exists and runs guardrail-check.sh against a stubbed revenium CLI"
    - "stub-revenium.sh answers config show / guardrails capability probes / enforcement-rules get / budget-rules list and supports a STUB_REVENIUM_GUARDRAILS_FAIL switch"
    - "The three open Wave-0 CLI questions (transaction-id optional, zero tokens accepted, COST_LIMIT valid stop-reason) are answered and recorded"
  artifacts:
    - path: "tests/test_guardrail_argv.sh"
      provides: "Hermetic argv-capture test harness for guardrail event metering (GRDEV-01..05)"
      min_lines: 120
    - path: "tests/stub-revenium.sh"
      provides: "guardrails subcommand stub responses + STUB_REVENIUM_GUARDRAILS_FAIL failure switch"
      contains: "STUB_REVENIUM_GUARDRAILS_FAIL"
  key_links:
    - from: "tests/test_guardrail_argv.sh"
      to: "scripts/guardrail-check.sh"
      via: "bash invocation with OPENCLAW_HOME + stubbed PATH"
      pattern: "guardrail-check.sh"
    - from: "tests/test_guardrail_argv.sh"
      to: "tests/stub-revenium.sh"
      via: "symlink on PATH as revenium, argv captured to STUB_REVENIUM_ARGV_FILE"
      pattern: "STUB_REVENIUM_ARGV_FILE"
---

<objective>
Build the Wave 0 test scaffolding and resolve the live-CLI open questions BEFORE any production code is touched, so the implementation waves have a red-then-green target to satisfy.

This plan creates `tests/test_guardrail_argv.sh` (no guardrail-check.sh test exists today) and extends `tests/stub-revenium.sh` with the `guardrails` subcommand responses and a failure switch the harness needs. It also resolves the three open `revenium meter completion` flag questions (RESEARCH Assumptions A1–A3) via a live `--help`/test invocation on the test host, recording answers so the implementation plan uses the correct flag set.

Purpose: Nyquist compliance — every implementation requirement (GRDEV-01..05) gets an automated assertion target that exists before the code it verifies. Resolving the CLI questions now prevents a mid-implementation flag-set rework.
Output: A runnable (initially red) `tests/test_guardrail_argv.sh`, an extended `tests/stub-revenium.sh`, and a recorded answer to the three CLI questions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guardrail-event-metering/09-CONTEXT.md
@.planning/phases/09-guardrail-event-metering/09-RESEARCH.md
@.planning/phases/09-guardrail-event-metering/09-PATTERNS.md
@.planning/phases/09-guardrail-event-metering/09-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Resolve the three open revenium meter completion CLI questions (live host)</name>
  <read_first>
    - .planning/phases/09-guardrail-event-metering/09-RESEARCH.md (Assumptions Log A1–A3, Open Questions 1–3, Q2 flag set)
    - MEMORY note: test machine is remote host 172.16.1.247 (not this laptop); revenium CLI lives there
  </read_first>
  <what-built>
    Nothing is built in this task — it gathers three facts the implementation depends on. The executor (or user) runs, on the test host 172.16.1.247 where the real `revenium` CLI is installed:
      1. `revenium meter completion --help` and inspect whether `--transaction-id` is required or optional (Open Question 1 / A1).
      2. A trial `revenium meter completion --operation-type GUARDRAIL --task-type budget_guardrail_warn --stop-reason COST_LIMIT --input-tokens 0 --output-tokens 0 --total-tokens 0 --model guardrail-enforcement --provider revenium --quiet` (omitting --transaction-id) to confirm zero token values are accepted (Open Question 2 / A2) and that COST_LIMIT is a valid --stop-reason enum (Open Question 3 / A3).
    Decision rule to record per RESEARCH recommendations: if --transaction-id is required, the implementation will generate a synthetic id `GUARDRAIL_<type>_<sha1(onset_key)[:8]>`; if zero tokens are rejected, use --total-tokens 1 sentinel; if COST_LIMIT is invalid, fall back to the closest valid enum and note it.
  </what-built>
  <how-to-verify>
    1. SSH to 172.16.1.247 (user agent socket per MEMORY) and run the two commands above.
    2. Confirm and report: (a) is --transaction-id optional? (b) does the zero-token call succeed? (c) is COST_LIMIT accepted?
    3. Record the three answers in the SUMMARY for this plan so the Wave 1 implementer reads them.
  </how-to-verify>
  <acceptance_criteria>
    - The SUMMARY records a definitive yes/no for each of the three questions (A1: --transaction-id optional, A2: zero tokens accepted, A3: COST_LIMIT valid).
    - If any answer is "no", the SUMMARY records the chosen fallback (synthetic id format / sentinel token value / alternate stop-reason) so the implementation uses it.
  </acceptance_criteria>
  <action>
    Run the live `revenium meter completion --help` and trial-call verification on host 172.16.1.247. Do NOT meter against production unless the trial event is cleaned up. Capture answers to A1/A2/A3. This is a checkpoint because the only authoritative source is the live CLI on the remote test host.
  </action>
  <resume-signal>Report the three answers (transaction-id optional? zero tokens ok? COST_LIMIT valid?) and any fallback chosen, then type "approved"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extend stub-revenium.sh with guardrails responses + failure switch</name>
  <files>tests/stub-revenium.sh</files>
  <read_first>
    - tests/stub-revenium.sh (the file being modified — esp. the leading STUB_REVENIUM_ARGV_FILE argv-capture block ~lines 58–62, the config show block ~lines 69–71, and the existing STUB_REVENIUM_HALT_JOBS_FAIL switch ~lines 34–52 and 108–115)
    - .planning/phases/09-guardrail-event-metering/09-PATTERNS.md (section "tests/stub-revenium.sh — ADD guardrail stub responses + STUB_REVENIUM_GUARDRAILS_FAIL switch")
    - scripts/guardrail-check.sh (capability probe ~lines 58–73 and the `config show` Team ID parse ~line 101 — confirms exactly what the stub must answer)
  </read_first>
  <action>
    Modify `tests/stub-revenium.sh`. Keep the leading argv-capture block first and unchanged (it must capture ALL invocations before any branching). Change the `config show` branch to echo a line `Team ID:    test-team-id` before `exit 0` (guardrail-check.sh parses this; report.sh only checks exit code via `&>/dev/null` so this is safe — note this in the SUMMARY). Add branches that `exit 0` for the capability probes `guardrails --help`, `guardrails budget-rules --help`, and `guardrails enforcement-events --help`. Add a `guardrails enforcement-rules get` branch that: if `STUB_REVENIUM_GUARDRAILS_FAIL` is non-empty, prints an EOF-style error to stderr and exits 1; else echoes `${STUB_REVENIUM_ENFORCEMENT_JSON}` when set, defaulting to `{"rules":[]}`. Add a `guardrails budget-rules list` branch that echoes `${STUB_REVENIUM_BUDGET_RULES_JSON}` when set, defaulting to `[]`. Document the three new env switches (STUB_REVENIUM_GUARDRAILS_FAIL, STUB_REVENIUM_ENFORCEMENT_JSON, STUB_REVENIUM_BUDGET_RULES_JSON) in the header comment block, mirroring the existing switch docs. Do NOT touch the `meter completion` path — it must keep falling through to the silent `exit 0` so argv is captured. Use exact subcommand-matching via positional `"$1 $2 $3"` comparisons per the PATTERNS analog.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/openclaw-revenium && bash -n tests/stub-revenium.sh && grep -c 'STUB_REVENIUM_GUARDRAILS_FAIL' tests/stub-revenium.sh | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/stub-revenium.sh` succeeds (syntax valid).
    - `tests/stub-revenium.sh` contains `STUB_REVENIUM_GUARDRAILS_FAIL`, `guardrails enforcement-rules get`, and `guardrails budget-rules list` branches.
    - The `config show` branch emits `Team ID:    test-team-id`.
    - The leading argv-capture block remains the first conditional in the file (unchanged).
    - The `meter completion` path is NOT intercepted (still falls through to capture-and-exit-0).
  </acceptance_criteria>
  <done>stub-revenium.sh answers all guardrail-check.sh CLI probes, supports the failure switch, and still silently captures meter completion argv.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_guardrail_argv.sh covering GRDEV-01..05 (initially red)</name>
  <files>tests/test_guardrail_argv.sh</files>
  <read_first>
    - tests/test_report_argv.sh (structural analog — header/boilerplate ~lines 1–43, fake HOME + stub placement ~lines 164–177, argv_vals assertion helper ~lines 190–200, summary footer ~lines 302–309)
    - tests/test_report_jobs_argv.sh (make_openclaw_home factory ~lines 48–63, run_report helper ~lines 98–108)
    - .planning/phases/09-guardrail-event-metering/09-PATTERNS.md (section "tests/test_guardrail_argv.sh — CREATE hermetic test" — fixtures, run_guardrail_check, idempotency + fail-open assertions)
    - .planning/phases/09-guardrail-event-metering/09-RESEARCH.md (Q8 test structure; Validation Architecture → Test Map for the exact assertion list)
    - scripts/guardrail-check.sh (to understand fixture shape: guardrail-status.json prev-state schema, config.json ruleIds, SESSIONS_DIR session-file expectation, Team ID parse)
  </read_first>
  <action>
    Create `tests/test_guardrail_argv.sh` mirroring `test_report_argv.sh`/`test_report_jobs_argv.sh`. Implement a `make_openclaw_home` factory that builds a temp OPENCLAW_HOME with: `agents/main/sessions/` containing one fake `<uuid>.jsonl` session file (header `{"type":"session","version":3,"id":"...","timestamp":"...","cwd":"..."}` so attribution resolves), `skills/revenium/scripts/get-root-session-id.py` symlinked from the repo, `skills/revenium/config.json` with `organizationName` + `ruleIds:["rule-abc123"]`, a prev-state `skills/revenium/guardrail-status.json` (no rules breached), an empty `revenium-guardrail.ledger`, and a `revenium-jobs.ledger` containing one open job (a `JOB:<id>:created:` line with no matching outcome). Place stub-revenium.sh symlinked as `revenium` on a temp PATH/HOME, export `STUB_REVENIUM_ARGV_FILE`. Implement `run_guardrail_check` mirroring `run_report`, passing `STUB_REVENIUM_ENFORCEMENT_JSON` fixtures that produce a halted rule, a warned rule, and a shadow rule. Add the `argv_vals` helper. Write assertions for: GRDEV-01a (--operation-type GUARDRAIL + --task-type budget_guardrail_halt + --stop-reason COST_LIMIT), GRDEV-01b (run twice → exactly one budget_guardrail_halt in argv, ledger dedup), GRDEV-02a (--task-type budget_guardrail_warn once), GRDEV-02b (warn→ok→warn re-fires using a recovered-then-rebreached fixture across two prev-state files), GRDEV-03 (--task-type budget_guardrail_shadow once), GRDEV-04a (--agent openclaw-<expected_root_sid> present on every meter call), GRDEV-04b (--agentic-job-id present with open job; omitted with empty jobs ledger), GRDEV-05 (with STUB_REVENIUM_GUARDRAILS_FAIL=1 and/or a forced meter failure, guardrail-check.sh exits 0 and guardrail-status.json is written). Use the pass/fail counters + summary footer that exits 1 on any failure. The test is expected to FAIL until Wave 1 implements the production code — that is correct (red-first). Honor the CLI answers recorded in Task 1 (e.g. assert a synthetic --transaction-id only if Task 1 found it required).
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/openclaw-revenium && bash -n tests/test_guardrail_argv.sh && echo SYNTAX_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/test_guardrail_argv.sh` succeeds.
    - The file contains assertions referencing each of: `budget_guardrail_halt`, `budget_guardrail_warn`, `budget_guardrail_shadow`, `--operation-type` `GUARDRAIL`, `COST_LIMIT`, `--agent`, `--agentic-job-id`.
    - The file runs guardrail-check.sh twice in at least one group to assert idempotency (exactly one meter call per event type).
    - The file contains a fail-open group asserting exit 0 under `STUB_REVENIUM_GUARDRAILS_FAIL` / forced meter failure.
    - Running `bash tests/test_guardrail_argv.sh` executes to the summary footer (it MAY report failures pre-implementation; it must not crash with a syntax/setup error).
  </acceptance_criteria>
  <done>A red-first hermetic test exists that fully specifies GRDEV-01..05 behavior and is ready to turn green in Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → guardrail-check.sh | Test injects fixtures (session files, ledgers, config, enforcement JSON) into a temp OPENCLAW_HOME — all controlled by the test author, not external input |
| stub → captured argv | The stub records argv to a file the test reads; no privilege crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00-01 | Tampering | test temp dirs under TMPDIR | accept | Tests use mktemp -d and trap-cleanup; no production paths touched; runs only in dev/CI |
| T-09-00-02 | Information disclosure | live trial meter call (Task 1) on test host | mitigate | Trial event is zero-token/zero-cost and cleaned up; run only on test host 172.16.1.247, never production tenant |
| T-09-00-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH: "Not applicable — no new packages") |
</threat_model>

<verification>
- `bash -n tests/stub-revenium.sh` and `bash -n tests/test_guardrail_argv.sh` both pass.
- `bash tests/test_report_argv.sh` and `bash tests/test_report_jobs_argv.sh` still pass (the config-show output change must not regress existing tests).
- The three CLI questions are answered and recorded in the SUMMARY.
</verification>

<success_criteria>
- Wave 0 deliverables from 09-VALIDATION.md are complete: test_guardrail_argv.sh exists, stub-revenium.sh extended with guardrails responses + failure switch, and the three CLI questions resolved.
- test_guardrail_argv.sh encodes automated assertions for GRDEV-01 through GRDEV-05.
- No production scripts modified in this plan.
</success_criteria>

<output>
Create `.planning/phases/09-guardrail-event-metering/09-00-SUMMARY.md` when done. MUST include the three resolved CLI answers (A1/A2/A3) and any chosen fallbacks.
</output>
