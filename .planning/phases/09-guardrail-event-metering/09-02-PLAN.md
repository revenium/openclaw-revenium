---
phase: 09-guardrail-event-metering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/report.sh
  - tests/test_report_argv.sh
autonomous: true
requirements: [GRDEV-06]
must_haves:
  truths:
    - "report.sh no longer contains the dead operation-type GUARDRAIL heuristic that grepped toolCall arguments for budget-status.json (GRDEV-06)"
    - "After removal, report.sh assigns operation_type only CHAT or TOOL_CALL"
    - "An automated assertion proves no operation_type GUARDRAIL is ever emitted by report.sh"
  artifacts:
    - path: "scripts/report.sh"
      provides: "operation_type detection limited to CHAT / TOOL_CALL"
    - path: "tests/test_report_argv.sh"
      provides: "Assertion that report.sh never emits --operation-type GUARDRAIL"
      contains: "GUARDRAIL"
  key_links:
    - from: "scripts/report.sh operation_type detection"
      to: "revenium meter completion --operation-type"
      via: "only TOOL_CALL (toolUse) or CHAT branches remain"
      pattern: "operation_type"
---

<objective>
Remove the dead/buggy operation-type GUARDRAIL heuristic from report.sh (D-12 / GRDEV-06). The branch greps toolCall arguments for the literal `budget-status.json` — but the current filename is `guardrail-status.json`, so the match never fires correctly and the heuristic would tag completions wrongly. After removal, report.sh only ever assigns `CHAT` (default) or `TOOL_CALL` (stopReason toolUse). Add an assertion to the existing report.sh test that no `--operation-type GUARDRAIL` is emitted.

Purpose: Normal agent completions must never be mislabeled as GUARDRAIL — GUARDRAIL transactions now come exclusively from guardrail-check.sh (Plan 01). This is the cleanup half of the phase.
Output: Modified scripts/report.sh (3-line branch deleted, comment updated); extended tests/test_report_argv.sh assertion.

This plan implements locked decision D-12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-guardrail-event-metering/09-CONTEXT.md
@.planning/phases/09-guardrail-event-metering/09-RESEARCH.md
@.planning/phases/09-guardrail-event-metering/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the dead GUARDRAIL heuristic from report.sh and assert it is gone</name>
  <files>scripts/report.sh, tests/test_report_argv.sh</files>
  <read_first>
    - scripts/report.sh (the file being modified — the operation_type detection block ~lines 843–853: the comment block, `local raw_stop_reason operation_type="CHAT"`, the `raw_stop_reason=$(... jq ... stopReason ...)` line, the `if echo "${line}" | jq -e '.message.content[] | select(.type=="toolCall") | .arguments' ... grep -q "budget-status.json"` GUARDRAIL branch, and the `elif [[ ... toolUse ... ]]` TOOL_CALL branch)
    - .planning/phases/09-guardrail-event-metering/09-PATTERNS.md (section "scripts/report.sh — DELETE lines 849–851 (D-12 GUARDRAIL heuristic)" — exact before/after)
    - .planning/phases/09-guardrail-event-metering/09-RESEARCH.md (Q6 D-12 removal — exact branch and why it is dead)
    - tests/test_report_argv.sh (the file being modified — argv assertion helpers and structure, to add the new no-GUARDRAIL assertion)
  </read_first>
  <action>
    In `scripts/report.sh`, delete the dead GUARDRAIL branch so the operation_type detection collapses to: keep `local raw_stop_reason operation_type="CHAT"` and `raw_stop_reason=$(echo "${line}" | jq -r '.message.stopReason // "stop"')`, then a single `if [[ "${raw_stop_reason}" == "toolUse" || "${raw_stop_reason}" == "tool_use" ]]; then operation_type="TOOL_CALL"; fi`. Remove the `if echo "${line}" | jq -e ... grep -q "budget-status.json"; then operation_type="GUARDRAIL"` lines and convert the former `elif` into the surviving `if`. Update the adjacent comment block to drop the GUARDRAIL line, leaving only the TOOL_CALL and CHAT descriptions. Do not touch any other report.sh logic. In `tests/test_report_argv.sh`, add an assertion that the captured argv never contains an `--operation-type` value of `GUARDRAIL` (use the existing argv_vals helper: assert `argv_vals "--operation-type"` produces no `GUARDRAIL` line), confirming a normal completion run yields only CHAT/TOOL_CALL.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/openclaw-revenium && bash -n scripts/report.sh && bash -n tests/test_report_argv.sh && ! grep -q 'operation_type="GUARDRAIL"' scripts/report.sh && ! grep -q 'budget-status.json' scripts/report.sh && bash tests/test_report_argv.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/report.sh` and `bash -n tests/test_report_argv.sh` both succeed.
    - `grep -q 'operation_type="GUARDRAIL"' scripts/report.sh` returns no match (heuristic removed).
    - `grep -q 'budget-status.json' scripts/report.sh` returns no match (the dead filename grep is gone).
    - report.sh's operation_type is reachable only as CHAT (default) or TOOL_CALL (toolUse / tool_use).
    - `bash tests/test_report_argv.sh` passes, including a new assertion that no `--operation-type GUARDRAIL` appears in captured argv.
  </acceptance_criteria>
  <done>report.sh's GUARDRAIL heuristic is removed; completions are only CHAT or TOOL_CALL, proven by the extended test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenClaw session JSONL → report.sh operation_type | completion message content is parsed to classify operation_type; the removed heuristic was an unreliable classifier over this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | operation_type misclassification | mitigate | Removing the filename-grep heuristic eliminates the false-positive GUARDRAIL tagging; classification reduces to the deterministic stopReason check |
| T-09-02-02 | Repudiation | accurate transaction typing in Revenium | mitigate | After removal, GUARDRAIL is sourced only from guardrail-check.sh (Plan 01), so transaction types are unambiguous and auditable |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH: "Not applicable") |
</threat_model>

<verification>
- `bash tests/test_report_argv.sh` passes with the new no-GUARDRAIL assertion.
- `bash tests/test_report_jobs_argv.sh` still passes (no regression to report.sh job handling).
- Source grep confirms neither `operation_type="GUARDRAIL"` nor `budget-status.json` remains in report.sh.
</verification>

<success_criteria>
- GRDEV-06: the dead operation-type GUARDRAIL heuristic is removed from report.sh; normal completions are only ever CHAT or TOOL_CALL, proven by an automated assertion.
</success_criteria>

<output>
Create `.planning/phases/09-guardrail-event-metering/09-02-SUMMARY.md` when done.
</output>
