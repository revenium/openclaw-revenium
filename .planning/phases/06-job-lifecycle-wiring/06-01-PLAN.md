---
phase: 06-job-lifecycle-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/stub-revenium.sh
  - tests/stub-meta-check.sh
  - tests/test_report_jobs_argv.sh
autonomous: true
requirements: [JLIFE-01, JLIFE-02, JLIFE-03, JLIFE-04, JLIFE-05]
must_haves:
  truths:
    - "A new tests/test_report_jobs_argv.sh exists and runs hermetically in <30s"
    - "The new test fails RED today (report.sh has no job wiring yet) — proving it exercises real behavior"
    - "tests/test_report_argv.sh stays GREEN (its no-agentic-job assertion is preserved by keeping its fixtures job-free) — Pitfall 4 decision recorded"
    - "The revenium stub can fake a 409 conflict on jobs create/outcome via STUB_REVENIUM_409_FOR"
    - "The revenium stub answers `meter completion --help` with --agentic-job-id so the happy-path capability probe passes, and a STUB_REVENIUM_NO_JOBS switch forces the probe to fail"
  artifacts:
    - path: "tests/stub-revenium.sh"
      provides: "argv capture (unchanged) + jobs create/outcome 409 fake + --help responses driving JOBS_CLI_CAPABLE"
      contains: "STUB_REVENIUM_409_FOR"
    - path: "tests/test_report_jobs_argv.sh"
      provides: "JLIFE-01..05 integration fixtures + argv/ledger assertions (RED until Plans 02/03 land)"
      min_lines: 120
  key_links:
    - from: "tests/test_report_jobs_argv.sh"
      to: "tests/stub-revenium.sh"
      via: "ln -sf onto fake HOME/.local/bin/revenium"
      pattern: "ln -sf.*stub-revenium"
    - from: "tests/test_report_jobs_argv.sh"
      to: "scripts/report.sh"
      via: "OPENCLAW_HOME + HOME env override invocation"
      pattern: "bash .*report.sh"
---

<objective>
Build the Wave 0 test scaffolding for Phase 6 BEFORE any `report.sh` edits: a new hermetic
integration test `tests/test_report_jobs_argv.sh` covering JLIFE-01..05, plus an extension to
`tests/stub-revenium.sh` that fakes the `jobs` subcommands, a 409-conflict path, and the
`--help` outputs that drive the `JOBS_CLI_CAPABLE` capability probe (D-11).

This is the Nyquist RED gate: the new test MUST fail today (report.sh emits no job tokens),
and `tests/test_report_argv.sh` MUST stay green.

**Pitfall 4 decision (RECORDED, per RESEARCH §Open Questions Q2 / PATTERNS §test harness):**
`tests/test_report_argv.sh` is left UNTOUCHED and job-free. ALL Phase 6 job fixtures live in
the new `tests/test_report_jobs_argv.sh`. This preserves the existing task-type regression
test's `grep -q "agentic-job"` negative assertion (test_report_argv.sh:285-289). A plan or
test that adds job markers to Sessions A-D would silently break that suite — do not do it.

Purpose: Establish the feedback loop so Plans 02 and 03 are verified turn-green against a
fixed, behavior-exercising test rather than ad-hoc inspection.
Output: tests/stub-revenium.sh (extended), tests/stub-meta-check.sh (new helper),
tests/test_report_jobs_argv.sh (new, RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-job-lifecycle-wiring/06-CONTEXT.md
@.planning/phases/06-job-lifecycle-wiring/06-RESEARCH.md
@.planning/phases/06-job-lifecycle-wiring/06-PATTERNS.md
@.planning/phases/06-job-lifecycle-wiring/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/stub-revenium.sh for jobs subcommands, --help capability probe, and 409 path</name>
  <files>tests/stub-revenium.sh, tests/stub-meta-check.sh</files>
  <read_first>
    - tests/stub-revenium.sh (the file being modified — current capture block at lines 15-20 captures every argv token one-per-line then `exit 0`, and emits NOTHING on stdout)
    - .planning/phases/06-job-lifecycle-wiring/06-PATTERNS.md §"tests/stub-revenium.sh (MODIFIED — test double)" (~lines 418-444 — exact extension shape, STUB_REVENIUM_409_FOR contract, the note that a SEPARATE fail-open stub forces JOBS_CLI_CAPABLE=false)
    - .planning/phases/06-job-lifecycle-wiring/06-RESEARCH.md §"Stub extension for jobs" (~lines 403-416) and Pattern 1 (the dual probe: `revenium jobs --help` AND `revenium meter completion --help | grep -- '--agentic-job-id'`)
    - scripts/report.sh lines 96-111 (the startup guards the stub must satisfy: `revenium config show` must exit 0)
  </read_first>
  <action>
    Extend tests/stub-revenium.sh. Keep the existing argv-capture block (lines 15-20) FIRST and
    unchanged so every token (`jobs`, `create`, `outcome`, `--agentic-job-id`, `--agentic-job-name`,
    `--agentic-job-type`, `--result`, `--metadata`, `--task-type`, `--agent`) stays assertable.
    BEFORE the final `exit 0`, add behaviors keyed on the leading positional args:

    1. Config + capability-probe responses (satisfy report.sh:106 guard and the D-11 dual probe):
       - `config show` → exit 0 silently.
       - `jobs --help` (i.e. `"$1 $2" == "jobs --help"`) → unless STUB_REVENIUM_NO_JOBS is set, print
         a one-line usage string and exit 0. When STUB_REVENIUM_NO_JOBS is set, exit 1 (probe fails →
         JOBS_CLI_CAPABLE stays false — the fail-open fixture in Task 2 uses this).
       - `meter completion --help` (i.e. `"$1 $2 $3" == "meter completion --help"`) → print a line
         literally containing the token `--agentic-job-id` to stdout and exit 0. This is what makes
         the dual probe set JOBS_CLI_CAPABLE=true. Print `--agentic-job-id` ONLY for this `--help`
         invocation, never for a real `meter completion` post.
       Mirror the dual-probe shape named in RESEARCH Pattern 1.

    2. 409 fake (opt-in, D-06 idempotency net): when `"$1 $2"` is "jobs create" OR "jobs outcome"
       AND STUB_REVENIUM_409_FOR is non-empty AND that value appears among "$@", write the literal
       string `Error: 409 Conflict: job already exists` to stderr and exit 1. This is the stub's
       only non-zero exit besides the NO_JOBS probe failure. Mirror PATTERNS §stub extension verbatim.

    3. Default tail: `exit 0`.

    Document STUB_REVENIUM_NO_JOBS and STUB_REVENIUM_409_FOR in the stub header comment so Task 2
    can use them. SECURITY (T-04-09 / V5): never `eval` or string-interpolate captured argv into a
    command; keep the `printf '%s\n' "${arg}"` capture and only string-COMPARE positional args.

    Also create tests/stub-meta-check.sh: a tiny self-check script that asserts the stub behaviors
    listed in this task's acceptance criteria (valid bash, --help prints --agentic-job-id, NO_JOBS
    probe fails, 409 path, capture-unchanged) and exits 0 only when all hold. This makes the
    <automated> verify a single command and is part of this task's deliverable.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/openclaw-revenium && bash tests/stub-meta-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/stub-revenium.sh` exits 0 (valid bash).
    - `bash tests/stub-revenium.sh meter completion --help` prints output containing `--agentic-job-id` and exits 0.
    - `bash tests/stub-revenium.sh jobs --help` exits 0; with `STUB_REVENIUM_NO_JOBS=1` it exits non-zero.
    - `STUB_REVENIUM_409_FOR=abc bash tests/stub-revenium.sh jobs create --agentic-job-id abc` writes a string matching `conflict` (case-insensitive) to stderr and exits non-zero.
    - A normal `bash tests/stub-revenium.sh meter completion --agentic-job-id x` (no `--help`) exits 0 and does NOT print `--agentic-job-id` on stdout (only `--help` does).
    - The existing argv-capture block is unchanged (a `meter completion` invocation with STUB_REVENIUM_ARGV_FILE set still appends every token one-per-line).
    - `tests/stub-meta-check.sh` exits 0 (it encodes all of the above).
  </acceptance_criteria>
  <done>Stub captures all argv (unchanged), answers the dual capability probe on the happy path, fails the probe under STUB_REVENIUM_NO_JOBS, and fakes a 409 under STUB_REVENIUM_409_FOR; tests/stub-meta-check.sh passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_report_jobs_argv.sh (RED) covering JLIFE-01..05</name>
  <files>tests/test_report_jobs_argv.sh</files>
  <read_first>
    - tests/test_report_argv.sh (FULL FILE — clone its structure: pass/fail counters + exit at lines 38-42 and 294-299; tmp OPENCLAW_HOME build at 47-64; stub-on-PATH via fake HOME/.local/bin at 158-177; run-under-stubbed-env at 182-186; argv assertions via `awk '/^--flag$/{getline;print}'` and `grep -c "^token$"` at 191-192, 262-264; the existing no-agentic-job assertion at 285-289 — DO NOT replicate that negative assertion in the new test)
    - tests/stub-revenium.sh (AFTER Task 1 — the STUB_REVENIUM_409_FOR and STUB_REVENIUM_NO_JOBS switches)
    - scripts/write-job-marker.sh lines 228-237 (the exact emitted marker JSON shape: keys kind="job", ts, sid, agentic_job_id, job_name, job_type, status, optional failure_reason, optional completion_id) and line 111 (status allowlist SUCCESS|FAILED|CANCELLED)
    - .planning/phases/06-job-lifecycle-wiring/06-PATTERNS.md §"tests/test_report_jobs_argv.sh (NEW)" (~lines 373-414 — fixtures list, the job-marker JSON shape, the Pitfall 4 decision)
    - .planning/phases/06-job-lifecycle-wiring/06-RESEARCH.md §"Phase Requirements → Test Map" (~lines 491-499) and §"Validation Architecture"
    - .planning/phases/06-job-lifecycle-wiring/06-VALIDATION.md §"Per-Task Verification Map" (the canonical per-requirement assertion text)
  </read_first>
  <action>
    Create tests/test_report_jobs_argv.sh by cloning the structure of tests/test_report_argv.sh
    (pass/fail counters, tmp OPENCLAW_HOME build with agents/main/sessions + skills/revenium/markers
    + empty `{}` offsets + touched revenium-reported.ledger + config.json, stub symlinked onto a fake
    HOME/.local/bin/revenium, STUB_REVENIUM_ARGV_FILE exported, run report.sh under
    `OPENCLAW_HOME=... HOME=...`). Add `touch "${TMP_HOME}/revenium-jobs.ledger"` and inspect that
    file after the run. Build these session+marker fixtures, mirroring the marker JSON shape from
    write-job-marker.sh:228-237 and the completion_id exact-match pattern of MARKER_C
    (test_report_argv.sh:131):

    - SUCCESS job (JLIFE-01/02/03/05): one session with one completion whose `.id` equals the job
      marker's `completion_id`; marker `kind:"job"`, status SUCCESS, a job_name and a taxonomy-valid
      job_type. Assert: exactly one `^create$` token; `awk '/^--agentic-job-id$/{getline;print}'`
      equals the marker's agentic_job_id; `--agentic-job-name` and `--agentic-job-type` present;
      exactly one `^outcome$` token with `awk '/^--result$/{getline;print}'` == SUCCESS; NO
      `--metadata` token for this job; jobs ledger has one `^JOB:<id>:created:` and one
      `^JOB:<id>:outcome:.*:SUCCESS` line.
    - FAILED job (JLIFE-03 + D-08): a second session, status FAILED, with a `failure_reason`. Assert
      `--result` value FAILED and that a `--metadata` token is present whose following value parses
      as JSON containing key `failure_reason` (json.loads via env-passing python3 — do not eval).
    - CANCELLED job (JLIFE-03 + D-07): a third session, status CANCELLED. Assert `--result` value
      CANCELLED, NO `--metadata` token for it, and that NO `--outcome-type` token EVER appears in the
      captured argv (D-07 — `grep -c "^--outcome-type$" == 0`). Also assert `--environment` never
      appears (D-04 — `grep -c "^--environment$" == 0`).
    - Fail-open (JLIFE-04): a SEPARATE report.sh run against a fresh argv file with
      STUB_REVENIUM_NO_JOBS=1 exported (probe fails → JOBS_CLI_CAPABLE=false). Assert zero `^jobs$`
      tokens AND zero `agentic-job` tokens in that argv, AND `--task-type` and `--agent` ARE still
      present (metering byte-identical to v1.0).
    - 409-as-success (JLIFE-04 extra / D-06): a run with STUB_REVENIUM_409_FOR set to the SUCCESS
      job's id on a FRESH jobs ledger. Assert the `^JOB:<id>:created:` ledger row is STILL written
      and report.sh exits 0.
    - Idempotency / re-run (JLIFE-01/05): run report.sh TWICE against the same OPENCLAW_HOME (do not
      reset offsets/ledgers between runs). Assert across BOTH runs combined exactly one `^create$`
      and one `^outcome$` token total per job, and the jobs ledger has exactly one `:created:` and
      one `:outcome:` line per job id.

    Use the same one-token-per-line grep/awk idiom as the existing test. Each fixture run that needs
    distinct argv capture must point STUB_REVENIUM_ARGV_FILE at a distinct mktemp file. Keep the test
    hermetic and <30s. Add a final `Results: PASS passed, FAIL failed` line and `exit 1` when FAIL>0
    (clone test_report_argv.sh:294-299).

    SECURITY: when parsing the captured `--metadata` JSON, pass the captured value to python3 via an
    env var and `json.loads(os.environ[...])` — never `eval`/string-interpolate (T-04-09).

    EXPECTED RESULT THIS PLAN: the test FAILS RED (report.sh has no job wiring yet — no `^create$`,
    `^outcome$`, or `agentic-job` tokens appear). That is correct. The test turns green across Plans
    02 (stamping + probe + ledger file) and 03 (create/outcome). Do NOT stub report.sh or weaken
    assertions to make it pass now.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/openclaw-revenium && bash -n tests/test_report_jobs_argv.sh && { bash tests/test_report_jobs_argv.sh; rc=$?; bash tests/test_report_argv.sh; rc2=$?; [ "$rc" -ne 0 ] && [ "$rc2" -eq 0 ] && echo RED_AS_EXPECTED_AND_REGRESSION_GREEN; }</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n tests/test_report_jobs_argv.sh` exits 0 (valid bash).
    - Running `tests/test_report_jobs_argv.sh` against the CURRENT (unmodified) report.sh exits NON-ZERO (RED) — no `^create$`/`^outcome$`/`agentic-job` tokens are produced yet.
    - `tests/test_report_argv.sh` still exits 0 (regression GREEN — Pitfall 4 preserved; the new test added zero job markers to the old test's fixtures).
    - The test creates and inspects `revenium-jobs.ledger` inside its tmp OPENCLAW_HOME.
    - The test contains fixtures for SUCCESS, FAILED (+failure_reason), CANCELLED, fail-open, 409, and re-run idempotency, with the argv/ledger assertions named in the action.
    - No `eval` or string-interpolation of captured argv anywhere in the test (T-04-09): the `--metadata` JSON is parsed via env-passing `json.loads`.
  </acceptance_criteria>
  <done>tests/test_report_jobs_argv.sh exists, is valid bash, runs hermetically, fails RED against the current report.sh, and leaves test_report_argv.sh green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture marker file → report.sh Python read | Test-authored marker JSON is trusted input within the test, but the test must model the same sanitization-at-write boundary; fixtures use already-valid values |
| captured argv → test assertion logic | The stub-captured tokens (incl. agent-supplied `failure_reason` echoed into `--metadata`) are parsed by the test; must be treated as data, never eval'd |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / EoP | stub-revenium.sh argv handling | mitigate | Stub only string-COMPARES positional args and `printf '%s\n'`-captures; never `eval`/interpolates argv into a command. |
| T-06-02 | Tampering | test parsing of captured `--metadata` JSON | mitigate | Test parses the captured metadata value via env-passing `python3 json.loads(os.environ[...])`, never `eval` or `${VAR}`-in-program (T-04-09). |
| T-06-03 | Information Disclosure | tmp OPENCLAW_HOME / jobs ledger created under mktemp dirs | accept | mktemp dirs are user-owned 0700; test cleans up on EXIT trap; no secrets written. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs ZERO packages (RESEARCH §Package Legitimacy Audit: not applicable — only pre-guarded host tools revenium/jq/python3/bash). No install tasks; no slopcheck surface. |
</threat_model>

<verification>
- `bash tests/stub-meta-check.sh` exits 0 (stub behaviors correct).
- `bash tests/test_report_jobs_argv.sh` exits NON-ZERO against current report.sh (RED — proves the test exercises real, not-yet-implemented behavior).
- `bash tests/test_report_argv.sh` exits 0 (regression unbroken).
- `bash -n` on both new/modified shell files passes.
</verification>

<success_criteria>
- tests/stub-revenium.sh extended: captures all argv (unchanged), answers the dual capability probe (`--agentic-job-id` on `meter completion --help`), supports STUB_REVENIUM_NO_JOBS (probe-fail) and STUB_REVENIUM_409_FOR (409 fake).
- tests/test_report_jobs_argv.sh created with JLIFE-01..05 fixtures and argv/ledger assertions; RED against current report.sh.
- test_report_argv.sh remains green (Pitfall 4 decision honored).
- All assertions use the one-token-per-line grep/awk idiom; no eval; <30s runtime.
</success_criteria>

<output>
Create `.planning/phases/06-job-lifecycle-wiring/06-01-SUMMARY.md` when done.
</output>
