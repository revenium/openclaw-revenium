---
phase: 16-skill-deploy-docs
plan: "03"
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - .planning/phases/16-skill-deploy-docs/16-VALIDATION.md
autonomous: false
requirements: [NCDEPLOY-01, NCDEPLOY-02]
tags: [nemoclaw, live-validation, e2e, honesty-gate]

must_haves:
  truths:
    - "On the live host (34.224.27.67), following ONLY docs/nemoclaw-setup.md, the NemoClaw install path runs end-to-end and the D-02 ✓ ready assertion fires and passes — SC1 / NCDEPLOY-01"
    - "openclaw skills list in-sandbox shows the revenium skill as ready, captured as real command/exit/output evidence — SC1"
    - "Any step that required an action NOT in docs/nemoclaw-setup.md is flagged as an undocumented-step doc bug (SC2); a clean run with zero deviations proves SC2"
    - "All live evidence (commands, exit codes, observed output, observed ✓ ready) is recorded in 16-VALIDATION.md under a live-evidence section in the Phase-15 evidence format, under the CRITICAL HONESTY RULE — no claimed pass that did not happen"
    - "A blocking human checkpoint reviews the evidence and the SC1/SC2 decision"
  artifacts:
    - path: ".planning/phases/16-skill-deploy-docs/16-VALIDATION.md"
      provides: "Live clean-host validation evidence section (commands / exit codes / observed output / observed ✓ ready), plus any flagged undocumented-step doc bugs"
      contains: "LIVE VALIDATION"
  key_links:
    - from: "docs/nemoclaw-setup.md install sequence"
      to: "live sandbox on 34.224.27.67"
      via: "operator follows ONLY the doc; install_skill_nemoclaw runs the ✓ ready assertion"
      pattern: "openclaw skills list"
    - from: "install_skill_nemoclaw() ✓ ready assertion"
      to: "16-VALIDATION.md live-evidence section"
      via: "captured assertion output (info 'revenium skill confirmed ready in sandbox')"
      pattern: "confirmed ready"
---

<objective>
Honestly prove SC1 and SC2 on a real host. On the live NemoClaw host (34.224.27.67), run the documented NemoClaw install path following ONLY `docs/nemoclaw-setup.md`, and record real evidence (commands, exit codes, observed output, the D-02 `✓ ready` assertion firing and passing, and `openclaw skills list` showing the revenium skill ready) in `16-VALIDATION.md` under a live-evidence section. Any step the tester needs that is NOT in the doc is flagged as an undocumented-step doc bug — that is exactly what SC2 ("no undocumented manual steps required") tests. A blocking human checkpoint reviews the evidence and renders the SC1/SC2 decision.

Purpose: This is the D-03 live fresh-sandbox validation gate. SC1 (real `✓ ready` discovery) and SC2 (no undocumented manual steps) can only be honestly marked verified with live, doc-driven evidence — mirroring the Phase 15 live-validation honesty gate (Plans 15-03/15-05/15-07).
Output: a `## LIVE VALIDATION` evidence section appended to `16-VALIDATION.md`; a blocking human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.claude/skills/spike-findings-openclaw-revenium/SKILL.md
@.claude/skills/spike-findings-openclaw-revenium/references/install-and-bootstrap.md
@.claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md
@docs/nemoclaw-setup.md
@.planning/phases/16-skill-deploy-docs/16-VALIDATION.md
@.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md
@.planning/phases/15-per-turn-enforcement-plugin/15-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the documented clean-host install and capture live ✓ ready evidence</name>
  <files>.planning/phases/16-skill-deploy-docs/16-VALIDATION.md</files>
  <read_first>
    - docs/nemoclaw-setup.md (the runbook to follow — follow ONLY this; the install sequence, the `✓ ready` verification step, prerequisites; any step you need that is NOT here is an undocumented-step doc bug to flag)
    - .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md (the evidence format to replicate: `## SC<n> — <name>` sections, `### Status: PASSED / PARTIALLY EVIDENCED / STILL FAILING`, `### Evidence` blocks with `Command:` / `Exit:` / `Output:`; the CRITICAL HONESTY RULE applied at the end; Deviation 1 the SSHFS ~/ skill-install workaround the Plan 16-01 guard now turns into an actionable error)
    - .planning/phases/16-skill-deploy-docs/16-VALIDATION.md (the existing validation-strategy file — append the live-evidence section; do not overwrite the per-task map or sign-off)
    - scripts/post-install-nemoclaw.sh install_skill_nemoclaw() (the deployed D-02 ✓ ready assertion + SKILL.md guard whose live firing is the evidence — the `info "revenium skill confirmed ready in sandbox"` line and the fail path)
    - .claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md (`openclaw skills list` discovery; expected `✓ ready` output)
    - MEMORY: nemoclaw-spike-host (live host 34.224.27.67 + sandbox access) / revenium-cli-sandbox-gotchas (nemoclaw exec rejects newline argv; api-key field) / worktree-cleanup-stray-summary (worktree merge stray-SUMMARY cleanup)
  </read_first>
  <action>
    On the live host 34.224.27.67, perform the clean-host validation by following ONLY `docs/nemoclaw-setup.md` — do not use prior tribal knowledge of `post-install-nemoclaw.sh`. Pick the honest "clean skill install" approach per CONTEXT discretion: clear the `skill-installed-nemoclaw` ledger key on `revenium-spike` (and the `enforcement-plugin-installed` key if needed to exercise the assertion) and re-run, OR provision a fresh sandbox name — choose whichever more honestly tests a clean host without leaving the shared host broken (RESEARCH §6 recommends clearing the ledger key on revenium-spike). Honor the live constraints: nemoclaw exec rejects newline argv (single-line `sh -lc` only); the share mount targets `/sandbox/.openclaw`; revenium reads the key from the `api-key:` field in config.yaml; bootstrap/turns are slow — use generous timeouts.

    Capture four evidence items: (SC1-a) `nemoclaw <name> skill install <dir>` succeeds (exit 0, "Skill 'revenium' installed/updated" output); (SC1-b) the deployed D-02 assertion fires and passes — capture the `info "revenium skill confirmed ready in sandbox"` line from the install output (and confirm it ran, not skipped via ledger); (SC1-c) an independent `nemoclaw <name> exec -- sh -lc "openclaw skills list"` shows the revenium skill ready (the `✓ ready  💰 revenium` shape); (SC2) record EVERY command you ran that was NOT in docs/nemoclaw-setup.md as an explicit "undocumented step — doc bug" entry. A clean run with zero such entries is the SC2 pass; any entry is a doc bug to surface for the checkpoint.

    Append a `## LIVE VALIDATION (Phase 16, clean-host, doc-driven)` section to `16-VALIDATION.md` in the 15-VALIDATION.md format (per-SC status + Command/Exit/Output evidence blocks), with SC1 and SC2 sub-sections and a deviations/undocumented-steps list. Apply the CRITICAL HONESTY RULE: record real output verbatim; never claim a `✓ ready` that was not observed. If the assertion or discovery did NOT pass, record it as STILL FAILING with the observed output and root cause — do not paper over it.

    Restore the sandbox/host to a healthy state when done (re-set any cleared ledger keys appropriately, recover the sandbox if touched). If a stray SUMMARY.md appears in a worktree merge, `rm` it and `git merge --ff-only` per the known worktree-cleanup gotcha.
  </action>
  <verify>
    <human-check>
      The `## LIVE VALIDATION` section in 16-VALIDATION.md shows real, doc-driven evidence: the `nemoclaw skill install` exit 0, the D-02 assertion's "confirmed ready" line firing live, an independent `openclaw skills list` showing the revenium skill ready, and an explicit (ideally empty) list of undocumented steps — all from the live sandbox, in the Phase-15 Command/Exit/Output format under the CRITICAL HONESTY RULE.
    </human-check>
  </verify>
  <acceptance_criteria>
    - 16-VALIDATION.md contains a new `## LIVE VALIDATION` section in the 15-VALIDATION.md Command/Exit/Output format.
    - SC1 evidence shows: `nemoclaw skill install` exit 0; the deployed D-02 assertion's "revenium skill confirmed ready in sandbox" line captured live (ran, not ledger-skipped); and an independent `openclaw skills list` showing the revenium skill ready.
    - SC2 evidence lists every command run that was NOT in docs/nemoclaw-setup.md (a doc-driven run); a zero-length list = SC2 pass, any entry = a flagged doc bug.
    - Each SC is marked PASSED / PARTIALLY EVIDENCED / STILL FAILING with verbatim output; no claimed pass without captured output (CRITICAL HONESTY RULE).
    - Sandbox/host restored healthy (restore commands recorded).
  </acceptance_criteria>
  <done>
    16-VALIDATION.md has a live, doc-driven `## LIVE VALIDATION` section proving (or honestly recording the failure of) SC1's real `✓ ready` discovery and SC2's no-undocumented-steps claim, in the Phase-15 evidence format, with the host left healthy.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human reviews the live clean-host evidence and renders the SC1/SC2 decision</name>
  <files>.planning/phases/16-skill-deploy-docs/16-VALIDATION.md</files>
  <action>
    PAUSE for human verification. No automated change in this task — the human reviews the `## LIVE VALIDATION` evidence recorded by Task 1, confirms the D-02 `✓ ready` assertion fired and passed live (SC1) and that the install was runnable following only the docs (SC2), and either approves or names a remaining gap (e.g., an undocumented step that must be added to docs/nemoclaw-setup.md, which would route to a docs fix). The how-to-verify steps below are the script for that review.
  </action>
  <what-built>
    Plan 16-01 added the D-02 `✓ ready` assertion + SKILL.md guard to install_skill_nemoclaw() and fixed the success banner; Plan 16-02 created docs/nemoclaw-setup.md and the README pointer. Task 1 ran the documented install path on the live sandbox (34.224.27.67) following ONLY the new docs, and recorded SC1 (skill install exit 0, the assertion's "confirmed ready" line, an independent `openclaw skills list` ready) and SC2 (the list of any undocumented steps) in 16-VALIDATION.md under the CRITICAL HONESTY RULE.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/16-skill-deploy-docs/16-VALIDATION.md` and read the `## LIVE VALIDATION` section.
    2. SC1: confirm `nemoclaw skill install` exited 0; the deployed D-02 assertion printed "revenium skill confirmed ready in sandbox" live (and was NOT ledger-skipped); and an independent `openclaw skills list` shows the revenium skill ready (the `✓ ready  💰 revenium` shape).
    3. SC2: read the "undocumented steps" list. Empty list → SC2 PASS (the install was runnable from the docs alone). Any entry → it is a doc bug: the step must be added to docs/nemoclaw-setup.md before the phase closes.
    4. Confirm the evidence is real verbatim output (CRITICAL HONESTY RULE) and the host was restored healthy.
  </how-to-verify>
  <verify>
    <human-check>Human reads the LIVE VALIDATION section, confirms SC1 (live ✓ ready assertion + discovery) and SC2 (no undocumented steps, or names the doc bug to fix), and approves or names the remaining gap.</human-check>
  </verify>
  <resume-signal>Type "approved: SC1+SC2 verified" if the live ✓ ready and doc-driven run are proven, or "gap found: &lt;undocumented step / failing assertion&gt;" to name what must be fixed in docs/nemoclaw-setup.md or the install script.</resume-signal>
  <done>
    Human has reviewed the live clean-host evidence and rendered the SC1/SC2 decision: approved (both verified) or a named gap (an undocumented step or a failing assertion to fix).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host operator → live sandbox (34.224.27.67) | The validation runs `nemoclaw exec` / `nemoclaw skill install` into a live OpenShell sandbox; the in-sandbox `openclaw skills list` output is untrusted and is only read (grepped), never evaluated. |
| live evidence → SC1/SC2 verification claim | The recorded evidence backs a roadmap success-criteria pass; it must be real captured output, not an unverified assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Repudiation | live-validation evidence accuracy | mitigate | Evidence is captured as raw Command/Exit/Output blocks (Phase-15 format); a false pass cannot be asserted without fabricated output, and the blocking human checkpoint independently reviews it (CRITICAL HONESTY RULE). |
| T-16-03-02 | Denial of Service | live sandbox left broken | mitigate | Task 1 restores the host/sandbox (re-set cleared ledger keys, recover the sandbox if touched); the checkpoint confirms restore commands are recorded. |
| T-16-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — it runs the already-built install path and records evidence; no new dependencies. |
</threat_model>

<verification>
- 16-VALIDATION.md has a `## LIVE VALIDATION` section with SC1 (skill install exit 0 + live `✓ ready` assertion + independent `openclaw skills list` ready) and SC2 (undocumented-steps list) in the Phase-15 Command/Exit/Output format.
- Blocking human checkpoint approved with an explicit SC1/SC2 decision.
- Live sandbox/host restored healthy.
</verification>

<success_criteria>
- SC1 / NCDEPLOY-01: real `✓ ready` discovery observed live, and the D-02 assertion fired and passed.
- SC2: the install path was runnable end-to-end following only the docs (zero undocumented steps), or every undocumented step is flagged as a doc bug to fix.
- Evidence recorded honestly; human checkpoint approves the decision.
</success_criteria>

<output>
Create `.planning/phases/16-skill-deploy-docs/16-03-SUMMARY.md` when done.
</output>
