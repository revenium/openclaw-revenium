---
phase: 15-per-turn-enforcement-plugin
plan: "07"
type: execute
gap_closure: true
wave: 2
depends_on: ["15-06"]
files_modified:
  - .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [NCENF-01, NCENF-02]

must_haves:
  truths:
    - "CR-01 re-validated live (NCENF-01 / SC1): a Gate A run against a no-injection condition on host 34.224.27.67 prints the actionable 'could not parse currentTurn.promptChars' diagnostic AND exits non-zero (fails closed WITH the message) — and a real install with the plugin injecting still passes Gate A with promptChars above threshold."
    - "B-05 re-validated live (NCENF-02 / SC3): a substantive Nemotron turn (one that routes a shell command through tool_search_code) drives the transcript-scan observation in before_agent_finalize end-to-end so a marker .jsonl appears under the host share mount markers/ — OR, if the marker is still not produced, the user's SC3 scope-renegotiation fallback is executed honestly in this same round."
    - "WR-01 re-validated live (NCENF-02): a marker exec followed by a non-string-command exec across a nemoclaw recover boundary does not downgrade the disk run-state to marked:false (no spurious revise after recover)."
    - "All live evidence (commands, exit codes, observed output, marker filename + first line, or the documented limitation) is recorded in 15-VALIDATION.md in the Plan 03/15-05 format; a blocking human checkpoint reviews it and the SC3 decision point."
  artifacts:
    - path: ".planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md"
      provides: "Re-validation addendum (round 3): CR-01 diagnostic-on-no-injection, B-05 transcript-scan end-to-end marker (or documented limitation + SC3 renegotiation), WR-01 non-downgrade-across-recover — with command/exit/output evidence and the SC3 decision outcome"
      contains: "RE-VALIDATION"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 15 SC3 either confirmed PASS, or (fallback) renegotiated to scope NCENF-02's marker revise loop to agents that invoke exec directly, with the Nemotron tool_search_code indirection documented as a known limitation"
      contains: "SC3"
  key_links:
    - from: "scripts/post-install-nemoclaw.sh Gate A (no-injection condition)"
      to: "live sandbox revenium-spike (34.224.27.67)"
      via: "nemoclaw exec openclaw agent --json with the plugin disabled/absent → diagnostic fires, exits non-zero"
      pattern: "could not parse currentTurn.promptChars"
    - from: "Nemotron tool_search_code exec turn"
      to: "host mount markers/<sid>.jsonl"
      via: "before_agent_finalize transcript scan → revise → write-marker.sh → local markers/ → share mount"
      pattern: "\\.jsonl"
---

<objective>
Re-validate all three closed gaps on the LIVE sandbox (34.224.27.67, sandbox revenium-spike) — the honesty gate proving the plan 15-06 code fixes work end-to-end, not just in unit tests (per phase CONTEXT: "Validate on the live sandbox host — not only an isolated unit test").

- **CR-01 / SC1:** Deploy the rebuilt plugin + `|| true`-guarded `post-install-nemoclaw.sh`. Confirm (a) a real install with the plugin injecting still passes Gate A (promptChars above threshold, exit 0), AND (b) a no-injection condition (plugin disabled/absent) now prints the actionable "could not parse currentTurn.promptChars" diagnostic and exits non-zero — fails closed WITH the message, not a bare pipefail abort.
- **B-05 / SC3:** Run a substantive Nemotron turn that routes a shell command through `tool_search_code`. With the new transcript-scan observation in `before_agent_finalize`, confirm a marker `.jsonl` appears under the host share mount `markers/`. **Terminal decision point (USER-SELECTED FALLBACK):** if the marker is STILL not produced end-to-end after the fix, execute the user's SC3 scope-renegotiation — document the Nemotron `tool_search_code` indirection as a known limitation and renegotiate SC3 in ROADMAP.md to scope NCENF-02's marker revise loop to agents that invoke exec directly — closing the phase honestly in ONE round. Do NOT add a "try tool_search_code interception" branch; scope-renegotiation is the single chosen fallback.
- **WR-01 / NCENF-02:** Confirm a marker exec followed by a non-string-command exec across a `nemoclaw recover` boundary leaves the disk run-state at marked:true (no spurious revise after recover).

Purpose: SC1 and SC3 are the FAILED roadmap truths; they can only be honestly marked verified with live evidence — and SC3 must be resolved this round either as a live PASS or via the user's pre-approved scope renegotiation.

Output: a round-3 RE-VALIDATION addendum in 15-VALIDATION.md, a blocking human checkpoint reviewing the evidence and the SC3 decision, and (only on the fallback branch) the ROADMAP.md SC3 renegotiation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md
@.planning/phases/15-per-turn-enforcement-plugin/15-VERIFICATION.md
@.planning/phases/15-per-turn-enforcement-plugin/15-CONTEXT.md
@.planning/phases/15-per-turn-enforcement-plugin/15-B05-SCHEMA-PROBE.md
@.planning/phases/15-per-turn-enforcement-plugin/15-06-SUMMARY.md
@./CLAUDE.md
@.claude/skills/spike-findings-openclaw-revenium/SKILL.md
@.claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the 15-06 fixes to the live sandbox and gather CR-01, B-05, and WR-01 evidence</name>
  <files>.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md</files>
  <read_first>
    - .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md (the Plan 03 + 15-05 evidence format to replicate: command / Exit / Output blocks; the SC1 promptChars section; the SC3 marker section; Deviation 1 the SSHFS ~/ skill-install workaround)
    - .planning/phases/15-per-turn-enforcement-plugin/15-06-SUMMARY.md (what 15-06 changed: the Gate A || true guard, the WR-01 non-downgrade fix, the transcript-scan observation + which ctx field index.ts now forwards)
    - .planning/phases/15-per-turn-enforcement-plugin/15-B05-SCHEMA-PROBE.md (the confirmed transcript schema — so the live turn is driven the same way and the marker outcome is interpreted correctly)
    - scripts/post-install-nemoclaw.sh (the Gate A region — confirm the diagnostic message text to grep for in the no-injection run; Gate B/C/D intact)
    - plugin/src/gate.js + plugin-nemoclaw/dist/index.js (the transcript-scan logic + the committed dist that gets deployed)
    - .claude/skills/spike-findings-openclaw-revenium/references/skill-deploy-and-enforcement.md (install → recover → gateway-turn validation sequence; skill install upload mechanic)
    - MEMORY: nemoclaw-spike-host / clawhub-test-host / revenium-cli-sandbox-gotchas / worktree-cleanup-stray-summary / b05-nemotron-tool-search-code-exec (live host access; nemoclaw exec rejects newline argv; api-key field; SSHFS ~/ skill-dir workaround; worktree stray-SUMMARY cleanup)
  </read_first>
  <action>
    On the live host 34.224.27.67 (sandbox revenium-spike), deploy the rebuilt `plugin-nemoclaw/` (15-06 dist with the transcript scan + WR-01 fix) and the `|| true`-guarded `scripts/post-install-nemoclaw.sh`, then gather three evidence blocks. Honor the known live constraints: nemoclaw exec rejects newline argv (single-line sh -lc only); skill install must use a clean staging dir, not ~/ (SSHFS node_modules unsafe filenames — 15-05 Deviation 1); the share mount is the Phase 14 mount → /sandbox/.openclaw; revenium reads the key from the api-key field in config.yaml. Turns take ~70-90s with the plugin — use a 120-180s timeout.

    CR-01 evidence (SC1) — TWO sub-checks:
    1. Positive: re-run install_enforcement_plugin (clear the enforcement-plugin-installed ledger key so it re-runs) and capture Gate A passing — currentTurn.promptChars above the threshold, exit 0, the "Gate A passed: currentTurn.promptChars=<n>" line.
    2. Negative (the CR-01 fix): create a no-injection condition (disable/uninstall the plugin, or point Gate A at an agent JSON with no promptChars) and run the Gate A path. Capture that it now prints the actionable "guard directive NOT injected — could not parse currentTurn.promptChars" diagnostic AND exits non-zero — NOT a bare pipefail exit with no message. This is the exact CR-01 regression being closed.

    B-05 evidence (SC3): with the rebuilt plugin loaded (after nemoclaw revenium-spike recover), in a fresh session id run a substantive turn that makes Nemotron run a shell command (it will route through tool_search_code + openclaw:core:exec per the schema probe). The transcript-scan observation in before_agent_finalize should classify the run from the conversation and issue the revise so the agent runs write-marker.sh. Then list `markers/*.jsonl` over the host mount and capture the marker filename + its first JSON line. Capture corroborating evidence: the plugin logger lines showing the transcript scan fired, and (if present) the in-sandbox run-state file.

    WR-01 evidence (NCENF-02): drive a turn that runs a marker exec then a non-string-command exec on the same run, trigger a nemoclaw recover, and confirm before_agent_finalize does NOT issue a spurious revise (disk record stayed marked:true). Capture the run-state file content (marked:true) and the absence of a post-recover revise.

    Append a `## RE-VALIDATION (round 3, plan 15-06 fixes)` section to 15-VALIDATION.md with exact commands, exit codes, and output for CR-01, B-05, and WR-01. Mark each gap RESOLVED with evidence, OR — for B-05 specifically — if the marker is STILL not produced end-to-end, record the honest failure (the tool_search_code indirection still defeats observation) and note that Task 2's checkpoint will decide the SC3 scope-renegotiation fallback. Do NOT claim a pass that did not happen (CRITICAL HONESTY RULE, same as Plan 03/15-05).

    Restore the sandbox to healthy when done (re-enable/disable as appropriate, recover, restore guardrail-status.json if touched) exactly as 15-05 did. If a stray SUMMARY.md appears in a worktree merge, rm it and git merge --ff-only per the known worktree-cleanup gotcha.
  </action>
  <verify>
    <human-check>
      The round-3 RE-VALIDATION addendum in 15-VALIDATION.md shows: (1) a live Gate A pass with the plugin injecting AND a no-injection run that prints the actionable diagnostic and exits non-zero (CR-01); (2) either a real marker .jsonl filename + first line from the host mount after a tool_search_code exec turn (B-05 PASS) or an honest "marker still not produced" record; (3) the WR-01 non-downgrade-across-recover evidence. All from the live sandbox, not a unit test.
    </human-check>
  </verify>
  <acceptance_criteria>
    - 15-VALIDATION.md contains a new round-3 `RE-VALIDATION` section referencing the 15-06 fixes.
    - CR-01 block shows BOTH: (a) a Gate A pass with a captured promptChars value at/above threshold and exit 0; (b) a no-injection run where the script prints the literal "could not parse currentTurn.promptChars" diagnostic and exits non-zero (the pipefail-abort regression is gone).
    - B-05 block shows the substantive Nemotron turn command, exit code, and EITHER a real markers/*.jsonl filename + first JSON line captured from the host mount (RESOLVED) OR an explicit honest "marker not produced end-to-end" record (which routes to the Task 2 SC3 fallback decision).
    - WR-01 block shows a marker→non-string exec sequence across a recover boundary leaving the disk record marked:true with no spurious revise.
    - Each gap is explicitly marked RESOLVED or honestly recorded as still-failing with root cause; sandbox-restore commands recorded (host left healthy).
  </acceptance_criteria>
  <done>
    15-VALIDATION.md has a round-3 RE-VALIDATION addendum proving on the live sandbox that Gate A fails closed with its diagnostic on no-injection and passes on injection (CR-01), that a tool_search_code exec turn either writes an end-to-end marker .jsonl or is honestly recorded as still-failing (B-05), and that the disk run-state survives a recover without downgrade (WR-01) — Plan 03/15-05 evidence format, host restored healthy.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human reviews live evidence and decides the SC3 outcome (PASS or scope-renegotiation fallback)</name>
  <files>.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md, .planning/ROADMAP.md</files>
  <action>
    PAUSE for human verification. No automated change in this task — the human reviews the round-3 live re-validation evidence recorded by Task 1 in 15-VALIDATION.md, confirms CR-01 and WR-01 are resolved on the live sandbox, and renders the SC3 decision (PASS if the end-to-end marker was produced, or "renegotiate" to direct Task 3 to execute the documented SC3 scope-renegotiation fallback). The how-to-verify steps below are the script for that review.
  </action>
  <what-built>
    Plan 15-06 fixed CR-01 (Gate A `|| true` guard → fails closed with its diagnostic), WR-01 (non-string exec no longer downgrades marked:true), and added the B-05 transcript-scan observation in before_agent_finalize (classifies Nemotron's tool_search_code exec from the conversation). Task 1 deployed those to the live sandbox revenium-spike (34.224.27.67) and recorded round-3 evidence in 15-VALIDATION.md: Gate A diagnostic-on-no-injection + pass-on-injection, the WR-01 non-downgrade-across-recover result, and the B-05 end-to-end marker outcome.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md` and read the round-3 `RE-VALIDATION` section.
    2. CR-01: confirm BOTH a Gate A pass (plugin injecting, promptChars above threshold, exit 0) and a no-injection run that prints the actionable "could not parse currentTurn.promptChars" diagnostic and exits non-zero (no bare pipefail abort).
    3. WR-01: confirm the marker→non-string exec sequence across a recover left the disk record marked:true with no spurious revise.
    4. B-05 / SC3 DECISION POINT:
       - If the evidence shows a real marker `.jsonl` was produced end-to-end from a tool_search_code exec turn (filename + first line from the host mount) → SC3 PASS. Approve; no ROADMAP change needed.
       - If the marker was STILL not produced end-to-end → the user's pre-approved fallback applies: SC3 is renegotiated. Confirm the evidence honestly documents the Nemotron tool_search_code indirection as a known limitation, and direct that Task 3 update ROADMAP.md to scope NCENF-02's marker revise loop to agents that invoke exec directly. Do NOT request a "try tool_search_code interception" branch — scope-renegotiation is the single chosen fallback.
    5. Confirm the sandbox was restored healthy (restore commands recorded).
  </how-to-verify>
  <verify>
    <human-check>Human reads the round-3 RE-VALIDATION section, confirms CR-01 and WR-01 are resolved live, and decides SC3: marker produced end-to-end → PASS; not produced → approve the documented SC3 scope-renegotiation fallback (directs Task 3).</human-check>
  </verify>
  <resume-signal>Type "approved: SC3 PASS" if the end-to-end marker was produced, or "approved: SC3 renegotiate" to execute the documented scope-renegotiation fallback in Task 3, or describe a remaining gap.</resume-signal>
  <done>
    Human has reviewed the live round-3 evidence, confirmed CR-01 + WR-01 resolved, and rendered the SC3 decision: either SC3 PASS (no ROADMAP change) or SC3 renegotiate (Task 3 runs the documented scope-renegotiation fallback).
  </done>
</task>

<task type="auto">
  <name>Task 3: (Fallback branch only) Execute the SC3 scope-renegotiation in ROADMAP.md if the human chose "renegotiate"</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 199-209 (Phase 15 Success Criteria SC1-SC5; SC3 is the marker-gate deployed + markers written under NemoClaw truth being renegotiated)
    - .planning/phases/15-per-turn-enforcement-plugin/15-VALIDATION.md (round-3 RE-VALIDATION — the documented Nemotron tool_search_code limitation evidence the renegotiation cites)
    - .planning/phases/15-per-turn-enforcement-plugin/15-VERIFICATION.md (B-05 remediation option (c) lines 206: "scope NCENF-02 to agents that invoke exec directly and document the Nemotron limitation — requires renegotiating SC3")
    - .planning/REQUIREMENTS.md (NCENF-02 wording, to keep the requirement and the renegotiated SC3 consistent)
  </read_first>
  <action>
    RUN THIS TASK ONLY IF the Task 2 human decision was "approved: SC3 renegotiate". If the decision was "approved: SC3 PASS", make NO change to ROADMAP.md SC3 and record in the SUMMARY that the fallback was not triggered (SC3 passed live).

    On the renegotiate branch: edit the Phase 15 Success Criteria in ROADMAP.md so SC3 scopes NCENF-02's marker revise loop to agents that invoke `exec` directly (the standalone / non-Nemotron OpenClaw path the before_tool_call + transcript-scan observation reliably covers), and explicitly document the Nemotron `tool_search_code` exec indirection as a KNOWN LIMITATION (the model routes shell exec through tool_search_code + openclaw.tools.call so neither before_tool_call nor a transcript scan reliably triggers the marker on this host). Cite the 15-VALIDATION.md round-3 evidence. Keep SC1, SC2, SC4, SC5 unchanged. Do NOT add a new "interception" success criterion or spawn another gap-closure cycle — this renegotiation closes the phase honestly in this round (the user's single chosen fallback). Add a short note under the Phase 15 entry recording that the renegotiation was applied with the date and a pointer to 15-VALIDATION.md.
  </action>
  <verify>
    <automated>grep -q "SC3" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - If Task 2 = "SC3 PASS": ROADMAP.md SC3 is UNCHANGED and the SUMMARY notes the fallback was not triggered. (Verify by no diff to the SC3 line.)
    - If Task 2 = "SC3 renegotiate": the Phase 15 SC3 text now scopes NCENF-02's marker revise loop to agents that invoke exec directly, documents the Nemotron tool_search_code indirection as a known limitation, and cites the 15-VALIDATION.md round-3 evidence.
    - SC1, SC2, SC4, SC5 are byte-unchanged in either branch.
    - No new "try tool_search_code interception" criterion was added (the single chosen fallback is scope-renegotiation, not another remediation cycle).
    - A dated note under the Phase 15 entry records the SC3 outcome (PASS or renegotiated) with a pointer to 15-VALIDATION.md.
  </acceptance_criteria>
  <done>
    On the renegotiate branch, ROADMAP.md Phase 15 SC3 is scoped to direct-exec agents with the Nemotron tool_search_code indirection documented as a known limitation (citing the live evidence), closing the phase honestly in one round; on the PASS branch, SC3 is left unchanged and the SUMMARY records that the fallback was not triggered. SC1/SC2/SC4/SC5 untouched in both.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host install/operator → live sandbox revenium-spike | Commands run over nemoclaw exec into the sandbox; a substantive Nemotron turn runs untrusted model-chosen shell via the (tool_search_code-routed) exec tool. |
| sandbox markers/ → host share mount | Marker .jsonl files written in-sandbox become visible to the host metering loop over SSHFS. |
| live evidence / SC3 decision → ROADMAP success criteria | The renegotiation (if triggered) changes a roadmap success criterion; it must be backed by recorded live evidence and a human decision, not an unverified claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-G07-01 | Repudiation | re-validation evidence accuracy | mitigate | Evidence is captured as raw command/exit/output blocks (Plan 03/15-05 format); a false pass cannot be asserted without fabricated output; the blocking human checkpoint independently reviews it (CRITICAL HONESTY RULE). |
| T-15-G07-02 | Tampering | SC3 renegotiation without justification | mitigate | The renegotiation runs only on an explicit human "SC3 renegotiate" decision, must cite the 15-VALIDATION.md round-3 limitation evidence, and leaves SC1/SC2/SC4/SC5 unchanged — preventing silent scope reduction. |
| T-15-G07-03 | Denial of Service | live sandbox left broken/halted | mitigate | Task 1 restores the sandbox (disable/enable + recover + restore guardrail-status.json) exactly as 15-05 did; the checkpoint confirms restore commands are recorded. |
| T-15-G07-04 | Tampering | model-chosen shell during the live turn | accept | The substantive turn runs in the hardened OpenShell sandbox with the egress policy applied; the test command is a benign echo. The turn is observed, not trusted; worst case is a missed marker (fail-open), recorded honestly. |
| T-15-G07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — deploy uses the already-committed 15-06 dist + existing scripts; no new dependencies. |
</threat_model>

<verification>
- 15-VALIDATION.md round-3 RE-VALIDATION section present with live CR-01 (diagnostic-on-no-injection + pass-on-injection), B-05 (end-to-end marker OR documented limitation), and WR-01 (non-downgrade-across-recover) evidence.
- Blocking human checkpoint approved with an explicit SC3 decision (PASS or renegotiate).
- On renegotiate: ROADMAP.md SC3 scoped to direct-exec agents with the Nemotron limitation documented; SC1/SC2/SC4/SC5 unchanged. On PASS: SC3 unchanged.
- Live sandbox restored healthy.
</verification>

<success_criteria>
- CR-01 re-validated live: Gate A fails closed WITH its diagnostic on no-injection and passes on injection.
- WR-01 re-validated live: disk run-state survives a recover without downgrade.
- SC3 resolved this round: either B-05 marker produced end-to-end live (PASS), or the user's documented scope-renegotiation applied (NCENF-02 scoped to direct-exec agents, Nemotron tool_search_code indirection documented) — no further gap-closure cycle spawned.
- Evidence recorded honestly in 15-VALIDATION.md; human checkpoint approves the SC3 decision.
</success_criteria>

<output>
Create `.planning/phases/15-per-turn-enforcement-plugin/15-07-SUMMARY.md` when done.
</output>
