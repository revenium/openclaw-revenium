---
phase: 07-root-session-job-rollup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_report_jobs_argv.sh
autonomous: true
requirements: [JROLL-01, JROLL-02, JROLL-03]

must_haves:
  truths:
    - "Running `bash tests/test_report_jobs_argv.sh` exercises three new GROUPS F/G/H that assert subagent rollup behavior"
    - "GROUP F/G/H FAIL RED before report.sh is modified (no --agentic-job-id rollup, no root-only gate yet)"
    - "Phase 6 GROUPS A–E still run and pass unchanged (root path byte-identical)"
    - "Each new group builds a sessions_spawn parent->child JSONL link so get-root-session-id.py resolves CHILD->ROOT and subagent detection fires"
  artifacts:
    - path: "tests/test_report_jobs_argv.sh"
      provides: "GROUP F (inherit), GROUP G (race-omit + orphan-drop), GROUP H (suppress) with inline sessions_spawn + markers fixtures"
      contains: "GROUP F"
  key_links:
    - from: "tests/test_report_jobs_argv.sh GROUP F/G/H"
      to: "scripts/get-root-session-id.py"
      via: "sessions_spawn JSONL with details.childSessionKey agent:main:subagent:<CHILD_UUID>"
      pattern: "sessions_spawn"
    - from: "tests/test_report_jobs_argv.sh GROUP F"
      to: "markers/{ROOT_UUID}.jsonl"
      via: "root job marker read by report.sh root_aid resolver"
      pattern: "kind.*job"
---

<objective>
Add the Wave-0 RED test harness for Phase 7 subagent job rollup: three new groups (F/G/H) in `tests/test_report_jobs_argv.sh` plus their inline `sessions_spawn` session fixtures and root/child markers, covering the four behavioral cases (inherit / race-omit / suppress / orphan-drop) named in VALIDATION.md.

Purpose: Tests must exist BEFORE the report.sh implementation so the implementation plan can sample against them (Nyquist). These groups FAIL RED now and turn GREEN when 07-02 lands. This plan never modifies production code.

Output: `tests/test_report_jobs_argv.sh` extended with GROUP F (JROLL-01), GROUP G (JROLL-02 + D-07 orphan-drop), GROUP H (JROLL-03), each with its own TMP_HOME, inline JSONL fixtures, and assertions wired to the exact tokens in VALIDATION.md. GROUPS A–E remain untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-root-session-job-rollup/07-VALIDATION.md
@.planning/phases/07-root-session-job-rollup/07-PATTERNS.md
@.planning/phases/07-root-session-job-rollup/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GROUP F + GROUP G with sessions_spawn fixtures (inherit + race-omit/orphan-drop)</name>
  <files>tests/test_report_jobs_argv.sh</files>
  <read_first>
    - tests/test_report_jobs_argv.sh (read lines 1-160 for `make_openclaw_home`, `count_grep`, `run_report`, `pass`/`fail`, and the GROUP A fixture/marker heredoc patterns; read lines 320-593 for GROUP B negative-assertion pattern and the `cleanup_all` trap at the tail)
    - .planning/phases/07-root-session-job-rollup/07-VALIDATION.md (GROUP F and GROUP G fixture structure + exact assertions; "Required Fixtures for Wave 0"; the sessions_spawn line-shape note that toolName must be literally `sessions_spawn` and childSessionKey must use the `agent:main:subagent:<UUID>` prefix)
    - .planning/phases/07-root-session-job-rollup/07-PATTERNS.md (the `tests/test_report_jobs_argv.sh — GROUP F/G/H Addition` section: session fixture pattern, marker fixture pattern, sessions_spawn fixture pattern, the `awk '/^--agentic-job-id$/{getline; print}'` argv assertion pattern, the negative `count_grep "^create$"` pattern)
    - tests/fixtures/sessions/a1b2c3d4-0001-0001-0001-000000000001.jsonl (the canonical sessions_spawn JSONL shape to mirror inline)
  </read_first>
  <action>
    Append GROUP F and GROUP G to `tests/test_report_jobs_argv.sh` after the existing GROUP E block (the new groups go before the `cleanup_all`/Summary tail; do NOT touch GROUPS A–E or their assertions, per the Pitfall 4 note at the top of the file).

    For GROUP F (JROLL-01 — subagent inherits root's job): declare distinct UUID constants `ROOT_UUID_F` and `CHILD_UUID_F` and root job constants `JOB_ID_ROOT_F="root-job-1a2b"`, `JOB_NAME_ROOT_F="Root Job"`, `JOB_TYPE_ROOT_F="feature_development"`. Build `TMP_HOME_F=$(make_openclaw_home)` and `ARGV_FILE_F=$(mktemp ...)`. Write the ROOT session JSONL into `${TMP_HOME_F}/agents/main/sessions/${ROOT_UUID_F}.jsonl` as a `sessions_spawn` tool-result line (toolName literally `sessions_spawn`, `details.childSessionKey` = `agent:main:subagent:${CHILD_UUID_F}`) — this is REQUIRED so `get-root-session-id.py` resolves CHILD->ROOT; without it the child looks like a root and detection never fires. Write the CHILD session JSONL into `.../${CHILD_UUID_F}.jsonl` with exactly one assistant completion carrying non-zero `usage.totalTokens`. Write the root job marker into `${TMP_HOME_F}/skills/revenium/markers/${ROOT_UUID_F}.jsonl` (`kind:"job"`, `agentic_job_id:"${JOB_ID_ROOT_F}"`, `job_name`, `job_type`, `status:"SUCCESS"`). Write a CHILD marker with a DIFFERENT id (`child-job-9z9z`) to prove the child's own id is NOT shipped. Run `run_report "${TMP_HOME_F}" "${ARGV_FILE_F}"`. Assert: (1) `awk '/^--agentic-job-id$/{getline; print}'` over the argv file contains `${JOB_ID_ROOT_F}` and does NOT contain `child-job-9z9z`; (2) `--agentic-job-name` value is `Root Job`; (3) `--agentic-job-type` value is `feature_development`; (4) `count_grep "^create$"` equals exactly 1 (only the root's job created — but note GROUP F's child completion processing must NOT create); (5) `revenium-jobs.ledger` contains exactly one `JOB:${JOB_ID_ROOT_F}:created:` line. End with `rm -f "${ARGV_FILE_F}"`.

    For GROUP G (JROLL-02 race-omit + D-07 orphan-drop): declare `ROOT_UUID_G`/`CHILD_UUID_G`. Build `TMP_HOME_G`/`ARGV_FILE_G`. Write the ROOT session JSONL with the same `sessions_spawn` link to `CHILD_UUID_G`. Write the CHILD session JSONL with one completion. Do NOT write any `markers/${ROOT_UUID_G}.jsonl` file (root has not declared its job — the race). Write `markers/${CHILD_UUID_G}.jsonl` WITH the child's own `kind:"job"` marker (id `orphan-job-7x7x`) to prove the orphan id is dropped, not shipped (D-04/D-07). Run report. Assert: (1) `count_grep "^--agentic-job-id$" "${ARGV_FILE_G}"` equals 0 (no job id shipped at all); (2) `--agent` IS present in the argv (v1.0 rollup via `openclaw-` still ships); (3) `--task-type` IS present (metering byte-identical); (4) `count_grep "^create$"` equals 0; (5) `count_grep "^outcome$"` equals 0; (6) the completion IS reported (a `TX:` line exists in `${TMP_HOME_G}/revenium-reported.ledger`). End with `rm -f "${ARGV_FILE_G}"`.

    Extend the `cleanup_all()` function's `rm -rf` list to include `"${TMP_HOME_F}" "${TMP_HOME_G}"`.

    Use `cat > ... <<JSONL` for session files and `printf '%s\n'` for single-line markers, matching GROUP A exactly. Use `pass`/`fail` with messages prefixed `JROLL-01 F:` and `JROLL-02 G:` so test output maps to requirements.
  </action>
  <verify>
    <automated>bash tests/test_report_jobs_argv.sh; test $? -ne 0 && echo "RED-AS-EXPECTED" ; grep -c 'GROUP F' tests/test_report_jobs_argv.sh; grep -c 'GROUP G' tests/test_report_jobs_argv.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GROUP F' tests/test_report_jobs_argv.sh` returns >= 1 and `grep -c 'GROUP G' tests/test_report_jobs_argv.sh` returns >= 1
    - `grep -c 'sessions_spawn' tests/test_report_jobs_argv.sh` returns >= 2 (one spawn line per new group F and G)
    - `grep -c 'agent:main:subagent:' tests/test_report_jobs_argv.sh` returns >= 2 (childSessionKey prefix present in both groups)
    - Running `bash tests/test_report_jobs_argv.sh` exits non-zero (the new groups FAIL RED — report.sh has no rollup yet) while GROUPS A–E still emit `PASS:` lines
    - `bash -n tests/test_report_jobs_argv.sh` exits 0 (valid bash syntax)
    - `cleanup_all` `rm -rf` line contains `${TMP_HOME_F}` and `${TMP_HOME_G}`
    - GROUP G asserts `count_grep "^--agentic-job-id$"` equals 0 and a TX: ledger line exists (orphan id never leaks; spend still ships)
  </acceptance_criteria>
  <done>
    GROUP F and GROUP G are appended with valid bash syntax, build sessions_spawn parent->child fixtures, and FAIL RED. GROUPS A–E remain byte-identical and still pass. cleanup_all covers the new tmp homes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add GROUP H (subagent job markers suppressed; root still creates once)</name>
  <files>tests/test_report_jobs_argv.sh</files>
  <read_first>
    - tests/test_report_jobs_argv.sh (re-read the GROUP F block just added for the UUID/fixture/marker pattern to mirror; re-read the GROUP B negative-assertion pattern around lines 320-417; re-read the `cleanup_all` trap at the tail)
    - .planning/phases/07-root-session-job-rollup/07-VALIDATION.md (GROUP H fixture structure + the five assertions: zero create/outcome for the subagent's own id, no JOB:sub-job ledger row, child completion DOES ship the ROOT's id, root completion's create fires exactly once)
    - .planning/phases/07-root-session-job-rollup/07-PATTERNS.md (negative assertion pattern `count_grep "^create$"`; argv id extraction via awk getline)
  </read_first>
  <action>
    Append GROUP H to `tests/test_report_jobs_argv.sh` after GROUP G (before the `cleanup_all`/Summary tail). Declare `ROOT_UUID_H`/`CHILD_UUID_H` and root job constants `JOB_ID_ROOT_H="root-job-5e6f"`, `JOB_NAME_ROOT_H="Root H Job"`, `JOB_TYPE_ROOT_H="bug_fix"`, plus the subagent's own job id `SUB_JOB_ID_H="sub-job-3c4d"`. Build `TMP_HOME_H`/`ARGV_FILE_H`. Write the ROOT session JSONL with the `sessions_spawn` link to `CHILD_UUID_H` AND a ROOT completion line (so the root path still exercises create/outcome). Write the CHILD session JSONL with one completion. Write `markers/${ROOT_UUID_H}.jsonl` with the root job marker (enables the JROLL-01 inherit path). Write `markers/${CHILD_UUID_H}.jsonl` with the subagent's OWN `kind:"job"` marker carrying `agentic_job_id:"${SUB_JOB_ID_H}"`. Run report.

    Assert (messages prefixed `JROLL-03 H:`): (1) the argv contains NO `create` token whose following/associated id is `${SUB_JOB_ID_H}` — concretely, assert there is no `JOB:${SUB_JOB_ID_H}:` line anywhere in `${TMP_HOME_H}/revenium-jobs.ledger` (`count_grep "^JOB:${SUB_JOB_ID_H}:"` equals 0); (2) `count_grep "^JOB:${JOB_ID_ROOT_H}:created:"` over the ledger equals 1 (root's job created exactly once); (3) the child's completion DOES ship `--agentic-job-id ${JOB_ID_ROOT_H}` (awk getline extraction contains the root id) — JROLL-01 and JROLL-03 are non-conflicting; (4) `--agentic-job-id` never resolves to `${SUB_JOB_ID_H}` in the captured argv. End with `rm -f "${ARGV_FILE_H}"`.

    Extend `cleanup_all()`'s `rm -rf` list to include `"${TMP_HOME_H}"`.
  </action>
  <verify>
    <automated>bash -n tests/test_report_jobs_argv.sh && grep -c 'GROUP H' tests/test_report_jobs_argv.sh && grep -c 'sub-job-3c4d' tests/test_report_jobs_argv.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GROUP H' tests/test_report_jobs_argv.sh` returns >= 1
    - `grep -c 'sub-job-3c4d' tests/test_report_jobs_argv.sh` returns >= 1 (the subagent's own id appears only as a suppression-target constant/assertion)
    - GROUP H asserts `count_grep "^JOB:sub-job-3c4d:"` (or `${SUB_JOB_ID_H}`) over the jobs ledger equals 0 AND `count_grep "^JOB:root-job-5e6f:created:"` (or `${JOB_ID_ROOT_H}`) equals 1
    - GROUP H asserts the child completion's `--agentic-job-id` equals the ROOT id `${JOB_ID_ROOT_H}`, never `${SUB_JOB_ID_H}`
    - `bash -n tests/test_report_jobs_argv.sh` exits 0
    - `cleanup_all` `rm -rf` line contains `${TMP_HOME_H}`
    - Running `bash tests/test_report_jobs_argv.sh` still exits non-zero overall (GROUP H RED) while GROUPS A–E continue to print `PASS:`
  </acceptance_criteria>
  <done>
    GROUP H is appended with valid syntax, asserts subagent-own-job suppression (zero ledger rows for sub-job id, root creates once, child inherits root id), FAILs RED, and cleanup_all covers TMP_HOME_H. GROUPS A–E unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture JSONL → report.sh marker/session readers | Test writes synthetic session + marker files that report.sh reads; this plan only constructs fixtures, the reader hardening lives in 07-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | GROUP G/H fixtures must prove orphan/sub-session ids are NOT shipped (JROLL-02/D-04, JROLL-03/D-06) | mitigate | GROUP G asserts `count_grep "^--agentic-job-id$"` == 0 with an orphan child marker present; GROUP H asserts zero `JOB:sub-job-3c4d:` ledger rows — these negative assertions are the data-integrity guard that the implementation never mis-attributes spend |
| T-07-02 | Information Disclosure | Test argv capture file may contain synthetic ids | accept | Synthetic test data only (`root-job-1a2b`, `sub-job-3c4d`); no real secrets; tmp files removed by cleanup_all |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH.md "Package Legitimacy Audit: Not applicable") |
</threat_model>

<verification>
- `bash -n tests/test_report_jobs_argv.sh` exits 0 (syntax valid)
- `bash tests/test_report_jobs_argv.sh` exits non-zero (GROUPS F/G/H RED before implementation) but prints `PASS:` lines for GROUPS A–E (Phase 6 regression intact)
- New groups present: `grep -c 'GROUP F'`, `'GROUP G'`, `'GROUP H'` each >= 1
- sessions_spawn linkage present: `grep -c 'sessions_spawn'` >= 3 and `grep -c 'agent:main:subagent:'` >= 3
</verification>

<success_criteria>
- GROUP F asserts subagent ships ROOT's `agentic_job_id`/name/type, child's own id NOT shipped, exactly one root create (JROLL-01)
- GROUP G asserts race window omits `--agentic-job-id` entirely with orphan child marker present, `--agent`/`--task-type` still ship, completion still reported (JROLL-02 / D-04 / D-07)
- GROUP H asserts subagent's own job marker produces zero `JOB:sub-job-*` ledger rows while root creates once and child inherits root id (JROLL-03)
- GROUPS A–E byte-identical and passing; full file passes `bash -n`
</success_criteria>

<output>
Create `.planning/phases/07-root-session-job-rollup/07-01-SUMMARY.md` when done
</output>
