---
phase: 07-root-session-job-rollup
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - scripts/report.sh
autonomous: true
requirements: [JROLL-01, JROLL-02, JROLL-03]

must_haves:
  truths:
    - "A completion originating in a subagent session (root_sid != session_id) ships the ROOT session's agentic_job_id, name, and type (JROLL-01)"
    - "When the root job id cannot be resolved (root markers file absent or has no kind:job line), the subagent completion omits --agentic-job-id entirely and never substitutes the subagent's own orphan id (JROLL-02 / D-04 / D-07)"
    - "A subagent session skips both jobs create and jobs outcome; its own job markers never become Revenium jobs (JROLL-03 / D-06)"
    - "Root sessions (root_sid == session_id) take the unchanged Phase 6 path byte-identical — GROUPS A–E still pass and no task-type metering regresses"
  artifacts:
    - path: "scripts/report.sh"
      provides: "root_aid cross-session resolver, subagent override block, root-only gates on jobs create and jobs outcome"
      contains: "root_aid"
  key_links:
    - from: "scripts/report.sh root_aid resolver"
      to: "markers/{root_sid}.jsonl"
      via: "env-passing python3 heredoc reading latest kind:job marker"
      pattern: "ROOT_SID=.*MARKERS_DIR=.*python3"
    - from: "scripts/report.sh subagent override block"
      to: "post_to_revenium --agentic-job-* args (call site ~847-848)"
      via: "agentic_job_id/name/type reassigned to root values for subagents"
      pattern: "root_sid.*!=.*session_id"
    - from: "scripts/report.sh jobs create / jobs outcome gates"
      to: "root-only execution"
      via: "&& root_sid == session_id appended to existing if conditions"
      pattern: "root_sid.*==.*session_id"
---

<objective>
Port the Hermes root-session job rollup into `scripts/report.sh` via three surgical changes: (1) a once-per-subagent-session `root_aid` cross-session resolver, (2) an in-loop subagent override that feeds the root's job values into the existing `--agentic-job-*` stamping (or omits them on race), and (3) a root-only gate appended to both the `jobs create` and `jobs outcome` `if` conditions. Turns GROUPS F/G/H (from 07-01) GREEN while keeping GROUPS A–E byte-identical.

Purpose: One agentic job spans the whole agent tree — subagent completions roll up under the root's job (JROLL-01), never ship a wrong/sub-session id on race (JROLL-02), and a subagent's own job markers are never shipped as separate jobs (JROLL-03). This extends the Phase 6 single-session lifecycle without regressing the root path.

Output: `scripts/report.sh` with the three change sites at the exact line landmarks confirmed in PATTERNS.md (after line 330; after line 690; line 699; line 864). No function signatures change; the `post_to_revenium` call site at ~847-848 is unchanged — only the VALUES of `agentic_job_id`/`agentic_job_name`/`agentic_job_type` differ for subagents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-root-session-job-rollup/07-CONTEXT.md
@.planning/phases/07-root-session-job-rollup/07-PATTERNS.md
@.planning/phases/07-root-session-job-rollup/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add root_aid cross-session resolver + subagent override block</name>
  <files>scripts/report.sh</files>
  <read_first>
    - scripts/report.sh (read lines 320-402 for the `root_sid` resolution at 328-330, the `_cleanup_session_tmp` helper at 343-345, and the env-passing markers-cache heredoc at 357-401 — the exact discipline to mirror; read lines 670-727 for the `job_resolve_result` tab-split at 677-684, the `Job correlation:` info log at 686-689, and the existing `jobs create` gate at 699)
    - .planning/phases/07-root-session-job-rollup/07-PATTERNS.md (CHANGE SITE 1 — the adapted `root_aid` heredoc that emits `latest_aid TAB latest_name TAB latest_type` and the bash three-field tab-split; CHANGE SITE 2 — the subagent override block with the mandatory zeroing else-branch; the Tab-Split Parse shared pattern at report.sh 677-684)
    - .planning/phases/07-root-session-job-rollup/07-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 2 "orphan id shipped on race", Pitfall 3 "name/type not threaded", Pitfall 4 "per-completion re-read", Security Domain sanitization table)
    - .planning/phases/07-root-session-job-rollup/07-CONTEXT.md (D-02, D-04, D-05, D-08, D-09, D-07 — the load-bearing invariants)
  </read_first>
  <action>
    Make two edits in `scripts/report.sh`, both inside `process_session`.

    EDIT 1 — root_aid resolver (insert immediately AFTER line 330 `root_sid="${root_sid:-${session_id}}"`, before the line-332 markers-cache comment). Declare `local root_aid="" root_job_name="" root_job_type=""`. Guard the whole block with `if [[ "${root_sid}" != "${session_id}" ]]; then` so root sessions skip it entirely (D-09). Inside, run a NEW env-passing python3 heredoc, single-quoted `<<'PY'` delimiter, suffixed `2>/dev/null || true`, passing ONLY `ROOT_SID="${root_sid}"` and `MARKERS_DIR="${MARKERS_DIR}"` via env (T-04-09 — never interpolate into the heredoc body). The python reads `Path(markers_dir) / f"{root_sid}.jsonl"`; if absent, prints nothing. It linearly scans every line, `json.loads` per line wrapped in try/except (skip malformed), and for each `rec.get('kind') == 'job'` with a non-empty string `agentic_job_id`, sanitizes the id by replacing each of `|`, `\n`, `\r`, `:` with `_` (parity with the marker writer / D-08 security), and records it as `latest_aid` along with `latest_name = str(rec.get('job_name',''))` and `latest_type = str(rec.get('job_type',''))` — LAST kind:job line in file order wins (D-05, no sort). After the loop, if `latest_aid` is non-empty, `print(f"{latest_aid}\t{latest_name}\t{latest_type}")`. Capture the heredoc output into `_root_resolve`, strip a trailing newline with `_root_resolve="${_root_resolve%%$'\n'*}"`, and when non-empty split the three tab-separated fields into `root_aid`, `root_job_name`, `root_job_type` using the same `%%$'\t'*` / `#*$'\t'` idiom as the job_resolve_result split at lines 677-684. Use NO new temp file (bash locals only — do NOT add anything to `_cleanup_session_tmp`, per Pitfall 5). Place this block at SESSION scope (outside any per-completion loop) so the python3 process launches once per session (D-09 / Pitfall 4).

    EDIT 2 — subagent override block (insert AFTER line 690 `fi` that closes the `Job correlation:` log block, BEFORE the line-692 `# jobs create` comment). This runs per-completion inside the loop. Guard with `if [[ "${root_sid}" != "${session_id}" ]]; then` (root sessions skip — Phase 6 path byte-identical). Inside: if `[[ -n "${root_aid}" ]]` then reassign `agentic_job_id="${root_aid}"`, `agentic_job_name="${root_job_name}"`, `agentic_job_type="${root_job_type}"` (inherit root's job — JROLL-01 / D-02); ELSE (race window or orphan subagent) explicitly zero ALL THREE: `agentic_job_id=""`, `agentic_job_name=""`, `agentic_job_type=""`. The else-branch zeroing is load-bearing — it overrides the Phase 6 jobs_cache_file resolution so the subagent's own orphan id can never leak (D-04 / D-07 / Pitfall 2). Optionally add an info log after the override, gated on `root_sid != session_id && -n root_aid`, using a 64-char-truncated `root_aid_log` (mirror the `agentic_job_id_log` truncation at line 686). Do NOT alter the `post_to_revenium` call site at lines 835-848 — it already passes `agentic_job_id`/`agentic_job_name`/`agentic_job_type`; this edit changes only their values.
  </action>
  <verify>
    <automated>bash -n scripts/report.sh && grep -cE 'ROOT_SID=.*MARKERS_DIR=.*python3' scripts/report.sh && grep -cE 'root_sid.*!=.*session_id' scripts/report.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/report.sh` exits 0 (valid syntax)
    - `grep -cE 'ROOT_SID=.*MARKERS_DIR=.*python3' scripts/report.sh` returns >= 1 (env-passing resolver heredoc present; ROOT_SID/MARKERS_DIR never interpolated into the body)
    - `grep -c 'root_aid' scripts/report.sh` returns >= 4 (declaration, resolve capture, override inherit, optional log)
    - The override block zeroes all three vars in its else branch: `grep -A6 'root_aid' scripts/report.sh` shows `agentic_job_id=""` inside a subagent else branch (no orphan leak — D-04)
    - The resolver heredoc body contains the sanitization replacing `:` (e.g. a `replace` over `('|', '\n', '\r', ':')`) — `grep -F "':'" scripts/report.sh` or equivalent shows the colon sanitize
    - No new entry added to `_cleanup_session_tmp` (root_aid uses bash locals only): the `_cleanup_session_tmp` rm line still references only the four Phase 6 temp files
    - The resolver and override are guarded by `root_sid != session_id` so root sessions are untouched (Phase 6 byte-identical)
  </acceptance_criteria>
  <done>
    The root_aid resolver reads `markers/{root_sid}.jsonl` once per subagent session (latest kind:job wins, sanitized), and the in-loop override feeds root values into the existing stamping or zeroes all three on race/orphan. Root sessions skip both. `bash -n` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add root-only gate to jobs create and jobs outcome; turn GROUPS F/G/H green</name>
  <files>scripts/report.sh</files>
  <read_first>
    - scripts/report.sh (read lines 692-727 for the `jobs create` outer gate at 699 and its byte-identical inner block; read lines 855-905 for the `jobs outcome` outer gate at 864 and its inner block)
    - .planning/phases/07-root-session-job-rollup/07-PATTERNS.md (CHANGE SITE 3A and 3B — append `&& "${root_sid}" == "${session_id}"` as the third compound term; inner blocks preserved byte-for-byte)
    - .planning/phases/07-root-session-job-rollup/07-RESEARCH.md (Pattern 3, Pitfall 1 "subagent creates its own job", Pitfall 6 "regression on root sessions")
    - .planning/phases/07-root-session-job-rollup/07-CONTEXT.md (D-06 — root-only create/outcome)
    - .planning/phases/07-root-session-job-rollup/07-VALIDATION.md (the GROUPS F/G/H assertions this task must turn green; the full-suite command)
  </read_first>
  <action>
    Make two edits in `scripts/report.sh`, each appending a third compound term to an existing `if` condition. Do NOT modify the inner blocks — they stay byte-for-byte identical.

    EDIT 1 — jobs create gate (line 699): change `if [[ "${JOBS_CLI_CAPABLE}" == "true" && -n "${agentic_job_id}" ]]; then` to add the third term `&& "${root_sid}" == "${session_id}"`. The full condition becomes `if [[ "${JOBS_CLI_CAPABLE}" == "true" && -n "${agentic_job_id}" && "${root_sid}" == "${session_id}" ]]; then`. This makes a subagent session (where `root_sid != session_id`) skip the entire create block even though, after Task-1's override, its `agentic_job_id` now holds the ROOT's id — without this gate a subagent would attempt to re-create the root's job (Pitfall 1 / D-06).

    EDIT 2 — jobs outcome gate (line 864): apply the identical compound condition `&& "${root_sid}" == "${session_id}"` to the `jobs outcome` `if [[ "${JOBS_CLI_CAPABLE}" == "true" && -n "${agentic_job_id}" ]]; then`. Subagent sessions skip outcome too (D-06).

    After both edits, run the full Phase 7 test suite. Expect GROUPS A–E (Phase 6 regression — root-only sessions, `root_sid == session_id`) to remain GREEN and GROUPS F/G/H (Phase 7) to turn GREEN: GROUP F child inherits root id with exactly one root create; GROUP G race omits `--agentic-job-id` with zero create/outcome and a TX: line present; GROUP H subagent's own `sub-job-3c4d` never appears in the jobs ledger while the root creates once. If any group is RED, the failure is in Task-1's override or these gates — fix here before completing (do NOT weaken the tests).
  </action>
  <verify>
    <automated>bash tests/test_report_argv.sh && bash tests/test_report_jobs_argv.sh && python3 -m pytest tests/test_get_root_session_id.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'root_sid.*==.*session_id' scripts/report.sh` returns >= 2 (the create and outcome gates both carry the root-only term)
    - `bash tests/test_report_jobs_argv.sh` exits 0 — ALL groups (A–E Phase 6 regression AND F/G/H Phase 7) print `PASS:` and zero `FAIL:`
    - `bash tests/test_report_argv.sh` exits 0 (v1.0 metering path unchanged — the no-agentic-job assertion still holds for non-job sessions)
    - `python3 -m pytest tests/test_get_root_session_id.py -v` exits 0 (resolver untouched)
    - GROUP F output includes `PASS: JROLL-01 F:` lines; GROUP G includes `PASS: JROLL-02 G:` lines; GROUP H includes `PASS: JROLL-03 H:` lines
    - The `jobs create` and `jobs outcome` inner blocks are unchanged from Phase 6 (only the outer `if` conditions gained the third term) — `git diff scripts/report.sh` shows additions only on the two `if [[ ... ]]; then` lines plus the Task-1 resolver/override blocks
  </acceptance_criteria>
  <done>
    Both `jobs create` and `jobs outcome` are gated root-only; subagent sessions skip both. The full suite (test_report_argv.sh + test_report_jobs_argv.sh GROUPS A–H + pytest resolver) is GREEN. Phase 6 root path is byte-identical (inner blocks unchanged).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markers/{root_sid}.jsonl → report.sh shell args | Untrusted marker-file content (agent-written `agentic_job_id`/name/type) crosses into `revenium meter completion` / `jobs create` argv |
| root_sid (UUID) → python3 heredoc env | Session-derived value passed to the cross-session marker reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-INT | Tampering (mis-attribution) | Shipping a WRONG or sub-session `agentic_job_id` would mis-bill spend to the wrong job | mitigate | D-04 omit-on-uncertainty: the override else-branch ALWAYS zeroes all three vars on empty root_aid; GROUP G asserts zero `--agentic-job-id` with an orphan child marker present; GROUP H asserts zero `JOB:sub-job-3c4d:` ledger rows. This is the load-bearing data-integrity invariant. |
| T-07-INJ | Tampering (argv injection) | Untrusted marker `agentic_job_id` flowing into CLI args | mitigate | The new root_aid python reader applies the same `:`/`|`/newline → `_` sanitization the marker writer uses, before printing; bash passes values via array discipline (`cmd+=(--agentic-job-id "${...}")`), never word-split |
| T-07-HEREDOC | Tampering (shell injection) | Heredoc interpolation of session-derived `root_sid` into python | mitigate | Env-passing discipline (T-04-09): `ROOT_SID="${root_sid}" MARKERS_DIR="${MARKERS_DIR}" python3 - <<'PY'` — single-quoted delimiter prevents expansion; values arrive as env vars, never interpolated; `Path(markers_dir)/f"{root_sid}.jsonl"` confines reads to MARKERS_DIR |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH.md "Package Legitimacy Audit: Not applicable"; zero new dependencies) |
</threat_model>

<verification>
- `bash -n scripts/report.sh` exits 0
- Full suite GREEN: `bash tests/test_report_argv.sh && bash tests/test_report_jobs_argv.sh && python3 -m pytest tests/test_get_root_session_id.py -v`
- `grep -cE 'root_sid.*==.*session_id' scripts/report.sh` >= 2 (both gates carry root-only term)
- `grep -cE 'ROOT_SID=.*MARKERS_DIR=.*python3' scripts/report.sh` >= 1 (env-passing resolver)
- Phase 6 inner blocks unchanged (git diff shows additions only on the two gate conditions + Task-1 resolver/override)
</verification>

<success_criteria>
- Subagent completions ship the ROOT's `agentic_job_id`/name/type (JROLL-01 / GROUP F green)
- Race/orphan → `--agentic-job-id` omitted entirely, never the sub-session id; spend still ships via `--agent` (JROLL-02 / D-04 / D-07 / GROUP G green)
- Subagent's own job markers produce zero `jobs create`/`outcome` and zero ledger rows; root creates once (JROLL-03 / D-06 / GROUP H green)
- Root sessions byte-identical: GROUPS A–E and test_report_argv.sh remain green (Phase 6 no regression)
</success_criteria>

<output>
Create `.planning/phases/07-root-session-job-rollup/07-02-SUMMARY.md` when done
</output>
