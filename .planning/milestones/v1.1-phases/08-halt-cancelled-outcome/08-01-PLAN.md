---
phase: 08-halt-cancelled-outcome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/stub-revenium.sh
  - tests/test_report_jobs_argv.sh
autonomous: true
requirements: [JHALT-01, JHALT-02]

must_haves:
  truths:
    - "A halted guardrail-status.json fixture (halted:true, haltedAt, haltedRule) can be written into a tmp OPENCLAW_HOME and read by report.sh"
    - "Test groups assert: open real job -> CANCELLED under its own id (JHALT-01)"
    - "Test groups assert: zero open jobs -> synthetic guardrail-halt-<hex> created interrupted then closed CANCELLED (JHALT-02)"
    - "Test groups assert: multiple open jobs -> all closed CANCELLED, no synthetic record (D-08)"
    - "Test groups assert: second halted tick with same haltedAt produces no duplicate jobs calls (JOB:halt:<haltedAt> gate, D-03)"
    - "Test groups assert: JOBS_CLI_CAPABLE=false -> halt handler skipped, zero halt jobs tokens, exit 0 (D-10 fail-open)"
    - "GROUP A-H assertions are unchanged and still pass (Phase 6/7 invariant not regressed)"
  artifacts:
    - path: "tests/test_report_jobs_argv.sh"
      provides: "GROUP I/J/K/L/M halt-handler RED tests + guardrail-status.json fixture writer"
      contains: "guardrail-halt-"
    - path: "tests/stub-revenium.sh"
      provides: "halt-handler stub switches (STUB_REVENIUM_HALT_JOBS_FAIL)"
      contains: "STUB_REVENIUM_HALT_JOBS_FAIL"
  key_links:
    - from: "tests/test_report_jobs_argv.sh"
      to: "tests/stub-revenium.sh"
      via: "STUB_REVENIUM_ARGV_FILE argv capture + STUB_REVENIUM_HALT_JOBS_FAIL"
      pattern: "STUB_REVENIUM_HALT_JOBS_FAIL"
    - from: "tests/test_report_jobs_argv.sh"
      to: "scripts/report.sh"
      via: "run_report against a tmp OPENCLAW_HOME containing a halted guardrail-status.json"
      pattern: "guardrail-status.json"
---

<objective>
Create the RED test scaffolding for the Phase 8 halt handler: extend `tests/stub-revenium.sh` with a halt-specific jobs-failure switch and `tests/test_report_jobs_argv.sh` with five new test groups (I/J/K/L/M) that write a halted `guardrail-status.json` fixture into the tmp OPENCLAW_HOME and assert the halt-handler behavior across the five locked cases (D-04/05/08/03/10).

These tests MUST fail RED on the current `report.sh` (which has no halt handler) and turn GREEN when Plan 08-02 lands. Do not modify `report.sh`. Do not weaken existing GROUP A-H assertions.

Purpose: Lock the JHALT-01/JHALT-02 acceptance behavior into executable assertions before implementation, mirroring the Phase 6 (06-01) and Phase 7 (07-01) Wave 0 RED-test discipline.
Output: Two modified test files; running the suite shows the new GROUP I-M failing RED while GROUP A-H still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-halt-cancelled-outcome/08-CONTEXT.md
@.planning/phases/08-halt-cancelled-outcome/08-PATTERNS.md
@.planning/phases/06-job-lifecycle-wiring/06-CONTEXT.md
@.planning/phases/07-root-session-job-rollup/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend stub-revenium.sh with halt-handler switches</name>
  <files>tests/stub-revenium.sh</files>
  <read_first>
    - tests/stub-revenium.sh (current state — see the existing STUB_REVENIUM_NO_JOBS, STUB_REVENIUM_409_FOR, STUB_REVENIUM_JOBS_FAIL switches and the argv-capture block at the top, ~lines 41-45 and 56-93)
    - .planning/phases/08-halt-cancelled-outcome/08-PATTERNS.md (section "tests/stub-revenium.sh halt fixture stubs" — copy-ready new-switch documentation and the argv-capture discipline that runs BEFORE any routing)
    - .planning/phases/06-job-lifecycle-wiring/06-CONTEXT.md (why the stub exists and the 409/fail switch decoupling seam)
  </read_first>
  <action>
    Add one new opt-in switch to the existing `jobs create` / `jobs outcome` routing block: `STUB_REVENIUM_HALT_JOBS_FAIL=1`. When set, `jobs outcome` and `jobs create` calls whose `--agentic-job-id` argument begins with `guardrail-halt-` (synthetic) OR any `jobs outcome ... --result CANCELLED` call (halt-driven close) fail with a non-409 error (`echo "Error: 500 halt jobs service unavailable" >&2; exit 1`); normal per-session `jobs create`/`outcome` calls remain unaffected. This is the decoupling seam that proves D-10 fail-open without breaking the normal-path argv assertions in GROUP A-H.

    Detection must use the existing `printf '%s\n' "$@" | grep -qF -- ...` idiom (the same pattern STUB_REVENIUM_409_FOR uses) to match `guardrail-halt-` or `CANCELLED` in argv — do NOT eval or string-interpolate argv. Place the new switch in the same `if [[ "$1 $2" == "jobs create" || "$1 $2" == "jobs outcome" ]]` block, AFTER the existing STUB_REVENIUM_409_FOR and STUB_REVENIUM_JOBS_FAIL checks so the more specific halt switch can take precedence when explicitly set. Keep the top-of-file argv-capture block (writes every token to STUB_REVENIUM_ARGV_FILE) untouched and ensure it still runs before any routing so halt tokens like `outcome guardrail-halt-<hex>` are always capturable. Add a documentation comment block describing the new switch in the same style as the existing switch docs.
  </action>
  <verify>
    <automated>bash -n tests/stub-revenium.sh &amp;&amp; grep -c 'STUB_REVENIUM_HALT_JOBS_FAIL' tests/stub-revenium.sh | grep -qv '^0$' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'STUB_REVENIUM_HALT_JOBS_FAIL' tests/stub-revenium.sh` is >= 2 (documentation comment + routing check)
    - Source: the new switch lives inside the existing `jobs create`/`jobs outcome` routing `if` block (no new top-level command branch added)
    - Source: detection uses `printf '%s\n' "$@" | grep -qF --` for `guardrail-halt-` / `CANCELLED` (no `eval`, no `${1}` interpolation into a command)
    - Behavior: `bash -n tests/stub-revenium.sh` exits 0 (no syntax error)
    - Behavior: with STUB_REVENIUM_HALT_JOBS_FAIL unset, a normal `jobs create --agentic-job-id add-x-1ab2` call still exits 0 (no regression to GROUP A-H)
  </acceptance_criteria>
  <done>stub-revenium.sh has a documented STUB_REVENIUM_HALT_JOBS_FAIL switch that fails only halt-driven jobs calls, leaves normal-path calls untouched, and preserves the global argv-capture block.</done>
</task>

<task type="auto">
  <name>Task 2: Add GROUP I-M halt-handler RED tests + halted fixture</name>
  <files>tests/test_report_jobs_argv.sh</files>
  <read_first>
    - tests/test_report_jobs_argv.sh (current state — the make_openclaw_home helper ~lines 48-63, run_report helper ~lines 98-108, count_grep helper ~lines 86-91, GROUP B fail-open assertions ~lines 329-387, GROUP D exit-code-capture pattern ~lines 452-456 (`bash "${REPORT_SH}" 2>&1 || report_rc=$?`), GROUP E idempotency pattern ~lines 517-578, and the no-agentic-job baseline assertion these groups must not break)
    - tests/stub-revenium.sh (the STUB_REVENIUM_HALT_JOBS_FAIL switch added in Task 1, plus STUB_REVENIUM_NO_JOBS for the capability-probe-false case)
    - .planning/phases/08-halt-cancelled-outcome/08-PATTERNS.md (sections "tests/test_report_jobs_argv.sh halt test groups" — the fixture-write snippet, run_report reuse, count_grep assertions; and "sha1 synthetic-id derivation" for the deterministic guardrail-halt-<hex> id the tests must predict)
    - .planning/phases/08-halt-cancelled-outcome/08-CONTEXT.md (Worked timelines in <specifics> — add-auth-9f3c open->CANCELLED; zero-open->guardrail-halt-a3f9; multi-open all CANCELLED; and the five-case test strategy in <decisions>)
  </read_first>
  <action>
    Add a `write_halt_fixture` helper (or inline writer) that, given a tmp OPENCLAW_HOME and a haltedAt value, writes `${home}/skills/revenium/guardrail-status.json` containing `{"halted":true,"haltedAt":"<haltedAt>","autonomousMode":true,"haltedRule":{"name":"token-budget","ruleId":"test-rule-id","metricType":"TOKEN","windowType":"ROLLING","currentValue":1000,"hardLimit":500}}`. Build the JSON with `printf`/quoting only (no eval). Reuse make_openclaw_home, run_report, and count_grep unchanged.

    Add five test groups, each seeding a fresh OPENCLAW_HOME, pre-populating `revenium-jobs.ledger` to set up the open-job state, writing the halt fixture, then running report.sh and asserting on the captured argv and the jobs ledger:

    - GROUP I (JHALT-01, D-04 single open real job): pre-seed ledger with `JOB:add-auth-9f3c:created:1700000000.000` (no outcome line) and a haltedAt of `2026-06-03T10:00:00.000Z`. Assert argv contains exactly one `revenium jobs outcome add-auth-9f3c --result CANCELLED` (grep argv for the `add-auth-9f3c` token AND a `CANCELLED` token; count `^outcome$` halt tokens == expected), the ledger gains exactly one `JOB:add-auth-9f3c:outcome:.*:CANCELLED` line, NO `guardrail-halt-` token appears anywhere (open-count was 1 -> no synthetic, D-08), and exactly one `JOB:halt:2026-06-03T10:00:00.000Z` line is appended.

    - GROUP J (JHALT-02, D-05/D-09 zero open jobs -> synthetic): empty/no-open ledger; haltedAt such that the test predicts the synthetic id via `hex = sha1(haltedAt)[:4]`. Compute the expected hex in-test with the same env-passing python3 sha1 one-liner the implementation uses (`HALTED_AT=... python3 -c '...hashlib.sha1...[:4]'`) so the assertion is not hard-coded to a stale hash. Assert argv contains `jobs create --agentic-job-id guardrail-halt-<hex>` with `--type interrupted`, followed by `jobs outcome guardrail-halt-<hex> --result CANCELLED`; ledger gains both `JOB:guardrail-halt-<hex>:created:` and `JOB:guardrail-halt-<hex>:outcome:.*:CANCELLED`; and `JOB:halt:<haltedAt>` is appended.

    - GROUP K (D-08 multi-open): pre-seed ledger with two open jobs `JOB:add-auth-9f3c:created:...` and `JOB:refactor-api-1b1b:created:...` (no outcome lines). Assert BOTH are closed CANCELLED (two distinct `:outcome:.*:CANCELLED` ledger lines, both ids present as outcome argv tokens) and NO `guardrail-halt-` synthetic token appears.

    - GROUP L (D-03 idempotency across ticks): same OPENCLAW_HOME, same haltedAt fixture, run report.sh twice without resetting the ledger (mirror GROUP E two-run pattern). Assert that across both runs combined, the number of halt-driven CANCELLED outcome calls for the open job is exactly 1 and `JOB:halt:<haltedAt>` appears exactly once (second tick hits the gate and skips).

    - GROUP M (D-10 fail-open): two sub-cases proving the handler never endangers metering and exits 0 — (M1) run with STUB_REVENIUM_NO_JOBS=1 so JOBS_CLI_CAPABLE=false; assert ZERO `guardrail-halt-` tokens and zero halt-driven `outcome ... CANCELLED` tokens in argv, and that report.sh exited 0. (M2) run with an open job present and STUB_REVENIUM_HALT_JOBS_FAIL=1; this sub-case MUST capture report.sh's exit code, so do NOT use the GROUP B `bash "${REPORT_SH}" 2>&1 || true` form (it discards the exit code, making the exit-0 assertion unreachable). Instead mirror the GROUP D exit-code-capture analog (~lines 452-456 of test_report_jobs_argv.sh per PATTERNS.md): pre-initialize `report_rc_m2=0`, then invoke `STUB_REVENIUM_HALT_JOBS_FAIL=1 STUB_REVENIUM_ARGV_FILE=... OPENCLAW_HOME=... HOME=... bash "${REPORT_SH}" 2>&1 || report_rc_m2=$?`, and assert `report_rc_m2` is 0 (handler failure did not abort the tick). Also assert the per-session metering argv tokens are still present, and NO `JOB:halt:<haltedAt>` line was appended (failed halt close is retried next tick, not gated as done).

    All assertions use the existing pass/fail helpers and count_grep. Do NOT touch GROUP A-H. These tests MUST fail RED on the current report.sh.
  </action>
  <verify>
    <automated>bash -n tests/test_report_jobs_argv.sh &amp;&amp; grep -Ec 'GROUP I|GROUP J|GROUP K|GROUP L|GROUP M' tests/test_report_jobs_argv.sh | grep -qv '^0$' &amp;&amp; ! bash tests/test_report_jobs_argv.sh >/dev/null 2>&1 &amp;&amp; echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'guardrail-halt-' tests/test_report_jobs_argv.sh` >= 1 (synthetic-id assertions present)
    - Source: `grep -c 'guardrail-status.json' tests/test_report_jobs_argv.sh` >= 1 (halt fixture is written)
    - Source: GROUP J computes the expected hex via an env-passing `python3 ... hashlib.sha1` call, not a hard-coded literal hash
    - Source: GROUP M includes both a JOBS_CLI_CAPABLE=false sub-case (STUB_REVENIUM_NO_JOBS) and a halt-jobs-fail sub-case (STUB_REVENIUM_HALT_JOBS_FAIL)
    - Source: GROUP M sub-case M2 captures report.sh's exit code via the `|| report_rc_m2=$?` pattern (GROUP D analog), NOT the GROUP B `|| true` form that discards it
    - Source: no edits appear in the GROUP A through GROUP H line ranges (those groups byte-unchanged)
    - Behavior: `bash -n tests/test_report_jobs_argv.sh` exits 0
    - Behavior: `bash tests/test_report_jobs_argv.sh` exits NON-zero on the current report.sh (RED — at least one of GROUP I-M fails) while GROUP A-H still print PASS
  </acceptance_criteria>
  <done>GROUP I-M exist, write a halted guardrail-status.json fixture, assert the five locked halt cases against argv + jobs ledger, fail RED on current report.sh, and leave GROUP A-H passing untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> guardrail-status.json | Test writes a halt fixture; values flow into report.sh's status read in Plan 02. Fixture must be JSON-safe and never shell-interpolated. |
| stub argv -> assertions | Captured argv is read for assertions; must never be eval'd. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | guardrail-status.json fixture build | mitigate | Build fixture JSON with `printf`/static quoting only; haltedAt is a controlled test literal, never user input; no `eval`. |
| T-08-02 | Tampering | captured argv assertions | mitigate | Assert via `grep`/`count_grep` on STUB_REVENIUM_ARGV_FILE only; never `eval` or interpolate captured tokens (continues T-06-01 discipline). |
| T-08-03 | Information Disclosure | sha1 hex prediction in GROUP J | accept | Test computes hex via env-passing python3 sha1; haltedAt is a non-secret timestamp, no sensitive value hashed. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (tests only); slopcheck N/A. |
</threat_model>

<verification>
- `bash -n` passes on both modified test files.
- New GROUP I-M fail RED on current report.sh; GROUP A-H still pass.
- STUB_REVENIUM_HALT_JOBS_FAIL only affects halt-driven jobs calls.
</verification>

<success_criteria>
- tests/stub-revenium.sh has a documented STUB_REVENIUM_HALT_JOBS_FAIL switch scoped to halt jobs calls.
- tests/test_report_jobs_argv.sh has GROUP I (single open->CANCELLED), J (zero open->synthetic interrupted/CANCELLED), K (multi-open all CANCELLED), L (idempotent across ticks), M (fail-open) covering JHALT-01/JHALT-02 and D-03/04/05/08/09/10.
- Suite is RED on current report.sh, with GROUP A-H untouched and passing.
</success_criteria>

<output>
Create `.planning/phases/08-halt-cancelled-outcome/08-01-SUMMARY.md` when done.
</output>
</tasks>
