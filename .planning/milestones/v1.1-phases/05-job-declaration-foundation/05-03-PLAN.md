---
phase: 05-job-declaration-foundation
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - SKILL.md
  - references/job-declaration.md
autonomous: true
requirements: [JOBDEC-02]
must_haves:
  truths:
    - "SKILL.md contains a JOB DECLARATION section instructing the agent to append a kind:'job' marker when a goal arc concludes"
    - "JOB DECLARATION sits AFTER TASK CLASSIFICATION and BEFORE Path Resolution (guard-first ordering preserved)"
    - "The directive references the write-job-marker.sh invocation shape and the agentic_job_id kebab-slug+4-hex format"
    - "references/job-declaration.md holds the full arc definition, status bar, pivot-cancel rule, and worked examples"
  artifacts:
    - path: "SKILL.md"
      provides: "JOB DECLARATION directive section"
      contains: "JOB DECLARATION"
    - path: "references/job-declaration.md"
      provides: "operational detail: arc rules, status bar, pivot-cancel, worked examples"
      contains: "write-job-marker.sh"
  key_links:
    - from: "SKILL.md"
      to: "scripts/write-job-marker.sh"
      via: "Step 3 invocation: write-job-marker.sh --job-id ... --status ..."
      pattern: "write-job-marker.sh"
    - from: "SKILL.md"
      to: "references/job-declaration.md"
      via: "see-also reference for full worked examples"
      pattern: "references/job-declaration.md"
---

<objective>
Add the agent-facing JOB DECLARATION directive (JOBDEC-02): a SKILL.md section modeled structurally on TASK CLASSIFICATION (D-05), plus a new `references/job-declaration.md` with the full operational detail ported/adapted from Hermes. The directive is arc-boundary triggered (D-01), carries the SUCCESS/FAILED/CANCELLED status bar with uncertainty-bias-to-CANCELLED (D-02), the pivot-cancel rule (D-03), the soft granularity floor (D-04), the agentic_job_id kebab-slug+4-hex format with example `add-pagination-endpoint-3b1e` (D-08), and the write-job-marker.sh invocation shape.

Purpose: Tells the agent WHEN and HOW to declare a job — the human-facing half of the foundation, depending on the writer command shape shipped in plan 02.
Output: modified `SKILL.md`, new `references/job-declaration.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-job-declaration-foundation/05-CONTEXT.md
@.planning/phases/05-job-declaration-foundation/05-RESEARCH.md
@.planning/phases/05-job-declaration-foundation/05-PATTERNS.md
@.planning/phases/05-job-declaration-foundation/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create references/job-declaration.md (operational detail)</name>
  <files>references/job-declaration.md</files>
  <read_first>
    - references/task-classification.md (role analog: opening "Operational Detail" framing + how the reference file relates to the SKILL.md section)
    - scripts/write-job-marker.sh (the exact flag names/invocation the worked examples must use — from plan 02)
    - 05-RESEARCH.md §"Hermes Job-Declaration Content to Port" (arc definition, trigger, status bar, 4 worked examples adapted to OpenClaw CLI)
    - 05-PATTERNS.md §"references/job-declaration.md (config — NEW)" (open-with shape + content checklist)
    - 05-CONTEXT.md D-01..D-04, D-08 (arc model, status bar, pivot-cancel, soft floor, id format)
  </read_first>
  <action>
    Create `references/job-declaration.md` opening with the "Job Declaration — Operational Detail" framing (mirror references/task-classification.md's opener). Port and adapt from 05-RESEARCH.md §"Hermes Job-Declaration Content to Port": the arc definition (same-arc = same goal incl. follow-up fixes; new-arc = genuine pivot); the binary trigger (3 fire conditions + the skip-only-when ≤2 sentences AND no arc in progress); the status bar verbatim from D-02 (SUCCESS = positive self-verified evidence, "made the change but couldn't verify" = CANCELLED not SUCCESS; FAILED = narrow definitive-negative with a brief failure_reason; CANCELLED = catch-all + uncertainty-bias default); the pivot-cancel rule (D-03 — write a CANCELLED marker for the abandoned arc first, then begin the new arc); the soft granularity-floor note (D-04 — aim for ≥1 job/session, no hard enforcement, zero jobs is legitimate). Include the 4 worked examples using the real OpenClaw writer command shape (`bash ~/.openclaw/skills/revenium/scripts/write-job-marker.sh --job-id ... --job-name ... --job-type ... --status ...`): SUCCESS (tests passed), CANCELLED (change made but unverified), FAILED (with --failure-reason), and the pivot-cancel sequence. Use the anchor id `add-pagination-endpoint-3b1e` (D-08).
  </action>
  <verify>
    <automated>test -f references/job-declaration.md && grep -q "write-job-marker.sh" references/job-declaration.md && grep -Eq "SUCCESS|FAILED|CANCELLED" references/job-declaration.md && grep -q "add-pagination-endpoint-3b1e" references/job-declaration.md && grep -qi "pivot" references/job-declaration.md && echo "OK reference"</automated>
  </verify>
  <acceptance_criteria>
    - references/job-declaration.md exists and opens with an Operational Detail framing.
    - Contains the arc definition, binary trigger, the full SUCCESS/FAILED/CANCELLED status bar, the pivot-cancel rule, and the soft granularity floor.
    - Contains 4 worked examples using the actual `write-job-marker.sh` flag invocation (not Hermes inline-marker syntax, not unix_float ts).
    - Uses the `add-pagination-endpoint-3b1e` id example (D-08 kebab-slug + 4-hex format).
  </acceptance_criteria>
  <done>references/job-declaration.md ships the full ported/adapted operational detail with OpenClaw-correct writer examples.</done>
</task>

<task type="auto">
  <name>Task 2: Insert the JOB DECLARATION directive into SKILL.md</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md lines 63-118 (TASK CLASSIFICATION section structure to mirror; Path Resolution begins at line 116 — insertion point is between them)
    - references/job-declaration.md (created in Task 1 — the see-also target)
    - scripts/write-job-marker.sh (Step 3 invocation shape + flags)
    - job-taxonomy.json (the 11 labels for the job_type table)
    - 05-PATTERNS.md §"SKILL.md (config — MODIFY)" (10-point structural checklist + status-bar / confirmation-handling excerpts)
    - 05-RESEARCH.md §"SKILL.md Directive: Structural Model" and §"Common Pitfalls" Pitfall 6 (ordering)
  </read_first>
  <action>
    Insert a new `## JOB DECLARATION` section into SKILL.md STRICTLY AFTER the TASK CLASSIFICATION section and BEFORE `## Path Resolution` (Pitfall 6 / D-05 — guard-first ordering: HALT CHECK → Guardrail Check → TASK CLASSIFICATION → JOB DECLARATION → Path Resolution). Do NOT move or alter the HALT CHECK, Guardrail Check Procedure, or TASK CLASSIFICATION sections.

    Structure the section mirroring TASK CLASSIFICATION (D-05): MANDATORY framing stating the trigger is arc-boundary (a job = a goal-arc), NOT per-turn (D-01); a `### Trigger (binary — no judgment calls)` subsection with the 3 fire conditions (arc completed + self-verified / arc definitively failed / user pivoted before the arc was declared → CANCELLED first) and the skip-only-when condition; a `### Required action` subsection — Step 1 pick a job_type from the 11-label table; Step 2 mint an agentic_job_id as a kebab-case goal slug + 4-hex suffix, anchor example `add-pagination-endpoint-3b1e` (D-08); Step 3 call `bash ~/.openclaw/skills/revenium/scripts/write-job-marker.sh --job-id ... --job-name ... --job-type ... --status SUCCESS|FAILED|CANCELLED [--failure-reason ...]`. Include the 11-label job_type table (from job-taxonomy.json). Include the status bar (SUCCESS/FAILED/CANCELLED with uncertainty-bias-to-CANCELLED, D-02). Include failure_reason guidance (FAILED-only, D-13). Include confirmation/error handling mirroring write-marker.sh ("job marker written: <path>" = success; non-zero exit / no such output = protocol error, log but do not block — fail-loud-but-don't-block). Add a `### Why this matters` subsection and a closing pointer to `references/job-declaration.md` for full worked examples and the pivot-cancel rule.
  </action>
  <verify>
    <automated>grep -c "JOB DECLARATION" SKILL.md | awk '{exit ($1>=1)?0:1}' && python3 -c "import sys; t=open('SKILL.md').read(); jd=t.find('## JOB DECLARATION'); tc=t.find('## TASK CLASSIFICATION'); pr=t.find('## Path Resolution'); assert tc>=0 and jd>tc and pr>jd, (tc,jd,pr); print('OK ordering TASK CLASSIFICATION < JOB DECLARATION < Path Resolution')"</automated>
    <automated>grep -q "write-job-marker.sh" SKILL.md && grep -q "add-pagination-endpoint-3b1e" SKILL.md && grep -Eq "SUCCESS.*FAILED.*CANCELLED|CANCELLED" SKILL.md && grep -q "references/job-declaration.md" SKILL.md && echo "OK directive content"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "JOB DECLARATION" SKILL.md` ≥ 1.
    - Ordering assertion passes: `## TASK CLASSIFICATION` < `## JOB DECLARATION` < `## Path Resolution` (Pitfall 6 guard-first ordering).
    - Section references the write-job-marker.sh invocation, the 11-label job_type table, the SUCCESS/FAILED/CANCELLED status bar, the `add-pagination-endpoint-3b1e` id format, and points to references/job-declaration.md.
    - HALT CHECK, Guardrail Check Procedure, and TASK CLASSIFICATION sections are unchanged.
  </acceptance_criteria>
  <done>SKILL.md has a JOB DECLARATION directive in the correct guard-first position telling the agent when and how to append a kind:"job" marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| directive → agent behavior | SKILL.md text shapes when/how the agent invokes the writer; mis-ordering could imply markers before the guardrail halt check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | SKILL.md section ordering (guard-first contract) | mitigate | JOB DECLARATION inserted strictly after TASK CLASSIFICATION; ordering enforced by an automated assertion (Pitfall 6) |
| T-05-10 | Information Disclosure | worked-example ids leaking real data | accept | Examples use synthetic ids (add-pagination-endpoint-3b1e); no secrets in directive text |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No external packages this phase (docs/markdown only) |
</threat_model>

<verification>
- `grep -c "JOB DECLARATION" SKILL.md` ≥ 1.
- Ordering assertion: TASK CLASSIFICATION < JOB DECLARATION < Path Resolution.
- references/job-declaration.md exists with status bar, pivot-cancel, and writer-command worked examples.
</verification>

<success_criteria>
- SKILL.md contains a JOB DECLARATION directive telling the agent to append a kind:"job" marker when a unit of work concludes (ROADMAP success criterion 2, JOBDEC-02).
- The directive prescribes the agentic_job_id format and references the write-job-marker.sh command and the operational-detail reference.
</success_criteria>

<output>
Create `.planning/phases/05-job-declaration-foundation/05-03-SUMMARY.md` when done.
</output>
