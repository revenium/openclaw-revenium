---
phase: 06-job-lifecycle-wiring
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/report.sh
autonomous: true
requirements: [JLIFE-02, JLIFE-04, JLIFE-05]
must_haves:
  truths:
    - "A new revenium-jobs.ledger path is declared in report.sh's own config block and touch'd at startup"
    - "JOBS_CLI_CAPABLE is probed once per tick via the dual --help probe and cached for the whole tick; on probe failure report.sh warns once and does no job work, and metering ships byte-identical to v1.0"
    - "The markers-cache read also surfaces kind:job rows (it no longer silently drops them) without changing the existing task-type rows"
    - "A completion that correlates to a closing job marker (completion_id exact -> ts fallback) ships --agentic-job-id/-name/-type on meter completion, only when JOBS_CLI_CAPABLE and the id is non-empty"
    - "Prior-tick already-TX:-ledgered completions are never re-stamped (D-02); the TX: ledger and offset arithmetic are untouched"
  artifacts:
    - path: "scripts/report.sh"
      provides: "JOBS_LEDGER_FILE decl + touch; JOBS_CLI_CAPABLE dual probe; markers-cache job rows; per-completion job correlation; --agentic-job-* append in post_to_revenium"
      contains: "JOBS_CLI_CAPABLE"
  key_links:
    - from: "report.sh process_session job correlation"
      to: "report.sh post_to_revenium"
      via: "three new positional params agentic_job_id/name/type appended to the call site"
      pattern: "agentic-job-id"
    - from: "report.sh markers-cache read"
      to: "report.sh job correlation"
      via: "job rows emitted to a job cache the correlation scans"
      pattern: "kind.*job"
---

<objective>
Lay the report.sh foundation for the job lifecycle WITHOUT yet firing create/outcome: declare
and `touch` the separate `revenium-jobs.ledger` (D-10), add the one-time `JOBS_CLI_CAPABLE`
dual capability probe (D-11), extend the markers-cache read to surface `kind:"job"` rows
(Pitfall 2), reuse the existing two-phase correlation engine to match a closing job marker to
its same-tick completion (D-01), and append `--agentic-job-id/-name/-type` to the
`meter completion` cmd array in `post_to_revenium` (JLIFE-02, Pattern 4).

This delivers JLIFE-02 (stamping), the JLIFE-04 capability probe + fail-open skip, and the
JLIFE-05 ledger FILE (Plan 03 adds the create/outcome ledger WRITES/gates). The `TX:`
completion ledger, the offset arithmetic, and the CR-02 gate (report.sh:693) stay UNTOUCHED
(D-02, Pitfall 1, Pitfall 6).

After this plan, the SUCCESS/FAILED/CANCELLED stamping assertions and the fail-open assertion
in tests/test_report_jobs_argv.sh should pass; the `^create$`/`^outcome$`/ledger-row
assertions remain RED until Plan 03.

Purpose: Establish the read+correlate+stamp half of the lifecycle, gated behind the capability
probe, so Plan 03 only adds the in-loop create/outcome calls.
Output: scripts/report.sh (config block, probe, markers-cache, correlation, post_to_revenium).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-job-lifecycle-wiring/06-CONTEXT.md
@.planning/phases/06-job-lifecycle-wiring/06-RESEARCH.md
@.planning/phases/06-job-lifecycle-wiring/06-PATTERNS.md
@.planning/phases/06-job-lifecycle-wiring/06-VALIDATION.md
@scripts/report.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare JOBS_LEDGER_FILE, touch it at startup, and add the JOBS_CLI_CAPABLE dual probe</name>
  <files>scripts/report.sh</files>
  <read_first>
    - scripts/report.sh lines 28-34 (the config/path block where LEDGER_FILE/OFFSETS_FILE are declared — report.sh sources NOTHING, so the new path goes HERE, NOT in common.sh)
    - scripts/report.sh lines 96-111 (startup guards: revenium/jq presence, `revenium config show`, then `touch "${LEDGER_FILE}"` at line 111 — add the sibling touch and the probe after this, before main at line 728)
    - .planning/phases/06-job-lifecycle-wiring/06-RESEARCH.md §"Code Examples / Config-block declaration" (~lines 389-401) and Pattern 1 (~lines 202-216)
    - .planning/phases/06-job-lifecycle-wiring/06-PATTERNS.md §"config-block path declaration" (~lines 37-66) and §"JOBS_CLI_CAPABLE dual-probe" (~lines 70-88) — including the explicit warning NOT to port Hermes' job_outcome_queue
    - ../hermes-revenium/skills/revenium/scripts/hermes-report.sh lines 34-43 (the proven dual-probe to adapt) and line 52 (the sibling touch)
  </read_first>
  <action>
    In report.sh's config block (after OFFSETS_FILE at line 34) declare:
    `JOBS_LEDGER_FILE="${REVENIUM_JOBS_LEDGER_FILE:-${OPENCLAW_HOME}/revenium-jobs.ledger}"`
    (D-10; env-override mirrors Hermes). Immediately after the existing `touch "${LEDGER_FILE}"`
    (line 111) add `touch "${JOBS_LEDGER_FILE}"`.

    After the touch and before `main "$@"` (line 728), add the one-time D-11 dual capability probe
    that sets a `JOBS_CLI_CAPABLE` boolean (default false): set true only if BOTH
    `revenium jobs --help` exits 0 AND `revenium meter completion --help` output contains the literal
    `--agentic-job-id` token (grep -q -- '--agentic-job-id'); else `warn` ONCE that job work is
    skipped and metering continues as v1.0, leaving JOBS_CLI_CAPABLE=false. Mirror the exact probe
    shape in RESEARCH Pattern 1 / PATTERNS §dual-probe. Probe ONCE per tick at startup (not per
    session/completion); the boolean is read by Task 3's stamping and by Plan 03's create/outcome.

    Do NOT declare a `job_outcome_queue` or any post-loop accumulator (PATTERNS explicitly forbids
    porting Hermes' queue — OpenClaw is single-session in-loop, D-09). Do NOT modify LEDGER_FILE,
    OFFSETS_FILE, or any `TX:`/offset logic.
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/openclaw-revenium && bash -n scripts/report.sh && grep -v '^#' scripts/report.sh | grep -c 'JOBS_LEDGER_FILE\|JOBS_CLI_CAPABLE' | { read n; [ "$n" -ge 4 ] && echo "DECLS_PRESENT($n)"; } && grep -v '^#' scripts/report.sh | grep -q 'touch "${JOBS_LEDGER_FILE}"' && echo TOUCH_PRESENT && ! grep -v '^#' scripts/report.sh | grep -q 'job_outcome_queue' && echo NO_QUEUE</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/report.sh` exits 0.
    - `JOBS_LEDGER_FILE` is declared in the config block (lines ~28-35) with the `${REVENIUM_JOBS_LEDGER_FILE:-...}` env-override form, referencing `${OPENCLAW_HOME}/revenium-jobs.ledger`.
    - A `touch "${JOBS_LEDGER_FILE}"` appears immediately after the existing `touch "${LEDGER_FILE}"`.
    - A `JOBS_CLI_CAPABLE` variable is set once at startup (default false) via the dual `--help` probe; on probe failure exactly one `warn` fires and the variable stays false.
    - No `job_outcome_queue` (or any post-loop job accumulator) is introduced.
    - `LEDGER_FILE`, `OFFSETS_FILE`, and all `TX:`/offset/`set_offset`/`get_offset` lines are byte-unchanged (verify via `git diff` showing no edits inside the offset/`TX:` logic).
  </acceptance_criteria>
  <done>JOBS_LEDGER_FILE declared + touch'd; JOBS_CLI_CAPABLE probed once per tick with fail-open warn; no queue; TX:/offset logic untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the markers-cache read for kind:job rows</name>
  <files>scripts/report.sh</files>
  <read_first>
    - scripts/report.sh lines 332-360 (the existing markers-cache Python heredoc — note the env-passing `_MARKER_FILE` discipline and that the filter `r.get('ts') and r.get('task_type')` silently DROPS job markers because they have no task_type)
    - scripts/write-job-marker.sh lines 228-237 (the emitted job-marker keys: kind="job", ts, sid, agentic_job_id, job_name, job_type, status, optional failure_reason, optional completion_id)
    - .planning/phases/06-job-lifecycle-wiring/06-RESEARCH.md Pattern 5 (~lines 300-314 — the discriminator `kind == "job"`; absence of `kind` means task marker, Phase 5 D-11)
    - .planning/phases/06-job-lifecycle-wiring/06-PATTERNS.md §"markers-cache read extended for kind:job rows" (~lines 92-131)
  </read_first>
  <behavior>
    - A marker line with `kind:"job"` and a non-empty `agentic_job_id` produces a JOB cache row carrying (ts, agentic_job_id, job_name, job_type, status, failure_reason, completion_id).
    - A task marker (no `kind`, has `task_type`) still produces exactly the same task-row output as today (existing rows unchanged).
    - A malformed/empty/non-job/non-task line is skipped without aborting the read (per-line try/except preserved).
    - Job rows are kept separable from task rows so the per-completion correlation (Task 3) can scan them independently — write job rows to a distinct cache file (e.g. a sibling `jobs_cache_file` mktemp) OR use a distinct line-prefix; the executor picks one and documents it. Track the new temp file in `_cleanup_session_tmp` (report.sh:329-331) so it is removed on every return path (WR-01).
  </behavior>
  <action>
    Extend the markers-cache read in process_session (report.sh:332-360) to ALSO emit `kind:"job"`
    rows. Branch the per-line filter: if `isinstance(r, dict) and r.get('kind') == 'job' and
    r.get('agentic_job_id')` emit a job row with fields agentic_job_id/job_name/job_type/status/
    failure_reason/completion_id (+ts); ELSE keep the existing `r.get('ts') and r.get('task_type')`
    task-row branch unchanged. Preserve the env-passing heredoc (`_MARKER_FILE` via env, NEVER
    `${VAR}` inside `<<'PY'` — T-04-09). Write job rows to a SEPARATE cache file (recommended,
    NP-1: still one file-read per session) declared alongside `markers_cache_file` and cleaned in
    `_cleanup_session_tmp`. Sort job rows by ts like the task rows. Do not change the task-row tab
    layout (the existing correlation at report.sh:489-552 depends on it).
  </action>
  <verify>
    <automated>bash -n scripts/report.sh && bash tests/test_report_argv.sh && echo TASKTYPE_REGRESSION_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/report.sh` exits 0.
    - `tests/test_report_argv.sh` still exits 0 (task-type rows unchanged; Sessions A-D classify exactly as before).
    - The markers-cache read contains a `kind` discriminator branch capturing agentic_job_id/job_name/job_type/status/failure_reason/completion_id.
    - The new job cache temp file is added to `_cleanup_session_tmp` (no temp-file leak under cron — WR-01).
    - The read still uses env-passing (`_MARKER_FILE`) with no `${VAR}` interpolation inside the heredoc.
  </acceptance_criteria>
  <done>Job markers are surfaced into a job cache; task rows and the task-type regression test are unchanged; new temp file is cleaned up.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Correlate the closing job marker to its same-tick completion and stamp --agentic-job-* in post_to_revenium</name>
  <files>scripts/report.sh</files>
  <read_first>
    - scripts/report.sh lines 476-552 (the two-phase task_type correlation — Phase A exact completion_id, Phase D earliest-marker-after-completion using the `parse_ts` datetime helper; reuse this EXACT engine for jobs)
    - scripts/report.sh lines 212-303 (post_to_revenium — the 21-positional-param signature at 213-233 and the optional-flag append blocks at 256-274 to mirror)
    - scripts/report.sh lines 663-675 (the post_to_revenium call site — append the three resolved job values after `"${task_type:-unclassified}"`)
    - scripts/report.sh lines 654-661 (zero-usage skip + `TX:` dedup `continue` — the D-03 case where a correlated completion was already shipped means this `continue` fires; correlation/stamping must NOT depend on a completion being shipped this tick)
    - .planning/phases/06-job-lifecycle-wiring/06-RESEARCH.md Pattern 4 (~lines 287-298) and Pitfall 5 (lexicographic vs parsed-ts) and Pitfall 6 (do not re-stamp prior-tick completions)
    - .planning/phases/06-job-lifecycle-wiring/06-PATTERNS.md §"per-completion job correlation" (~lines 134-180) and §"--agentic-job-* append in post_to_revenium" (~lines 183-218)
  </read_first>
  <behavior>
    - For a completion whose `.id` equals a job row's completion_id, post_to_revenium ships `--agentic-job-id` (=that row's agentic_job_id), plus `--agentic-job-name` and `--agentic-job-type` when non-empty.
    - When no job row exact-matches and a no-completion_id job row's ts >= the completion ts, the same completion is stamped via the ts fallback (mirrors task-type Phase D), using parsed-datetime compare (Pitfall 5), never lexicographic.
    - When JOBS_CLI_CAPABLE is false OR the resolved agentic_job_id is empty, ZERO `--agentic-job-*` tokens are appended and the rest of the meter completion argv (incl. `--task-type`, `--agent`) is byte-identical to v1.0.
    - A prior-tick completion already in the `TX:` ledger is never re-stamped (D-02/Pitfall 6): no re-reading of TX:-ledgered lines, no offset manipulation.
  </behavior>
  <action>
    Add a per-completion job-resolution block parallel to the task_type lookup (report.sh:487-552),
    reusing the SAME completion_id-exact -> ts-fallback logic and the SAME `parse_ts` helper
    (Pitfall 5 — do NOT lexicographically compare). Scan the job cache from Task 2; resolve
    (agentic_job_id, job_name, job_type, status, failure_reason) for the current completion, defaulting
    all empty when no job row matches. Apply the 64-char log-truncation idiom (report.sh:555) to any
    job field that reaches info/warn (T-04-08).

    Extend post_to_revenium: append three new positional params after task_type (`${22}`/`${23}`/
    `${24}`), `local agentic_job_id="${22:-}"` etc. Add a conditional append block mirroring the
    `--trace-id`/`--is-streamed`/`--organization-name` blocks (report.sh:256-274): when
    `JOBS_CLI_CAPABLE == "true"` AND agentic_job_id non-empty, `cmd+=(--agentic-job-id "${agentic_job_id}")`,
    then `--agentic-job-name`/`--agentic-job-type` each only when non-empty. Use bash-array discipline
    (`cmd+=(--flag "$val")`) — never eval/unquoted (V5 / argument injection). Pass the three resolved
    values at the call site (report.sh:663-675) after `"${task_type:-unclassified}"`.

    Do NOT add create/outcome here (that is Plan 03). Do NOT port Hermes' owning_job_id root-rollup
    branch (Phase 7). Stamp ONLY the single same-tick correlated completion (D-01); never re-stamp
    prior-tick completions and never touch the `TX:` ledger or offset (D-02, Pitfall 6).
  </action>
  <verify>
    <automated>cd /Users/johndemic/Development/projects/revenium/openclaw-revenium && bash -n scripts/report.sh && bash tests/test_report_argv.sh && echo REGRESSION_GREEN; bash tests/test_report_jobs_argv.sh; echo "jobs-test rc=$? (create/outcome assertions still RED until Plan 03; stamping + fail-open assertions should now PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/report.sh` exits 0.
    - `tests/test_report_argv.sh` still exits 0 (task-type regression unbroken).
    - In tests/test_report_jobs_argv.sh: the SUCCESS job's completion ships `--agentic-job-id` == the marker id, plus `--agentic-job-name` and `--agentic-job-type`; the FAILED and CANCELLED jobs' correlated completions are likewise stamped.
    - The fail-open run (STUB_REVENIUM_NO_JOBS=1 → JOBS_CLI_CAPABLE=false) produces ZERO `agentic-job` tokens while `--task-type` and `--agent` are still present.
    - `git diff` shows NO edits to the `TX:` dedup (report.sh:659), `set_offset`/`get_offset`, the CR-02 gate (report.sh:693), or any offset arithmetic.
    - post_to_revenium uses bash-array append (`cmd+=(--agentic-job-id "$id")`), never eval/unquoted (V5).
    - The create/outcome (`^create$`/`^outcome$`) and jobs-ledger-row assertions in the jobs test remain RED (those land in Plan 03) — this is expected and acceptable for this plan.
  </acceptance_criteria>
  <done>The correlated same-tick completion ships --agentic-job-id/-name/-type behind the JOBS_CLI_CAPABLE gate; fail-open ships v1.0-identical argv; TX:/offset/CR-02 logic untouched; task-type regression green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-written job marker fields → report.sh Python read → CLI argv | `agentic_job_id`, `job_name`, `job_type`, `status`, `failure_reason` are agent-supplied; sanitized at write time (write-job-marker.sh:88-113), consumed here as data |
| marker values → log lines (info/warn) | job fields reaching the log are a log-injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering / EoP | `--agentic-job-*` append in post_to_revenium | mitigate | Build via bash array `cmd+=(--agentic-job-id "${agentic_job_id}")`; never `eval`, never unquoted expansion (mirrors the existing optional-flag blocks at report.sh:256-274). Phase-5 sanitization is the upstream net; array discipline is the downstream net. |
| T-06-05 | Tampering / EoP | markers-cache + correlation inline Python | mitigate | Env-passing heredoc (`_MARKER_FILE`/env vars), never `${VAR}` inside `<<'PY'` (T-04-09). Untrusted marker prose never enters a program string. |
| T-06-06 | Tampering | job fields written to info/warn logs | mitigate | 64-char truncation (T-04-08, mirrors task_type_log at report.sh:555) on any job field that reaches a log call. |
| T-06-07 | DoS | job-CLI capability probe / per-tick cost | accept | Probe runs once per tick (cached boolean), negligible overhead; fail-open by design. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs ZERO packages (RESEARCH: Package Legitimacy Audit not applicable). No install tasks. |
</threat_model>

<verification>
- `bash -n scripts/report.sh` passes.
- `bash tests/test_report_argv.sh` exits 0 (task-type regression unbroken).
- `bash tests/test_report_jobs_argv.sh` advances: stamping + fail-open assertions pass; create/outcome/ledger-row assertions remain RED (Plan 03).
- `git diff scripts/report.sh` shows the `TX:`/offset/CR-02 logic unchanged.
</verification>

<success_criteria>
- JOBS_LEDGER_FILE declared in report.sh's own config block and touch'd at startup (D-10).
- JOBS_CLI_CAPABLE dual probe runs once per tick, fail-open with a single warn (D-11, JLIFE-04).
- markers-cache read surfaces kind:job rows into a separate job cache; task rows unchanged (Pitfall 2).
- per-completion job correlation reuses the parse_ts two-phase engine (Pitfall 5); stamps only the same-tick completion (D-01/D-02).
- post_to_revenium appends --agentic-job-id/-name/-type behind the capability gate via bash arrays (JLIFE-02, Pattern 4).
- No queue, no owning_job_id rollup, no TX:/offset edits.
</success_criteria>

<output>
Create `.planning/phases/06-job-lifecycle-wiring/06-02-SUMMARY.md` when done.
</output>
