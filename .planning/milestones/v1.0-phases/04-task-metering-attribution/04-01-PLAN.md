---
phase: 04-task-metering-attribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - task-taxonomy.json
  - scripts/common.sh
  - scripts/get-root-session-id.py
  - tests/stub-revenium.sh
  - tests/fixtures/sessions/parent-with-spawn.jsonl
  - tests/fixtures/sessions/plain-session.jsonl
  - tests/test_get_root_session_id.py
autonomous: true
requirements: [METER-01, TRACE-01]
must_haves:
  truths:
    - "task-taxonomy.json exists with the 8 canonical labels (METER-01)"
    - "common.sh exports TAXONOMY_FILE, MARKERS_DIR, SESSIONS_DIR, REVENIUM_AGENT_PREFIX (D-07)"
    - "get_root_session_id resolves a child sid to its root via JSONL childSessionKey, fails open to the input sid (D-05/D-06, TRACE-01)"
    - "A test harness (stubbed revenium + session fixtures) exists for downstream plans"
  artifacts:
    - path: "task-taxonomy.json"
      provides: "8-label task vocabulary (METER-01)"
      contains: "code_review"
    - path: "scripts/common.sh"
      provides: "REVENIUM_AGENT_PREFIX + path constants + get_root_session_id wrapper"
      contains: "REVENIUM_AGENT_PREFIX"
    - path: "scripts/get-root-session-id.py"
      provides: "JSONL childSessionKey reverse-walk resolver (TRACE-01)"
      contains: "childSessionKey"
    - path: "tests/stub-revenium.sh"
      provides: "argv-capturing revenium stub for integration tests"
    - path: "tests/test_get_root_session_id.py"
      provides: "resolver unit tests"
  key_links:
    - from: "scripts/common.sh"
      to: "scripts/get-root-session-id.py"
      via: "python3 invocation in get_root_session_id wrapper"
      pattern: "get-root-session-id\\.py"
---

<objective>
Establish the Phase 4 foundation contracts that every downstream plan consumes: the task-type vocabulary asset, the shared `common.sh` constants and the root-session resolver, plus the test harness (stubbed `revenium` + session fixtures + resolver unit tests) the rest of the phase verifies against.

Purpose: Downstream plans (`write-marker.sh`, `report.sh`, `setup-guardrails.sh`) read these constants and call the resolver. Defining them first (interface-first ordering) prevents a scavenger hunt and lets Waves 2-3 run in parallel against a stable interface.
Output: `task-taxonomy.json`, modified `scripts/common.sh`, new `scripts/get-root-session-id.py`, and a `tests/` harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-task-metering-attribution/04-RESEARCH.md
@.planning/phases/04-task-metering-attribution/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy task-taxonomy.json (repo root) + Wave-0 test harness</name>
  <files>task-taxonomy.json, tests/stub-revenium.sh, tests/fixtures/sessions/parent-with-spawn.jsonl, tests/fixtures/sessions/plain-session.jsonl</files>
  <read_first>
    - ../hermes-revenium/skills/revenium/task-taxonomy.json (source of truth — copy verbatim)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (the "task-taxonomy.json" assignment; Pitfall 6 single-path)
    - .planning/phases/04-task-metering-attribution/04-RESEARCH.md (the "Validation Architecture" section, "Wave 0 Gaps" and "Phase Requirements to Test Map")
    - scripts/report.sh lines 17-34 (SESSIONS_DIR shape; fixtures must mirror the real session JSONL header keys)
  </read_first>
  <action>
    Copy `../hermes-revenium/skills/revenium/task-taxonomy.json` verbatim to repo-root `task-taxonomy.json` (sibling of SKILL.md/BUDGET-GUARD.md so post-install.sh can seed it; Pitfall 6 single-path — do NOT create a second live copy). It MUST contain exactly the 8 labels research, analysis, generation, review, code_review, refactor, planning, debugging (METER-01). Create `tests/stub-revenium.sh`: an executable script that appends each of its argv (one per line) to the file named by env var STUB_REVENIUM_ARGV_FILE then exits 0 — downstream integration tests place a copy/symlink named `revenium` pointing at it on PATH and assert captured argv. Create `tests/fixtures/sessions/parent-with-spawn.jsonl`: line 1 a session header of shape `type:session, version:3, id:<parent-uuid>, timestamp, cwd`, and a later line a `sessions_spawn` toolResult whose `message.details.childSessionKey` equals `agent:main:subagent:<child-uuid>` (use fixed concrete UUIDs noted in a comment at the top of the test file). Create `tests/fixtures/sessions/plain-session.jsonl`: a header line with its own uuid and one ordinary assistant message line, NO sessions_spawn. These fixtures back the resolver test (Task 3) and downstream report.sh integration tests.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; d=json.load(open('task-taxonomy.json')); sys.exit(0 if set(d['labels'])=={'research','analysis','generation','review','code_review','refactor','planning','debugging'} else 1)" && grep -q sessions_spawn tests/fixtures/sessions/parent-with-spawn.jsonl && test -x tests/stub-revenium.sh && echo OK</automated>
  </verify>
  <done>task-taxonomy.json at repo root has exactly the 8 labels; stub-revenium.sh is executable and captures argv to STUB_REVENIUM_ARGV_FILE; both session fixtures exist and the parent fixture contains a childSessionKey.</done>
</task>

<task type="auto">
  <name>Task 2: Add common.sh Phase-4 constants + resolver wrapper</name>
  <files>scripts/common.sh</files>
  <read_first>
    - scripts/common.sh (full — current constants at lines 36-56; REVENIUM_AGENT_NAME at line 53; RULES_LOCK_FILE at line 47)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (the "scripts/common.sh" assignment: Add 1 / Add 2 / Add 3 with exact text; NP-2 wrapper shape)
    - .planning/phases/04-task-metering-attribution/04-RESEARCH.md (the "common.sh resolver wrapper" code example with OPENCLAW_HOME pass-through)
    - ../hermes-revenium/skills/revenium/scripts/common.sh lines 96-106 (wrapper analog)
  </read_first>
  <action>
    Make three additive edits to `scripts/common.sh`; leave all existing lines otherwise unchanged. Add 1 — after RULES_LOCK_FILE (line 47): define TAXONOMY_FILE as STATE_DIR/task-taxonomy.json, MARKERS_DIR as STATE_DIR/markers, SESSIONS_DIR as OPENCLAW_HOME/agents/main/sessions. Add 2 — beside REVENIUM_AGENT_NAME (line 53), KEEP that constant and add REVENIUM_AGENT_PREFIX defaulting to the literal openclaw- (lowercase, trailing hyphen) with a comment citing D-07: it is the metering/filter scheme (report.sh ships `--agent "${REVENIUM_AGENT_PREFIX}${root_sid}"`; base rule filters `AGENT:STARTS_WITH:${REVENIUM_AGENT_PREFIX}`) superseding the static AGENT:IS:${REVENIUM_AGENT_NAME} model (per D-07). Add 3 — append the get_root_session_id() bash wrapper: empty-sid guard returns 0; if python3 absent, print the sid and return; else invoke get-root-session-id.py under SKILL_DIR with OPENCLAW_HOME passed through, falling back to printing the sid on any failure (fail-open, D-05/D-06). This is a sourced library — keep `set -uo pipefail` (no -e). Adapt to current line numbers; do not paste the read_first code verbatim.
  </action>
  <verify>
    <automated>bash -c 'source scripts/common.sh; [[ "${REVENIUM_AGENT_PREFIX}" == "openclaw-" ]] && [[ -n "${MARKERS_DIR}" ]] && [[ -n "${TAXONOMY_FILE}" ]] && [[ -n "${SESSIONS_DIR}" ]] && type get_root_session_id >/dev/null' && echo OK</automated>
  </verify>
  <done>Sourcing common.sh exposes REVENIUM_AGENT_PREFIX==openclaw-, non-empty MARKERS_DIR/TAXONOMY_FILE/SESSIONS_DIR, and a get_root_session_id function; REVENIUM_AGENT_NAME is retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: get-root-session-id.py JSONL resolver + unit tests (TRACE-01)</name>
  <files>scripts/get-root-session-id.py, tests/test_get_root_session_id.py</files>
  <read_first>
    - ../hermes-revenium/skills/revenium/scripts/get-root-session-id.py (contract analog: signature, max_depth=10 cycle guard, fail-open, __main__ block — KEEP these; SWAP the SQLite query)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (NP-2 — exact JSONL-scan replacement block, cheap pre-filter, sessions_dir default)
    - .planning/phases/04-task-metering-attribution/04-RESEARCH.md (the "Root-Session Resolution Algorithm" section + "Code Examples" resolver skeleton)
    - tests/fixtures/sessions/parent-with-spawn.jsonl, tests/fixtures/sessions/plain-session.jsonl (fixtures from Task 1)
  </read_first>
  <behavior>
    - no-parent sid (a session not declared as anyone's child) -> returns itself
    - one-hop: resolving the child-uuid from parent-with-spawn.jsonl -> returns the parent-uuid (basename of the file declaring the childSessionKey)
    - cycle (A declares B, B declares A) -> terminates at max_depth, returns deepest resolved (never hangs/raises)
    - missing/unreadable sessions_dir or malformed JSON line -> returns the input sid (fail-open, never raises)
    - empty sid argument -> sys.exit(0) with no output
  </behavior>
  <action>
    Create `scripts/get-root-session-id.py`. Keep the Hermes contract verbatim: function signature get_root_session_id(sid, sessions_dir=None, max_depth=10), the `if not sid: return sid` guard, the `for _ in range(max_depth)` walk, the blanket `except Exception: return sid`, and the __main__ block `if len(sys.argv) < 2 or not sys.argv[1]: sys.exit(0)` then `print(get_root_session_id(sys.argv[1]))`. SWAP the SQLite block for the JSONL reverse-map scan (NP-2): for every *.jsonl in sessions_dir, parent_sid = filename without extension; for each line, cheap pre-filter on the raw substring `"sessions_spawn"` before json.loads; read message.details.childSessionKey; child_to_parent[childSessionKey.rsplit(":",1)[-1]] = parent_sid. Default sessions_dir to OPENCLAW_HOME/agents/main/sessions (expanduser ~/.openclaw fallback), matching report.sh SESSIONS_DIR. Wrap each json.loads in try/except (skip malformed). Write `tests/test_get_root_session_id.py` using stdlib unittest, importing get_root_session_id via importlib from scripts/get-root-session-id.py, covering the five behaviors above against the Task-1 fixtures (build a tmp sessions_dir for the cycle case). Do NOT port any jobs / state.db / classifier references from the Hermes header.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_get_root_session_id -v 2>/dev/null || python3 tests/test_get_root_session_id.py</automated>
  </verify>
  <done>Resolver returns parent-uuid for the fixture child sid, returns self for a non-child sid, fails open to input sid on missing dir / malformed JSON, terminates on a cycle, and exits 0 silently on empty arg; unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session JSONL files -> resolver | resolver parses attacker-influenceable JSONL content from disk |
| child sid -> marker/agent path construction (downstream) | sid suffix derived from childSessionKey flows into filenames in later plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | get-root-session-id.py (malformed JSONL crashing the cron tick) | mitigate | per-line try/except around json.loads; blanket `except Exception: return sid`; never raise (fail-open) |
| T-04-02 | Denial of Service | resolver cycle in child->parent map | mitigate | max_depth=10 walk cap (Hermes contract preserved); returns deepest resolved |
| T-04-03 | Tampering | childSessionKey UUID suffix later used in a path | accept (here) | resolver only emits the suffix; path-traversal guard is enforced at the marker write boundary in plan 04-02 (T-04-06). Resolver never opens a path derived from the sid. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "Package Legitimacy Audit": not applicable). No registry packages introduced. |
</threat_model>

<verification>
- `python3 -c` taxonomy label-set assertion passes (METER-01).
- Sourcing `scripts/common.sh` exposes the four new constants and the resolver function (D-07).
- `python3 -m unittest tests.test_get_root_session_id` passes all five behaviors (TRACE-01).
- `tests/stub-revenium.sh` captures argv to STUB_REVENIUM_ARGV_FILE (harness ready for Waves 2-3).
</verification>

<success_criteria>
- ROADMAP criterion 1 satisfied: `task-taxonomy.json` present with the 8 labels.
- common.sh contract (constants + resolver) ready for Wave 2 consumers.
- Resolver implements TRACE-01 root resolution with fail-open semantics.
- Test harness exists so downstream argv-capture integration tests can run.
</success_criteria>

<output>
Create `.planning/phases/04-task-metering-attribution/04-01-SUMMARY.md` when done.
</output>
