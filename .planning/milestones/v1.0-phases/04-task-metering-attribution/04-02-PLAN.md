---
phase: 04-task-metering-attribution
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - scripts/write-marker.sh
  - scripts/report.sh
  - tests/test_write_marker.sh
  - tests/test_report_argv.sh
autonomous: true
requirements: [METER-02, METER-03, TRACE-01, TRACE-02]
must_haves:
  truths:
    - "write-marker.sh validates <task_type> against task-taxonomy.json and appends an ISO8601 marker line, rejecting unknown labels with a non-zero exit (METER-02)"
    - "report.sh tags each completion with the most recent marker whose ts precedes the completion timestamp, defaulting to unclassified (METER-03, D-01)"
    - "report.sh resolves root once per session and ships --agent openclaw-<root_sid> (TRACE-01/02, D-05/D-07)"
    - "report.sh always passes --task-type on every meter completion (METER-03)"
  artifacts:
    - path: "scripts/write-marker.sh"
      provides: "marker writer with taxonomy allowlist + flock append (D-03)"
      contains: "task-taxonomy"
    - path: "scripts/report.sh"
      provides: "task-type correlation + --agent/--task-type wiring"
      contains: "--task-type"
    - path: "tests/test_report_argv.sh"
      provides: "stubbed-revenium argv-capture integration test"
  key_links:
    - from: "scripts/report.sh"
      to: "scripts/get-root-session-id.py"
      via: "get_root_session_id wrapper sourced from common.sh"
      pattern: "get_root_session_id"
    - from: "scripts/report.sh"
      to: "markers/<sid>.jsonl"
      via: "timestamp-precedence marker read in process_session"
      pattern: "MARKERS_DIR"
    - from: "scripts/write-marker.sh"
      to: "task-taxonomy.json"
      via: "label allowlist validation"
      pattern: "TAXONOMY_FILE"
---

<objective>
Implement the marker producer (`write-marker.sh`) and the marker/agent consumer (`report.sh`): timestamp-precedence task-type correlation (NP-1), the `--agent "openclaw-{root}"` switch and the always-present `--task-type` flag.

Purpose: This is the metering core of Phase 4 — METER-02 (marker write), METER-03 (per-completion task-type), TRACE-01/02 (root rollup). It consumes the constants, resolver, and harness from plan 04-01.
Output: new `scripts/write-marker.sh`, modified `scripts/report.sh`, plus two integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-task-metering-attribution/04-RESEARCH.md
@.planning/phases/04-task-metering-attribution/04-PATTERNS.md
@.planning/phases/04-task-metering-attribution/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: write-marker.sh — validate + append marker (METER-02/D-03)</name>
  <files>scripts/write-marker.sh, tests/test_write_marker.sh</files>
  <read_first>
    - scripts/common.sh (post-04-01 — sources TAXONOMY_FILE, MARKERS_DIR, SESSIONS_DIR, info/warn, ensure_path; header style lines 1-15)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (NP-3 — exact heredoc, simplifications to make, exit-code contract; Pitfall 5 cron-session exclusion)
    - .planning/phases/04-task-metering-attribution/04-RESEARCH.md (the "Pattern 2: write-marker.sh" section + Pitfall 2 ISO8601 ts + Pitfall 5)
    - ../hermes-revenium/skills/revenium/references/task-classification.md lines 37-83 (write_marker py snippet to port-and-simplify)
    - task-taxonomy.json (label allowlist source)
  </read_first>
  <behavior>
    - `write-marker.sh research` (valid label) -> appends one line `{"ts":"<ISO8601Z>","task_type":"research"}` to markers/<sid>.jsonl, prints `marker written: <path>`, exits 0
    - `write-marker.sh bogus` (label not in taxonomy) -> non-zero exit, no marker line written (allowlist, ASVS V5)
    - sid resolved from the newest non-cron *.jsonl in SESSIONS_DIR; a sid not matching the UUID/pseudo charset is rejected (path-traversal guard)
    - two rapid invocations append two lines (flock, no interleave corruption); markers/ created mode 0700
  </behavior>
  <action>
    Create `scripts/write-marker.sh`. Header per common.sh style; `source` common.sh for STATE_DIR, TAXONOMY_FILE, MARKERS_DIR, SESSIONS_DIR, info/warn, ensure_path. Core is a bash-3.2 env-passing python heredoc (NEVER interpolate `${VAR}` inside `<<'PY'`): load labels from TAXONOMY_FILE's `labels` key into a set; if argv task_type not in set, `raise SystemExit("unknown task_type: ...")` (non-zero). Resolve sid as the newest-mtime *.jsonl in SESSIONS_DIR; EXCLUDE cron sessions (Pitfall 5) — read SESSIONS_DIR/sessions.json if present and skip files whose session-key starts with `agent:main:cron:`; fall back to a `pseudo-<epoch>` sid if no candidate. Guard sid against the regex `[0-9a-fA-F-]+` or `pseudo-[0-9]+` and SystemExit("unsafe sid") otherwise (path-traversal, ASVS V5). makedirs MARKERS_DIR mode 0o700. Build the record exactly `{"ts": <ISO8601 UTC, %Y-%m-%dT%H:%M:%SZ>, "task_type": <label>}` (ISO8601 so it sorts lexicographically against report.sh completion timestamps — Pitfall 2). Append under fcntl.LOCK_EX with O_APPEND, json.dumps compact separators. Print `marker written: <path>` on success. Drop the Hermes muid / dual GUARDRAIL+CHAT records / unix-float ts / operation_type fields. Write `tests/test_write_marker.sh`: run against a tmp STATE_DIR (export OPENCLAW_HOME to a tmp tree containing agents/main/sessions and a fake session file), assert the valid-label marker line shape and exit 0, assert `write-marker.sh bogus` exits non-zero and writes no line, and assert two invocations yield two lines.
  </action>
  <verify>
    <automated>bash tests/test_write_marker.sh</automated>
  </verify>
  <done>write-marker.sh appends a valid ISO8601 marker for a taxonomy label and exits 0; rejects an unknown label with a non-zero exit and no line; markers/ is 0700; concurrent appends do not corrupt; test_write_marker.sh passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: report.sh — task-type correlation + --agent/--task-type wiring (METER-03/TRACE-01/02)</name>
  <files>scripts/report.sh, tests/test_report_argv.sh</files>
  <read_first>
    - scripts/report.sh (full — OPENCLAW_HOME probe 17-26; SESSIONS_DIR/SKILL_DIR 28-34; post_to_revenium 186-274 with --agent "OpenClaw" at 221; process_session 279-509 with session_id at 282, timestamp at 372, call site 478-489)
    - scripts/common.sh (post-04-01 — MARKERS_DIR, REVENIUM_AGENT_PREFIX, get_root_session_id)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (the "scripts/report.sh" assignment: Change 1-4; NP-1 correlation heredoc + performance note)
    - .planning/phases/04-task-metering-attribution/04-RESEARCH.md (Pattern 1 precedence heredoc; Pattern 3 --agent wiring; Pitfall 3 resolve-once-per-session; A4 always-send-flag)
    - ../hermes-revenium/skills/revenium/scripts/hermes-report.sh lines 196-202 (resolve-root-once-per-session placement analog)
    - tests/stub-revenium.sh, tests/fixtures/sessions/* (harness from 04-01)
  </read_first>
  <behavior>
    - a session with markers research@T1 then generation@T2: a completion at ts between T1 and T2 is tagged --task-type research; a completion after T2 is tagged generation (timestamp-precedence, D-01)
    - a session with no marker file -> every completion tagged --task-type unclassified (METER-03 default, A4)
    - every meter completion argv contains --task-type (always present) and --agent with the openclaw- prefix (TRACE-02)
    - root is resolved once per session file (not per completion line); a plain session resolves --agent openclaw-<own-sid> (fail-open, D-05)
  </behavior>
  <action>
    Modify `scripts/report.sh` with four changes (do NOT introduce any jobs / --agentic-job-id machinery). Change 1 — make MARKERS_DIR, REVENIUM_AGENT_PREFIX, and get_root_session_id available: source common.sh after the OPENCLAW_HOME probe, OR add the three constants + wrapper inline; verify no double-definition with report.sh's existing PATH/log block (36-68) and that SESSIONS_DIR (28) is not clobbered. Change 2 — in process_session after `session_id=$(basename ...)` (282) and before the line loop: `root_sid=$(get_root_session_id "${session_id}")` then `root_sid="${root_sid:-${session_id}}"` (belt-and-suspenders fail-open), and read+sort the marker list from MARKERS_DIR/<session_id>.jsonl ONCE into a cached temp file (mirror the msg-metadata caching at report.sh 305-319) — do NOT spawn python3 per completion line (Pitfall 3). Change 3 — in the per-line loop after `timestamp` is set (372), compute task_type via NP-1 precedence lookup against the cached markers (most recent marker ts <= completion timestamp), default `unclassified`; ISO8601 string compare. Change 4 — extend post_to_revenium (186-205 signature + 478-489 call site) to accept root_sid and task_type; replace line 221 `--agent "OpenClaw"` with `--agent "${REVENIUM_AGENT_PREFIX}${root_sid}"`, and add `--task-type "${task_type:-unclassified}"` to the cmd array (always present). Reuse get_provider/clean_model_name/map_stop_reason/get_offset/set_offset and the trace-id walk verbatim. Write `tests/test_report_argv.sh`: build a tmp OPENCLAW_HOME with the 04-01 session fixtures + a markers file with two timestamped labels, put a copy of stub-revenium.sh named `revenium` first on PATH (STUB_REVENIUM_ARGV_FILE set), run report.sh, and assert the captured argv contains `--task-type` (correct label per precedence and `unclassified` for the no-marker session) and `--agent` with the `openclaw-` prefix.
  </action>
  <verify>
    <automated>bash tests/test_report_argv.sh</automated>
  </verify>
  <done>report.sh sources/defines MARKERS_DIR + REVENIUM_AGENT_PREFIX + get_root_session_id; resolves root once per session; every captured meter-completion argv has --task-type (precedence-correct, unclassified default) and --agent openclaw-<root>; no jobs flags present; test_report_argv.sh passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-supplied <task_type> -> write-marker.sh -> marker file | untrusted label string crosses into a JSONL append + downstream parse |
| resolved sid -> markers/<sid>.jsonl path | sid (from session filename / childSessionKey suffix) builds a filename |
| marker ts/task_type -> report.sh log lines + --task-type argv | marker content flows to logs and the revenium CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | write-marker.sh task_type (newline/pipe injection corrupting JSONL) | mitigate | allowlist validation against task-taxonomy.json labels set; reject otherwise (non-zero exit); json.dumps compact for the marker line so no raw label bytes hit the file unescaped (ASVS V5) |
| T-04-05 | Denial of Service | report.sh correlation on malformed marker JSONL | mitigate | per-line try/except around json.loads in the NP-1 heredoc; default to `unclassified`; never abort the tick |
| T-04-06 | Tampering | path traversal via sid in markers/<sid>.jsonl | mitigate | sid charset guard `[0-9a-fA-F-]+ | pseudo-[0-9]+` with SystemExit("unsafe sid") before any path construction in write-marker.sh |
| T-04-07 | Tampering | concurrent cron+agent marker appends corrupting the log | mitigate | fcntl.LOCK_EX + O_APPEND in write-marker.sh; markers/ dir mode 0700 (ASVS V4) |
| T-04-08 | Tampering | log injection via marker task_type in report.sh log lines | mitigate | 64-char truncation on any marker-derived value logged (03-PATTERNS "Log Injection Mitigation") |
| T-04-09 | Tampering | shell interpolation of ${VAR} inside python heredoc | mitigate | env-passing heredoc discipline (`<<'PY'`); never interpolate (03-PATTERNS) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "Package Legitimacy Audit": not applicable) |
</threat_model>

<verification>
- `tests/test_write_marker.sh` green: valid label appends ISO8601 marker, unknown label rejected non-zero (METER-02).
- `tests/test_report_argv.sh` green: `--task-type` precedence-correct + `unclassified` default; `--agent openclaw-<root>` present (METER-03, TRACE-01/02).
- No `--agentic-job-id`/`--agentic-job-name`/`--agentic-job-type` token appears in report.sh.
</verification>

<success_criteria>
- ROADMAP criterion 3: report.sh passes `--task-type <label>` (default unclassified) on every completion.
- ROADMAP criterion 4: report.sh resolves root session id and passes `--agent "openclaw-{root}"`.
- write-marker.sh is the safe marker producer SKILL.md will invoke (criterion 2 producer).
</success_criteria>

<output>
Create `.planning/phases/04-task-metering-attribution/04-02-SUMMARY.md` when done.
</output>
