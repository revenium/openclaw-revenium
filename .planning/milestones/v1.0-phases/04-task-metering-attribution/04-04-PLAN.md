---
phase: 04-task-metering-attribution
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - SKILL.md
  - references/task-classification.md
  - scripts/cron.sh
  - scripts/post-install.sh
autonomous: true
requirements: [METER-02]
must_haves:
  truths:
    - "SKILL.md contains a mandatory TASK CLASSIFICATION section that fires before substantive turns and directs a single write-marker.sh call (METER-02, D-09)"
    - "SKILL.md /revenium flow detects legacy AGENT:IS:OpenClaw rules and surfaces a one-time reconfigure notice with no auto-rewrite (D-08)"
    - "cron.sh prunes marker files older than ~7 days each tick, fail-open (D-04)"
    - "post-install.sh chmods the 2 new scripts, seeds task-taxonomy.json, and creates markers/ at 0700"
  artifacts:
    - path: "SKILL.md"
      provides: "TASK CLASSIFICATION section + legacy reconfigure notice"
      contains: "write-marker.sh"
    - path: "references/task-classification.md"
      provides: "trigger rules + worked examples (ported, plugin/job refs stripped)"
      contains: "task-taxonomy"
    - path: "scripts/cron.sh"
      provides: "marker prune stage (D-04)"
      contains: "markers"
    - path: "scripts/post-install.sh"
      provides: "chmod new scripts + seed taxonomy + create markers dir"
      contains: "task-taxonomy.json"
  key_links:
    - from: "SKILL.md"
      to: "scripts/write-marker.sh"
      via: "TASK CLASSIFICATION directive invokes the helper"
      pattern: "write-marker\\.sh"
    - from: "scripts/cron.sh"
      to: "markers/<sid>.jsonl"
      via: "find -mtime prune stage"
      pattern: "markers"
    - from: "scripts/post-install.sh"
      to: "task-taxonomy.json"
      via: "seed copy into SKILL_DIR"
      pattern: "task-taxonomy"
---

<objective>
Wire the agent-facing and install-time surfaces: the mandatory TASK CLASSIFICATION directive in SKILL.md (the marker producer trigger), the D-08 legacy-install reconfigure notice, the D-04 marker-prune cron stage, and the post-install changes (chmod new scripts, seed taxonomy, create markers/ at 0700).

Purpose: Completes ROADMAP criterion 2 (SKILL.md classification + per-session marker), the D-08 safety notice for the agent-name migration, and the runtime plumbing (prune + install) that makes the rest of the phase operational. Depends on the write-marker.sh contract (04-02) and the filter scheme (04-03).
Output: modified `SKILL.md`, ported `references/task-classification.md`, modified `scripts/cron.sh` and `scripts/post-install.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-task-metering-attribution/04-RESEARCH.md
@.planning/phases/04-task-metering-attribution/04-PATTERNS.md
@.planning/phases/04-task-metering-attribution/04-02-SUMMARY.md
@.planning/phases/04-task-metering-attribution/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md TASK CLASSIFICATION section + ported reference (METER-02/D-09)</name>
  <files>SKILL.md, references/task-classification.md</files>
  <read_first>
    - SKILL.md (full — headings at 7/34/63/67/132; Guardrail Check Procedure 34-62; /revenium Command 132-157)
    - ../hermes-revenium/skills/revenium/SKILL.md lines 218-259 (TASK CLASSIFICATION FINAL ACTION + JOB DECLARATION to DROP)
    - ../hermes-revenium/skills/revenium/references/task-classification.md (full — trigger rules 5-16, worked examples 94-116; strip plugin/job refs)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (the "SKILL.md" assignment, Change 1 — what to port, the single write-marker.sh directive, the DROP list)
    - scripts/write-marker.sh (post-04-02 — the exact invocation + `marker written: <path>` confirmation contract)
  </read_first>
  <action>
    Add a TASK CLASSIFICATION section to `SKILL.md` (place after the Guardrail Check Procedure, before the `/revenium` Command). Port the Hermes trigger wording verbatim-in-spirit (per D-09): classify the turn if it called any non-read-only tool OR produced > 200 words OR answered a multi-step reasoning question; skip ONLY when the entire response is <= 2 sentences AND zero tools were called (no borderline path). Replace the Hermes execute_code write_marker snippet with a SINGLE directive: call `bash ~/.openclaw/skills/revenium/scripts/write-marker.sh <task_type>` where <task_type> is one of the 8 taxonomy labels (research, analysis, generation, review, code_review, refactor, planning, debugging); success confirmation is `marker written: <path>`; default attribution is `unclassified` when no marker is written. Link to references/task-classification.md for full detail. DROP entirely (out of scope): the JOB DECLARATION section, classifier-plugin references, the subagent trace_id/agentic_job_id paragraph, the GUARDRAIL/CHAT two-record invariant, and any references/job-declaration.md link. Also create `references/task-classification.md` by porting the Hermes file with all plugin/job references stripped (keep trigger rules + worked examples + the blocklist of read-only tools), and replace its write_marker snippet with the same single write-marker.sh directive.
  </action>
  <verify>
    <automated>grep -qi 'TASK CLASSIFICATION' SKILL.md && grep -Fq 'write-marker.sh' SKILL.md && grep -q 'unclassified' SKILL.md && ! grep -qi 'agentic_job_id\|JOB DECLARATION' SKILL.md && ! grep -Fqi 'job-declaration.md' SKILL.md && test -f references/task-classification.md && grep -Fq 'write-marker.sh' references/task-classification.md && ! grep -qi 'agentic_job\|classifier plugin' references/task-classification.md && echo OK</automated>
  </verify>
  <done>SKILL.md has a TASK CLASSIFICATION section with the D-09 trigger and a single write-marker.sh directive defaulting to unclassified; no job/plugin references remain in SKILL.md or the ported reference; references/task-classification.md exists with trigger rules + examples.</done>
</task>

<task type="auto">
  <name>Task 2: SKILL.md legacy-install reconfigure notice (D-08)</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md (full — /revenium Command 132-157; reconfigure action at line 149-151)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (the "SKILL.md" assignment, Change 2 — detection idiom, one-time flag in config.json, exact notice string, NO auto-rewrite)
    - .planning/phases/04-task-metering-attribution/04-RESEARCH.md (the "Runtime State Inventory" + Pitfall 1 — why legacy AGENT:IS:OpenClaw rules silently stop matching; the canonical-question paragraph)
    - .planning/phases/03-guardrail-engine/03-PATTERNS.md ("Atomic JSON Write" for the one-time flag persistence; "API fetch pattern" --output json idiom)
    - .planning/phases/03-guardrail-engine/03-CONTEXT.md (D-02 no-migration ethos honored by D-08)
  </read_first>
  <action>
    In the SKILL.md `/revenium` command flow (132-157), add a one-time legacy-install detection step. Detection mechanism is Claude's discretion (per D-08): either inspect live rules via `revenium guardrails budget-rules list --output json` for any rule still filtering `AGENT:IS:OpenClaw`, OR add a `config.json` schema/version marker compared against the current scheme. When a legacy rule is detected, surface EXACTLY ONCE the notice copied byte-for-byte from 04-PATTERNS.md line 357 / 04-CONTEXT.md D-08 (the canonical em-dash `—` and apostrophe in "won't"): "Your budget rules use the old filter and won't track spend — run reconfigure." Direct the user to the existing reconfigure action (line 151 — `setup-guardrails.sh --interactive`). NO silent auto-rewrite (honors Phase 3 D-02, per D-08). Persist the one-time signal as a flag in config.json (atomic temp-then-rename write per 03-PATTERNS) so the notice does not repeat every turn. Document in the SKILL.md prose WHY this matters (D-07 changed --agent to openclaw-{root}; rules filtering AGENT:IS:OpenClaw now match nothing — the user must reconfigure since repo edits cannot fix server-side rules).
  </action>
  <verify>
    <automated>grep -Fq "old filter and won't track spend" SKILL.md && grep -qi 'AGENT:IS:OpenClaw' SKILL.md && ! grep -qi 'auto-rewrite\|automatically rewrite\|silently recreate' SKILL.md && echo OK</automated>
  </verify>
  <done>SKILL.md /revenium flow detects legacy AGENT:IS:OpenClaw rules, surfaces the verbatim one-time notice persisted via a config.json flag, points to reconfigure, and performs no auto-rewrite.</done>
</task>

<task type="auto">
  <name>Task 3: cron.sh marker prune (D-04) + post-install.sh wiring</name>
  <files>scripts/cron.sh, scripts/post-install.sh</files>
  <read_first>
    - scripts/cron.sh (full — flock branch run_report+guardrail-check 78-80; mkdir branch 91-96; portable find -mmin usage at 88; LOCK_FILE)
    - scripts/post-install.sh (full — chmod loop line 114; guardrail-status seed step 416-430; BUDGET-GUARD copy idiom; verification section)
    - .planning/phases/04-task-metering-attribution/04-PATTERNS.md (the "scripts/cron.sh" assignment — prune stage, fail-open, MARKERS_DIR sourcing decision; the "scripts/post-install.sh" assignment — chmod / seed / markers dir)
    - .planning/phases/04-task-metering-attribution/04-RESEARCH.md (D-04 prune; the "Runtime State Inventory" build-artifacts row)
    - task-taxonomy.json (the repo-root asset post-install seeds)
  </read_first>
  <action>
    cron.sh (D-04): add a marker-prune stage in BOTH lock branches, AFTER the existing report.sh + guardrail-check.sh calls (after 80 in the flock branch and after 94 in the mkdir branch), fail-open (`|| true` / `2>/dev/null`). Prune with `find "${MARKERS_DIR}" -name '*.jsonl' -mtime +7 -delete 2>/dev/null` (BSD/GNU portable, mirrors the existing portable find usage; ~7-day threshold is Claude's discretion). MARKERS_DIR is not currently available in cron.sh — either source common.sh or inline `${OPENCLAW_HOME}/skills/revenium/markers` (be explicit which; sourcing is cleaner if it does not clobber cron.sh's own probe). A prune failure must never block the tick. post-install.sh: (1) add `write-marker.sh` and `get-root-session-id.py` to the chmod loop at line 114; (2) seed `task-taxonomy.json` — copy the repo-root taxonomy into `${SKILL_DIR}/task-taxonomy.json` if absent (reuse the existing copy-from-SKILL_DIR idiom used for BUDGET-GUARD.md; single path per Pitfall 6); (3) create the markers dir mode 0700 (`mkdir -p "${SKILL_DIR}/markers" && chmod 700 "${SKILL_DIR}/markers"`, ASVS V4). Leave all other post-install/cron content unchanged.
  </action>
  <verify>
    <automated>grep -q "markers" scripts/cron.sh && grep -q "mtime +7" scripts/cron.sh && grep -Fq 'write-marker.sh' scripts/post-install.sh && grep -Fq 'get-root-session-id.py' scripts/post-install.sh && grep -Fq 'task-taxonomy.json' scripts/post-install.sh && grep -Eq 'chmod 700.*markers|markers.*chmod 700|chmod 0700.*markers' scripts/post-install.sh && bash -n scripts/cron.sh && bash -n scripts/post-install.sh && echo OK</automated>
  </verify>
  <done>cron.sh prunes markers older than ~7d in both lock branches, fail-open; post-install.sh chmods the 2 new scripts, seeds task-taxonomy.json into SKILL_DIR, and creates markers/ at 0700; both scripts pass `bash -n`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent LLM turn -> write-marker.sh invocation (via SKILL.md directive) | the agent chooses the <task_type> argument the helper then validates |
| live revenium rule list -> SKILL.md legacy detection | server rule data parsed to decide whether to notify |
| marker files on disk -> cron prune | filesystem housekeeping over the markers dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering | invalid <task_type> chosen by the LLM | mitigate | write-marker.sh allowlist (plan 04-02 T-04-04) rejects it; SKILL.md directs only the 8 taxonomy labels and treats non-`marker written:` output as a protocol error |
| T-04-16 | Repudiation/Spoofing | legacy rules silently orphaned after the --agent migration (no spend tracked) | mitigate | D-08 one-time reconfigure notice on /revenium so the user is informed; detection is explicit, no silent state change |
| T-04-17 | Denial of Service | prune stage failure aborting the cron tick | mitigate | fail-open `|| true` / `2>/dev/null`; prune runs AFTER report.sh + guardrail-check.sh so it cannot starve enforcement |
| T-04-18 | Information Disclosure | markers dir world-readable | mitigate | post-install creates markers/ mode 0700 (ASVS V4) |
| T-04-19 | Tampering | config.json one-time flag write corrupting config | mitigate | atomic temp-then-rename write (03-PATTERNS "Atomic JSON Write") |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "Package Legitimacy Audit": not applicable) |
</threat_model>

<verification>
- SKILL.md TASK CLASSIFICATION section present with D-09 trigger + single write-marker.sh directive + unclassified default; no job/plugin refs (METER-02).
- references/task-classification.md ported, plugin/job refs stripped.
- SKILL.md /revenium surfaces the verbatim legacy notice, persisted once, no auto-rewrite (D-08).
- cron.sh prunes markers older than ~7d, fail-open (D-04); post-install chmods new scripts, seeds taxonomy, creates markers/ 0700.
- `bash -n` passes on cron.sh and post-install.sh.
</verification>

<success_criteria>
- ROADMAP criterion 2: SKILL.md mandatory TASK CLASSIFICATION fires before substantive turns and writes a per-session marker (via write-marker.sh).
- D-08 legacy-install safety notice in place (no auto-rewrite).
- D-04 marker pruning wired into cron; install plumbing complete (chmod, taxonomy seed, markers dir 0700).
</success_criteria>

<output>
Create `.planning/phases/04-task-metering-attribution/04-04-SUMMARY.md` when done.
</output>
