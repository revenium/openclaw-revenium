---
phase: 03-guardrail-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/common.sh
autonomous: false
requirements: [GUARD-01, GUARD-06]
must_haves:
  truths:
    - "OpenClaw CLI is upgraded to 2026.5.28 before any script is authored"
    - "openclaw message send still accepts --channel, --target, and -m flags"
    - "common.sh defines all shared path constants and helper functions"
    - "Scripts sourcing common.sh resolve STATE_DIR to ~/.openclaw/skills/revenium even in sandbox HOME"
  artifacts:
    - path: "scripts/common.sh"
      provides: "Shared path constants, ensure_path, log/info/warn/error, has_guardrails_cli probe, REVENIUM_AGENT_NAME default"
      contains: "GUARDRAIL_STATUS_FILE="
      min_lines: 60
  key_links:
    - from: "scripts/common.sh"
      to: "OPENCLAW_HOME multi-candidate probe"
      via: "checks ${candidate}/agents existence"
      pattern: "candidate.*/agents"
---

<objective>
Establish the Phase 3 execution prerequisites and the shared library every downstream script depends on. Two things must be true before any guardrail script is written: (1) the OpenClaw CLI is upgraded to 2026.5.28 and its `message send` flag form is re-verified (D-01), and (2) `scripts/common.sh` exists with the OpenClaw-adapted path constants and helpers (D-22, D-23).

Purpose: common.sh is sourced by guardrail-check.sh and setup-guardrails.sh (Wave 2). Authoring those scripts against an unverified CLI or absent shared library produces flag-drift and path-resolution bugs. This plan removes both risks first.

Output: upgraded openclaw CLI, verified notification flag form, and `scripts/common.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guardrail-engine/03-CONTEXT.md
@.planning/phases/03-guardrail-engine/03-RESEARCH.md
@.planning/phases/03-guardrail-engine/03-PATTERNS.md
@../hermes-revenium/skills/revenium/scripts/common.sh
@scripts/cron.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Upgrade OpenClaw CLI to 2026.5.28 and re-verify notification flags (D-01)</name>
  <read_first>
    - .planning/phases/03-guardrail-engine/03-RESEARCH.md (Pitfall 3, Pattern 5, Open Question 1 — flag stability)
    - .planning/phases/03-guardrail-engine/03-CONTEXT.md (D-01, D-10)
  </read_first>
  <action>
    Run `npm update -g openclaw` (fallback `npm install -g openclaw@latest`) to upgrade the OpenClaw CLI from 2026.3.13 to 2026.5.28 (D-01). Then run `openclaw --version` to confirm the version, `openclaw message send --help` to capture the current flag set, and `revenium config show` to confirm its output contains a `Team ID:` label (Open Question 2 / Assumption A2) parseable by `sed -n 's/.*Team ID:[ \t]*//p'`. Record the observed `openclaw message send` flag names (channel/target/message) and the Team ID label presence in the plan summary so Wave 2 scripts use the confirmed forms. This is automated work performed before the blocking human-verify gate below.
  </action>
  <what-built>
    Claude runs `npm update -g openclaw` (fallback `npm install -g openclaw@latest`), then runs `openclaw --version` to confirm 2026.5.28, and runs `openclaw message send --help` to capture the current flag set. Claude also runs `revenium config show` to confirm the output contains a `Team ID:` label (Open Question 2 / Assumption A2) parseable by `sed -n 's/.*Team ID:[ \t]*//p'`.
  </what-built>
  <how-to-verify>
    1. Confirm `openclaw --version` prints `2026.5.28` (or newer).
    2. Confirm `openclaw message send --help` still lists `--channel`, `--target`, and `-m` (short for `--message`). Report any renamed flags — downstream scripts in Wave 2 use `openclaw message send --channel X --target Y -m "MSG"` verbatim (D-10).
    3. Confirm `revenium config show` output contains the literal label `Team ID:` so guardrail-check.sh can parse teamId.
  </how-to-verify>
  <acceptance_criteria>
    - `openclaw --version` reports 2026.5.28 or newer
    - The Task 1 summary records the exact `openclaw message send` flag set observed in 2026.5.28 (channel/target/message flag names confirmed or flagged as renamed)
    - The summary records whether `revenium config show` emits a `Team ID:` label
  </acceptance_criteria>
  <resume-signal>Type "approved" with the confirmed flag names, or describe any flag renames so Wave 2 scripts can be adjusted</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author scripts/common.sh shared library (D-22, D-23)</name>
  <files>scripts/common.sh</files>
  <read_first>
    - ../hermes-revenium/skills/revenium/scripts/common.sh (canonical analog — header, ensure_path, log/info/warn/error, has_guardrails_cli)
    - scripts/cron.sh (lines 11-21 — OPENCLAW_HOME multi-candidate probe to replicate)
    - .planning/phases/03-guardrail-engine/03-PATTERNS.md (common.sh section — exact constants to include and omit)
    - .planning/phases/03-guardrail-engine/03-RESEARCH.md (Pitfall 1 — OPENCLAW_HOME multi-candidate probe required)
  </read_first>
  <action>
    Create `scripts/common.sh` as a sourced library. Use shebang `#!/usr/bin/env bash` and `set -uo pipefail` (NOT `-e` — this is a sourced library, per 03-PATTERNS.md). Replicate the OPENCLAW_HOME multi-candidate probe from cron.sh: honor `OPENCLAW_HOME` env override, else loop candidates `${HOME}/.openclaw` and `/home/ubuntu/.openclaw` selecting the one whose `${candidate}/agents` directory exists, falling back to `${HOME}/.openclaw`. Do NOT use the Hermes single-candidate HERMES_HOME fallback (Pitfall 1).

    Define these path constants (collapsed single-path OpenClaw model — STATE_DIR equals the skill dir, unlike Hermes two-path model): `SKILL_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"`, `STATE_DIR="${OPENCLAW_HOME}/skills/revenium"`, `CONFIG_FILE="${STATE_DIR}/config.json"`, `GUARDRAIL_STATUS_FILE="${STATE_DIR}/guardrail-status.json"`, `LOCK_FILE="${STATE_DIR}/revenium-metering.lock"`, `LOG_FILE="${STATE_DIR}/revenium-metering.log"`, `RULES_LOCK_FILE="${STATE_DIR}/rules.lock"`, and `REVENIUM_AGENT_NAME="${REVENIUM_AGENT_NAME:-OpenClaw}"` (D-23 — defaults to OpenClaw, never Hermes). Run `mkdir -p "${STATE_DIR}"`.

    Omit all Hermes-only constants: LEDGER_FILE, STATE_DB, TAXONOMY_FILE, MARKERS_DIR, WARN_FLAGS_DIR, PRUNE_LOCK_FILE, JOBS_LEDGER_FILE, HOOKS_CONFIG_FILE, TOOL_EVENTS_DIR, MIGRATION_NOTIFY_FILE, and the get_root_session_id() function (Phase 4 concern).

    Port these helpers verbatim from Hermes common.sh: `ensure_path()` (brew prefix detection + PATH extension across the documented dir list), `log()` (UTC timestamp, `[level] [revenium]` prefix, append to LOG_FILE, mirror to stderr only when `[ -t 2 ]`), and `info()`/`warn()`/`error()` wrappers. Port `has_guardrails_cli()` verbatim: returns true only when both `revenium guardrails budget-rules --help` and `revenium guardrails enforcement-events --help` succeed (verified against revenium 1.1.2).
  </action>
  <verify>
    <automated>bash -n scripts/common.sh && grep -q 'GUARDRAIL_STATUS_FILE=' scripts/common.sh && grep -q 'REVENIUM_AGENT_NAME:-OpenClaw' scripts/common.sh && grep -q '/agents' scripts/common.sh && [ "$(grep -c 'HERMES_HOME' scripts/common.sh)" -eq 0 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/common.sh` exits 0 (syntax valid)
    - `grep -c GUARDRAIL_STATUS_FILE= scripts/common.sh` is at least 1
    - common.sh contains `REVENIUM_AGENT_NAME="${REVENIUM_AGENT_NAME:-OpenClaw}"`
    - common.sh contains the multi-candidate probe checking `${candidate}/agents` (grep `/agents` matches)
    - `grep -c 'HERMES_HOME\|LEDGER_FILE\|TAXONOMY_FILE\|get_root_session_id' scripts/common.sh` is 0
    - `bash -c 'source scripts/common.sh; declare -f has_guardrails_cli ensure_path log'` lists all three functions
    - Sourcing in a temp env: `bash -c 'OPENCLAW_HOME=/tmp/xyz source scripts/common.sh; echo $STATE_DIR'` prints `/tmp/xyz/skills/revenium`
  </acceptance_criteria>
  <done>scripts/common.sh exists, passes `bash -n`, defines all required constants and the four helpers, includes the OpenClaw multi-candidate probe, and contains no Hermes-only identifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local machine | `npm update -g openclaw` pulls a new global binary; supply-chain trust on the openclaw package |
| common.sh → filesystem | common.sh creates STATE_DIR and the log file under HOME |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm global openclaw upgrade | accept | openclaw is a pre-existing, approved project dependency (RESEARCH.md Package Legitimacy Audit); upgrade is gated behind a blocking human-verify checkpoint (Task 1) that confirms version and flags |
| T-03-02 | Information Disclosure | LOG_FILE written under HOME | mitigate | log() writes only structured `[level] [revenium]` lines to STATE_DIR; no API keys or secrets are logged. STATE_DIR is created with default user umask under HOME (current-user-only on standard installs) |
| T-03-03 | Elevation of Privilege | common.sh PATH extension via ensure_path | accept | ensure_path only prepends well-known brew/system bin dirs; no user-controlled input enters the PATH list |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed; only an upgrade of the approved openclaw CLI. Legitimacy audit in RESEARCH.md shows zero [ASSUMED]/[SUS] packages. Task 1 checkpoint is the blocking human gate |
</threat_model>

<verification>
- `openclaw --version` >= 2026.5.28
- `bash -n scripts/common.sh` exits 0
- common.sh sources cleanly and exports STATE_DIR/CONFIG_FILE/GUARDRAIL_STATUS_FILE and the four helper functions
</verification>

<success_criteria>
- OpenClaw upgraded and notification flag form confirmed for use by Wave 2 scripts
- scripts/common.sh authored with OpenClaw path model, multi-candidate HOME probe, and all D-22 helpers; no Hermes-only identifiers remain
</success_criteria>

<output>
Create `.planning/phases/03-guardrail-engine/03-01-SUMMARY.md` when done. Record the confirmed `openclaw message send` flag names and whether `revenium config show` emits a `Team ID:` label, so Wave 2 can rely on them.
</output>
