---
phase: 03-guardrail-engine
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/setup-guardrails.sh
autonomous: true
requirements: [GUARD-04, GUARD-06]
must_haves:
  truths:
    - "setup-guardrails.sh --interactive prompts for hard limit, period, autonomous mode, and shadow mode"
    - "setup-guardrails.sh creates at least one budget rule via revenium guardrails budget-rules create"
    - "setup-guardrails.sh writes the ruleIds array to config.json atomically without stripping legacy alertId"
    - "Migration mode (--from-alert --auto) is not present"
    - "Created rules use the OpenClaw {Period} Budget naming convention"
  artifacts:
    - path: "scripts/setup-guardrails.sh"
      provides: "Interactive guardrail rule creation; writes ruleIds to config.json"
      contains: "budget-rules create"
      min_lines: 150
  key_links:
    - from: "scripts/setup-guardrails.sh"
      to: "config.json"
      via: "atomic ruleIds write preserving alertId"
      pattern: "ruleIds"
    - from: "scripts/setup-guardrails.sh"
      to: "revenium guardrails budget-rules create"
      via: "rule creation with --filter AGENT:IS:OpenClaw"
      pattern: "AGENT:IS:"
---

<objective>
Author `scripts/setup-guardrails.sh`: the interactive rule-creation entry point called by SKILL.md's Setup Flow. It ports the canonical Hermes setup-guardrails.sh, collapsing the three-mode dispatch to two (`default | interactive`) by dropping migration mode entirely (D-02), substituting the OpenClaw agent name and rule-naming convention (D-23), and adding the shadow-mode prompt (D-08).

Purpose: this is the sole owner of guardrail rule creation and the config.json `ruleIds` write (D-18 — SKILL.md delegates entirely to this script). It establishes the autonomous-mode (grace mode, GUARD-06) and shadow-mode choices.

Output: `scripts/setup-guardrails.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-guardrail-engine/03-CONTEXT.md
@.planning/phases/03-guardrail-engine/03-RESEARCH.md
@.planning/phases/03-guardrail-engine/03-PATTERNS.md
@../hermes-revenium/skills/revenium/scripts/setup-guardrails.sh
@.planning/phases/03-guardrail-engine/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author setup-guardrails.sh core (mode dispatch, validation, rule creation, config write)</name>
  <files>scripts/setup-guardrails.sh</files>
  <read_first>
    - ../hermes-revenium/skills/revenium/scripts/setup-guardrails.sh (canonical analog — mode dispatch, create_rule, validate_*, compute_warn_threshold, write_rule_ids_and_config)
    - scripts/common.sh (RULES_LOCK_FILE, CONFIG_FILE, REVENIUM_AGENT_NAME, helpers — authored in 03-01)
    - .planning/phases/03-guardrail-engine/03-PATTERNS.md (setup-guardrails.sh section — mode simplification, flock, create_rule, validators, write helpers)
    - .planning/phases/03-guardrail-engine/03-RESEARCH.md (Pitfall 5 bash 3.2; verified budget-rules create flag form; Code Examples)
  </read_first>
  <action>
    Create `scripts/setup-guardrails.sh` with shebang `#!/usr/bin/env bash` and `set -euo pipefail`. Resolve SCRIPT_DIR, `source "${SCRIPT_DIR}/common.sh"`, call `ensure_path`. Annotate bash 3.2 compatibility: use the env-passing heredoc pattern for ALL python calls (`KEY=value python3 - <<'PY'`); never use `declare -A`, `${var,,}`, or `<<<` herestrings inside subshells (Pitfall 5).

    Collapse the mode dispatch to two cases (D-02): `case "${MODE}" in interactive) run_interactive ;; default) run_default ;; *) error "unknown mode ${MODE}"; exit 2 ;; esac`. Remove every migration artifact: `--from-alert`, `--auto`, `MODE="from-alert"` parser branches, `run_migration()`, `migration_notify_once()`, `migration_notify_reset()`, MIGRATION_NOTIFY_FILE, and the FROM_ALERT / AUTO variables.

    Acquire the rules lock via the Hermes Python flock pattern on `RULES_LOCK_FILE` (`exec 9>"${RULES_LOCK_FILE}"` + python3 fcntl.flock LOCK_EX|LOCK_NB, warn + exit 0 on contention).

    Port verbatim: `validate_hard_limit()` (regex `^[0-9]+(\.[0-9]+)?$`), `validate_period()` (DAILY|WEEKLY|MONTHLY|QUARTERLY), `compute_warn_threshold()` (80% of hard limit via python float, trailing-zero stripped), `period_titled()`. Port `create_rule()` with the verified flag form `revenium guardrails budget-rules create --output json --name ... --description "" --metric-type TOTAL_COST --window-type DAILY --action BLOCK --group-by AGENT --warn-threshold ... --hard-limit ... --filter "AGENT:IS:${REVENIUM_AGENT_NAME}"` and append `--shadow-mode` when shadow is requested. Use the rule name `"OpenClaw ${period_title} Budget"` (D-23) — never "Hermes". Port `write_rule_ids_to_config()` and `write_rule_ids_and_config()` verbatim (they use CONFIG_FILE from common.sh): write `ruleIds` atomically (tempfile + os.rename/os.replace) and NEVER strip `alertId` (D-04 — leave legacy alertId as orphan). On success print the exact contract line `Created N rule(s). config.json updated. ruleIds=[...]`.

    Omit entirely the Hermes task-type picker section (no TAXONOMY_FILE in Phase 3) — run_interactive creates only the single base budget rule.
  </action>
  <verify>
    <automated>bash -n scripts/setup-guardrails.sh && grep -q 'budget-rules create' scripts/setup-guardrails.sh && grep -q 'AGENT:IS:' scripts/setup-guardrails.sh && grep -q 'OpenClaw ' scripts/setup-guardrails.sh && grep -c 'from-alert\|run_migration\|MIGRATION_NOTIFY' scripts/setup-guardrails.sh | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/setup-guardrails.sh` exits 0
    - Mode dispatch contains exactly `interactive` and `default` cases plus the unknown-mode error; `grep -c 'from-alert\|run_migration\|migration_notify\|MIGRATION_NOTIFY' scripts/setup-guardrails.sh` is 0
    - `revenium guardrails budget-rules create` uses `--output json` and `--filter "AGENT:IS:${REVENIUM_AGENT_NAME}"`
    - Rule name uses the `OpenClaw {Period} Budget` convention; `grep -c 'Hermes' scripts/setup-guardrails.sh` is 0
    - config.json write is atomic (tempfile + os.rename/os.replace) and does not delete the `alertId` key (no `pop('alertId')` or `del .*alertId`)
    - No `declare -A`, `${var,,}`, or `<<<` inside subshells (bash 3.2 safety)
    - Success path prints `Created N rule(s). config.json updated. ruleIds=`
    - No task-type picker (`grep -c 'TAXONOMY_FILE\|task-type' scripts/setup-guardrails.sh` is 0)
  </acceptance_criteria>
  <done>setup-guardrails.sh core is complete: two-mode dispatch, all validators and create_rule with the OpenClaw naming/filter, atomic ruleIds write preserving alertId, the success contract line, and no migration or task-type code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement run_interactive prompts including autonomous (grace) and shadow mode (D-06, D-08, GUARD-06)</name>
  <files>scripts/setup-guardrails.sh</files>
  <read_first>
    - ../hermes-revenium/skills/revenium/scripts/setup-guardrails.sh (run_interactive structure, autonomous/notify prompts)
    - .planning/phases/03-guardrail-engine/03-PATTERNS.md (run_interactive shadow-mode prompt addition; rule_name substitution at run_interactive)
    - .planning/phases/03-guardrail-engine/03-CONTEXT.md (D-08 shadow prompt wording; D-06 grace mode)
  </read_first>
  <action>
    Implement `run_interactive()` porting the Hermes prompt flow with these requirements:
    - Prompt for hard limit (loop until `validate_hard_limit` passes), period (loop until `validate_period` passes), and compute warn threshold via `compute_warn_threshold` (80% — Claude's discretion confirmed in CONTEXT.md).
    - Prompt for autonomous mode (grace mode, GUARD-06 / D-06): ask whether the agent should hard-stop on halt (autonomous) or warn-and-ask. Persist the chosen `autonomousMode` boolean into config.json alongside ruleIds (write via the same atomic write helper). This is the warn-and-ask vs hard-stop choice — default to the project's established default of warn-and-ask unless the operator chooses autonomous.
    - Add the D-08 shadow-mode prompt AFTER the autonomous/notify prompts and BEFORE create_rule: `read -r -p "Run in shadow mode (observe-only rules, no blocking)? (yes/no, default no): " shadow_response`, mapping yes/y/YES/Y -> `SHADOW_MODE="true"`, anything else -> `SHADOW_MODE="false"`. Pass SHADOW_MODE into create_rule so it appends `--shadow-mode` for shadow rules. Also accept a `--shadow-mode` CLI flag for non-interactive invocations (D-08).
    - On user cancellation (empty/declined at a confirmation prompt), print exactly `Cancelled.` and exit 0 (the SKILL.md contract from D-18).
    - Use `"OpenClaw ${period_title} Budget"` as the rule name here too.
  </action>
  <verify>
    <automated>bash -n scripts/setup-guardrails.sh && grep -q 'shadow mode' scripts/setup-guardrails.sh && grep -q 'autonomousMode' scripts/setup-guardrails.sh && grep -q 'shadow-mode' scripts/setup-guardrails.sh && grep -q 'Cancelled.' scripts/setup-guardrails.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/setup-guardrails.sh` exits 0
    - run_interactive prompts for hard limit, period, autonomous mode, and shadow mode (each prompt string present)
    - The shadow prompt text matches D-08: `Run in shadow mode (observe-only rules, no blocking)?`
    - `--shadow-mode` CLI flag is parsed for non-interactive use
    - `autonomousMode` is written to config.json (grace-mode persistence, GUARD-06)
    - Cancellation prints exactly `Cancelled.` and exits 0
    - Behavior: invoking with a piped `no` at the shadow prompt sets SHADOW_MODE=false (no `--shadow-mode` passed to create_rule)
  </acceptance_criteria>
  <done>run_interactive prompts for hard limit, period, autonomous (grace) mode, and shadow mode; honors the --shadow-mode flag; writes autonomousMode + ruleIds to config.json; and prints the Cancelled./Created contract lines per D-18.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator input → setup-guardrails.sh | hard limit, period, autonomous/shadow choices entered interactively |
| setup-guardrails.sh → revenium budget-rules create | user-entered values become CLI arguments |
| setup-guardrails.sh → config.json | ruleIds and autonomousMode written; must not corrupt or drop existing keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | operator hard-limit / period input passed to CLI | mitigate | `validate_hard_limit()` enforces numeric regex and `validate_period()` enforces the DAILY/WEEKLY/MONTHLY/QUARTERLY allowlist before any value reaches `revenium budget-rules create` (ASVS V5 input validation) |
| T-03-10 | Tampering | partial config.json write during ruleIds save | mitigate | Atomic write via tempfile + os.rename/os.replace; never write config.json in place. alertId key preserved (never popped) per D-04 |
| T-03-11 | Denial of Service | concurrent setup-guardrails runs racing on config.json | mitigate | Python fcntl.flock LOCK_EX|LOCK_NB on RULES_LOCK_FILE; second concurrent run warns and exits 0 |
| T-03-12 | Repudiation | rule-creation actions unlogged | accept | create_rule outcomes are logged via info(); rule creation is an explicit interactive operator action, low repudiation risk for a single-user skill |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; uses pre-existing revenium CLI only |
</threat_model>

<verification>
- `bash -n scripts/setup-guardrails.sh` exits 0
- No migration-mode or task-type identifiers remain (grep returns 0)
- Rule creation uses `--filter "AGENT:IS:OpenClaw"` and the `OpenClaw {Period} Budget` name
- config.json write is atomic and preserves alertId
- Shadow and autonomous prompts present per D-08 / D-06
</verification>

<success_criteria>
- setup-guardrails.sh exists, passes bash -n, creates rules via the verified CLI form, and writes ruleIds + autonomousMode to config.json atomically
- Migration mode dropped (D-02); shadow mode added (D-08); grace mode prompt present (GUARD-06)
- D-18 contract lines (`Created N rule(s)...` / `Cancelled.`) emitted
</success_criteria>

<output>
Create `.planning/phases/03-guardrail-engine/03-03-SUMMARY.md` when done. Record the exact success/cancel output strings and the config.json keys written (ruleIds, autonomousMode), so Plan 05 SKILL.md references the same contract.
</output>
