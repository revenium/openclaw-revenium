---
context: default
phase: post-v1.4-uat (NemoClaw install-path hardening)
status: paused
last_updated: 2026-06-11T18:06:06.401Z
head_commit: 58c52e6
---

# Resume: NemoClaw install-path UAT hardening (post-v1.4)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| "Milestone marked shipped ≠ works" | v1.4 NemoClaw was marked shipped/clean but live UAT found it broken end-to-end on a fresh host (12 bugs). | advisory | Do NOT trust the milestone "complete" status for the NemoClaw path; the install was only validated end-to-end during this UAT cycle. |
| SSHFS mount fragility | Dead/stale SSHFS mounts + cache lag repeatedly broke the install/metering on fresh VMs. | advisory | All mount sites now self-heal (fusermount -u before remount) + Gate D warns-not-aborts. Keep this pattern for any new mount use. |

<current_state>
Post-milestone live UAT of the v1.4 NemoClaw/OpenShell install path. The milestone was
marked shipped, but driving `install.sh --nemoclaw` on fresh VMs surfaced 12 real
bugs/gaps — all now fixed and pushed to origin/main (HEAD 58c52e6). No active GSD phase
execution; this is cross-cutting hardening of Phases 13–16 scripts + docs. User paused
while re-validating a full clean install on a fresh VM.
</current_state>

<completed_work>
12 fixes this cycle (all on origin/main, push via `gh` HTTPS helper):
- f673753 docs: de-dup REVENIUM export block
- 323a2c6 Gate A/B probes for OpenClaw v2026.5.22 (`--agent main`; assert `Status: loaded` + `allowConversationAccess: true`)
- c0e14e3 common.sh OPENCLAW_HOME sandbox normalization + Gate D label/path (in-sandbox markers)
- 405d568 host `revenium` CLI install for the metering cron (`deliver_revenium_cli_host`)
- dd20f81 per-sandbox-UUID provisioning ledger (multi-sandbox)
- 9382597 install-time budget guardrail provisioning (env-gated)
- 101b30a docs: simple install cmd + prominent README pointer
- b6772c3 stale-mount self-heal in cron tick
- ea921c7 stale-mount self-heal at install mount sites + budget env doc
- c2c22d0 docs: host-CLI + budget steps in install sequence
- 58c52e6 Gate D marker visibility resilient (warn-not-abort)
Tests: tests/test_nemoclaw_provisioning.sh 48/0, tests/test_common_paths.sh 5/0.
(Known unrelated pre-existing failure: test_nemoclaw_cron.sh GROUP-F 22/1.)
</completed_work>

<remaining_work>
1. **Reconcile docs with reality** (main follow-up): ROADMAP.md, PROJECT.md, Phase 13–16
   docs, and `.planning/todos/pending/nemoclaw-install-gate-a-exit1.md` still describe
   v1.4 as shipped/clean and don't reflect any of the 12 fixes. Consider a v1.4.1
   gap-closure phase record or at least updating the milestone notes.
2. User's fresh-VM end-to-end validation result (in progress) — confirm a clean
   `install.sh --nemoclaw` to exit 0 + budget + metering + guardrail-status.json.
3. Optional: /gsd-extract-learnings for this cycle.
</remaining_work>

<decisions_made>
- Per-sandbox-UUID ledger (`revenium-nemoclaw-<uuid>.ledger`) — host-global ledger skipped a 2nd/recreated sandbox.
- Install-time budget creation is **env-gated** on `REVENIUM_BUDGET_LIMIT` + `REVENIUM_BUDGET_PERIOD` (runs `setup-guardrails.sh` on host with `OPENCLAW_HOME=<mount>`; writes ruleIds into the in-sandbox config.json).
- Gate D marker mount-check **warns instead of aborts** (SSHFS cache lag); the in-sandbox write is the real proof.
- All SSHFS mount sites self-heal stale mounts (`fusermount -u` / `umount -l` before remount).
</decisions_made>

<blockers>
- `/revenium` TUI slash command degraded by an OpenClaw **gateway pairing/scope** issue (`gateway closed (1008): pairing required` → embedded fallback; TUI can't fall back). The command WORKS as a typed agent message and the guardrail check runs every turn via the plugin. NOT fixable in this repo — OpenClaw/NemoClaw infra.
</blockers>

## Required Reading (in order)
1. Memory `v14-nemoclaw-uat-fixes.md` — the canonical summary of this cycle.
2. `.planning/HANDOFF.json` — structured state + per-fix commit map.
3. `.planning/todos/pending/nemoclaw-install-gate-a-exit1.md` — Gate A/B root-cause history (now resolved; needs status update).

## Infrastructure State
- Test VMs: `ssh -i ~/.ssh/agent-sandbox.pem ubuntu@<host>`; `nemoclaw` is on the **login-shell PATH only** (`bash -lc`). Hosts seen: 50.19.132.72/nemoclaw-001, 98.81.139.232/hermes-ftw-3, 18.212.94.67/revenium-ftw-2, 34.224.27.67/revenium-spike (user is "from scratch" on fresh VMs).
- Budget rules land under Revenium **Team `DZxzEl`** (dev key) — check the right team in the UI.
- Push uses the `gh` HTTPS credential helper (SSH not configured here).

<context>
The milestone "shipped" but the NemoClaw path didn't actually work on a clean host; this
cycle made it work end-to-end (provision → skill → enforcement plugin → metering → budget
→ guardrail-status.json) and robust to SSHFS flakiness. The big open item is that the
project's own records (milestone/phase docs) don't reflect any of it.
</context>

<next_action>
Start with: ask the user for the fresh-VM validation result. If clean → reconcile the 12
fixes into ROADMAP/PROJECT/Phase 13–16 docs + update the gate-a-exit1 todo to resolved
(consider a v1.4.1 gap-closure record). If not clean → continue debugging the install path
(it's almost certainly another SSHFS-mount or env-gating edge case).
</next_action>
